---
title: "WIN-U: Woodbury-Informed Newton-Unlearning as a retain-free Machine Unlearning Framework"
title_zh: WIN-U：基于 Woodbury 信息的牛顿遗忘——一种无需保留数据的机器遗忘框架
authors: "Xingjian Zhao, Mohammad Mohammadi Amiri, Malik Magdon-Ismail"
date: 2026-04-15
pdf: "https://arxiv.org/pdf/2604.13438v1"
tags: ["query:mu-ce"]
score: 10.0
evidence: 无需保留数据的机器遗忘框架
tldr: 针对大模型隐私保护中的“被遗忘权”，本文提出了一种名为WIN-U的机器遗忘框架。该框架无需访问保留数据集，仅利用原始模型的二阶信息，通过Woodbury矩阵恒等式和广义高斯-牛顿近似执行单步牛顿更新。WIN-U在视觉和语言基准测试中达到了SOTA性能，在有效移除遗忘集影响的同时保持了模型效用，且能有效抵御重学习攻击，为高效遗忘提供了新思路。
selection_source: fresh_fetch
motivation: 现有的机器遗忘方法通常依赖于访问保留数据集，这在隐私保护和计算成本方面存在显著局限性。
method: 提出一种基于Woodbury恒等式的单步牛顿更新方法，利用二阶曲率信息在无需保留数据的情况下近似重新训练的最优解。
result: 实验表明，WIN-U在视觉与语言任务中的遗忘效果、效用保持及抵御重学习攻击的能力均达到SOTA水平。
conclusion: WIN-U证明了仅利用二阶信息即可实现高效且安全的机器遗忘，为无需保留数据的遗忘任务提供了强有力的解决方案。
---

## 摘要
大语言模型（LLM）中的隐私问题导致对执行数据“被遗忘权”的需求迅速增长。机器遗忘（Machine unlearning）正是为了解决这一任务，即从已训练的模型中消除特定数据（即遗忘集）的影响。机器遗忘的金标准是生成一个仅在剩余训练数据（即保留集）上训练得到的模型。大多数现有的遗忘方法依赖于直接访问保留数据，由于隐私或成本限制，这在实际中可能并不可行。我们提出了 WIN-U，这是一种无需保留数据的遗忘框架，仅需要原始模型在全量数据上的二阶信息。遗忘过程通过单个牛顿式步骤完成。利用 Woodbury 矩阵恒等式和针对遗忘集曲率的广义高斯-牛顿近似，WIN-U 更新恢复了闭式线性解，并作为金标准重新训练最优解的局部二阶近似。在各种视觉和语言基准测试上的广泛实验表明，WIN-U 在遗忘效能和效用保持方面达到了 SOTA 性能，同时与现有方法相比，对重新学习攻击更具鲁棒性。重要的是，WIN-U 不需要访问保留数据。

## Abstract
Privacy concerns in LLMs have led to the rapidly growing need to enforce a data's "right to be forgotten". Machine unlearning addresses precisely this task, namely the removal of the influence of some specific data, i.e., the forget set, from a trained model. The gold standard for unlearning is to produce the model that would have been learned on only the rest of the training data, i.e., the retain set. Most existing unlearning methods rely on direct access to the retained data, which may not be practical due to privacy or cost constraints. We propose WIN-U, a retained-data free unlearning framework that requires only second order information for the originally trained model on the full data. The unlearning is performed using a single Newton-style step. Using the Woodbury matrix identity and a generalized Gauss-Newton approximation for the forget set curvature, the WIN-U update recovers the closed-form linear solution and serves as a local second-order approximation to the gold-standard retraining optimum. Extensive experiments on various vision and language benchmarks demonstrate that WIN-U achieves SOTA performance in terms of unlearning efficacy and utility preservation, while being more robust against relearning attacks compared to existing methods. Importantly, WIN-U does not require access to the retained data.

---

## 论文详细总结（自动生成）

### 论文总结：WIN-U —— 基于 Woodbury 信息的无需保留数据机器遗忘框架

#### 1. 核心问题与整体含义（研究动机和背景）
随着数据隐私法规（如 GDPR、CCPA）的实施，用户拥有“被遗忘权”，要求从已训练的模型中删除特定数据（遗忘集 $D_f$）的影响。
*   **研究动机**：传统的“金标准”是重新训练模型，但对大语言模型（LLM）而言成本极高。现有的遗忘方法（如梯度上升）通常需要访问“保留集” $D_r$ 以维持模型效用，但在实际场景中，保留集可能因隐私或存储限制而无法获取。
*   **核心挑战**：如何在**不访问保留数据**的前提下，高效且准确地近似重新训练的最优解，并确保遗忘是彻底的（而非仅仅是暂时掩盖）。

#### 2. 方法论：核心思想与关键技术
WIN-U 提出了一种基于二阶优化（牛顿法）的单步遗忘框架，其核心思想是利用原始模型的曲率信息来补偿移除数据后的参数偏差。
*   **核心公式推导**：从重新训练的目标函数出发，利用**广义高斯-牛顿（GGN）近似**和 **Woodbury 矩阵恒等式**。Woodbury 恒等式的妙处在于，它允许通过原始全量数据的逆海森矩阵（Inverse Hessian）和遗忘集的雅可比矩阵，直接计算出移除遗忘集后的新逆海森矩阵，从而实现单步牛顿更新。
*   **可扩展性技术（针对 LLM）**：
    *   **MC-WIN-U**：引入蒙特卡洛（MC）采样估计遗忘集的曲率，避免了在 LLM 巨大的输出空间（词表大小）中直接计算。
    *   **LoRA 适配**：将更新限制在低秩适配器参数空间内，显著降低计算和内存开销。
    *   **对角近似**：使用全量数据海森矩阵的对角线近似作为预计算的逆矩阵，进一步提升效率。
*   **算法流程**：预计算原始模型的逆海森信息 $\rightarrow$ 计算遗忘集的梯度与 MC 曲率 $\rightarrow$ 应用 Woodbury 更新公式 $\rightarrow$ 调整步长 $\eta$ 得到最终参数。

#### 3. 实验设计
论文设计了从合成数据到大规模语言模型的多层次验证：
*   **小规模验证**：
    *   **合成岭回归**：验证在线性情况下 WIN-U 是否能精确恢复重新训练的最优解。
    *   **MNIST + MLP**：在非线性分类任务中验证其对类别遗忘的效果。
*   **大规模 LLM 实验**：
    *   **基准测试**：使用 **OpenUnlearning** 框架，涵盖 **TOFU**（虚构作者信息）、**MUSE**（多维评估）和 **WMDP**（危险知识消除）。
    *   **对比方法**：GradDiff, NPO, RMU, SimNPO, GradAscent。
    *   **评估指标**：遗忘概率（Forget QA Prob）、模型效用（MU）、提取强度（ES）、隐私性以及**重学习鲁棒性**（Benign Relearning）。

#### 4. 资源与算力
*   **硬件环境**：所有实验均在单台 **NVIDIA H100 NVL (96 GB)** GPU 上完成。
*   **耗时情况**：
    *   在 TOFU 任务上，WIN-U 的遗忘时间约为 11s 到 411s（取决于遗忘集大小），虽然比简单的梯度上升慢，但远快于重新训练。
    *   在 WMDP 任务（大规模遗忘集）上，耗时较长（约 50,679s），主要受限于流式 Woodbury 求解的 $O(m^2)$ 复杂度。

#### 5. 实验数量与充分性
*   **实验规模**：涵盖了线性模型、简单神经网络和 1B 到 7B 参数规模的 LLM（如 Llama-3.2-1B, Llama-2-7B, Qwen2.5-1.5B）。
*   **消融实验**：对步长 $\eta$ 和 MC 采样数 $S$ 进行了详细的消融研究。
*   **充分性评价**：实验设计非常充分且客观。特别是引入了“重学习攻击”测试，揭示了许多现有方法只是“隐藏”了数据而非“删除”，而 WIN-U 在此项测试中表现优异，证明了其方法的彻底性。

#### 6. 主要结论与发现
*   **理论一致性**：WIN-U 在线性情况下能完美恢复重训解，在非线性情况下是优秀的局部二阶近似。
*   **无需保留数据**：在完全不接触保留集的情况下，WIN-U 达到了与需要保留集的方法相当甚至更好的遗忘效果。
*   **重学习鲁棒性**：WIN-U 改变了模型权重中的深层表示，使得模型在经过少量保留数据微调后，也不会轻易找回已遗忘的知识，这是其相对于 NPO 或 RMU 等方法的显著优势。

#### 7. 优点（亮点）
*   **无需保留数据（Retain-free）**：解决了隐私保护场景中保留数据不可用的痛点。
*   **理论支撑扎实**：不同于启发式的梯度操作，WIN-U 有明确的二阶优化理论背景。
*   **抗攻击性强**：通过牛顿步真正调整了参数分布，而非简单的负偏好优化，有效抵御了重学习攻击。
*   **灵活性**：通过步长 $\eta$ 可以精细调节遗忘与效用之间的权衡。

#### 8. 不足与局限
*   **预计算依赖**：虽然遗忘过程很快，但需要预先计算并存储原始模型的逆海森信息（或其近似），这对于超大规模模型仍有存储压力。
*   **计算复杂度**：随着遗忘集样本数 $m$ 的增加，Woodbury 核心矩阵的求逆成本呈 $O(m^3)$ 或 $O(m^2)$ 增长，处理极大规模遗忘请求时效率会下降。
*   **超参数敏感性**：步长 $\eta$ 的选择对最终效果影响较大，且目前仍需依赖少量验证数据来确定最佳步长。

（完）

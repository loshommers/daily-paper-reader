---
title: "SHIFT: Steering Hidden Intermediates in Flow Transformers"
title_zh: SHIFT：流式 Transformer 中的隐藏中间表示引导
authors: "Nina Konovalova, Andrey Kuznetsov, Aibek Alanov"
date: 2026-04-10
pdf: "https://arxiv.org/pdf/2604.09213v1"
tags: ["query:mu-ce"]
score: 9.0
evidence: 通过激活引导在DiT扩散模型中进行概念移除
tldr: 本文提出针对DiT架构扩散模型的轻量级框架SHIFT，通过在推理阶段动态干预中间层激活值来实现概念擦除与风格引导。该方法受大语言模型激活转向启发，通过学习转向向量并将其应用于特定层和时间步，无需重新训练即可有效抑制特定视觉概念、转换图像风格或修改目标物体，在保持生成质量的同时提供了灵活的控制能力。
selection_source: fresh_fetch
motivation: 旨在解决现有扩散模型在概念移除和内容控制时需要耗时重训的问题，探索一种更高效的推理侧干预手段。
method: 通过学习并动态应用转向向量到DiT模型的中间激活层，在特定时间步对生成路径进行针对性引导。
result: 实验表明该方法能有效抑制违规概念、实现风格迁移及物体增删，且能良好保持图像质量和提示词一致性。
conclusion: SHIFT为DiT模型提供了一种无需重训、灵活且高效的控制方案，显著提升了扩散模型在内容编辑和安全过滤方面的实用性。
---

## 摘要
扩散模型已成为高保真图像生成的领先方法。特别是近期基于 DiT 的扩散模型，在生成高质量样本的同时，实现了极强的提示词遵循能力。受大语言模型中激活引导（activation steering）的启发，我们提出了 SHIFT，这是一个简单、有效且轻量级的框架，通过在推理阶段对中间激活进行针对性操作，实现 DiT 扩散模型中的概念移除。SHIFT 学习引导向量，并将其动态应用于选定的层和时间步，以抑制不需要的视觉概念，同时保留提示词的其余内容和整体图像质量。除了抑制，该机制还可以将生成内容转向所需的风格域，或引导样本添加或更改目标对象。我们证明了 SHIFT 在各种提示词和目标下，都能对 DiT 生成提供有效且灵活的控制，且无需耗时的重新训练。

## Abstract
Diffusion models have become leading approaches for high-fidelity image generation. Recent DiT-based diffusion models, in particular, achieve strong prompt adherence while producing high-quality samples. We propose SHIFT, a simple but effective and lightweight framework for concept removal in DiT diffusion models via targeted manipulation of intermediate activations at inference time, inspired by activation steering in large language models. SHIFT learns steering vectors that are dynamically applied to selected layers and timesteps to suppress unwanted visual concepts while preserving the prompt's remaining content and overall image quality. Beyond suppression, the same mechanism can shift generations into a desired \emph{style domain} or bias samples toward adding or changing target objects. We demonstrate that SHIFT provides effective and flexible control over DiT generation across diverse prompts and targets without time-consuming retraining.

---

## 论文详细总结（自动生成）

这是一份关于论文《SHIFT: Steering Hidden Intermediates in Flow Transformers》的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：如何在不进行耗时的模型重训或微调的情况下，在现代扩散 Transformer（DiT，如 FLUX）中实现可靠的概念擦除（如过滤有害内容、版权对象）和生成控制。
*   **研究动机**：
    *   **架构演进**：扩散模型正从 UNet 转向 DiT 架构，传统的基于交叉注意力修改的方法在 DiT 的统一注意力机制中不再适用。
    *   **成本挑战**：现代 DiT 模型（如 FLUX.1 拥有 12B 参数）规模巨大，传统的微调方法（如 ESD、CA）计算成本极高。
    *   **蒸馏模型限制**：蒸馏后的模型（如 Flux.1[schnell]）通常固定了引导缩放（Guidance Scale），使得依赖负向引导的优化方法难以奏效。
*   **整体含义**：借鉴大语言模型（LLM）中的“激活引导”思想，提出一种在推理阶段直接干预中间层激活值的轻量级框架。

### 2. 方法论
*   **核心思想**：通过对比实验找到代表特定概念的“转向向量”（Steering Vectors），在推理生成过程中，将该向量加到模型的中间激活值上，从而改变生成路径。
*   **关键技术细节**：
    1.  **干预位置**：针对 FLUX 架构，干预点选在：(i) 文本编码器的池化嵌入（CLIP 向量）；(ii) DiT 骨干网络中双流块（Double-stream blocks）的文本标记激活值。
    2.  **向量构建**：收集少量对比提示词对（如“大象” vs “赛博朋克风格的大象”），记录模型在这些提示词下的激活差异。
    3.  **估计方法**：
        *   **均值差（Mean-difference）**：计算正负样本激活值的平均差。
        *   **SVM 估计**：训练线性 SVM 寻找区分概念的超平面，以其法向量作为转向方向。
    4.  **推理干预公式**：$\tilde{a} = a + \alpha \cdot v$。其中 $\alpha$ 是引导强度。
    5.  **动态调节机制**：引入轻量级分类器（SVM）信号，根据当前激活值中目标概念的强弱动态调整 $\alpha$，防止过度干预导致图像质量下降。

### 3. 实验设计
*   **数据集/场景**：
    *   **抽象概念擦除**：使用 **I2P 基準测试**（4,703 个提示词）评估裸露内容擦除。
    *   **通用质量评估**：使用 **MS-COCO 5K** 采样图像评估 FID（图像质量）和 CLIP 分数（提示词一致性）。
    *   **具体对象擦除**：遵循 SPM 协议，测试擦除“史努比”等特定角色。
    *   **风格与物体控制**：测试梵高风格移除、添加眼镜/帽子/红唇等。
*   **对比方法**：ESD、Concept Ablation (CA)、Unified Concept Editing (UCE)、Erase-and-Preserve (EAP)、EraseAnything 等。
*   **测试模型**：Flux.1[dev]（基础版）和 Flux.1[schnell]（蒸馏版）。

### 4. 资源与算力
*   **算力说明**：论文**未明确列出**具体的 GPU 型号、数量或总训练时长。
*   **效率强调**：作者强调该方法是“轻量级”且“无需重训”的。构建转向向量仅需处理极少量样本（如对象擦除仅需 20 对提示词，裸露擦除需 135 对），这相比于需要数千次迭代的微调方法，在算力需求上降低了几个数量级。

### 5. 实验数量与充分性
*   **实验规模**：
    *   裸露擦除使用了 135 对提示词构建向量，并在 4,700+ 提示词上验证。
    *   具体对象和风格任务各使用了约 20-100 对提示词。
    *   进行了详细的消融实验，涵盖了干预层数（Block）、时间步（Timestep）、引导强度（Strength）以及分类器正则化的影响。
*   **充分性评价**：实验设计较为全面，不仅对比了多种基线方法，还验证了转向向量在蒸馏模型与非蒸馏模型之间的**跨模型迁移性**。实验结果涵盖了定量指标（FID, CLIP, NudeNet 检测数）和定性视觉对比，客观性较强。

### 6. 主要结论与发现
*   **高效性**：SHIFT 在裸露内容抑制方面优于现有基线（抑制效果提升 3-4 倍），且能保持较好的图像质量。
*   **时间一致性**：研究发现 DiT 的激活空间具有显著的时间一致性，使用单一的时间无关转向向量即可在所有扩散时间步有效工作。
*   **早期关键性**：引导在扩散过程的早期阶段（靠近步数 0）最为有效，后期干预效果减弱。
*   **可迁移性**：从轻量级蒸馏模型（schnell）中提取的转向向量可以直接应用于更强大的基础模型（dev），效果依然稳健。

### 7. 亮点
*   **无需训练**：完全在推理侧实现，避免了对 12B 参数模型进行梯度下降的巨大开销。
*   **动态调节**：引入 SVM 分类器分数来实时控制引导强度，有效解决了“过度擦除”导致的非目标物体变形问题。
*   **多功能性**：一个框架同时解决了概念擦除、风格迁移和物体添加/修改等多种任务。

### 8. 不足与局限
*   **非图像编辑工具**：SHIFT 侧重于全局生成流的重定向，无法像专门的图像编辑方法那样保持背景或空间布局的严格一致性（背景会随干预发生变化）。
*   **几何对齐挑战**：在进行大幅度的领域偏移（Domain Shift）时，如何更好地解耦目标概念与全局图像结构仍是一个挑战。
*   **超参数敏感性**：虽然有动态调节，但基础引导强度 $\alpha$ 仍需针对不同任务进行一定程度的手动调优。

（完）

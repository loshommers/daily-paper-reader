---
title: Randomized Antipodal Search Done Right for Data Pareto Improvement of LLM Unlearning
title_zh: 针对大语言模型遗忘学习中数据帕累托改进的随机对跖搜索优化实现
authors: "Ziwen Liu, Huawei Lin, Yide Ran, Denghui Zhang, Jianwen Xie, Chuan Li, Weijie Zhao, Zhaozhuo Xu"
date: 2026-04-17
pdf: "https://arxiv.org/pdf/2604.16591v1"
tags: ["query:mu-ce"]
score: 10.0
evidence: 大语言模型机器遗忘中的数据检索与权衡
tldr: 针对大语言模型（LLM）遗忘任务中数据检索这一核心挑战，本文提出了数据帕累托改进的概念，并设计了RASLIK算法。该算法结合置换投影哈希与随机对跖搜索，在保证亚线性复杂度的同时，显著降低了选择方差。实验证明，RASLIK在多个模型和数据集上均优于确定性基准和真值采样，为以数据为中心的LLM遗忘提供了一种高效且可扩展的解决方案。
selection_source: fresh_fetch
motivation: 现有的LLM遗忘研究多关注参数优化，却忽略了在推理阶段如何高效检索相关的遗忘与保留数据集这一实际难题。
method: 提出RASLIK算法，通过置换投影哈希和随机对跖搜索在影响核空间内进行高效的数据检索。
result: 实验结果显示，RASLIK在遗忘质量和计算效率上均实现了双重提升，且性能超越了确定性基准和理想采样。
conclusion: 随机搜索是实现以数据为中心的LLM遗忘的一种原则性且可扩展的有效方案。
---

## 摘要
大语言模型（LLMs）有时会记忆不当知识，这些知识在部署后必须被移除。先前的机器遗忘研究主要集中在优化方法上，通过调整参数来强制执行遗忘，同时保留有用知识。然而，这些方法假设遗忘集和保留集是现成可用的，这在实践中很少成立。遗忘通常由推理时的不当生成触发，这使得相关数据的检索成为核心挑战。我们为大语言模型遗忘引入了“数据帕累托改进”的概念，形式化地说明了检索如何扩展遗忘与保留之间可实现的权衡边界。为了实现这一原则，我们提出了基于线性化影响核的随机对跖搜索（RASLIK），这是一种结合了置换投影哈希与随机对跖搜索的检索算法。RASLIK 降低了选择方差，实现了次线性复杂度，并在质量和效率上获得了双重提升。在多个模型、数据集和遗忘算法上，RASLIK 一致优于确定性基准甚至真值采样（oracle sampling），确立了随机搜索作为以数据为中心的遗忘学习中一种有原则且可扩展的解决方案。

## Abstract
Large language models (LLMs) sometimes memorize undesirable knowledge, which must be removed after deployment. Prior work on machine unlearning has focused largely on optimization methods that adjust parameters to enforce forgetting while preserving retention. However, these approaches assume that the forget and retain sets are readily available, which rarely holds in practice. Unlearning is typically triggered by an undesired generation at inference time, making the retrieval of relevant data the central challenge.   We introduce the notion of data Pareto improvement for LLM unlearning, which formalizes how retrieval can expand the achievable trade-off frontier between forgetting and retention. To realize this principle, we propose Randomized Antipodal Search on Linearized Influence Kernel (RASLIK), a retrieval algorithm that combines permutation-projection hashing with randomized antipodal search. RASLIK reduces selection variance, achieves sublinear complexity, and yields a double gain in both quality and efficiency. Across multiple models, datasets, and unlearning algorithms, RASLIK consistently outperforms deterministic baselines and even oracle sampling, establishing randomized search as a principled and scalable solution for data-centric unlearning.

---

## 论文详细总结（自动生成）

### 论文总结：针对大语言模型遗忘学习中数据帕累托改进的随机对跖搜索优化实现

#### 1. 核心问题与整体含义（研究动机和背景）
大语言模型（LLM）在训练过程中会记忆敏感或有害知识。现有的“机器遗忘”（Machine Unlearning）研究多聚焦于优化算法（如梯度上升），但这些方法通常假设“遗忘集”和“保留集”是预先给定的。
**研究动机：** 在实际应用中，遗忘需求往往由推理时的单次错误生成触发，从业者面临的是海量原始训练语料，如何高效、准确地检索出哪些数据该忘、哪些该留才是真正的瓶颈。论文提出了**数据帕累托改进（Data Pareto Improvement）**的概念，即通过优化数据检索质量，同时提升遗忘效果和通用能力的保留，从而扩展两者的权衡边界（Pareto Frontier）。

#### 2. 方法论：RASLIK 算法
论文提出了 **RASLIK**（Randomized Antipodal Search on Linearized Influence Kernel），其核心思想是在梯度空间内进行高效的随机化检索。
*   **线性化影响核：** 使用模型梯度之间的余弦相似度作为衡量数据相关性的指标。
*   **置换投影哈希（Permute-Project Hashing）：** 为了解决高维梯度（数十亿参数）计算开销巨大的问题，RASLIK 将梯度投影到低维的随机 Sketch 空间（使用 Rademacher 向量投影和固定置换），将复杂度从 $O(d)$ 降至 $O(k)$（$k \ll d$）。
*   **随机对跖搜索（Randomized Antipodal Search）：** 
    *   **遗忘集检索：** 寻找与目标生成梯度方向一致（正相关）的样本。
    *   **保留集检索：** 通过简单的“符号翻转”（Sign Flipping），在同一 Sketch 空间内寻找方向相反（负相关）的样本作为保留集。
*   **理论支撑：** 论文证明了引入受控的随机性可以平滑检索边界，减少更新方向的方差，从而使遗忘过程更稳定。

#### 3. 实验设计
*   **数据集与场景：**
    1.  **Howdy-Alpaca：** 触发词遗忘场景（由特定 Token 触发的科幻风格输出）。
    2.  **Virtual-Alpaca：** 领域知识遗忘场景（虚构世界的问答知识）。
    3.  **TOFU：** 事实属性遗忘基准测试。
*   **模型：** OLMo-2-7B、Pythia-2.8B 以及 OLMo-2-13B。
*   **对比基准（Baselines）：** 随机选择（Random）、嵌入相似度（Embedding Similarity）、BM25（文本匹配）以及真值采样（Oracle Sampling，即直接使用标注好的目标集合）。
*   **遗忘算法：** 结合了 GA GDR（梯度上升+保留集梯度下降）和 GA KLR（梯度上升+KL散度正则化）。

#### 4. 资源与算力
*   **硬件环境：** 使用了 NVIDIA **GH200** GPU（拥有 480GB HBM3 显存，其中 96GB 为可用显存），配备 64 核 ARM Neoverse-V2 CPU 和 1.5TB 系统内存。
*   **软件框架：** 基于 PyTorch 2.7.1 和 CUDA 12.8，使用 Muse-Bench 框架进行评估。
*   **效率提升：** 相比于全梯度计算，RASLIK 实现了超过 **150倍** 的检索加速（例如在 5.2万条数据上，检索时间从 6480 秒降至 42 秒）。

#### 5. 实验数量与充分性
*   **实验规模：** 涵盖了 3 种模型规模、3 个核心数据集、2 种主流遗忘算法。
*   **消融实验：** 专门设计了针对“检索随机性”的消融实验（CR-x 变体），验证了适度随机性为何优于纯确定性检索。
*   **客观性：** 实验采用了马氏距离（Mahalanobis distance）和帕累托前沿分析，不仅关注单一指标，还考虑了遗忘与保留之间的相关性，评价维度较为全面且客观。

#### 6. 主要结论与发现
*   **检索是核心：** 检索质量是遗忘学习的第一决定因素，而非单纯的优化器设计。
*   **RASLIK 表现卓越：** 在所有测试块中，RASLIK 均处于帕累托前沿，且在马氏距离（接近理想点的程度）上一致优于 BM25 和嵌入相似度。
*   **超越 Oracle：** 令人惊讶的是，RASLIK 的效果有时甚至优于 Oracle Sampling，这证明了在影响核空间中寻找“对跖”样本比单纯使用分类标签更能提供有效的优化方向。
*   **鲁棒性：** 该方法在不同规模的模型和不同类型的遗忘任务（触发词 vs 领域知识）中均表现稳健。

#### 7. 优点
*   **视角独特：** 将遗忘学习重新定义为“以数据为中心”的任务，而非单纯的参数微调。
*   **效率与质量双赢：** 通过 Sketch 技术解决了大模型梯度检索的计算难题，同时利用随机化理论提升了稳定性。
*   **理论与实践结合：** 既有严谨的方差缩减证明，又有在真实开源模型上的大规模验证。

#### 8. 不足与局限
*   **梯度依赖：** 尽管有 Sketch 优化，但仍需计算一次全量数据的梯度缓存，对于超大规模数据集（如万亿 Token）的预处理成本依然存在。
*   **LoRA 框架限制：** 实验主要在 LoRA 适配器上进行，虽然这是目前主流做法，但在全参数微调下的表现尚需进一步验证。
*   **随机性调优：** 虽然论文给出了建议阈值，但在不同分布的数据集上，如何确定最优的 Sketch 维度 $k$ 和随机化程度可能需要一定的经验调试。

（完）

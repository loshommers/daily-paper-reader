---
title: "Label Leakage Attacks in Machine Unlearning: A Parameter and Inversion-Based Approach"
title_zh: 机器遗忘中的标签泄露攻击：一种基于参数和逆向的方法
authors: "Weidong Zheng, Kongyang Chen, Yao Huang, Yuanwei Guo, Yatie Xiao"
date: 2026-04-08
pdf: "https://arxiv.org/pdf/2604.07386v1"
tags: ["query:mu-ce"]
score: 9.0
evidence: 隐私保护机器遗忘与标签泄露攻击
tldr: 本文针对机器遗忘中的标签泄露风险，提出了四种从模型参数和模型逆向角度出发的攻击方法。研究利用参数差异分析及原型样本重构技术，在白盒与黑盒场景下推断被遗忘的数据类别。通过在多个数据集和主流遗忘算法上的实验，验证了这些攻击能有效识别遗忘类别，揭示了现有遗忘技术在保护“被遗忘权”时存在的隐私隐患。
selection_source: fresh_fetch
motivation: 现有的机器遗忘技术在删除特定数据时，可能会无意中泄露被遗忘数据的类别信息，从而引发隐私安全风险。
method: 提出了基于模型参数差异的聚类分析方法，以及基于模型逆向重构类别原型的白盒与黑盒攻击技术。
result: 在四个标准数据集上针对五种主流遗忘算法的实验结果显示，所提方法能够高效且准确地推断出被遗忘的类别。
conclusion: 研究证明了机器遗忘算法普遍存在标签泄露漏洞，强调了在开发遗忘技术时加强类别隐私保护的必要性。
---

## 摘要
随着人工智能技术在人脸识别等领域的广泛应用，数据隐私安全问题受到了广泛关注，特别是众多隐私保护法律所强调的“被遗忘权”。现有技术已经提出了各种遗忘方法，但它们可能会在无意中泄露被遗忘数据的类别。本文关注类别遗忘场景，分析了多种场景下被遗忘数据类别泄露的潜在问题，并针对具有不同知识背景的攻击者，从模型参数和模型逆向的角度提出了四种攻击方法。在模型参数层面，我们通过计算目标模型参数与分别在保留数据子集和无关数据上训练的辅助模型参数之间的点积或向量差，来构建判别性特征。随后，通过 k-means 聚类、约登指数（Youden's Index）和决策树算法处理这些特征，以实现对遗忘类别的准确识别。在模型逆向领域，我们设计了基于梯度优化的白盒攻击和基于遗传算法的黑盒攻击，以重构类原型样本。随后，利用阈值准则和信息熵准则分析这些合成样本的预测分布，从而推断出遗忘类别。我们在四个标准数据集上针对五种最先进的遗忘算法评估了所提出的攻击，并详细分析了每种方法的优缺点。实验结果表明，我们的方法能够有效地推断出目标模型所遗忘的类别。

## Abstract
With the widespread application of artificial intelligence technologies in face recognition and other fields, data privacy security issues have received extensive attention, especially the \textit{right to be forgotten} emphasized by numerous privacy protection laws. Existing technologies have proposed various unlearning methods, but they may inadvertently leak the categories of unlearned data. This paper focuses on the category unlearning scenario, analyzes the potential problems of category leakage of unlearned data in multiple scenarios, and proposes four attack methods from the perspectives of model parameters and model inversion based on attackers with different knowledge backgrounds. At the level of model parameters, we construct discriminative features by computing either dot products or vector differences between the parameters of the target model and those of auxiliary models trained on subsets of retained data and unrelated data, respectively. These features are then processed via k-means clustering, Youden's Index, and decision tree algorithms to achieve accurate identification of the forgotten class. In the model inversion domain, we design a gradient optimization-based white-box attack and a genetic algorithm-based black-box attack to reconstruct class-prototypical samples. The prediction profiles of these synthesized samples are subsequently analyzed using a threshold criterion and an information entropy criterion to infer the forgotten class. We evaluate the proposed attacks on four standard datasets against five state-of-the-art unlearning algorithms, providing a detailed analysis of the strengths and limitations of each method. Experimental results demonstrate that our approach can effectively infer the classes forgotten by the target model.

---

## 论文详细总结（自动生成）

以下是对论文《Label Leakage Attacks in Machine Unlearning: A Parameter and Inversion-Based Approach》的结构化深入总结：

### 1. 论文的核心问题与整体含义（研究动机和背景）
随着隐私保护法律（如 GDPR）强调“被遗忘权”，机器遗忘（Machine Unlearning, MU）技术应运而生，旨在从已训练模型中彻底擦除特定数据的影响。然而，遗忘过程本身可能成为新的隐私泄露源。
*   **核心问题**：现有的隐私攻击（如成员推理攻击）通常需要访问遗忘前后的两个模型，这在实际中很难实现（旧模型通常会被立即覆盖）。
*   **研究动机**：本文探讨在**仅能访问遗忘后模型**的严苛条件下，攻击者能否推断出“哪一类数据被遗忘了”。这种标签泄露会暴露企业的商业机密或用户的敏感属性，使“被遗忘权”失效。

### 2. 论文提出的方法论
论文从**模型参数**和**模型逆向**两个维度提出了四种攻击方案：

*   **基于模型参数的攻击（白盒）**：
    *   **核心思想**：模型参数反映了训练数据的统计分布。通过比较目标模型与辅助模型（在少量保留数据或无关数据上训练）的参数差异来识别遗忘类。
    *   **技术细节**：
        *   **点积相似度**：计算目标模型全连接层参数与辅助模型参数的点积。理论证明，保留类的参数相似度显著高于遗忘类。利用**约登指数（Youden’s Index）**或 **k-means** 确定分类阈值。
        *   **向量差值**：计算参数间的差异向量，利用**决策树**对差异特征进行分类，以识别遗忘类。
*   **基于模型逆向的攻击（白盒/黑盒）**：
    *   **核心思想**：尝试为每个类别重构“原型样本”。如果某类被遗忘，模型对该类重构样本的预测置信度会显著下降，且不确定性（熵）增加。
    *   **技术细节**：
        *   **白盒（梯度优化）**：通过最小化交叉熵损失和正则项（TV正则、L2正则），利用梯度下降反向生成样本。
        *   **黑盒（遗传算法）**：在无法获取梯度时，利用遗传算法（选择、交叉、变异、精英保留策略）在输入空间搜索能使模型输出特定类概率最大的样本。
        *   **筛选准则**：采用**阈值准则**（最大预测概率低于动态阈值）或**熵准则**（预测分布的熵值较高）来判定遗忘类。

### 3. 实验设计
*   **数据集**：MNIST、Fashion-MNIST、SVHN、CIFAR-10。
*   **模型架构**：LeNet（用于简单数据集）、ResNet18（用于复杂数据集）。
*   **对比的遗忘算法（Benchmark）**：
    1.  **Re-Train (RT)**：从头训练（性能上限）。
    2.  **Fine-Tune (FT)**：利用剩余数据微调。
    3.  **Random Label (RL)**：将遗忘数据标记为随机标签后训练。
    4.  **Amnesiac Unlearn (AU)**：基于参数贡献抹除。
    5.  **Negative Gradient (NG)**：对遗忘数据执行梯度上升。
*   **评估指标**：攻击成功率（ASR）。

### 4. 资源与算力
*   **算力说明**：论文**未明确说明**具体的硬件配置（如 GPU 型号、数量）及具体的训练时长。但考虑到涉及 ResNet18 的多次重训练和遗传算法的迭代搜索，实验具有一定的计算开销。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 4 个标准数据集、5 种主流遗忘算法、单类别遗忘与多类别（3类）遗忘场景。
*   **充分性与客观性**：
    *   实验对比了白盒与黑盒两种攻击背景，考虑了攻击者拥有少量数据和零数据两种极端情况。
    *   进行了**消融实验**，分析了遗忘类别数量（从1到9）对攻击成功率的影响。
    *   使用了统计平均值来评估 ASR，实验设计较为全面、客观，能够反映不同遗忘算法的共性漏洞。

### 6. 论文的主要结论与发现
*   **攻击有效性**：所提方法在多数场景下 ASR 超过 70%-90%，远超随机猜测水平，证明机器遗忘普遍存在标签泄露风险。
*   **参数 vs 逆向**：基于模型参数的攻击（尤其是决策树方法）在多类别遗忘场景下表现更**稳健**；而基于模型逆向的攻击在遗忘类别增多时，成功率会迅速下降。
*   **算法差异**：Negative Gradient (NG) 算法在某些场景下对参数攻击表现出较强的抵抗力，但仍难逃脱逆向攻击。

### 7. 优点
*   **场景真实**：针对“仅有遗忘后模型”的受限场景，比需要双模型的攻击更具现实威胁。
*   **理论支撑**：为参数相似度攻击提供了严谨的数学推导（基于 Hessian 矩阵和正交性假设）。
*   **黑盒突破**：引入遗传算法解决了无法获取梯度时的标签推理问题，扩大了攻击适用范围。

### 8. 不足与局限
*   **理论假设较强**：参数攻击的数学证明依赖于特征白化、Hessian 矩阵正定等理想假设，在极深或非线性极强的网络中可能存在偏差。
*   **黑盒效率**：基于遗传算法的黑盒攻击需要大量查询模型，且在复杂高维输入（如高分辨率图像）下的收敛速度和效果可能受限。
*   **防御缺失**：论文主要侧重于“破”，未深入探讨如何针对性地“立”（即如何设计能抵御此类攻击的防御性遗忘算法）。

（完）

---
title: "$\\oslash$ Source Models Leak What They Shouldn't $\\nrightarrow$: Unlearning Zero-Shot Transfer in Domain Adaptation Through Adversarial Optimization"
title_zh: $\oslash$ 源模型泄露了不应泄露的信息 $\nrightarrow$：通过对抗优化在领域自适应中实现零样本迁移的机器遗忘
authors: "Arnav Devalapally, Poornima Jain, Kartik Srinivas, Vineeth N. Balasubramanian"
date: 2026-04-09
pdf: "https://arxiv.org/pdf/2604.08238v1"
tags: ["query:mu-ce"]
score: 10.0
evidence: 遗忘源域特定信息以保护隐私
tldr: 本文揭示了无源域自适应（SFDA）中存在的隐私风险，即模型会无意中将源域特有类别的知识迁移至目标域。针对这一问题，作者提出了SCADA-UL任务，旨在从自适应模型中遗忘这些敏感类别。通过引入对抗性样本生成、重缩放标签策略和对抗优化，该方法在域自适应过程中有效消除了特定类别的记忆。实验证明，该方法在多个基准上达到了接近重新训练的遗忘效果，并能扩展至持续学习等复杂场景。
selection_source: fresh_fetch
motivation: 现有的无源域自适应方法在目标域上对源域特有类别表现出强零样本性能，导致敏感源域信息的隐私泄露。
method: 提出一种利用对抗性生成的遗忘类样本，并结合重缩放标签策略与对抗优化在自适应过程中进行机器遗忘的方法。
result: 该方法在SCADA-UL设置下一致优于现有基准，且在多个基准数据集上实现了与重新训练相当的遗忘性能。
conclusion: 本研究通过对抗优化有效地解决了域自适应中的源域类别泄露问题，为跨域模型隐私保护提供了新思路。
---

## 摘要
视觉模型在卫星图像和医学扫描等跨领域中的应用日益广泛，这引发了一个新兴的隐私风险：模型可能会在目标领域中无意中保留并泄露敏感的源领域特定信息。这为机器遗忘（Machine Unlearning）提供了一个极具说服力的应用场景，以保护敏感源领域数据的隐私。在各种自适应技术中，无源领域自适应（SFDA）对机器遗忘（MU）有着迫切的需求，因为在这种情况下，源数据本身虽然受到保护，但在自适应过程中暴露的源模型却编码了其影响。我们的实验表明，现有的 SFDA 方法在目标领域中对源领域特有类别表现出强大的零样本性能，这表明它们无意中将这些类别的知识泄露到了目标领域，即使这些类别在目标数据中并未出现。我们通过提出一种名为 SCADA-UL（领域自适应中源领域特有类别的遗忘）的机器遗忘设置，识别并解决了这一风险。现有的机器遗忘方法并未解决这一设置，因为它们并非为处理数据分布偏移而设计的。我们提出了一种新的遗忘方法，通过一种新颖的重缩放标签策略和对抗优化，使模型在领域自适应过程中遗忘对抗生成的遗忘类样本。我们还将研究扩展到了两个变体：该问题设置的持续版本，以及待遗忘的特定源类别可能未知的版本。除了理论解释外，我们全面的实证结果表明，我们的方法在所提出的设置中始终优于基准方法，同时在基准数据集上达到了重训练级别的遗忘性能。我们的代码可在 https://github.com/D-Arnav/SCADA 获取。

## Abstract
The increasing adaptation of vision models across domains, such as satellite imagery and medical scans, has raised an emerging privacy risk: models may inadvertently retain and leak sensitive source-domain specific information in the target domain. This creates a compelling use case for machine unlearning to protect the privacy of sensitive source-domain data. Among adaptation techniques, source-free domain adaptation (SFDA) calls for an urgent need for machine unlearning (MU), where the source data itself is protected, yet the source model exposed during adaptation encodes its influence. Our experiments reveal that existing SFDA methods exhibit strong zero-shot performance on source-exclusive classes in the target domain, indicating they inadvertently leak knowledge of these classes into the target domain, even when they are not represented in the target data. We identify and address this risk by proposing an MU setting called SCADA-UL: Unlearning Source-exclusive ClAsses in Domain Adaptation. Existing MU methods do not address this setting as they are not designed to handle data distribution shifts. We propose a new unlearning method, where an adversarially generated forget class sample is unlearned by the model during the domain adaptation process using a novel rescaled labeling strategy and adversarial optimization. We also extend our study to two variants: a continual version of this problem setting and to one where the specific source classes to be forgotten may be unknown. Alongside theoretical interpretations, our comprehensive empirical results show that our method consistently outperforms baselines in the proposed setting while achieving retraining-level unlearning performance on benchmark datasets. Our code is available at https://github.com/D-Arnav/SCADA

---

## 论文详细总结（自动生成）

这是一份关于论文《$\oslash$ Source Models Leak What They Shouldn’t $\nrightarrow$: Unlearning Zero-Shot Transfer in Domain Adaptation Through Adversarial Optimization》的深度结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **研究动机**：随着视觉模型在敏感领域（如医疗、卫星遥感）的跨域迁移，存在严重的隐私泄露风险。即使在**无源领域自适应（SFDA）**中，源数据不可见，但源模型仍会无意中将源域特有的敏感类别知识（如军事设施、特定病史）迁移到目标域。
*   **核心问题**：现有的自适应方法在目标域上对源域特有类别表现出极强的“零样本迁移”能力，这在隐私保护视角下属于非法泄露。
*   **研究目标**：提出一种名为 **SCADA-UL**（领域自适应中源域特有类别的遗忘）的新设置，要求模型在迁移到新域的同时，彻底遗忘源域中特定的敏感类别。

### 2. 方法论：核心思想与技术细节
*   **核心思想**：将遗忘过程与领域自适应过程**同步进行**。通过对抗优化生成代表遗忘类的伪样本，并利用一种新颖的标签策略引导模型消除这些类别的表征。
*   **关键技术步骤**：
    1.  **对抗样本生成**：在输入空间优化生成对抗样本 $\hat{x}$，使其在当前模型下的预测指向待遗忘类别 $c_F$，从而在无源数据的情况下模拟遗忘类的数据分布。
    2.  **重缩放标签策略（Rescaled Labeling）**：这是本文的核心创新。不同于传统的均匀分布或随机标签，该策略将遗忘类的概率根据模型当前的输出比例重新分配给所有保留类（Retain Classes）。公式上，它保留了模型对保留类的相对置信度，从而最小化了遗忘任务与自适应任务之间的冲突。
    3.  **联合优化目标**：构建一个包含自适应损失（$L_{SFDA}$）和机器遗忘损失（$L_{MU}$）的联合函数。通过交替优化模型参数 $w_T$ 和对抗样本 $\hat{x}$，实现“边迁移边遗忘”。
*   **扩展场景**：
    *   **UC-SCADA-UL**：针对遗忘类身份未知的情况，利用类别相关性项 $\gamma$ 自动识别并遗忘目标域中不活跃的源类。
    *   **C-SCADA-UL**：处理持续到达的多个遗忘请求。

### 3. 实验设计
*   **数据集与场景**：
    *   **基准数据集**：OfficeHome, Office31, DomainNet-126。
    *   **真实世界场景**：医疗影像（CheXpert $\rightarrow$ NIH Chest X-ray）和土地利用遥感图像（UCMerced $\rightarrow$ RSSCN7）。
*   **对比方法（Baselines）**：共对比了10种方法，包括原始SFDA方法（SF(DA)²）、重训练（Retrain，金标准）、微调、以及多种先进的机器遗忘方法（UNSIR, ZSMU, Nabla Tau等）和部分域自适应方法（PADA, SHOT）。
*   **评价指标**：目标域保留类准确率（$A_{DT}^r$）、目标域遗忘类准确率（$A_{DT}^f$）、遗忘得分（Unlearn Score）以及成员推理攻击（MIA）防御成功率。

### 4. 资源与算力
*   **硬件环境**：实验运行在 Linux 集群上，每项实验使用单张 **NVIDIA Tesla V100-SXM3 (32GB)** GPU。
*   **算力消耗**：单次实验的运行时间在 **300 到 1500 秒**之间（约5-25分钟）。
*   **并行设置**：使用了 20 个 CPU 核心进行数据处理。

### 5. 实验数量与充分性
*   **实验规模**：
    *   涵盖了单类别遗忘、多类别遗忘、未知类别遗忘和持续遗忘四种设置。
    *   在 DomainNet 等大规模数据集上进行了 7 个子任务的详尽测试。
    *   进行了关于损失权重 $\alpha$、对抗样本数量、初始化策略、标签策略的**消融实验**。
*   **充分性评价**：实验设计非常充分。作者不仅在标准基准上验证，还引入了医疗和遥感这两个对隐私极度敏感的真实场景。每项实验均运行 3 次并报告均值和标准差，确保了结果的客观性和公平性。

### 6. 主要结论与发现
*   **泄露证实**：证实了现有 SFDA 方法确实会严重泄露源域特有类别的知识（遗忘类准确率通常远高于0）。
*   **性能卓越**：所提方法在保持目标域保留类高准确率的同时，能将遗忘类的准确率降至 **0%**，表现接近甚至在某些指标上超过了耗时的“重新训练”方案。
*   **防御有效**：在成员推理攻击（MIA）测试中，该方法显著降低了攻击者的识别率，证明了其在参数层面抹除了敏感信息。

### 7. 优点与亮点
*   **问题定义新颖**：首次正式定义了领域自适应中的源类遗忘问题（SCADA-UL），具有极强的现实意义。
*   **标签策略巧妙**：提出的重缩放标签策略（Rescaled Labeling）通过数学证明（定理1）展示了其能诱导更合理的梯度流，有效解决了遗忘与迁移任务的冲突。
*   **理论与实证结合**：不仅有直观的算法设计，还提供了收敛性分析和梯度流强度的理论解释。

### 8. 不足与局限
*   **任务局限性**：目前仅在图像分类任务上进行了验证，尚未扩展到语义分割、目标检测或生成式模型等更复杂的领域自适应任务。
*   **未知类识别风险**：在 UC-SCADA-UL 设置下，依赖 $\gamma$ 参数识别遗忘类。在类别空间极大的数据集（如完整的 DomainNet）上，识别准确性可能会受噪声影响。
*   **数学保证**：虽然实验效果极佳，但对于“完全遗忘”在数学上尚缺乏绝对的硬性保证（这也是目前近似遗忘算法的通病）。

（完）

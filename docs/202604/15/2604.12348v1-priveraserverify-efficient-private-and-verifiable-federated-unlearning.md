---
title: "PrivEraserVerify: Efficient, Private, and Verifiable Federated Unlearning"
title_zh: PrivEraserVerify：高效、隐私且可验证的联邦遗忘学习
authors: "Parthaw Goswami, Md Khairul Islam, Ashfak Yeafi"
date: 2026-04-14
pdf: "https://arxiv.org/pdf/2604.12348v1"
tags: ["query:mu-ce"]
score: 10.0
evidence: 面向被遗忘权的联邦遗忘学习
tldr: 针对联邦学习中用户“被遗忘权”的需求，本文提出PrivEraserVerify (PEV)框架，旨在解决现有方案在效率、隐私保护和可验证性方面无法兼顾的问题。PEV通过自适应检查点加速模型重构，利用层自适应差分隐私校准消除客户端影响并维持精度，并引入基于指纹的去中心化验证机制。实验证明，PEV在保证隐私和可验证性的同时，遗忘效率比重新训练提升2-3倍，是首个同时实现这三大特性的联邦遗忘学习框架。
selection_source: fresh_fetch
motivation: 现有的联邦遗忘学习方案在效率、差分隐私保护和结果可验证性之间存在权衡，难以同时满足法规要求和实际部署需求。
method: 该框架集成了自适应检查点保留历史更新、层自适应差分隐私校准消除影响以及基于指纹的非侵入式验证机制。
result: 实验显示PEV的遗忘速度比重新训练快2至3倍，在提供形式化隐私保证的同时显著降低了精度损失，并支持可扩展的验证。
conclusion: PEV是首个集高效性、隐私性和可验证性于一体的联邦遗忘学习框架，为符合隐私法规的联邦学习部署提供了可行方案。
---

## 摘要
联邦学习 (FL) 能够在不共享原始数据的情况下实现协作模型训练，为实现隐私保护的人工智能提供了一条充满前景的路径。然而，FL 模型仍可能记忆参与者的敏感信息，这与“被遗忘权” (RTBF) 相冲突。为了满足这些要求，联邦遗忘学习作为一种移除退出客户端贡献的机制应运而生。现有的解决方案仅部分解决了这一挑战：FedEraser 提高了效率但缺乏隐私保护，FedRecovery 确保了差分隐私 (DP) 但降低了准确率，而 VeriFi 实现了可验证性但引入了额外开销，且缺乏效率或隐私保证。我们提出了 PrivEraserVerify (PEV)，这是一个将高效性、隐私性和可验证性集成到联邦遗忘学习中的统一框架。PEV 采用了：(i) 自适应检查点机制，以保留关键的历史更新用于快速重构；(ii) 层自适应差分隐私校准，在最小化准确率损失的同时选择性地移除客户端影响；以及 (iii) 基于指纹的验证，使参与者能够以去中心化且非侵入性的方式确认遗忘学习的效果。在图像、手写字符和医疗数据集上的实验表明，PEV 的遗忘学习速度比重新训练快 2 到 3 倍，在减少性能退化的同时提供了形式化的不可区分性保证，并支持可扩展的验证。据我们所知，PEV 是首个同时为联邦遗忘学习提供高效性、隐私性和可验证性的框架，使联邦学习更接近实际应用和符合监管要求的部署。

## Abstract
Federated learning (FL) enables collaborative model training without sharing raw data, offering a promising path toward privacy preserving artificial intelligence. However, FL models may still memorize sensitive information from participants, conflicting with the right to be forgotten (RTBF). To meet these requirements, federated unlearning has emerged as a mechanism to remove the contribution of departing clients. Existing solutions only partially address this challenge: FedEraser improves efficiency but lacks privacy protection, FedRecovery ensures differential privacy (DP) but degrades accuracy, and VeriFi enables verifiability but introduces overhead without efficiency or privacy guarantees. We present PrivEraserVerify (PEV), a unified framework that integrates efficiency, privacy, and verifiability into federated unlearning. PEV employs (i) adaptive checkpointing to retain critical historical updates for fast reconstruction, (ii) layer adaptive differentially private calibration to selectively remove client influence while minimizing accuracy loss, and (iii) fingerprint based verification, enabling participants to confirm unlearning in a decentralized and noninvasive manner. Experiments on image, handwritten character, and medical datasets show that PEV achieves up to 2 to 3 times faster unlearning than retraining, provides formal indistinguishability guarantees with reduced performance degradation, and supports scalable verification. To the best of our knowledge, PEV is the first framework to simultaneously deliver efficiency, privacy, and verifiability for federated unlearning, moving FL closer to practical and regulation compliant deployment.

---

## 论文详细总结（自动生成）

这是一份关于论文《PrivEraserVerify: Efficient, Private, and Verifiable Federated Unlearning》的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：在联邦学习（FL）中，模型会“记忆”参与者的敏感数据。随着《通用数据保护条例》（GDPR）等法规的出台，用户拥有“被遗忘权”（RTBF），要求从已训练的模型中移除其贡献。
*   **研究动机**：现有的联邦遗忘学习方案存在“三选一”的困境：
    *   **FedEraser**：效率高，但缺乏形式化的隐私保证。
    *   **FedRecovery**：提供差分隐私（DP），但由于全局注入噪声导致模型准确率大幅下降。
    *   **VeriFi**：引入了可验证性，但计算开销大，且不具备效率和隐私优势。
*   **目标**：提出一个名为 **PrivEraserVerify (PEV)** 的统一框架，同时实现**高效性（Efficiency）**、**隐私性（Privacy）**和**可验证性（Verifiability）**。

### 2. 方法论与核心技术
PEV 框架由三个核心模块组成：
*   **自适应检查点（Adaptive Checkpointing）**：
    *   **核心思想**：为了避免重新训练，服务器在训练期间选择性地存储压缩的更新检查点。
    *   **技术细节**：基于**梯度方差（Gradient Variance）**作为触发阈值。只有当更新包含显著信息（方差超过阈值 $\theta$）时才记录，从而在存储开销和重构精度之间取得平衡。
*   **层自适应差分隐私校准（Layer-Adaptive DP Calibration）**：
    *   **核心思想**：在移除目标客户端更新后，通过注入高斯噪声确保遗忘后的模型与重新训练的模型在统计上不可区分。
    *   **技术细节**：不同于传统方法全局注入等量噪声，PEV 进行层级敏感度分析。对梯度波动大的敏感层注入较多噪声，对稳定层注入较少噪声，从而在保证隐私的同时最大化保留模型效用。
*   **基于指纹的验证（Fingerprint-Based Verification）**：
    *   **核心思想**：提供一种非侵入式的“验证权”（RTV）。
    *   **技术细节**：客户端在初始训练时嵌入微小的**梯度指纹（Gradient Fingerprints）**。遗忘学习完成后，客户端通过查询模型。如果指纹信号低于预设阈值 $\delta$，则验证遗忘成功。

### 3. 实验设计
*   **数据集**：
    *   **CIFAR-10**：通用图像分类基准。
    *   **FEMNIST**：模拟非独立同分布（Non-IID）特性的手写字符识别。
    *   **Chest X-ray**：医疗影像数据集，代表对隐私极度敏感的真实场景。
*   **对比基准（Benchmarks）**：
    *   **Retraining**：从头开始重新训练（作为准确率和遗忘效果的金标准）。
    *   **FedEraser**：代表高效遗忘方案。
    *   **FedRecovery**：代表差分隐私遗忘方案。
    *   **VeriFi**：代表可验证遗忘方案。

### 4. 资源与算力
*   **软件环境**：基于 PyTorch 实现。
*   **实验规模**：模拟 100 个客户端，每轮随机选择 10 个进行训练，总计 200 个通信轮次。
*   **算力说明**：论文**未明确说明**具体的 GPU 型号、数量或总训练时长。但提供了遗忘过程的运行时间对比（例如，在医疗数据集上 PEV 耗时约 1317 秒，而重新训练需 2706 秒）。

### 5. 实验数量与充分性
*   **实验设置**：涵盖了图像、文本（手写）和医疗三个领域，具有较好的泛化性测试。
*   **消融与对比**：
    *   对比了不同噪声水平（$\sigma=0.2, 0.5, 0.8$）下的准确率下降情况。
    *   对比了不同数据集下的遗忘耗时。
    *   验证了指纹识别在遗忘前后的数值变化。
*   **评价**：实验设计较为客观，通过与三种不同侧重点的基准方法对比，证明了其综合性能的优越性。

### 6. 主要结论与发现
*   **效率提升**：PEV 的遗忘速度比完全重新训练快 **2-3 倍**，且优于 FedEraser。
*   **隐私与效用平衡**：相比于 FedRecovery，PEV 的准确率损失减少了超过一半（平均下降 3.1% vs 6.2%），这归功于层自适应的噪声策略。
*   **验证有效性**：指纹机制能够可靠地检测客户端贡献是否已被移除，且不会干扰正常训练。

### 7. 优点
*   **多目标优化**：首次在单一框架内解决了联邦遗忘学习中效率、隐私和验证的冲突。
*   **自适应机制**：利用梯度方差动态调整检查点和隐私噪声，比固定步长或全局噪声的方法更智能。
*   **非侵入性**：验证机制不需要复杂的密码学证明或破坏性的后门植入。

### 8. 不足与局限
*   **硬件细节缺失**：缺乏具体的算力消耗记录，难以评估其在边缘设备上的部署成本。
*   **多客户端遗忘**：实验主要集中在移除单个客户端，对于大规模、高并发的遗忘请求处理能力尚待验证。
*   **指纹冲突风险**：在客户端数量极多时，不同客户端的梯度指纹是否存在碰撞（Collision）风险，文中未做深入探讨。

（完）

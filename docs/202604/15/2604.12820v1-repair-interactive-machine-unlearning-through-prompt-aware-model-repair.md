---
title: "RePAIR: Interactive Machine Unlearning through Prompt-Aware Model Repair"
title_zh: RePAIR：通过提示感知的模型修复实现交互式机器遗忘
authors: "Jagadeesh Rachapudi, Pranav Singh, Ritali Vatsi, Praful Hambarde, Amit Shukla"
date: 2026-04-14
pdf: "https://arxiv.org/pdf/2604.12820v1"
tags: ["query:mu-ce"]
score: 10.0
evidence: 通过提示感知模型修复进行交互式机器遗忘
tldr: 本文针对大语言模型难以由用户直接控制知识擦除的问题，提出了交互式机器遗忘（IMU）范式及RePAIR框架。该框架允许用户通过自然语言指令在推理阶段实时修复模型，其核心是名为STAMP的无训练单样本遗忘算法，通过伪逆矩阵更新将MLP激活重定向至拒绝子空间。RePAIR不仅实现了极高的遗忘率和实用性保持，还通过低秩优化显著提升了端侧运行效率，为透明且用户驱动的模型编辑提供了新方案。
selection_source: fresh_fetch
motivation: 现有的机器遗忘方法通常由模型提供商主导且依赖复杂的重训练，导致终端用户无法自主、便捷地删除模型中的特定有害信息或个人数据。
method: 提出RePAIR框架，包含意图检测、修复生成及参数更新三个模块，并利用STAMP算法通过闭式解伪逆更新实现高效的激活重定向。
result: 实验证明RePAIR在有害知识抑制和隐私擦除等任务上达到了近乎零的遗忘得分，且推理速度比基于训练的基准方法快约3倍。
conclusion: RePAIR为用户驱动的模型编辑建立了有效且实用的框架，推动了透明、高效的端侧知识控制技术的发展。
---

## 摘要
大语言模型（LLMs）在大规模网络语料库的预训练过程中，不可避免地吸收了有害知识、错误信息和个人数据，且缺乏原生的选择性删除机制。虽然机器遗忘（Machine Unlearning）提供了一种原则性的解决方案，但现有方法多以提供商为中心，需要重新训练流水线、精选的保留数据集以及模型服务提供商（MSPs）的直接干预，从而将终端用户排除在自身数据的控制之外。我们引入了交互式机器遗忘（IMU），这是一种全新的范式，用户可以在推理阶段通过自然语言指令让 LLMs 遗忘特定知识。为了实现 IMU，我们提出了 RePAIR，这是一个提示感知的模型修复框架，包含：(i) 用于遗忘意图检测的监视器（watchdog）模型，(ii) 用于生成修复程序的医生（surgeon）模型，以及 (iii) 参数自主更新的患者（patient）模型。作为 RePAIR 的核心，我们开发了通过伪逆激活操纵进行引导（STAMP）的方法，这是一种无需训练的单样本遗忘方法，通过闭式解伪逆更新将 MLP 激活重定向至拒绝子空间。其低秩变体将计算复杂度从 O(d^3) 降低至 O(r^3 + r^2 * d)，实现了高效的设备端遗忘，速度比基于训练的基准方法快达 3 倍。在有害知识抑制、错误信息纠正和个人数据擦除方面的广泛实验表明，RePAIR 在保持模型效用（Acc_r 高达 84.47，R-RL 高达 0.88）的同时，实现了接近零的遗忘得分（Acc_f = 0.00, F-RL = 0.00），优于六种最先进的基准方法。这些结果证明了 RePAIR 是一个有效且实用的用户驱动模型编辑框架，推进了对已学习知识的透明化和设备端控制，并具有扩展至多模态基础模型的潜力。

## Abstract
Large language models (LLMs) inherently absorb harmful knowledge, misinformation, and personal data during pretraining on large-scale web corpora, with no native mechanism for selective removal. While machine unlearning offers a principled solution, existing approaches are provider-centric, requiring retraining pipelines, curated retain datasets, and direct intervention by model service providers (MSPs), thereby excluding end users from controlling their own data. We introduce Interactive Machine Unlearning (IMU), a new paradigm in which users can instruct LLMs to forget targeted knowledge through natural language at inference time. To realize IMU, we propose RePAIR, a prompt-aware model repair framework comprising (i) a watchdog model for unlearning intent detection, (ii) a surgeon model for generating repair procedures, and (iii) a patient model whose parameters are updated autonomously. At the core of RePAIR, we develop Steering Through Activation Manipulation with PseudoInverse (STAMP), a training-free, single-sample unlearning method that redirects MLP activations toward a refusal subspace via closed-form pseudoinverse updates. Its low-rank variant reduces computational complexity from O(d^3) to O(r^3 + r^2 * d), enabling efficient on-device unlearning with up to ~3x speedup over training-based baselines. Extensive experiments across harmful knowledge suppression, misinformation correction, and personal data erasure demonstrate that RePAIR achieves near-zero forget scores (Acc_f = 0.00, F-RL = 0.00) while preserving model utility (Acc_r up to 84.47, R-RL up to 0.88), outperforming six state-of-the-art baselines. These results establish RePAIR as an effective and practical framework for user-driven model editing, advancing transparent and on-device control over learned knowledge, with potential extensions to multimodal foundation models.

---

## 论文详细总结（自动生成）

### RePAIR：通过提示感知的模型修复实现交互式机器遗忘

#### 1. 核心问题与整体含义
*   **研究动机**：大语言模型（LLMs）在预训练中会吸收有害知识、错误信息和隐私数据。现有的“机器遗忘”（Machine Unlearning）方法主要面向模型提供商，依赖复杂的重训练流水线和精选数据集，普通用户无法自主控制模型删除特定信息。
*   **核心问题**：如何让终端用户在推理阶段，仅通过自然语言指令（如“忘记关于我的个人信息”），就能让模型实时、自主地擦除特定知识，而无需接触底层训练代码或依赖服务商干预？
*   **整体含义**：论文提出了**交互式机器遗忘（IMU）**新范式，将遗忘过程从“提供商中心”转向“用户中心”，实现了透明且高效的端侧模型编辑。

#### 2. 方法论
*   **RePAIR 框架**：由三个子模型组成：
    1.  **监视器（Watchdog）**：实时监测对话，识别用户的遗忘意图，并提取需要遗忘的“提示-响应”对。
    2.  **医生（Surgeon）**：根据提取的信息生成 Python 修复代码。
    3.  **患者（Patient）**：即目标 LLM，其参数根据生成的代码进行自主更新。
*   **核心算法 STAMP**：
    *   **核心思想**：通过操纵 MLP 层的激活值，将涉及遗忘知识的激活向量重定向到“拒绝回答”的子空间。
    *   **技术细节**：这是一种**无需训练（Training-free）**的方法。它计算一个“引导向量”（Steering Vector），该向量代表了从遗忘样本激活到拒绝样本激活的方向。
    *   **数学实现**：利用**闭式解伪逆更新（Moore-Penrose Pseudoinverse）**。通过公式 $W_{new} = X^+ O'$ 直接更新 MLP 的权重矩阵（$W_{gate}, W_{up}, W_{down}$），其中 $X$ 是输入激活，$O'$ 是期望的重定向输出。
    *   **低秩优化（STAMP-LR）**：为了降低计算复杂度，引入低秩近似，将复杂度从 $O(d^3)$ 降低到 $O(r^3 + r^2d)$，显著提升了在端侧设备上的运行速度。

#### 3. 实验设计
*   **场景与数据集**：
    1.  **有害知识抑制**：使用 WMDP-Bio 数据集（生物安全相关）。
    2.  **错误信息纠正**：使用 MMLU 数据集的子集（人为修改正确答案为错误信息）。
    3.  **个人数据擦除**：使用合成的生物特征配置文件（2K 条）。
*   **基准方法（Baselines）**：对比了 6 种最先进的方法，包括 GA（梯度上升）、NPO、RMU、FLAT、WGA 和 ASU。
*   **评估指标**：遗忘准确率（$Acc_f$）、保留准确率（$Acc_r$）、ROUGE-L 分数、模型效用（TinyStories 上的困惑度 PPL）以及运行效率（RTE）。

#### 4. 资源与算力
*   **算力说明**：论文未明确给出具体的 GPU 型号和数量（如 A100 或 H100），但详细记录了**运行耗时**。
*   **训练/运行时间**：STAMP-LR 在各项任务上的运行时间在 2.57 到 4.25 分钟之间，相比基于训练的基准方法（通常需 10 分钟以上）实现了约 **3 倍的加速**。

#### 5. 实验数量与充分性
*   **实验规模**：涵盖了三大类遗忘任务，每类任务包含 1K-2K 个样本。
*   **消融实验**：
    *   **层级分析**：测试了不同层对遗忘效果的影响，发现干预第 7 层效果最佳。
    *   **秩（Rank）分析**：测试了不同 $r$ 值对 STAMP-LR 性能的影响。
    *   **保留比例分析**：验证了在仅保留 10% 数据的情况下算法的稳定性。
    *   **单样本遗忘分析**：专门验证了在仅有一个遗忘样本时的表现。
*   **充分性评价**：实验设计较为全面，不仅对比了算法性能，还验证了整个 RePAIR 自动化流水线的有效性（如代码生成成功率、意图检测率等），结果具有较强的说服力。

#### 6. 主要结论与发现
*   **高效遗忘**：STAMP 及其低秩变体在所有任务中均实现了接近零的遗忘得分（$Acc_f = 0.00$），即完全擦除了目标知识。
*   **效用保持**：在遗忘的同时，模型对无关知识的保留能力（$Acc_r$）和语言生成的连贯性（PPL）优于大多数基于梯度的遗忘方法。
*   **单样本优势**：在单样本遗忘测试中，传统基于训练的方法全部失效（$Acc_f$ 仍为 100%），而 STAMP 依然能完美遗忘。

#### 7. 优点
*   **用户友好**：首个支持通过自然语言交互实现实时遗忘的框架。
*   **无需训练**：采用闭式解更新，无需反向传播和梯度计算，极大地降低了算力门槛。
*   **端侧潜力**：低秩优化和极短的运行时间使其非常适合部署在个人设备上。
*   **透明度高**：模型在遗忘后会向用户提供明确的反馈，符合隐私法规（如 GDPR）的要求。

#### 8. 不足与局限
*   **依赖保留缓冲区**：虽然比例降至 10%，但仍需存储少量保留数据以防止灾难性遗忘，这在极端隐私敏感场景下可能仍存风险。
*   **计算开销**：虽然比训练快，但在推理阶段花费数分钟进行模型修复对于某些实时应用场景来说可能仍然偏慢。
*   **结构限制**：目前主要针对 MLP 层进行修改，尚未探讨对 Attention 权重或其他架构组件的直接干预效果。
*   **多模态扩展**：目前仅在纯文本 LLM 上验证，尚未扩展到多模态模型。

（完）

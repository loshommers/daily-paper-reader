---
title: "BID-LoRA: A Parameter-Efficient Framework for Continual Learning and Unlearning"
title_zh: BID-LoRA：一种用于持续学习与遗忘学习的参数高效框架
authors: "Jagadeesh Rachapudi, Ritali Vatsi, Praful Hambarde, Amit Shukla"
date: 2026-04-14
pdf: "https://arxiv.org/pdf/2604.12686v1"
tags: ["query:mu-ce"]
score: 10.0
evidence: 形式化持续学习与遗忘（CLU）以精确删除无用知识
tldr: "本研究针对深度学习中持续学习（CL）与机器遗忘（MU）结合时出现的知识泄露问题，提出了“持续学习遗忘”（CLU）统一范式。为此开发了BID-LoRA框架，通过在注意力层引入保留、新增和遗忘三个专用适配器路径，并结合逃逸遗忘机制，仅需更新5%的参数即可实现精准的知识删除与高效的新知识集成。实验证明该方法在CIFAR-100和人脸识别任务中均优于现有基准，有效解决了多轮循环中的知识退化问题。"
selection_source: fresh_fetch
motivation: 旨在解决现有持续学习与机器遗忘方法简单叠加时，因知识泄露导致的基础知识随循环周期逐渐退化的问题。
method: 提出BID-LoRA框架，通过在注意力层部署保留、新增、遗忘三向适配器路径，并利用逃逸遗忘机制将遗忘类嵌入推离保留知识。
result: "实验显示BID-LoRA仅需更新5%的参数，在CIFAR-100和人脸识别数据集上的表现均优于现有的CLU基准方法。"
conclusion: BID-LoRA为需要频繁更新和删除知识的实际应用提供了一个参数高效且能有效抑制知识泄露的统一解决方案。
---

## 摘要
深度学习的最新进展强调了对系统的需求，这些系统不仅能通过持续学习（CL）获取新知识，还能通过机器遗忘（MU）移除过时、敏感或私密的信息。然而，尽管 CL 方法已较为成熟，但 MU 技术仍处于早期阶段，这为依赖这两种能力的统一框架留下了关键空白。我们发现，简单地结合现有的 CL 和 MU 方法会导致知识泄漏，即基础知识在重复的适应周期中逐渐退化。为了解决这一问题，我们将“持续学习遗忘”（CLU）形式化为一个统一范式，并设定了三个关键目标：(i) 精确删除不需要的知识；(ii) 在保留先验信息的同时高效整合新知识；(iii) 最小化跨周期的知识泄漏。我们提出了双向低秩自适应（BID-LoRA），这是一种新型框架，其特点是在注意力层应用了三个专门的自适应器路径——保留（retain）、新增（new）和遗忘（unlearn），并结合了“逃逸遗忘”（escape unlearning）机制，将待遗忘类别的嵌入推向与保留知识距离最远的位置，且仅需更新 5% 的参数。在 CIFAR-100 上的实验表明，BID-LoRA 在多个适应周期中均优于 CLU 基准方法。我们进一步在 CASIA-Face100（一个精选的人脸识别子集）上进行了评估，证明了其在现实世界身份管理系统中的实际适用性，即系统必须能够注册新用户并移除已注销的用户。

## Abstract
Recent advances in deep learning underscore the need for systems that can not only acquire new knowledge through Continual Learning (CL) but also remove outdated, sensitive, or private information through Machine Unlearning (MU). However, while CL methods are well-developed, MU techniques remain in early stages, creating a critical gap for unified frameworks that depend on both capabilities. We find that naively combining existing CL and MU approaches results in knowledge leakage a gradual degradation of foundational knowledge across repeated adaptation cycles. To address this, we formalize Continual Learning Unlearning (CLU) as a unified paradigm with three key goals: (i) precise deletion of unwanted knowledge, (ii) efficient integration of new knowledge while preserving prior information, and (iii) minimizing knowledge leakage across cycles. We propose Bi-Directional Low-Rank Adaptation (BID-LoRA), a novel framework featuring three dedicated adapter pathways-retain, new, and unlearn applied to attention layers, combined with escape unlearning that pushes forget-class embeddings to positions maximally distant from retained knowledge, updating only 5% of parameters. Experiments on CIFAR-100 show that BID-LoRA outperforms CLU baselines across multiple adaptation cycles. We further evaluate on CASIA-Face100, a curated face recognition subset, demonstrating practical applicability to real-world identity management systems where new users must be enrolled and withdrawn users removed.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **BID-LoRA** 的参数高效框架，旨在解决深度学习模型在持续演进过程中同时面临的“学习新知识”与“遗忘旧/敏感知识”的挑战。以下是对该论文的深度结构化总结：

### 1. 核心问题与研究动机
*   **研究背景**：现代 AI 系统需要具备双重能力：持续学习（CL）以获取新知识，以及机器遗忘（MU）以删除过时或隐私信息（如响应 GDPR/CCPA 法规）。
*   **核心问题**：简单地将现有的 CL 和 MU 方法叠加会导致**知识泄露（Knowledge Leakage）**。这不同于灾难性遗忘，它表现为基础知识在多次“学习-遗忘”循环中逐渐退化。
*   **研究动机**：目前 CL 较成熟而 MU 尚在早期，缺乏一个统一、高效且能抑制知识泄露的框架。特别是在人脸识别等隐私敏感领域，这种需求尤为迫切。

### 2. 方法论
BID-LoRA 的核心思想是通过**路径分离**和**几何空间推离**来实现精准的知识操纵。
*   **三向适配器路径（Three-Pathway Separation）**：
    *   在 Transformer 的注意力层中部署三个独立的 LoRA 适配器：**保留（Retain）**、**新增（New）**和**遗忘（Unlearn）**。
    *   这种设计避免了不同目标（增加 vs. 删除）之间的梯度干扰，每个路径专门负责其对应的知识任务。
*   **逃逸遗忘机制（Escape Unlearning）**：
    *   **核心逻辑**：计算一个在嵌入空间中与所有保留类质心距离最远的“逃逸点”（Escape Point）。
    *   **操作方式**：通过均方误差（MSE）损失，将待遗忘类的样本嵌入强制推向该逃逸点。这是一种“多对一”的映射，能彻底破坏该类别的判别性信息，使其难以被恢复。
*   **损失函数设计**：
    *   **遗忘损失**：将遗忘类推向逃逸点。
    *   **保留损失**：结合交叉熵和嵌入锚点（Embedding Anchor），防止模型表示漂移。
    *   **新增损失**：标准交叉熵学习新类别。
*   **参数效率**：仅更新约 **5%** 的参数（适配器和分类头），冻结主干网络。

### 3. 实验设计
*   **数据集**：
    *   **CIFAR-100**：用于通用图像分类评估。
    *   **CASIA-Face100**：人脸识别子集，模拟现实中身份管理系统的用户增删场景。
*   **评估协议（Benchmark）**：
    *   提出了一种**滑动窗口评估协议**：共 6 个任务，每个任务包含“保留 20 类、遗忘 10 类、学习 10 类”。这种长序列测试能有效验证系统的长期稳定性。
*   **对比方法**：
    *   包括 LSF、CLPU-DER++、UniCLUN、UG-CLU、UnCLe 等 5 种最新的统一框架基准。
    *   **Oracle（先知模型）**：作为性能上限（直接在目标类上训练）。
*   **评价指标**：遗忘准确率（Acc_f）、保留准确率（Acc_r）、新增准确率（Acc_n）、整体准确率（Acc_o）、成员推理攻击（MIA）成功率、KL 散度。

### 4. 资源与算力
*   **模型架构**：使用了 DeiT-Tiny（用于分类）和 Face Transformer（用于人脸识别）。
*   **算力说明**：论文**未明确列出**具体的 GPU 型号、数量或总训练时长。但提到 LoRA 秩（Rank）设置为 8 或 4，且仅更新 5% 的参数，强调了其在资源受限环境下的适用性。

### 5. 实验数量与充分性
*   **实验规模**：在两个不同领域的标准数据集上进行了 6 轮连续任务的完整循环测试。
*   **消融实验**：非常充分，涵盖了 LoRA 秩的影响、BID-LoRA 与标准 LoRA 的对比、重放缓冲区比例、各路径贡献的验证、以及逃逸点缩放因子的调节。
*   **可视化验证**：通过 t-SNE 和 3D 超球面对嵌入空间进行了几何验证，直观展示了遗忘类如何被推离。
*   **客观性**：引入了 MIA 攻击测试来验证隐私保护效果，并使用 KL 散度衡量与 Oracle 模型的相似度，评估维度全面且客观。

### 6. 主要结论与发现
*   **抑制泄露**：BID-LoRA 有效解决了知识泄露问题，在 6 轮循环后，整体准确率保持稳定，而基准方法普遍出现 3%-8% 的退化。
*   **性能优异**：在仅更新 5% 参数的情况下，其保留和新增知识的准确率接近 Oracle 模型，且遗忘准确率接近 0%。
*   **隐私安全**：MIA 分数接近 0.5（理想值），表明攻击者无法区分遗忘数据与未见过的数据。

### 7. 优点与亮点
*   **架构创新**：三路径分离设计巧妙解决了持续学习与遗忘之间的目标冲突。
*   **几何视角**：提出的“逃逸遗忘”比传统的噪声干扰或负梯度方法更稳定、更彻底。
*   **实用性强**：参数极其高效，且在人脸识别这一高价值现实场景中得到了验证。
*   **测试严谨**：滑动窗口协议比单次增删实验更能反映模型在真实动态环境下的表现。

### 8. 不足与局限
*   **缓冲区依赖**：虽然比例较低，但仍需保留 10% 的重放缓冲区，这在极端隐私敏感的场景下可能仍存在合规风险。
*   **模型规模限制**：实验主要基于较小的 Transformer 变体（如 DeiT-Tiny），尚未在大规模预训练模型（如 ViT-L 或 LLM）上验证其扩展性。
*   **任务类型**：目前仅限于分类和识别任务，未探讨在生成式任务（如扩散模型或文本生成）中的表现。

（完）

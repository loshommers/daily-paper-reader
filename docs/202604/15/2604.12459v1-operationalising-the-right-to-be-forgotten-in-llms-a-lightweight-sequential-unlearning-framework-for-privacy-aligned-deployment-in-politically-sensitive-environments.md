---
title: "Operationalising the Right to be Forgotten in LLMs: A Lightweight Sequential Unlearning Framework for Privacy-Aligned Deployment in Politically Sensitive Environments"
title_zh: 在大语言模型中落实被遗忘权：一种用于政治敏感环境下隐私对齐部署的轻量级顺序遗忘框架
authors: "Esen Kurt, Haithem Afli"
date: 2026-04-14
pdf: "https://arxiv.org/pdf/2604.12459v1"
tags: ["query:mu-ce"]
score: 10.0
evidence: 大语言模型中实现被遗忘权的顺序遗忘框架
tldr: 针对政治敏感环境下大语言模型面临的隐私合规与“被遗忘权”挑战，本文提出一种轻量级顺序遗忘框架。该框架通过分离保留与抑制目标，先利用正向微调稳定通用能力，再通过层受限的负向微调精准消除敏感信息。在SemEval-2025基准测试中，该方法在保持模型流畅度与准确性的同时，实现了高效的隐私擦除，为受监管环境下的模型部署提供了实用且可复现的方案。
selection_source: fresh_fetch
motivation: 在政治敏感环境中部署大模型时，需满足GDPR等法规对个人隐私和机密数据擦除的技术要求。
method: 提出一种顺序遗忘框架，先通过正向微调巩固良性能力，再利用层受限的负向微调抑制特定敏感模式。
result: 实验证明该方法能有效抑制目标敏感行为，且对模型的事实准确性和语言流畅度影响极小。
conclusion: 顺序遗忘框架是实现大模型数据擦除要求的一种实用机制，有助于模型在复杂监管环境下的合规部署。
---

## 摘要
大语言模型（LLMs）正越来越多地部署在政治敏感环境中，其中对个人数据或机密内容的记忆引发了诸如 GDPR 及其“被遗忘权”等框架下的监管担忧。将此类法律原则转化为大规模生成系统面临着重大的技术挑战。我们提出了一种轻量级顺序遗忘框架，该框架明确分离了保留与抑制目标。该方法首先通过正向微调稳定良性能力，随后应用层受限的负向微调来抑制指定的敏感模式，同时保持通用的语言能力。在 SemEval-2025 LLM Unlearning 基准测试上的实验表明，该方法实现了有效的行为抑制，且对事实准确性和流畅性的影响极小。GPT-2 表现出比 DistilGPT-2 更强的鲁棒性，突显了模型容量在隐私对齐适配中的作用。我们将顺序遗忘定位为一种实用且可复现的机制，用于在政治部署的 LLM 中落实数据擦除要求。

## Abstract
Large Language Models (LLMs) are increasingly deployed in politically sensitive environments, where memorisation of personal data or confidential content raises regulatory concerns under frameworks such as the GDPR and its Right to be Forgotten. Translating such legal principles into large-scale generative systems presents significant technical challenges.   We introduce a lightweight sequential unlearning framework that explicitly separates retention and suppression objectives. The method first stabilises benign capabilities through positive fine-tuning, then applies layer-restricted negative fine-tuning to suppress designated sensitive patterns while preserving general language competence.   Experiments on the SemEval-2025 LLM Unlearning benchmark demonstrate effective behavioural suppression with minimal impact on factual accuracy and fluency. GPT-2 exhibits greater robustness than DistilGPT-2, highlighting the role of model capacity in privacy-aligned adaptation. We position sequential unlearning as a practical and reproducible mechanism for operationalising data erasure requirements in politically deployed LLMs.

---

## 论文详细总结（自动生成）

### 论文总结：在大语言模型中落实“被遗忘权”的轻量级顺序遗忘框架

#### 1. 核心问题与研究动机
*   **核心问题**：如何在不进行昂贵的从头训练（Retraining）的情况下，使大语言模型（LLM）符合“被遗忘权”（Right to be Forgotten, RTBF）等法律监管要求（如 GDPR），特别是针对部署在政治敏感环境（如选举通信、政府聊天机器人）中的模型。
*   **研究背景**：LLM 在预训练中会无意中记忆敏感政治内容、公职人员个人数据或机密信息。现有的机器遗忘（Machine Unlearning）方法往往将“保留良性知识”与“消除敏感信息”作为联合优化目标，这会导致梯度干扰，造成模型性能不稳定或灾难性遗忘。

#### 2. 方法论
论文提出了一种**轻量级顺序遗忘框架（Sequential Unlearning Framework）**，其核心思想是将保留和抑制目标在时间轴上分离，分为三个阶段：
*   **阶段 1：正向微调（保留阶段）**：在良性数据集（Retain dataset, $D_R$）上使用标准交叉熵损失函数进行微调，锚定模型的良性行为和事实能力。
*   **阶段 2：层受限的负向微调（遗忘阶段）**：
    *   **冻结参数**：冻结模型大部分参数，仅允许更新最后两个 Transformer 块、最后的层归一化（Layer Norm）和语言模型头（LM Head）。
    *   **梯度上升**：在遗忘数据集（Forget dataset, $D_F$）上应用缩放后的负损失函数 $L_{neg} = -\alpha \cdot CE(y, \hat{y})$（其中 $\alpha=0.001$），通过梯度上升降低敏感模式的生成概率。
*   **阶段 3：稳定化（扩展保留微调）**：再次在 $D_R$ 上进行少量微调并采用早停策略，以恢复遗忘阶段可能造成的微小效用损失。

#### 3. 实验设计
*   **数据集/场景**：采用了 **SemEval-2025 LLM Unlearning Challenge** 基准测试。该基准提供了互斥的“保留集”和“遗忘集”。
*   **对比模型**：
    *   **GPT-2 (124M)**：作为主要评估对象。
    *   **DistilGPT-2 (82M)**：用于测试模型容量对遗忘过程鲁棒性的影响。
*   **评估指标**：训练/验证损失、困惑度（Perplexity, PPL）、行为探测（通过特定提示词诱导敏感信息输出）。

#### 4. 资源与算力
*   **算力说明**：论文**未明确列出**具体的 GPU 型号、数量或总训练时长。
*   **效率描述**：作者强调该框架是“轻量级”的，不需要全参数重新训练或构建大规模安全语料库，仅通过有限的参数更新和低学习率即可实现。

#### 5. 实验数量与充分性
*   **实验规模**：实验涵盖了两种不同容量的模型，并对比了遗忘前后的损失轨迹和生成质量。
*   **充分性评价**：
    *   **优点**：实验在受控的 SemEval 基准下进行，具有可复现性；包含了对模型容量（蒸馏模型 vs 原型模型）的对比分析。
    *   **不足**：实验仅限于较小规模的模型（GPT-2 级别），未在当前主流的百亿级（7B+）参数模型上验证；实验主要针对英文语境，缺乏多语言验证。

#### 6. 主要结论与发现
*   **有效性**：顺序遗忘框架能显著抑制敏感模式的生成，将原本可能泄露的结构化信息转化为合规的拒绝回答（如“我无法提供私人信息”）。
*   **效用保持**：在抑制敏感内容的同时，模型对通用知识（如“法国首都是哪里”）的回答保持准确，GPT-2 的困惑度保持在较低水平（12.3）。
*   **容量相关性**：模型容量对遗忘的鲁棒性至关重要。GPT-2 在遗忘后保持稳定，而较小的 DistilGPT-2 出现了性能崩溃（困惑度飙升至 39.3），说明遗忘过程可以作为模型表征韧性的一种压力测试。

#### 7. 优点与亮点
*   **梯度解耦**：通过顺序处理而非联合优化，有效解决了保留与遗忘信号之间的梯度冲突问题。
*   **层受限策略**：利用知识在 Transformer 高层集中的特性，仅修改顶层参数，最大限度地保护了底层语言表征。
*   **部署友好**：方法简单、计算成本低，适合需要快速响应监管要求的实际生产环境。

#### 8. 不足与局限
*   **行为抑制 vs. 彻底删除**：该方法实现的是“行为抑制”（输出端不可见），而非理论上的“参数级彻底擦除”。在法律严苛的司法管辖区，这可能不被视为完全履行了“被遗忘权”。
*   **模型规模限制**：实验模型（GPT-2）在当前大模型时代显得过时，其结论在 Llama 3 或 GPT-4 等超大规模模型上的适用性有待验证。
*   **对抗攻击风险**：论文未测试模型在面对恶意提示词攻击（Jailbreaking）时，已遗忘的信息是否会被重新诱导出来。
*   **规范性判断**：定义哪些内容属于“敏感政治内容”具有主观性，该框架若被滥用，可能导致合法的公共信息被选择性审查。

（完）

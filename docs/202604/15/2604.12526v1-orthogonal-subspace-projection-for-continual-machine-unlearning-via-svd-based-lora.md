---
title: Orthogonal Subspace Projection for Continual Machine Unlearning via SVD-Based LoRA
title_zh: 基于 SVD-LoRA 的正交子空间投影持续机器遗忘
authors: "Yogachandran Rahulamathavan, Nasir Iqbal, Juncheng Hu, Sangarapillai Lambotharan"
date: 2026-04-14
pdf: "https://arxiv.org/pdf/2604.12526v1"
tags: ["query:mu-ce"]
score: 10.0
evidence: 通过正交子空间投影实现持续机器遗忘
tldr: 本文针对连续机器遗忘中多个LoRA模块顺序更新导致的参数冲突问题，提出了一种基于SVD的正交子空间投影方法。该方法在训练时将新的LoRA更新限制在先前任务子空间的正交补空间中，从而实现任务隔离并避免干扰。实验表明，该方法在长序列遗忘任务中能有效维持模型性能，显著优于现有的静态融合技术。
selection_source: fresh_fetch
motivation: 顺序执行多个LoRA遗忘任务会导致参数碰撞和严重的任务间干扰，使得模型在保留数据上的表现随任务增加而迅速恶化。
method: 采用基于奇异值分解（SVD）的正交子空间投影，在训练过程中约束LoRA更新方向，使其始终与之前任务所占用的子空间保持正交。
result: "在CIFAR-100的30次连续遗忘实验中，该方法将保留数据的准确率维持在58.1%左右，而现有SOTA方法在同等条件下准确率仅剩12.7%。"
conclusion: 通过在训练阶段引入正交子空间约束，可以有效解决连续机器遗忘中的干扰问题，实现高效且稳定的知识擦除与保留。
---

## 摘要
持续机器遗忘旨在消除不再应保留的数据的影响，同时保持模型在其他数据上的效用。当删除请求按顺序到达时，这一设定变得尤为困难，因为模型必须反复进行调整，且不能抹除先前保留的知识。低秩自适应（LoRA）提供了一种实现此类更新的高效方式，但简单地组合多个顺序 LoRA 模块会导致参数冲突，从而在任务之间引起“强干扰”。我们提出了一种基于奇异值分解（SVD）引导的正交子空间投影的静态替代方案。我们的方法在训练期间约束每个新的 LoRA 更新，使其位于早期遗忘任务所使用子空间的正交补空间中。这在不需要部署时进行动态路由的情况下保持了任务隔离。在 CIFAR-100（使用 ResNet-20）和 MNIST 上的实验表明，该方法在长序列遗忘任务中表现出稳定的行为。在经历 30 个顺序遗忘任务后，最先进的静态融合方法将保留准确率从 60.39% 降低到 12.70%，而本文提出的训练内约束优化方法在保持基准性能（约 58.1%）的同时，保留了强大的遗忘效能。

## Abstract
Continual machine unlearning aims to remove the influence of data that should no longer be retained, while preserving the usefulness of the model on everything else. This setting becomes especially difficult when deletion requests arrive sequentially, because the model must repeatedly adapt without erasing previously retained knowledge. Low-Rank Adaptation (LoRA) offers an efficient way to implement such updates, but naively combining many sequential LoRA modules leads to parameter collision, causing \textit{strong interference} between tasks. We propose a static alternative based on Singular Value Decomposition (SVD)-guided orthogonal subspace projection. Our method constrains each new LoRA update during training so that it lies in the orthogonal complement of the subspaces used by earlier unlearning tasks. This preserves task isolation without requiring dynamic routing at deployment. Experiments on CIFAR-100 with ResNet-20 and on MNIST show stable behavior across long sequences of unlearning tasks. After thirty sequential unlearning tasks, state-of-the-art static fusion reduces retained accuracy from 60.39\% to 12.70\%, whereas the proposed in-training constrained optimization maintains baseline performance ($\sim$58.1\%) while preserving strong unlearning efficacy.

---

## 论文详细总结（自动生成）

以下是对论文《Orthogonal Subspace Projection for Continual Machine Unlearning via SVD-Based LoRA》的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
随着 GDPR 等法规对“被遗忘权”的重视，机器遗忘（Machine Unlearning）成为关键技术。但在实际应用中，删除请求通常是**连续且序列化**到达的。
*   **研究动机**：现有的低秩自适应（LoRA）方法虽然在单次遗忘中表现高效，但在处理长序列遗忘任务时，多个 LoRA 模块的顺序更新会导致**参数冲突（Parameter Collision）**。
*   **核心挑战**：如果直接融合这些冲突的更新，模型在保留数据上的性能会剧烈下降（灾难性遗忘）；如果采用动态路由（如 MoE 架构），则会增加部署时的存储负担和推理延迟。
*   **整体含义**：本文旨在寻找一种既能实现静态融合（不增加推理开销），又能有效隔离序列任务、防止参数干扰的持续遗忘方案。

### 2. 论文提出的方法论
论文提出了一种基于 **SVD（奇异值分解）引导的正交子空间投影** 方法，核心思想是在训练阶段就强制让新的遗忘更新与旧任务的参数空间保持正交。
*   **关键技术细节**：
    1.  **子空间提取**：对每个遗忘任务生成的 LoRA 更新矩阵进行 SVD 分解，提取前 $r$ 个左奇异向量，这些向量代表了该任务占用的主要参数方向。
    2.  **累积投影矩阵**：维护一个全局投影矩阵 $P$，通过递归减去已占用子空间的方式，定义出当前任务的“安全空空间（Null Space）”。
    3.  **训练内约束优化（核心创新）**：不同于训练后投影（Post-Hoc），该方法将投影矩阵直接嵌入到 LoRA 的前向传播中（$\tilde{A}_k = P_{k-1}A_k$）。
*   **算法流程**：在训练第 $k$ 个遗忘任务时，优化器被数学限制在与前 $k-1$ 个任务正交的子空间内进行梯度下降。这保证了新学习到的权重不会覆盖或干扰之前的知识，从而允许所有更新在训练后直接加和融合到基础模型中。

### 3. 实验设计
*   **数据集与场景**：
    *   **MNIST**：使用 2 层多层感知机（MLP），顺序遗忘 3 个数字类别。
    *   **CIFAR-100**：使用 ResNet-20 预训练模型，进行长达 **30 个任务** 的顺序遗忘（每个任务遗忘一个类别）。
*   **Benchmark（基准）与对比方法**：
    *   **Static Combined (AC-LoRA / I-LoRA)**：传统的静态 LoRA 融合方法。
    *   **MoE Router**：基于混合专家系统的动态路由方法（代表了性能上限，但部署成本高）。
*   **评估指标**：保留准确率（Retain Acc，越高越好）和遗忘准确率（Forget Acc，越低代表遗忘越彻底）。

### 4. 资源与算力
*   **算力说明**：论文中**未明确指出**具体的 GPU 型号、数量以及具体的训练时长。考虑到实验涉及 ResNet-20 在 CIFAR-100 上的 30 次顺序微调，预计在单张消费级 GPU（如 RTX 3090 或 4090）上即可在数小时内完成。

### 5. 实验数量与充分性
*   **实验规模**：论文涵盖了从简单线性模型（MNIST）到深层非线性模型（ResNet-20）的跨度。
*   **序列长度**：进行了 30 个任务的连续遗忘实验，这在同类研究中属于较长且极具挑战性的序列，足以验证方法的稳定性。
*   **充分性评价**：实验设计较为客观，通过对比静态融合和动态路由，清晰地展示了该方法在“性能保持”和“部署效率”之间的平衡。此外，论文还进行了子空间饱和度分析，探讨了模型容量对遗忘效果的限制，实验论证较为充分。

### 6. 主要结论与发现
*   **防止性能崩溃**：在 CIFAR-100 的 30 轮遗忘后，传统静态融合方法的保留准确率从 60.39% 崩塌至 12.74%，而本文方法稳定在 **58.06%**，接近动态路由的效果。
*   **静态部署优势**：该方法无需推理时的路由网络，实现了“训练时约束，部署时零开销”。
*   **容量瓶颈**：实验发现，随着遗忘任务增加，可用的正交子空间会逐渐耗尽（约 15-20 个任务后出现饱和），导致后续任务的遗忘深度（Forget Acc）有所下降。

### 7. 优点
*   **数学严谨性**：提供了梯度可行性的引理证明，确保优化过程严格限制在正交补空间内。
*   **高效部署**：解决了持续遗忘中“模型膨胀”的问题，融合后的模型与原始模型结构完全一致。
*   **长序列稳定性**：在极长序列任务下表现出极强的鲁棒性，远超现有的静态融合技术。

### 8. 不足与局限
*   **遗忘彻底性权衡**：为了保护保留数据，在任务后期（子空间饱和时），遗忘的效能（Forget Acc）不如一些激进的重训练方法。
*   **线性假设**：基于 SVD 的正交投影本质上是线性的，对于极度复杂的非线性任务几何空间，可能存在近似误差。
*   **应用范围限制**：实验主要集中在图像分类任务的最后几层，尚未在大规模语言模型（LLM）的全量参数微调或更复杂的生成任务中进行验证。

（完）

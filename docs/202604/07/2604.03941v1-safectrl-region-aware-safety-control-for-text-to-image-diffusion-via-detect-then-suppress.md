---
title: "SafeCtrl: Region-Aware Safety Control for Text-to-Image Diffusion via Detect-Then-Suppress"
title_zh: SafeCtrl：基于“检测-抑制”范式的文本到图像扩散模型区域感知安全控制
authors: "Lingyun Zhang, Yu Xie, Zhongli Fang, Yu Liu, Ping Chen"
date: 2026-04-05
pdf: "https://arxiv.org/pdf/2604.03941v1"
tags: ["query:mu-ce"]
score: 9.0
evidence: 用于安全控制的模型概念擦除
tldr: SafeCtrl 是一种针对文本生成图像扩散模型的区域感知安全控制框架，旨在解决安全干预与图像保真度之间的权衡问题。该框架采用“检测后抑制”范式，通过注意力引导精确定位风险区域，并利用图像级直接偏好优化（DPO）仅在特定区域中和有害语义。实验证明，该方法在保持上下文完整性的同时，显著提升了对抗性攻击的防御能力，为负责任的图像生成提供了精确且鲁棒的解决方案。
selection_source: fresh_fetch
motivation: 现有的安全干预手段往往在消除有害概念时导致图像整体质量下降，且容易被对抗性提示词绕过。
method: 提出了 SafeCtrl 框架，通过注意力机制定位风险区域，并结合局部 DPO 优化将有害对象转化为安全替代品，同时保留周围上下文。
result: 在多类风险测试中，SafeCtrl 在安全性和图像保真度之间取得了优于现有方法的平衡，并展现出更强的抗攻击韧性。
conclusion: 该研究证明了通过区域感知的局部干预可以实现更精准、更鲁棒的扩散模型安全控制，而不损害生成内容的保真度。
---

## 摘要
文本到图像扩散模型的广泛部署面临着生成视觉有害内容（如性显式内容、暴力和恐怖图像）的重大挑战。常见的安全干预措施，从输入过滤到模型概念擦除，通常面临两个关键局限性：(1) 安全性与上下文保留之间的严重权衡，即移除不安全概念会降低安全内容的保真度；(2) 易受对抗性攻击，安全机制容易被绕过。为了应对这些挑战，我们提出了 SafeCtrl，这是一个基于“检测-抑制”（Detect-Then-Suppress）范式的区域感知安全控制框架。与全局安全干预不同，SafeCtrl 首先采用注意力引导的检测（Detect）模块来精确锁定特定的风险区域。随后，通过图像级直接偏好优化（DPO）进行优化的局部抑制（Suppress）模块仅在检测到的区域内中和有害语义，从而有效地将不安全对象转化为安全替代品，同时保持周围上下文完好无损。在多个风险类别上的广泛实验表明，与现有最先进方法相比，SafeCtrl 在安全性和保真度之间实现了更优的权衡。至关重要的是，我们的方法对对抗性提示攻击表现出更强的韧性，为负责任的生成提供了一种精确且鲁棒的解决方案。

## Abstract
The widespread deployment of text-to-image diffusion models is significantly challenged by the generation of visually harmful content, such as sexually explicit content, violence, and horror imagery. Common safety interventions, ranging from input filtering to model concept erasure, often suffer from two critical limitations: (1) a severe trade-off between safety and context preservation, where removing unsafe concepts degrades the fidelity of the safe content, and (2) vulnerability to adversarial attacks, where safety mechanisms are easily bypassed. To address these challenges, we propose SafeCtrl, a Region-Aware safety control framework operating on a Detect-Then-Suppress paradigm. Unlike global safety interventions, SafeCtrl first employs an attention-guided Detect module to precisely localize specific risk regions. Subsequently, a localized Suppress module, optimized via image-level Direct Preference Optimization (DPO), neutralizes harmful semantics only within the detected areas, effectively transforming unsafe objects into safe alternatives while leaving the surrounding context intact. Extensive experiments across multiple risk categories demonstrate that SafeCtrl achieves a superior trade-off between safety and fidelity compared to state-of-the-art methods. Crucially, our approach exhibits improved resilience against adversarial prompt attacks, offering a precise and robust solution for responsible generation.

---

## 论文详细总结（自动生成）

以下是对论文《SafeCtrl: Region-Aware Safety Control for Text-to-Image Diffusion via Detect-Then-Suppress》的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：现有的文本到图像（T2I）扩散模型安全干预手段（如全局微调或推理引导）存在两大缺陷：
    1.  **上下文退化（Context Degradation）**：全局擦除有害概念时会误伤背景或无关对象，导致图像保真度下降。
    2.  **易受攻击（Vulnerability）**：高度依赖文本提示词，一旦面对对抗性提示词（绕过敏感词检测），安全机制往往失效。
*   **研究动机**：开发一种既能精准定位并消除局部风险区域，又能保持背景完整性，且对对抗性攻击具有鲁棒性的安全控制框架。

### 2. 方法论
SafeCtrl 采用**“检测-后-抑制”（Detect-Then-Suppress）**的区域感知范式，核心组件包括：
*   **注意力引导检测模块（Detect Module）**：
    *   利用 U-Net 内部的**交叉注意力（Cross-Attention）**和**自注意力（Self-Attention）**特征。
    *   通过融合这些特征生成空间风险掩码（Risk Mask $M_t$），从而在视觉特征层面而非仅在文本层面识别风险区域。
*   **偏好对齐抑制模块（Suppress Module）**：
    *   采用**直接偏好优化（DPO）**策略进行训练。
    *   利用 GPT-4 生成“安全-不安全”图像对，在潜空间内优化抑制模块，使其学习如何将有害语义转化为自然的安全替代品（如将武器变为玩具）。
*   **动态调度与局部融合**：
    *   **动态调度**：检测模块仅在去噪早期阶段（语义形成期）激活，抑制模块在后期激活，以平衡计算开销与生成质量。
    *   **硬融合（Hard Fusion）**：通过公式 $V_{new} = V_{orig} \odot (1 - M_t) + V_{safe} \odot M_t$ 确保掩码之外的背景区域在数学上与原始输出完全一致。

### 3. 实验设计
*   **数据集与场景**：
    *   **I2P (Inappropriate Image Prompts)**：评估多样化的风险类别（裸露、暴力、恐怖等）。
    *   **COCO-30k**：评估图像保真度（FID）和文本对齐度（CLIP Score）。
    *   **Ring-A-Bell**：评估针对对抗性提示词攻击的防御成功率（ASR）。
    *   **定位基准（Pascal-Car, CelebA-HQ 等）**：评估风险区域定位的准确性（mIoU）。
*   **对比方法**：包括原始 Stable Diffusion (SD v2.1)、SLD（推理引导）、ESD/UCE/AlignGuard（全局微调）、RDM（文本嵌入约束）以及 Concept Replacer (CR，局部替换方案)。
*   **评价指标**：引入了 **H-Score**（安全性与实用性的调和平均值），综合衡量安全干预与保真度之间的权衡。

### 4. 资源与算力
*   **模型规模**：SafeCtrl 作为一个轻量级插件，仅增加约 **75M** 参数，远低于竞争对手 Concept Replacer 的 860M（需复制整个 U-Net）。
*   **训练效率**：检测模块仅需约 10 张像素级标注图像进行少样本微调；抑制模块使用 200 对图像级偏好数据进行 DPO 训练。
*   **算力说明**：文中未明确给出具体的 GPU 型号、数量或总训练时长，但强调了其训练和推理的“高效性”和“低延迟”。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了从定量指标（FID, CLIP, Unsafe Ratio）到定性视觉对比，再到定位精度和抗攻击能力的全面测试。
*   **消融实验**：对去噪步数（Timestep）的影响进行了实证分析，验证了动态调度策略的合理性。
*   **客观性**：使用了多个第三方基准数据集和自动化分类器（NudeNet, Q16），实验设计较为客观、公平。

### 6. 主要结论与发现
*   **最优权衡**：SafeCtrl 在 I2P 基准上实现了最低的不安全比例（0.11），同时保持了最高的 H-Score (0.906)，证明其在安全性与保真度之间取得了最佳平衡。
*   **强鲁棒性**：在对抗性攻击下，SafeCtrl 的不安全比例仅为 0.28，远优于全局微调方法（如 ESD 的 0.54），证明基于视觉特征的检测比基于文本的检测更可靠。
*   **高保真度**：由于采用了局部干预和硬融合技术，SafeCtrl 能够完美保留非风险区域的艺术风格、光影和背景细节。

### 7. 优点
*   **区域感知**：解决了“牵一发而动全身”的上下文退化问题。
*   **鲁棒性强**：通过视觉特征锚定防御，有效抵御提示词混淆攻击。
*   **轻量化**：作为插件运行，推理延迟低，显存占用小，易于部署。
*   **泛化性**：不仅适用于裸露内容，还能有效处理恐怖、暴力等多种视觉风险。

### 8. 不足与局限
*   **依赖掩码精度**：如果检测模块未能准确识别风险区域（如极小或极模糊的对象），抑制效果会打折扣。
*   **概念覆盖**：虽然展示了多种风险类别，但对于更抽象的有害概念（如隐晦的歧视或偏见）是否同样有效尚待验证。
*   **训练数据需求**：虽然是少样本，但仍需要针对特定风险类别准备少量的标注数据或偏好对。

（完）

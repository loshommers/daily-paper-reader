---
title: Selective Forgetting for Large Reasoning Models
title_zh: 大推理模型的选择性遗忘
authors: "Tuan Le, Wei Qian, Mengdi Huai"
date: 2026-04-04
pdf: "https://arxiv.org/pdf/2604.03571v1"
tags: ["query:mu-ce"]
score: 10.0
evidence: 大推理模型的选择性遗忘
tldr: 针对大型推理模型（LRM）在思维链（CoT）中泄露敏感信息的问题，本文提出了一种选择性遗忘框架。该框架利用多模型协作和检索增强生成（RAG）识别并替换CoT中的敏感片段，并引入特征替换遗忘损失函数。实验证明，该方法在有效擦除特定知识的同时，能较好地保持模型的通用推理能力，解决了隐私泄露与性能维持之间的矛盾。
selection_source: fresh_fetch
motivation: 现有遗忘方法难以在不损害LRM通用推理能力的前提下，彻底清除思维链中蕴含的敏感或版权信息。
method: 利用多模型与RAG识别CoT中的敏感成分并替换为良性占位符，结合新型特征替换损失函数进行选择性遗忘。
result: 实验验证该方法在合成和医疗数据集上均能有效抑制敏感内容生成，同时保持了模型逻辑结构的完整性。
conclusion: 本文提出的框架为LRM提供了精准且保质的知识擦除方案，平衡了模型安全性和推理效能。
---

## 摘要
大推理模型 (LRMs) 在生成最终答案之前会产生结构化的思维链 (CoTs)，这使得它们特别容易通过中间推理步骤泄露知识。然而，训练数据中敏感信息（如受版权保护的内容和隐私内容）的记忆已引发了伦理和法律方面的担忧。为了解决这些问题，选择性遗忘（也称为机器遗忘）已成为 LRMs 的一种潜在补救措施。然而，现有的遗忘方法主要针对最终答案，并且在遗忘后可能会降低 LRMs 的整体推理能力。此外，直接对整个思维链 (CoTs) 应用遗忘可能会损害通用推理能力。LRM 遗忘的关键挑战在于实现对目标知识的精确遗忘，同时保持通用推理能力的完整性。为了填补这一空白，本文提出了一种新型的 LRM 遗忘框架，该框架在保留通用推理能力的同时，选择性地移除敏感的推理组件。我们的方法利用多个结合检索增强生成 (RAG) 的大语言模型 (LLMs) 来分析思维链 (CoT) 轨迹，识别与遗忘相关的片段，并用保持逻辑结构的良性占位符替换它们。我们还为 LRMs 引入了一种新的特征替换遗忘损失，该损失可以同时抑制生成被遗忘内容的概率，并强化结构有效的替换内容。在合成数据集和医学数据集上的广泛实验验证了我们所提方法的预期特性。

## Abstract
Large Reasoning Models (LRMs) generate structured chains of thought (CoTs) before producing final answers, making them especially vulnerable to knowledge leakage through intermediate reasoning steps. Yet, the memorization of sensitive information in the training data such as copyrighted and private content has led to ethical and legal concerns. To address these issues, selective forgetting (also known as machine unlearning) has emerged as a potential remedy for LRMs. However, existing unlearning methods primarily target final answers and may degrade the overall reasoning ability of LRMs after forgetting. Additionally, directly applying unlearning on the entire CoTs could degrade the general reasoning capabilities. The key challenge for LRM unlearning lies in achieving precise unlearning of targeted knowledge while preserving the integrity of general reasoning capabilities. To bridge this gap, we in this paper propose a novel LRM unlearning framework that selectively removes sensitive reasoning components while preserving general reasoning capabilities. Our approach leverages multiple LLMs with retrieval-augmented generation (RAG) to analyze CoT traces, identify forget-relevant segments, and replace them with benign placeholders that maintain logical structure. We also introduce a new feature replacement unlearning loss for LRMs, which can simultaneously suppress the probability of generating forgotten content while reinforcing structurally valid replacements. Extensive experiments on both synthetic and medical datasets verify the desired properties of our proposed method.

---

## 论文详细总结（自动生成）

以下是对论文《Selective Forgetting for Large Reasoning Models》的结构化深入总结：

### 1. 核心问题与整体含义
*   **研究背景**：大推理模型（LRMs，如 OpenAI o1、DeepSeek-R1）通过生成中间思维链（CoT）来解决复杂问题。然而，这些推理轨迹可能包含并泄露训练数据中的敏感信息（如隐私、版权内容）。
*   **核心问题**：现有的机器遗忘（Machine Unlearning）方法主要针对最终答案，或者盲目地抹除整个推理过程。这会导致两个问题：一是敏感信息仍可能残留在推理步骤中；二是过度遗忘会破坏模型的通用逻辑推理能力，导致模型输出乱码或逻辑断裂。
*   **研究意义**：本文旨在实现“精准手术式”的遗忘，即只切除推理链中的敏感片段，同时保留逻辑框架和通用推理效能。

### 2. 方法论
论文提出了 **FRUL（Feature Replacement-aware Unlearning framework）** 框架，核心步骤如下：
*   **敏感片段识别与替换**：
    *   利用**检索增强生成（RAG）**结合多个大语言模型（如 GPT-4）分析遗忘集（Forget Set）中的推理轨迹。
    *   识别出与敏感知识直接相关的特定片段（$c_f$），并将其替换为逻辑一致但内容中性的“良性占位符”（$c_m$），从而构建出结构完整但无害的推理模板。
*   **特征替换遗忘损失函数**：
    *   **CoT 损失 ($\ell_{CoT}$)**：同时进行“负向抑制”和“正向强化”。抑制模型生成敏感片段 $c_f$ 的概率，同时鼓励模型生成替换后的良性片段 $c_m$。
    *   **梯度差异损失 ($\ell_{GD}$)**：在遗忘集上降低答案准确度，在保留集（Retain Set）上维持答案准确度。
    *   **推理保留损失 ($\ell_{RP}$)**：在保留集上进行标准的语言建模训练，确保模型不会丢失通用的推理逻辑。
*   **优化策略**：使用 AdamW 优化器，通过交替训练遗忘集和保留集的小批量数据来实现端到端的参数更新。

### 3. 实验设计
*   **数据集**：
    *   **R-TOFU**：一个合成基准数据集，包含 4,000 条关于虚构人物传记的问答及详细 CoT，用于受控遗忘实验。
    *   **medical-o1-reasoning**：包含 19,700 条复杂的临床和生物医学推理案例，代表真实世界的高价值专业领域。
*   **基准模型（Backbones）**：Reasoning-Llama-3.2-1B 和 Nemotron-Nano-8B。
*   **对比方法**：**R2MU**（推理感知表示误导遗忘），该方法通过将推理轨迹的内部表示映射到随机向量来实施遗忘。
*   **评估指标**：使用 ROUGE-L 衡量生成内容与参考答案的重合度，并定义了**遗忘误差（UE）**来量化遗忘模型与“从头训练模型（Gold Standard）”之间的差距。

### 4. 资源与算力
*   **算力说明**：论文**未明确说明**具体的 GPU 型号、数量或总训练时长。
*   **实现细节**：提到了使用 AdamW 优化器、学习率调度器（带预热阶段）以及调用了外部 API（GPT-3.5-turbo 和 GPT-4o-mini）来生成替换文本。

### 5. 实验数量与充分性
*   **实验规模**：
    *   涵盖了两种不同规模的模型（1B 和 8B）。
    *   设置了三种遗忘比例（1%、3%、5%）。
    *   进行了消融实验，分析了超参数 $\beta_g$（遗忘强度）和 $\beta_r$（保留强度）对性能的影响。
    *   提供了推理输出的可视化对比。
*   **充分性评价**：实验设计较为全面，涵盖了合成数据与真实专业领域数据。通过与 R2MU 的对比，证明了其在保持推理逻辑连贯性方面的优势。实验过程考虑了遗忘率的变化，具有较好的客观性。

### 6. 主要结论与发现
*   **精准性**：FRUL 能有效擦除特定的敏感知识，其在遗忘集上的表现非常接近“从未见过该数据”的理想模型。
*   **逻辑保留**：相比于基线方法（R2MU）容易导致模型输出无意义的乱码，FRUL 能够让模型在遗忘特定事实后，依然输出结构清晰、语法正确的推理过程。
*   **通用性**：在医疗等复杂领域，该方法在移除特定医学知识的同时，不会损害模型处理其他医疗问题的通用推理能力。

### 7. 优点
*   **结构感知**：不同于粗暴的表示层抹除，FRUL 关注推理的逻辑结构，通过“占位符替换”维持了模型的思维连贯性。
*   **无需外部正则数据集**：与 R2MU 不同，FRUL 不需要额外的辅助推理数据集来防止过拟合，降低了实施门槛。
*   **多模型协作**：利用 RAG 和多个 LLM 进行不确定性感知的数据清洗，提高了识别敏感片段的准确率。

### 8. 不足与局限
*   **预处理成本**：依赖于 GPT-4 等高性能模型来生成替换文本，对于大规模遗忘任务，预处理的 API 调用成本和时间成本较高。
*   **幻觉风险**：如果负责识别片段的 LLM 出现幻觉，可能会误删非敏感的通用推理步骤，或未能完全识别所有敏感点。
*   **应用限制**：目前主要针对文本推理，对于包含多模态信息或极长推理链（Long CoT）的 LRMs，其片段定位的复杂度和准确性有待进一步验证。

（完）

---
title: "Jellyfish: Zero-Shot Federated Unlearning Scheme with Knowledge Disentanglement"
title_zh: Jellyfish：基于知识解耦的零样本联邦遗忘学习方案
authors: "Houzhe Wang, Xiaojie Zhu, Chi Chen"
date: 2026-04-05
pdf: "https://arxiv.org/pdf/2604.04030v1"
tags: ["query:mu-ce"]
score: 10.0
evidence: 具有知识解耦的零样本联邦遗忘
tldr: 针对联邦学习中的隐私保护需求，本文提出 Jellyfish 零样本联邦遗忘方案。该方案通过生成误差最小化噪声作为代理数据，利用知识解耦机制限制特定通道激活以分离待遗忘知识，并设计了包含多种损失项的综合函数来对齐遗忘与保留目标，最后通过零样本修复机制在不访问本地数据的情况下恢复模型性能，实现了高效且隐私安全的模型更新。
selection_source: fresh_fetch
motivation: 旨在解决联邦学习中如何在不依赖原始用户数据的前提下，彻底移除特定数据对模型的影响并保持模型效用的难题。
method: 采用合成代理数据、知识解耦正则化、多组件综合损失函数以及零样本模型修复机制来实现高效的联邦遗忘。
result: 在多种实验设置下的评估结果验证了该方案在实现数据遗忘的同时，能够有效维持模型的准确性和鲁棒性。
conclusion: Jellyfish 方案证明了结合知识解耦与零样本学习可以实现在保护隐私的前提下，对联邦学习模型进行稳健的遗忘处理。
---

## 摘要
随着数据隐私和安全性的日益重要，联邦遗忘学习作为一个新兴研究领域应运而生，致力于确保一旦特定数据被删除，联邦学习模型将不再保留或泄露相关信息。在本文中，我们提出了一种名为 Jellyfish 的零样本联邦遗忘学习方案。它在四个关键方面区别于传统的联邦遗忘学习框架：合成数据生成、知识解耦、损失函数设计以及模型修复。为了保护被遗忘数据的隐私，我们设计了一种零样本遗忘机制，通过生成误差最小化噪声作为待遗忘数据的代理数据。为了维持模型的效用，我们首先提出了一种知识解耦机制，通过限制待遗忘数据所激活的通道数量并鼓励激活稀疏性，来正则化最后一个卷积层的输出。接着，我们构建了一个包含多个组件的综合损失函数，包括硬损失（hard loss）、混淆损失（confusion loss）、蒸馏损失（distillation loss）、模型权重漂移损失（model weight drift loss）、梯度协调（gradient harmonization）和梯度掩码（gradient masking），以有效对齐“遗忘”与“保留”目标的学习轨迹。最后，我们提出了一种零样本修复机制，在不访问用户本地数据的情况下，利用代理数据将模型准确率恢复到可接受的范围内。为了评估所提零样本联邦遗忘学习方案的性能，我们在多种设置下进行了全面的实验。结果验证了该方案的有效性和鲁棒性。

## Abstract
With the increasing importance of data privacy and security, federated unlearning emerges as a new research field dedicated to ensuring that once specific data is deleted, federated learning models no longer retain or disclose related information. In this paper, we propose a zero-shot federated unlearning scheme, named Jellyfish. It distinguishes itself from conventional federated unlearning frameworks in four key aspects: synthetic data generation, knowledge disentanglement, loss function design, and model repair. To preserve the privacy of forgotten data, we design a zero-shot unlearning mechanism that generates error-minimization noise as proxy data for the data to be forgotten. To maintain model utility, we first propose a knowledge disentanglement mechanism that regularises the output of the final convolutional layer by restricting the number of activated channels for the data to be forgotten and encouraging activation sparsity. Next, we construct a comprehensive loss function that incorporates multiple components, including hard loss, confusion loss, distillation loss, model weight drift loss, gradient harmonization, and gradient masking, to effectively align the learning trajectories of the objectives of ``forgetting" and ``retaining". Finally, we propose a zero-shot repair mechanism that leverages proxy data to restore model accuracy within acceptable bounds without accessing users' local data. To evaluate the performance of the proposed zero-shot federated unlearning scheme, we conducted comprehensive experiments across diverse settings. The results validate the effectiveness and robustness of the scheme.

---

## 论文详细总结（自动生成）

### 论文总结：Jellyfish - 基于知识解耦的零样本联邦遗忘学习方案

#### 1. 核心问题与研究动机
*   **核心问题**：在联邦学习（FL）框架下，如何响应用户的“被遗忘权”（如 GDPR/CCPA 要求），在**不访问原始私有数据**（零样本）且**不重新训练模型**的前提下，高效、彻底地从全局模型中移除特定用户或特定类别的数据贡献，同时保持模型对剩余数据的性能。
*   **研究动机**：
    *   **隐私风险**：传统遗忘学习往往需要用户上传待删除数据，存在二次隐私泄露风险。
    *   **知识纠缠**：模型内部不同类别的特征往往耦合在一起，强行遗忘某类数据会导致其他无关类别的准确率大幅下降（过度遗忘）。
    *   **优化冲突**：遗忘（让模型变差）与保留（让模型维持性能）是两个相互冲突的优化目标，难以平衡。

#### 2. 方法论：核心思想与关键技术
Jellyfish 方案由四个核心模块组成：
*   **误差最小化噪声（Error-Minimization Noise）**：
    *   用户在本地通过对抗优化生成与待删除数据具有相似预测特性的“代理噪声数据”（Nf），服务器仅利用这些噪声进行遗忘操作，无需接触原始图像。
*   **知识解耦机制（Knowledge Disentanglement）**：
    *   通过正则化最后卷积层的输出，限制待遗忘数据激活的通道数量（仅保留 Top-$\alpha$ 通道），并鼓励激活稀疏性。这能将特定类别的知识“隔离”在少数通道中，减少对其他类别的影响。
*   **综合损失函数设计**：
    *   **硬损失（Hard Loss）**：惩罚模型对遗忘类别的正确预测。
    *   **混淆损失（Confusion Loss）**：引导模型将遗忘样本误分类为最相似的错误类别。
    *   **蒸馏损失（Distillation Loss）**：利用“无能教师模型”引入随机性，消除残余知识。
*   **多目标优化策略**：
    *   **模型权重漂移损失（Drift Loss）**：约束遗忘后的模型参数不偏离原始模型太远。
    *   **梯度协调（Gradient Harmonization）**：通过梯度投影解决遗忘与保留任务之间的梯度冲突。
    *   **梯度掩码（Gradient Masking）**：识别并屏蔽与遗忘数据强相关的参数更新，防止知识残留。
*   **零样本修复机制**：
    *   若遗忘后性能下降，用户可提供剩余数据的代理噪声（Nr），服务器据此进行微调修复。

#### 3. 实验设计
*   **数据集**：MNIST、CIFAR-10、CIFAR-100。
*   **模型架构**：LeNet-5、ResNet32、ResNet56。
*   **Benchmark（基准）**：以“从头开始重新训练（Retrain）”作为理想性能上限。
*   **对比方法（Baselines）**：共 9 种，包括传统的负梯度下降（B2）、随机标签微调（B3）、大学习率微调（B4）、无能教师法（B5）、CONFUSE（B6）、QuickDrop（B7）、SCRUB（B8）和 NegGrad+（B9）。
*   **评估指标**：剩余数据准确率、遗忘数据准确率、L2 距离、JSD 散度、T-test 显著性检验、成员推理攻击（MIA）成功率。

#### 4. 资源与算力
*   **硬件设备**：使用了两台机器，分别配备 **NVIDIA 2080 Ti** 和 **NVIDIA 4090 D** GPU。
*   **算力表现**：论文提供了详细的计算效率分析（表 5）。在 CIFAR-10 上，Jellyfish 的总执行时间约为 67.21 秒，远低于重新训练的 2379.3 秒。GPU 显存占用在 1.89GB 至 4.45GB 之间，具有较好的工程可行性。

#### 5. 实验数量与充分性
*   **实验规模**：涵盖了单类别遗忘和多类别遗忘（总数的 20%）。
*   **消融实验**：非常充分，分别验证了知识解耦、梯度协调、梯度掩码、各类损失项以及代理数据对最终效果的贡献。
*   **客观性**：引入了成员推理攻击（MIA）来量化隐私保护效果，并使用统计学检验（T-test）验证模型分布的差异，实验设计较为严谨、公平。

#### 6. 主要结论与发现
*   **遗忘彻底性**：Jellyfish 能将遗忘数据的准确率降至 0% 左右，且 MIA 成功率接近 50%（等同于随机猜测），证明了遗忘的彻底性。
*   **效用保持**：在移除特定知识后，模型对剩余数据的准确率保持在与重新训练模型相当的水平（差距通常在 1% 以内）。
*   **零样本可行性**：实验证明使用合成的代理噪声完全可以替代原始私有数据完成遗忘任务，且性能损失微乎其微。

#### 7. 优点与亮点
*   **隐私性极强**：实现了真正的零样本联邦遗忘，服务器全程不接触用户原始数据。
*   **解耦思路新颖**：通过卷积通道正则化解决知识纠缠问题，提升了遗忘的精确度。
*   **优化稳健**：引入梯度协调和掩码技术，有效解决了多目标优化中的冲突问题，使模型更新更稳定。
*   **闭环设计**：不仅考虑了遗忘，还提供了可选的零样本修复机制，增强了方案的实用性。

#### 8. 不足与局限
*   **超参数敏感性**：方案涉及多个损失项权重（如 $\mu_c, \mu_d$）和解耦阈值 $\alpha$，在不同数据集上可能需要精细调优。
*   **代理数据质量**：虽然噪声数据有效，但在极度复杂或高维的数据分布下，噪声生成的质量可能影响修复效果。
*   **应用限制**：目前主要针对分类任务（图像），对于生成式模型（如大语言模型）或回归任务的适用性尚未探讨。

（完）

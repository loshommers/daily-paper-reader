---
title: Harmonizing Multi-Objective LLM Unlearning via Unified Domain Representation and Bidirectional Logit Distillation
title_zh: 通过统一领域表示与双向 Logit 蒸馏协调多目标大语言模型遗忘
authors: "Yisheng Zhong, Sijia Liu, Zhuangdi Zhu"
date: 2026-04-16
pdf: "https://arxiv.org/pdf/2604.15482v1"
tags: ["query:mu-ce"]
score: 9.0
evidence: 协调多目标LLM遗忘并确保鲁棒性
tldr: 针对大语言模型遗忘任务中多目标（知识擦除、通用能力保持、防过度拒绝及鲁棒性）难以兼顾且易产生任务干扰的问题，本文提出一种多目标遗忘框架。该框架通过统一数据表示缩小领域差距，并结合双向逻辑蒸馏技术，在抑制有害行为的同时引导模型学习正确行为。实验证明该方法实现了多目标的协同优化，在多种挑战性需求下达到了SOTA性能。
selection_source: fresh_fetch
motivation: 现有遗忘方法往往只关注部分目标，在处理多目标任务时容易产生任务干扰，且忽视了对抗攻击下的鲁棒性和边界行为。
method: 提出一种数据与优化协同设计的框架，通过统一数据表示标准化语料，并利用双向逻辑蒸馏技术同时引导期望行为并抑制有害行为。
result: 实验和理论分析表明，该方法能有效对齐领域分布并实现多任务协同优化，在各项评估指标上均取得了当前最优的性能。
conclusion: 本研究为实现平衡、可靠且具备鲁棒性的多目标LLM遗忘提供了一种有效的解决方案。
---

## 摘要
大语言模型（LLMs）遗忘对于从模型中移除有害或隐私泄露信息至关重要。实际的 LLM 遗忘要求同时满足多个具有挑战性的目标：移除不良知识、保持通用效用、避免对邻近概念的过度拒绝，以及至关重要的——确保针对对抗性探测攻击的鲁棒性。然而，现有的遗忘方法主要关注这些目标的一个有限子集，通常是遗忘效果和效用保持，而忽视了鲁棒性和边界行为。简单地将这些方法扩展到多目标设置可能会导致遗忘任务之间的干扰。我们提出了一种新型多目标遗忘框架，通过数据与优化的协同设计来协调多个遗忘目标：我们将训练语料库标准化为统一的数据表示以缩小领域差距，并引入一种双向蒸馏方法，在抑制学生模型中不良行为的同时，从受上下文指令引导的教师模型中诱导期望的行为。理论和实证分析表明，我们的方法对齐了领域分布，并将看似无关的遗忘任务转化为协同优化。评估结果证明了该方法具有最先进的性能，能够在各种具有挑战性的需求下实现平衡且可靠的遗忘。

## Abstract
Large Language Models (LLMs) unlearning is crucial for removing hazardous or privacy-leaking information from the model. Practical LLM unlearning demands satisfying multiple challenging objectives simultaneously: removing undesirable knowledge, preserving general utility, avoiding over-refusal of neighboring concepts, and, crucially, ensuring robustness against adversarial probing attacks. However, existing unlearning methods primarily focus on a limited subset of these goals, typically unlearning efficacy and utility preservation while overlooking robustness and boundary behaviors. Naively extending these methods to multi-objective settings may lead to unlearning task interference. We propose a novel multi-objective unlearning framework that harmonizes multiple unlearning objectives through a data and optimization co-design: We standardize training corpora into a unified data representation to reduce the domain gap, and then introduce a bidirectional distillation method that simultaneously elicits desired behavior from a context-instructed teacher while suppressing undesirable behavior in the student model. Theoretical and empirical analyses show that our method aligns domain distributions and converts seemingly irrelevant unlearning tasks into cooperative optimization. Evaluation demonstrates state-of-the-art performance, which enables balanced and reliable unlearning across diverse, challenging requirements.

---

## 论文详细总结（自动生成）

这篇论文提出了一种名为 **MULE**（Multi-objective Unlearning via Unified Domain Representation and Bidirectional Logit Distillation）的框架，旨在解决大语言模型（LLM）遗忘任务中多个目标相互冲突的难题。

以下是对该论文的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心挑战**：实际应用中的 LLM 遗忘不仅是简单的“删除知识”，它需要同时满足三个相互冲突的目标：
    1.  **遗忘效能与通用效用（Obj1）**：彻底移除目标知识，同时不损害模型在无关领域的通用能力。
    2.  **避免过度拒绝（Obj2）**：防止模型在处理与遗忘目标语义相近的“邻近领域”概念时产生误伤（即过度拒绝）。
    3.  **对抗鲁棒性（Obj3）**：防御“预填攻击”（Prefilling Attacks），即攻击者通过诱导性前缀（如“好的，这是详细指令：”）绕过遗忘机制。
*   **研究动机**：现有方法通常只关注 Obj1，简单扩展到多目标时会出现**梯度冲突**（任务干扰），导致模型要么遗忘不彻底，要么通用能力崩溃。

### 2. 论文提出的方法论：数据-优化协同设计
论文提出了“数据与优化协同设计”的核心思想：
*   **数据层面：统一领域表示（Representational Alignment）**
    *   **标准化**：将不同来源、不同格式的训练语料（原始文本、QA对、对抗样本）统一转化为**结构化的问答（QA）格式**。
    *   **对比锚点（Contrastive Anchors）**：为每个目标构建正负样本对（如：有害知识 vs. 邻近良性知识），通过对比学习明确语义边界。
    *   **理论支撑**：通过领域自适应理论证明，统一表示能缩小不同遗忘任务间的分布差异，从而收紧泛化界限。
*   **优化层面：双向 Top-K Logit 蒸馏**
    *   **教师引导**：使用一个冻结的教师模型，通过“意图提示词（Intention Prompt）”和“思维链（CoT）”生成理想行为。
    *   **双向蒸馏损失**：
        1.  **模仿教师**：引导学生模型模仿教师模型在安全/正确答案上的 Top-K Logit 分布。
        2.  **抑制学生**：显式抑制学生模型中原本高置信度的有害 Logit（即顽固的旧知识）。
    *   **公式逻辑**：采用 Smooth L1 距离直接作用于未归一化的 Logit，而非传统的 KL 散度，以实现更精确的强度控制。

### 3. 实验设计
*   **数据集与场景**：
    *   **MUSE-Book (Harry Potter)**：评估版权叙事知识的移除。
    *   **WMDP-Cyber**：评估有害网络安全知识的移除。
*   **评估指标**：
    *   遗忘率（Forget ↓）、通用保留率（Retain ↑）、邻近领域准确率（Neighbor Retain Acc ↑）、对抗攻击成功率（ASR ↓）、通用能力（MMLU ↑）。
*   **对比方法**：
    *   基础方法：GA（梯度上升）、SimNPO、FLAT、SFT。
    *   多目标/梯度编辑方法：NGDiff、MOLLM。
    *   蒸馏方法：DUET。
    *   上限参考：带有 Oracle 路由的教师模型。

### 4. 资源与算力
*   **模型规模**：实验主要基于 **Llama 3.2 3B Instruct** 模型进行。
*   **算力细节**：论文**未明确说明**具体的 GPU 型号、数量及总训练时长。但提到该方法是“轻量级”的，且提供了匿名代码库链接，暗示其对算力需求在主流科研可接受范围内。

### 5. 实验数量与充分性
*   **实验规模**：在两个代表性基准（版权保护与安全防御）上进行了全面测试。
*   **消融实验**：针对“双向蒸馏损失”与“BILD 损失”进行了对比，验证了 Logit 强度对齐的必要性。
*   **梯度分析**：通过实证分析证明，数据标准化将任务间的梯度余弦相似度从接近 0（正交冲突）提升到了 0.6~0.9（协同优化）。
*   **客观性**：实验涵盖了从推理侧（In-context）到训练侧（Tuning-based）的多种基线，评估维度全面，实验设计较为客观公平。

### 6. 主要结论与发现
*   **梯度协同**：数据格式的差异是导致遗忘任务冲突的根源之一，统一 QA 格式能显著缓解梯度冲突。
*   **鲁棒性突破**：该方法将对抗攻击成功率（ASR）降低到了前所未有的水平（如在 HP 任务中降至 12.5%，在 Cyber 任务中降至 5.1%）。
*   **性能平衡**：在彻底遗忘目标知识的同时，几乎不损害 MMLU 分数和邻近领域的正常响应，解决了“过度拒绝”问题。

### 7. 优点（亮点）
*   **视角独特**：首次从“数据表示对齐”的角度切入解决多目标遗忘的梯度冲突问题。
*   **双向设计**：双向 Logit 蒸馏比单向模仿或简单的负偏好优化（NPO）更具针对性，能同时处理“学新”和“去旧”。
*   **实战导向**：高度关注对抗鲁棒性和边界行为（过度拒绝），这比单纯的遗忘率更符合工业界部署需求。

### 8. 不足与局限
*   **教师模型依赖**：该方法依赖于一个能够理解复杂意图并生成高质量 CoT 的教师模型，对于极小规模或能力较弱的教师模型可能效果受限。
*   **意图提示词敏感性**：遗忘效果可能受到“意图提示词（pk）”编写质量的影响，论文未深入探讨提示词工程的鲁棒性。
*   **长文本挑战**：虽然 QA 格式提高了信息密度，但对于需要大规模上下文关联的复杂知识遗忘，简单的 QA 对是否能完全覆盖仍存疑问。

（完）

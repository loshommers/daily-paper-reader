---
title: "CiPO: Counterfactual Unlearning for Large Reasoning Models through Iterative Preference Optimization"
title_zh: CiPO：通过迭代偏好优化实现大推理模型的反事实遗忘
authors: "Junyi Li, Yongqiang Chen, Ningning Ding"
date: 2026-04-17
pdf: "https://arxiv.org/pdf/2604.15847v1"
tags: ["query:mu-ce"]
score: 10.0
evidence: 通过反事实遗忘选择性删除无用的隐私或版权信息
tldr: 针对大推理模型（LRM）在遗忘任务中难以彻底清除思维链（CoT）信息或导致推理性能下降的问题，本文提出CiPO框架。该方法通过生成逻辑自洽的反事实推理轨迹，并利用迭代偏好优化引导模型学习错误目标，从而在不损害通用推理能力的前提下，实现对特定知识及其推理过程的彻底遗忘，解决了遗忘彻底性与推理性能保持之间的矛盾。
selection_source: fresh_fetch
motivation: 现有遗忘方法难以清除大推理模型长思维链中的敏感信息，且容易在遗忘过程中损害模型的逻辑推理能力。
method: 提出CiPO框架，通过构造反事实推理轨迹并进行迭代偏好优化，使模型在推理过程中主动偏离原始知识。
result: 实验表明CiPO能彻底移除中间推理步骤和最终答案中的目标知识，同时有效保留了模型的通用推理性能。
conclusion: CiPO通过反事实推理干预，为大推理模型的安全遗忘提供了一种兼顾彻底性和功能保留的新方案。
---

## 摘要
机器遗忘（Machine unlearning）近年来受到越来越多的关注，它是一种极具前景的技术，能够从在大规模人类数据上训练的大语言模型中，选择性地移除不需要的隐私或受版权保护的信息。然而，强调通过长思维链（CoT）推理来解决复杂问题的大推理模型（LRMs）的出现，给机器遗忘带来了困境：现有方法要么难以从思维链轨迹中完全消除不需要的知识，要么由于干扰推理过程而导致推理性能下降。为此，我们提出了通过迭代偏好优化的反事实遗忘（CiPO），这是一个将遗忘重新定义为对大推理模型中思维链推理进行针对性干预的新型框架。具体而言，给定一个需要遗忘的目标答案，CiPO 指导大推理模型生成逻辑有效的反事实推理轨迹，用于偏好微调。随着大推理模型适应反事实轨迹，CiPO 迭代地更新偏好学习数据，以增加与原始模型的差异。这种迭代循环既保证了理想的遗忘效果，又实现了平滑优化，从而有效缓解了上述困境。在具有挑战性的基准测试上的实验表明，CiPO 在遗忘方面表现出色，能够完全移除中间思维链步骤和最终答案中的知识，同时保留大推理模型的推理能力。

## Abstract
Machine unlearning has gained increasing attention in recent years, as a promising technique to selectively remove unwanted privacy or copyrighted information from Large Language Models that are trained on a massive scale of human data. However, the emergence of Large Reasoning Models (LRMs), which emphasize long chain-of-thought (CoT) reasoning to address complex questions, presents a dilemma to unlearning: existing methods either struggle to completely eliminate undesired knowledge from the CoT traces or degrade the reasoning performances due to the interference with the reasoning process. To this end, we introduce Counterfactual Unlearning through iterative Preference Optimization (CiPO), a novel framework that redefines unlearning as the targeted intervention of the CoT reasoning in LRMs. More specifically, given a desired unlearning target answer, CiPO instructs LRMs to generate a logically valid counterfactual reasoning trace for preference tuning. As the LRM adjusts to the counterfactual trace, CiPO iteratively updates the preference learning data to increase the discrepancy from the original model. This iterative loop ensures both desirable unlearning and smooth optimization, effectively mitigating the dilemma. Experiments on challenging benchmarks demonstrate that CiPO excels at unlearning, completely removing knowledge from both the intermediate CoT steps and the final answer, while preserving the reasoning abilities of LRMs.

---

## 论文详细总结（自动生成）

以下是对论文《CiPO: Counterfactual Unlearning for Large Reasoning Models through Iterative Preference Optimization》的结构化深度总结：

### 1. 核心问题与研究动机
*   **核心问题**：大推理模型（LRMs，如 DeepSeek-R1、OpenAI o1）通过长思维链（CoT）解决复杂问题，但这也带来了新的隐私风险——敏感或版权信息不仅存在于最终答案中，还可能泄露在中间推理步骤中。
*   **研究动机**：
    *   **现有方法的局限**：传统的遗忘方法（如梯度上升 GA、负偏好优化 NPO）主要针对最终输出，难以清除 CoT 中的知识。
    *   **拒绝策略的缺陷**：让模型回答“我不知道”（IDK）会导致训练不稳定、推理能力下降，且容易遭受成员推理攻击（通过一致的拒绝识别出哪些数据被遗忘）。
    *   **表示干预的副作用**：强行抑制内部表示（如 R2MU）会破坏模型的逻辑连贯性，导致输出乱码。
*   **目标**：在彻底清除 CoT 和答案中的特定知识的同时，保留模型的通用推理能力。

### 2. 方法论：CiPO 框架
CiPO 将遗忘任务重新定义为对 CoT 推理过程的**因果干预**，核心思想是“反事实替代”而非简单的“删除”。

*   **核心思想**：引导模型学习一条逻辑自洽但指向错误事实（反事实）的推理路径，从而在概率分布上覆盖原始的敏感知识。
*   **关键技术细节**：
    1.  **反事实生成器（Counterfactual Generator）**：利用目标模型自身生成一个看似合理但事实错误的答案 $a^*$，并反向构造一条通往该答案的逻辑思维链 $c^*$。这种“自给自足”的方式保证了生成内容与模型原有风格的一致性。
    2.  **迭代偏好优化（Iterative Preference Optimization）**：
        *   **构造偏好对**：将反事实轨迹 $\{c^*, a^*\}$ 作为“优选（Chosen）”样本，将模型当前实时生成的包含敏感信息的轨迹作为“弃选（Rejected）”样本。
        *   **在线采样**：在训练过程中动态采样模型输出，确保优化目标始终针对模型最新的知识泄露点。
    3.  **优化目标**：结合了 **SimPO**（一种无需参考模型的偏好优化算法）、**NLL 损失**（用于稳定反事实目标的学习）以及**保留损失（Retain Loss）**（确保模型在非遗忘数据集上的性能）。

### 3. 实验设计
*   **数据集/场景**：
    *   **R-TOFU**：针对 LRM 优化的合成遗忘基准，包含虚构的人物百科及对应的 CoT 轨迹。设置了 1%、5%、10% 三种遗忘比例。
    *   **真实世界场景**：基于 RE-TURN 数据集，筛选出模型产生深度记忆的 260 个真实公众人物隐私信息进行遗忘测试。
*   **对比方法**：包括 GA（梯度上升）、GD（梯度差异）、NPO、以及专门针对 LRM 的 DirectIDK、AnswerIDK、ReasonedIDK 和 R2MU。
*   **评估指标**：
    *   **遗忘有效性**：答案级遗忘（AFE）、CoT 级遗忘（CFE）。
    *   **模型效用**：模型效用得分（MU）、MMLU（通用知识）、GSM8K（推理能力）、WikiText（语言建模质量/困惑度）。

### 4. 资源与算力
*   **硬件环境**：使用了 **2 张 NVIDIA A800 GPU (80GB)**。
*   **辅助算力**：调用了 Azure 平台上的 **GPT-4o API** 作为实验中的“LLM-as-a-Judge”，用于评估 CoT 的泄露程度和答案准确性。
*   **训练细节**：在 R-TOFU 上训练 5 个 epoch，包含 3-5 个 epoch 的 SFT 预热。

### 5. 实验数量与充分性
*   **实验规模**：
    *   涵盖了从 1% 到 10% 不同规模的遗忘集。
    *   在 Llama-3-8B 和 Qwen-3-8B 两种不同架构的蒸馏版推理模型上进行了验证。
    *   进行了详细的**消融实验**，验证了迭代采样、NLL 损失、SimPO 算法以及预热阶段对最终效果的贡献。
*   **充分性评价**：实验设计较为全面，不仅关注遗忘率，还通过困惑度（PPL）和逻辑推理题（GSM8K）严格监控了模型是否出现“灾难性遗忘”或“能力坍塌”，实验结果具有较强的说服力和客观性。

### 6. 主要结论与发现
*   **CiPO 实现了最佳平衡**：在彻底移除 CoT 泄露和错误答案的同时，CiPO 对模型通用推理能力（GSM8K）的损害最小。
*   **反事实推理的优势**：相比于直接让模型闭嘴（IDK），给模型一个“错误的逻辑出口”能更有效地稳定训练梯度，避免模型输出乱码。
*   **迭代采样的必要性**：静态的偏好对无法应对模型在微调过程中不断演变的分布，只有迭代采样才能捕捉到残余的知识泄露。

### 7. 优点
*   **因果视角新颖**：从因果干预的角度切入，将遗忘视为对推理路径的重定向。
*   **非破坏性**：避免了传统方法中常见的“能力坍塌”问题，生成的 CoT 依然保持逻辑结构。
*   **自监督特性**：利用模型自身生成反事实数据，降低了对外部高质量标注数据或强力教师模型的依赖。

### 8. 不足与局限
*   **应用场景受限**：目前主要针对 QA 形式的事实性知识遗忘，对于非结构化文本或更复杂的版权内容遗忘效果尚待验证。
*   **反事实构造的质量**：如果模型无法生成逻辑自洽的反事实轨迹，CiPO 的效果可能会打折扣。
*   **计算开销**：迭代采样和偏好优化相比简单的梯度上升需要更多的计算资源和训练时间。

（完）

---
title: "Secure Forgetting: A Framework for Privacy-Driven Unlearning in Large Language Model (LLM)-Based Agents"
title_zh: 安全遗忘：一种基于大语言模型 (LLM) 智能体的隐私驱动遗忘框架
authors: "Dayong Ye, Tainqing Zhu, Congcong Zhu, Feng He, Qi He, Shang Wang, Bo Liu, Wanlei Zhou"
date: 2026-04-01
pdf: "https://arxiv.org/pdf/2604.00430v1"
tags: ["query:mu-ce"]
score: 10.0
evidence: LLM智能体中隐私驱动的机器遗忘框架
tldr: 针对LLM智能体在实际应用中积累敏感或过时知识的问题，本文首次提出了LLM智能体遗忘（Unlearning）框架。该框架将遗忘场景分为状态、轨迹和环境三类，并引入一种基于自然语言的方法，通过转换模型将遗忘请求变为可执行提示词。研究还设计了对抗性推理评估机制。实验表明，该框架能在不影响非目标任务性能的前提下，有效实现目标知识的清除并抵御隐私推断攻击。
selection_source: fresh_fetch
motivation: LLM智能体在集成到现实应用时会积累敏感或过时信息，需要一种机制使其能选择性地遗忘特定知识以确保隐私安全。
method: 提出了一个涵盖状态、轨迹和环境遗忘的综合框架，并开发了将自然语言请求转化为可执行遗忘提示词的转换模型。
result: 实验结果显示，该方法能有效清除目标知识并抵御对抗性推理攻击，同时保持智能体在非遗忘任务上的原有性能。
conclusion: 本研究为LLM智能体的隐私驱动遗忘奠定了基础，提供了一套兼顾遗忘效果、任务性能和安全性的系统化解决方案。
---

## 摘要
基于大语言模型 (LLM) 的智能体由于 LLM 强大的推理能力，近期受到了广泛关注。现有研究主要集中在增强这些智能体在不同场景下的任务性能。然而，随着基于 LLM 的智能体越来越多地集成到现实应用中，关于其积累敏感或过时知识的担忧日益凸显。解决这些担忧需要开发允许智能体选择性遗忘先前学习知识的机制，从而产生了一个新术语：基于 LLM 的智能体遗忘 (LLM-based agent unlearning)。本文开启了对基于 LLM 智能体遗忘的研究。具体而言，我们提出了一个新颖且全面的框架，将遗忘场景分为三种语境：状态遗忘（遗忘特定状态或项目）、轨迹遗忘（遗忘动作序列）和环境遗忘（遗忘整个环境或任务类别）。在该框架内，我们引入了一种基于自然语言的遗忘方法，通过训练一个转换模型将高层遗忘请求转化为可执行的遗忘提示词，引导智能体完成受控的遗忘过程。此外，为了评估所提框架的鲁棒性，我们引入了一个遗忘推理对手，该对手能够构建提示词、查询智能体并观察其行为，试图推断出已遗忘的知识。实验结果表明，我们的方法能有效地使智能体遗忘目标知识，同时保持在非目标任务上的性能，并防止对手推断出已遗忘的知识。

## Abstract
Large language model (LLM)-based agents have recently gained considerable attention due to the powerful reasoning capabilities of LLMs. Existing research predominantly focuses on enhancing the task performance of these agents in diverse scenarios. However, as LLM-based agents become increasingly integrated into real-world applications, significant concerns emerge regarding their accumulation of sensitive or outdated knowledge. Addressing these concerns requires the development of mechanisms that allow agents to selectively forget previously learned knowledge, giving rise to a new term LLM-based agent unlearning. This paper initiates research on unlearning in LLM-based agents. Specifically, we propose a novel and comprehensive framework that categorizes unlearning scenarios into three contexts: state unlearning (forgetting specific states or items), trajectory unlearning (forgetting sequences of actions) and environment unlearning (forgetting entire environments or categories of tasks). Within this framework, we introduce a natural language-based unlearning method that trains a conversion model to transform high-level unlearning requests into actionable unlearning prompts, guiding agents through a controlled forgetting process. Moreover, to evaluate the robustness of the proposed framework, we introduce an unlearning inference adversary capable of crafting prompts, querying agents, and observing their behaviors in an attempt to infer the forgotten knowledge. Experimental results show that our approach effectively enables agents to forget targeted knowledge while preserving performance on untargeted tasks, and prevents the adversary from inferring the forgotten knowledge.

---

## 论文详细总结（自动生成）

### 论文总结：安全遗忘（Secure Forgetting）——基于 LLM 智能体的隐私驱动遗忘框架

#### 1. 核心问题与整体含义（研究动机和背景）
随着基于大语言模型（LLM）的智能体（如 AI 助手、自动驾驶、家政机器人）广泛应用，智能体会通过交互积累大量敏感、私密或过时的知识。
*   **研究动机**：传统的“机器遗忘”主要针对神经网络参数更新，而 LLM 智能体的行为由 Prompt 和外部记忆驱动，且用户通常无法修改底层 LLM 的参数。
*   **核心问题**：如何在不修改底层 LLM 参数的前提下，使智能体选择性地遗忘特定知识，同时不损害其在非目标任务上的性能？本文首次定义了“LLM 智能体遗忘”这一研究方向。

#### 2. 方法论：核心思想与技术细节
论文提出了一个统一的框架，通过**提示词转换（Prompt Conversion）**和**记忆清理**实现遗忘。
*   **三大遗忘场景**：
    1.  **状态遗忘（State Unlearning）**：遗忘特定位置或物品（如私人住宅位置）。
    2.  **轨迹遗忘（Trajectory Unlearning）**：遗忘特定的动作序列（如去药柜取药的隐私路径），但允许访问单个状态。
    3.  **环境遗忘（Environment Unlearning）**：遗忘整个任务类别或环境布局（如不再熟悉某个特定工厂的结构）。
*   **关键技术：转换模型 $\mathcal{C}$**：
    *   **核心思想**：训练一个专门的转换模型，将用户的高层遗忘请求（如“忘记关于卧室的一切”）转化为智能体可执行的详细提示词（Actionable Prompts）。
    *   **训练流程**：
        1.  使用基础模型（如 GPT-Neo）生成多个候选提示词。
        2.  根据智能体在环境中的表现（是否成功遗忘且保持性能）标注“偏好”与“非偏好”样本。
        3.  使用类似 DPO（直接偏好优化）的损失函数进行微调，使模型学习生成高质量的遗忘指令。
*   **执行机制**：修改智能体的外部记忆（JSON 文件），并注入生成的遗忘提示词来约束 LLM 的决策逻辑。

#### 3. 实验设计
*   **实验平台（4个）**：
    *   **GridWorld**：自定义 2D 导航环境，用于基础逻辑验证。
    *   **AlfWorld**：文本驱动的虚拟家居环境，模拟复杂家务任务。
    *   **HotPotQA**：基于维基百科的问答数据集，模拟聊天机器人场景。
    *   **HumanEval**：代码生成评估，模拟防止生成恶意代码等对抗场景。
*   **对比基准（Baselines）**：
    *   **Code-based Prompting**：将请求转化为 Python 代码。
    *   **Example-based Prompting**：通过少样本示例引导遗忘。
*   **评估指标**：遗忘效能（Unlearning efficacy）、首次尝试遗忘成功率（Unlearn@1）、任务成功率（Success rate）。

#### 4. 资源与算力
*   **模型使用**：
    *   **转换模型基础**：GPT-Neo-2.7B（默认）、Pythia-2.8B、GPT-2-Medium。
    *   **底层智能体 LLM**：GPT-4o-Mini、Claude-3-haiku、Llama-3.3-70B。
*   **算力说明**：论文**未明确说明**具体的 GPU 型号、数量及训练总时长。但提到使用了 GPT-Neo-2.7B 进行微调，这类规模的模型通常在单张或少量消费级/企业级 GPU（如 A100/H100）上即可完成。

#### 5. 实验数量与充分性
*   **实验规模**：论文进行了极其详尽的实验，包括：
    *   在 4 个平台上针对 3 种遗忘场景的全面测试。
    *   **消融实验**：验证了转换模型 $\mathcal{C}$ 的必要性、微调的作用、不同基础模型的影响以及生成提示词数量 $m$ 的影响。
    *   **可扩展性研究**：测试了同时遗忘多个状态/轨迹/环境时的表现。
    *   **仿真研究**：在 Gazebo 机器人平台上进行了可视化验证。
    *   **对抗性测试**：设计了专门的推理对手进行隐私攻击测试。
*   **评价**：实验设计非常充分且客观，涵盖了从理论证明到多平台验证再到安全性评估的全过程。

#### 6. 主要结论与发现
*   **有效性**：所提的 NL-based（基于自然语言转换）方法在遗忘成功率和保持原有任务性能方面均优于代码化和示例化方法。
*   **模型差异**：GPT 和 Llama 在遗忘任务上表现强劲，而 Claude 在处理隐含上下文（如“放入柜子”隐含“加热”动作）时表现较弱。
*   **副作用**：过度推理（Over-reasoning）可能导致“过度遗忘”，例如为了忘记加热任务而拒绝所有涉及微波炉的操作。
*   **安全性**：该方法能显著降低对手通过观察行为重建环境或推断遗忘状态的成功率（从 96% 降至约 15%）。

#### 7. 优点
*   **开创性**：首次系统性地提出了 LLM 智能体遗忘框架，填补了该领域的空白。
*   **非侵入性**：无需修改 LLM 权重，适用于闭源模型 API。
*   **理论支撑**：提供了收敛性分析和隐私鲁棒性的数学证明。
*   **实战导向**：引入了对抗性推理模型，从攻击者视角验证了遗忘的彻底性。

#### 8. 不足与局限
*   **模型依赖**：转换模型 $\mathcal{C}$ 的性能高度依赖于其基础模型的表达能力（如 GPT-2 规模的模型效果较差）。
*   **过度遗忘风险**：在复杂场景下，智能体可能因提示词约束过于严格而误伤正常任务。
*   **动态环境挑战**：实验主要在静态或受控环境中进行，对于高度动态、多智能体协作的实时遗忘场景探讨较少。
*   **计算开销**：每次遗忘请求需要通过转换模型生成提示词，增加了推理环节的 Token 消耗。

（完）

---
title: "Oblivion: Self-Adaptive Agentic Memory Control through Decay-Driven Activation"
title_zh: Oblivion：通过衰减驱动激活实现的自适应智能体记忆控制
authors: "Ashish Rana, Chia-Chien Hung, Qumeng Sun, Julian Martin Kunkel, Carolin Lawrence"
date: 2026-03-31
pdf: "https://arxiv.org/pdf/2604.00131v1"
tags: ["query:mu-ce"]
score: 8.0
evidence: 通过衰减驱动的记忆激活在智能体中实现选择性遗忘
tldr: 针对LLM智能体在长程交互中面临的内存干扰和高延迟问题，本文提出Oblivion框架。该框架借鉴人类记忆的遗忘与激活机制，将遗忘视为可访问性的衰减而非彻底删除。通过解耦读写路径，Oblivion能根据不确定性动态决定检索时机，并强化对生成响应有贡献的记忆，实现了高效的层次化记忆管理，显著提升了智能体的推理效能。
selection_source: fresh_fetch
motivation: 现有的LLM智能体通常采用全量检索和扁平化存储，导致在处理长历史任务时出现严重的干扰和高延迟。
method: 提出一种基于衰减驱动的记忆控制框架，通过解耦读路径（基于不确定性触发检索）和写路径（基于贡献度强化记忆）来实现动态记忆管理。
result: 在长程交互基准测试中，Oblivion能够根据上下文动态调整记忆访问和强化，有效平衡了学习与遗忘。
conclusion: 实验证明，模拟人类记忆的动态控制机制是实现高效且具备长程推理能力的LLM智能体的关键。
---

## 摘要
人类记忆通过选择性遗忘进行适应：经验随时间变得难以获取，但可以通过强化或上下文线索重新激活。相比之下，记忆增强型大语言模型（LLM）智能体依赖“始终开启”的检索和“扁平化”的记忆存储，随着历史记录的增长，会导致严重的干扰和延迟。我们推出了 Oblivion，这是一个记忆控制框架，它将遗忘视为由衰减驱动的可访问性降低，而非显式的删除。Oblivion 将记忆控制解耦为读路径和写路径。读路径根据智能体的不确定性和记忆缓冲区的充足程度来决定何时查阅记忆，从而避免冗余的始终开启式访问。写路径通过强化对生成响应有贡献的记忆，来决定需要加强的内容。两者结合实现了一种分层记忆组织，在保持持久的高层策略的同时，根据需要动态加载细节。我们在静态和动态的长时程交互基准测试上进行了评估。结果表明，Oblivion 能够动态调整记忆访问和强化，在不断变化的上下文中平衡学习与遗忘，强调了记忆控制对于有效的 LLM 智能体推理至关重要。源代码可在 https://github.com/nec-research/oblivion 获取。

## Abstract
Human memory adapts through selective forgetting: experiences become less accessible over time but can be reactivated by reinforcement or contextual cues. In contrast, memory-augmented LLM agents rely on "always-on" retrieval and "flat" memory storage, causing high interference and latency as histories grow. We introduce Oblivion, a memory control framework that casts forgetting as decay-driven reductions in accessibility, not explicit deletion. Oblivion decouples memory control into read and write paths. The read path decides when to consult memory, based on agent uncertainty and memory buffer sufficiency, avoiding redundant always-on access. The write path decides what to strengthen, by reinforcing memories contributing to forming the response. Together, this enables hierarchical memory organization that maintains persistent high-level strategies while dynamically loading details as needed. We evaluate on both static and dynamic long-horizon interaction benchmarks. Results show that Oblivion dynamically adapts memory access and reinforcement, balancing learning and forgetting under shifting contexts, highlighting that memory control is essential for effective LLM-agentic reasoning. The source code is available at https://github.com/nec-research/oblivion.

---

## 论文详细总结（自动生成）

### 论文总结：Oblivion - 通过衰减驱动激活实现的自适应智能体记忆控制

#### 1. 核心问题与整体含义（研究动机和背景）
现有的记忆增强型大语言模型（LLM）智能体在处理长程交互时面临三大挑战：
*   **检索冗余（Always-on Retrieval）：** 无论是否需要，智能体在每一轮对话都会进行检索，导致高延迟和计算浪费。
*   **记忆结构扁平化：** 缺乏对高层策略和底层细节的区分，通常采用“硬删除”方式管理上下文，导致重要信息丢失。
*   **干扰与污染：** 随着记忆库增长，无关或过时的信息会干扰推理（即“大海捞针”难题）。
**研究动机：** 借鉴人类心理学中的“选择性遗忘”机制——遗忘并非删除，而是可访问性的下降。论文旨在构建一个能根据不确定性动态检索、并根据贡献度强化记忆的自适应控制框架。

#### 2. 方法论：核心思想与关键技术
**Oblivion** 框架将记忆管理解耦为**读路径（Read Path）**和**写路径（Write Path）**，由一个中央**执行器（Executor）**协调：

*   **层次化记忆表示：**
    *   **L1（动态程序记忆）：** 元索引和操作策略，常驻缓冲区。
    *   **L2（语义记忆）：** 带有时间戳的事实陈述。
    *   **L3（抢占式情节记忆）：** 将原始对话转化为带有反馈的行动指南。
*   **读路径（不确定性门控检索）：**
    *   **衰减器（Decayer）：** 使用 LLM 评判和嵌入相似度评估当前缓冲区是否足以回答问题。若不确定性高，则触发检索。
    *   **艾宾浩斯遗忘曲线：** 引入保留分数 $R_t(c) = \exp(-n_t(c)/S_t(c))$，其中 $n$ 是距离上次访问的轮数，$S$ 是稳定性（受使用频率和效用影响）。分数低的记忆逐渐“沉寂”但不删除。
    *   **激活器（Activator）：** 采用基于有向无环图（DAG）的查询扩展，进行多步证据发现。
*   **写路径（反馈驱动更新）：**
    *   **识别器（Recognizer）：** 进行“信用分配”，仅强化那些对生成当前响应有实际贡献的记忆条目。
    *   **存储管理器：** 更新效用元数据，新记忆持久化到外部存储。

#### 3. 实验设计
*   **数据集/场景：**
    *   **LONG MEM EVAL：** 静态长文本 QA 基准，包含约 100K 上下文，测试智能体在长对话历史中提取证据的能力。
    *   **GOOD AILTM：** 动态长程交互基准，包含约 120K 上下文，评估智能体在持续对话中的记忆保留、修正和更新能力。
*   **对比方法：**
    *   **Direct（直接处理）：** 全上下文输入或截断输入。
    *   **Memory-augmented（记忆增强）：** 包括 LME-RFT、Ever-Memos（自组织记忆系统）和 Beyond Prompts（类似 MemGPT 的扁平检索系统）。
*   **模型后端：** Phi-4-mini (4B), Qwen3-30B, GPT-4o-mini, GPT-4.1-mini。

#### 4. 资源与算力
*   论文**未明确说明**具体的硬件算力（如 GPU 型号或数量）。
*   由于该框架主要是一个**推理时（Inference-time）**的控制层，不涉及大规模的基础模型预训练，因此其算力消耗主要体现在 API 调用和本地小模型的推理延迟上。
*   论文提供了详细的计算效率分析，指出 Oblivion 相比全上下文模式可减少约 73%-77% 的 Token 消耗。

#### 5. 实验数量与充分性
*   **实验规模：** 涵盖了静态 QA 和动态交互两大类任务，使用了 4 种不同规模和来源的 LLM 后端。
*   **消融实验：** 针对记忆层次（L1/L2/L3 的组合）和遗忘曲线参数（温度 $T$）进行了详细分析。
*   **客观性：** 采用了 3 次运行取平均值及 LLM-as-a-judge 协议，并计算了 Hedges' g 效应量以验证结果的显著性。实验设计较为全面，对比了当前最前沿的记忆管理系统。

#### 6. 主要结论与发现
*   **性能提升：** Oblivion 在所有基准测试中均优于直接处理和现有的记忆增强基准，尤其在非隔离的动态任务中提升显著（如 GPT-4.1-mini 在 2K 跨度下提升 33.7%）。
*   **成本优化：** 通过按需检索和衰减机制，大幅降低了输入 Token 数，在 120K 长文本场景下比全上下文模式节省了 70% 以上的成本。
*   **遗忘的价值：** 实验证明，模拟人类的“锯齿状”强化与衰减模式能有效缓解“大海捞针”和“迷失中间”的问题。

#### 7. 优点与亮点
*   **读写解耦：** 改变了“始终检索”的思维定式，通过不确定性门控提高了效率。
*   **生物启发：** 将艾宾浩斯遗忘曲线成功引入智能体架构，实现了记忆的动态优先级管理而非简单删除。
*   **层次化结构：** L1-L3 的设计兼顾了高层策略（Procedural）和底层事实（Semantic/Episodic）。
*   **DAG 查询扩展：** 增强了处理复杂、多步推理问题的检索精度。

#### 8. 不足与局限
*   **模型依赖性：** 框架的性能高度依赖于后端 LLM 提取记忆和评估不确定性的能力，较弱的小模型可能导致质量下降。
*   **参数敏感性：** 衰减温度 $T$ 需要针对不同任务进行调优，不当的设置会导致过度遗忘或缓冲区饱和。
*   **覆盖范围：** 目前主要集中在文本交互，尚未在多模态、多语言或极大规模的生产环境（如百万级上下文）中进行验证。
*   **延迟开销：** 虽然节省了 Token，但多步检索和评估循环在某些情况下会增加首字响应延迟（TTFT）。

（完）

---
title: "PECKER: A Precisely Efficient Critical Knowledge Erasure Recipe For Machine Unlearning in Diffusion Models"
title_zh: PECKER：一种用于扩散模型机器遗忘的精确高效关键知识擦除方案
authors: "Zhiyong Ma, Zhitao Deng, Huan Tang, Jialin Chen, Zhijun Zheng, Zhengping Li, Qingyuan Chuai"
date: 2026-04-07
pdf: "https://arxiv.org/pdf/2604.05634v1"
tags: ["query:mu-ce"]
score: 10.0
evidence: 使用显著性掩码在扩散模型中实现高效机器遗忘
tldr: 针对扩散模型机器卸载（MU）中训练时间长和计算开销大的问题，本文提出了PECKER。该方法在蒸馏框架下引入显著性掩码，通过优先更新对目标数据遗忘贡献最大的关键参数，减少了无效梯度计算并加速收敛。实验证明，PECKER在CIFAR-10和STL-10数据集上能更高效地实现类别和概念遗忘，同时保持生成图像的质量。
selection_source: fresh_fetch
motivation: 现有的机器卸载方法因梯度更新方向不明确，导致训练效率低下且计算开销巨大。
method: 在蒸馏框架中引入显著性掩码，精准定位并优先更新与需遗忘知识最相关的模型参数。
result: 在CIFAR-10和STL-10上实现了更短的训练时间，且在类别和概念遗忘任务中表现优于现有方法。
conclusion: PECKER通过精准定位关键参数，为扩散模型提供了一种高效且稳定的机器卸载新方案。
---

## 摘要
机器遗忘（MU）已成为生成式人工智能（GenAI）模型安全合规运行的关键技术。尽管现有的机器遗忘方法行之有效，但大多数方法都会带来极高的训练时间和计算开销。我们的分析表明，其根本原因在于梯度更新方向不佳，这降低了训练效率并导致收敛不稳定。为了缓解这些问题，我们提出了 PECKER，这是一种高效的机器遗忘方法，其性能与现有主流方法相当甚至更优。在蒸馏框架内，PECKER 引入了显著性掩码（saliency mask），优先更新对遗忘目标数据贡献最大的参数，从而在不牺牲遗忘效果的前提下，减少了不必要的梯度计算并缩短了整体训练时间。我们的方法能够更快地生成遗忘相关类别或概念的样本，同时在 CIFAR-10 和 STL-10 数据集上与真实图像分布保持高度一致，在类别遗忘和概念遗忘方面均实现了更短的训练时间。

## Abstract
Machine unlearning (MU) has become a critical technique for GenAI models' safe and compliant operation. While existing MU methods are effective, most impose prohibitive training time and computational overhead. Our analysis suggests the root cause lies in poorly directed gradient updates, which reduce training efficiency and destabilize convergence. To mitigate these issues, we propose PECKER, an efficient MU approach that matches or outperforms prevailing methods. Within a distillation framework, PECKER introduces a saliency mask to prioritize updates to parameters that contribute most to forgetting the targeted data, thereby reducing unnecessary gradient computation and shortening overall training time without sacrificing unlearning efficacy. Our method generates samples that unlearn related class or concept more quickly, while closely aligning with the true image distribution on CIFAR-10 and STL-10 datasets, achieving shorter training times for both class forgetting and concept forgetting.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **PECKER** 的扩散模型机器遗忘（Machine Unlearning, MU）框架。以下是对该论文的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：现有的扩散模型机器遗忘方法（如全量重训或盲目的参数微调）存在**计算开销巨大、训练时间长、收敛不稳定**以及**容易损害非目标类别生成质量**（遗忘副作用）的问题。
*   **研究动机**：作者发现效率低下的根源在于梯度更新方向不明确。扩散模型的语义是分布式存储的，无差别的参数更新会浪费大量计算资源在无关权重上。因此，需要一种能精准定位并擦除“关键知识”且无需原始数据的方案。

### 2. 核心方法论
PECKER 结合了**行为级引导（蒸馏）**与**结构级参数选择（显著性掩码）**，其核心流程如下：
*   **交替伪评分监督（Alternating Pseudo-Score Supervision）**：引入一个轻量级的伪评分网络 $s_\psi$，在无需访问原始训练数据的情况下，通过生成伪样本提供类别偏移的监督信号。
*   **显著性评分与掩码（Saliency Scoring and Masking）**：
    1.  针对需要遗忘的类别，计算标准噪声预测目标下的梯度显著性。
    2.  根据梯度大小生成二值掩码 $m_s$，精准定位编码了“待遗忘语义”的关键参数区域。
*   **类别差异化更新（Class-differentiated Updates）**：
    *   **遗忘批次（Forget batches）**：仅更新掩码覆盖的显著参数，以收窄擦除区域并加速收敛。
    *   **保留批次（Retain batches）**：进行全参数更新，以确保模型对非目标类别的生成能力不受损。

### 3. 实验设计
*   **数据集与场景**：
    *   **类别遗忘**：在 CIFAR-10 和 STL-10 上测试（使用 DDPM 模型），目标是遗忘特定类别（如“飞机”）。
    *   **概念遗忘**：在 Stable Diffusion (SD v1.4) 上测试，包括名人（Brad Pitt, Angelina Jolie）和裸露内容（Nudity）的擦除。
*   **Benchmark 与对比方法**：
    *   对比了 Retrain（重训）、SA、SalUn、SFD（Score Forgetting Distillation）、ESD、SLD 等主流方法。
    *   使用 I2P（Inappropriate Image Prompts）基准评估裸露内容缓解效果。
*   **评估指标**：UA（遗忘准确率）、FID（图像质量）、IS（多样性）、GCD（名人检测得分）、不当内容概率等。

### 4. 资源与算力
*   **硬件环境**：实验使用了 **2 台 NVIDIA A100 GPU**。
*   **训练细节**：
    *   DDPM 实验中，Batch Size 分别为 128 (CIFAR-10) 和 64 (STL-10)。
    *   Stable Diffusion 实验中，在 100k 图像检查点处进行评估。
    *   相比于 SFD 需要 300k 图像的训练量，PECKER 在 100k 图像时已达到更优效果。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了从基础分类数据集到大规模文本生成图像模型的迁移，做了类别遗忘、名人遗忘、裸露内容遗忘三类核心实验。
*   **充分性评价**：实验设计较为全面，包含了定性（生成图对比）与定量分析。通过训练曲线（UA 和 FID 随 Step 变化）展示了收敛速度的优势。然而，论文中关于超参数 $\gamma$（掩码阈值）的消融实验细节相对较少。

### 6. 主要结论与发现
*   **高效性**：PECKER 在更短的训练时间内达到了与现有方法持平或更优的遗忘效果。例如在裸露内容擦除任务中，100k 步的效果优于 SFD 300k 步的效果。
*   **精准性**：通过显著性掩码，模型能更稳定地降低目标类别的准确率，同时 FID 指标显示其对保留类别的干扰极小。
*   **稳定性**：相比于 SFD，PECKER 的遗忘准确率曲线在训练后期更加平滑，不易出现波动。

### 7. 优点与亮点
*   **精准定位**：引入显著性掩码解决了扩散模型参数更新“眉毛胡子一把抓”的问题，提高了参数更新的信噪比。
*   **数据隐私**：采用数据免访问（Data-free）的蒸馏框架，符合隐私保护和合规性要求。
*   **收敛加速**：通过差异化更新策略，显著缩短了达到理想遗忘效果所需的迭代次数。

### 8. 不足与局限
*   **名人遗忘的副作用**：定量指标显示，PECKER 在遗忘名人时，有时是通过“移除面部特征”而非“替换身份”来实现的（Prop. w/o Faces 指标较高），这可能导致生成图像的结构完整性受损。
*   **超大规模验证不足**：虽然验证了 SD v1.4，但对于参数量更大的模型（如 SDXL 或更大规模的 Transformer-based Diffusion）的扩展性仍需进一步探索。
*   **掩码开销**：虽然减少了更新时的梯度计算，但计算显著性掩码本身需要额外的梯度回传步骤，论文未详细讨论这一部分的计算抵消情况。

（完）

---
title: "CURE:Circuit-Aware Unlearning for LLM-based Recommendation"
title_zh: CURE：面向基于大语言模型推荐的电路感知机器遗忘
authors: "Ziheng Chen, Jiali Cheng, Zezhong Fan, Hadi Amiri, Yunzhi Yao, Xiangguo Sun, Yang Zhang"
date: 2026-04-04
pdf: "https://arxiv.org/pdf/2604.04982v1"
tags: ["query:mu-ce"]
score: 10.0
evidence: 为基于大语言模型的推荐系统提出电路感知遗忘算法
tldr: 针对基于大语言模型的推荐系统（LLMRec）中的隐私风险，本文提出了CURE框架。该框架通过识别模型中负责特定任务的计算子图（电路），将模型组件划分为遗忘特定、保留特定和任务共享组，并实施差异化更新策略。这有效解决了传统遗忘学习中遗忘与保留目标间的梯度冲突问题，在保证推荐性能的同时实现了高效、透明的隐私数据擦除，提升了系统的可信度。
selection_source: fresh_fetch
motivation: 现有LLMRec遗忘学习方法因统一更新参数导致遗忘与保留目标间产生梯度冲突，且过程缺乏透明度。
method: 提出电路感知框架CURE，通过提取核心电路并将模块分类为遗忘、保留和共享组，执行功能特定的更新规则。
result: 在真实数据集上的实验表明，该方法在实现有效遗忘的同时，比现有基准更好地保持了模型的推荐效用。
conclusion: 通过电路分析实现模型组件的功能解耦和差异化更新，是优化LLMRec遗忘学习性能和透明度的关键。
---

## 摘要
大语言模型（LLMs）的最新进展通过实现对用户兴趣和物品属性的丰富语义理解与推理，为推荐系统开辟了新的机遇。然而，随着隐私法规的收紧，将用户数据纳入基于大语言模型的推荐（LLMRec）带来了显著的隐私风险，使得机器遗忘算法对于实际部署变得日益关键。尽管人们对 LLMRec 机器遗忘的兴趣日益浓厚，但大多数现有方法将遗忘过程表述为遗忘目标和保留目标的加权组合，并以统一的方式更新模型参数。这种表述方式不可避免地会导致两个目标之间的梯度冲突，从而引起优化不稳定，并导致遗忘效果不佳或模型效用严重下降。此外，遗忘过程在很大程度上仍是一个黑盒，削弱了其透明度和可信度。为了应对这些挑战，我们提出了 CURE，这是一个电路感知的机器遗忘框架，它将模型组件解耦为功能不同的子集并进行选择性更新。在这里，电路是指对特定任务行为负有因果责任的计算子图。具体而言，我们提取了物品推荐背后的核心电路，并分析了这些电路中的各个模块如何对遗忘和保留目标做出贡献。基于这一分析，这些模块被分为遗忘特定组、保留特定组和任务共享组，每组都遵循特定的更新规则，以减轻遗忘过程中的梯度冲突。在真实世界数据集上的实验表明，我们的方法比现有基准实现了更有效的机器遗忘。

## Abstract
Recent advances in large language models (LLMs) have opened new opportunities for recommender systems by enabling rich semantic understanding and reasoning about user interests and item attributes. However, as privacy regulations tighten, incorporating user data into LLM-based recommendation (LLMRec) introduces significant privacy risks, making unlearning algorithms increasingly crucial for practical deployment. Despite growing interest in LLMRec unlearning, most existing approaches formulate unlearning as a weighted combination of forgetting and retaining objectives while updating model parameters in a uniform manner. Such formulations inevitably induce gradient conflicts between the two objectives, leading to unstable optimization and resulting in either ineffective unlearning or severe degradation of model utility. Moreover, the unlearning procedure remains largely black-box, undermining its transparency and trustworthiness. To tackle these challenges, we propose CURE, a circuit-aware unlearning framework that disentangles model components into functionally distinct subsets and selectively updates them. Here, a circuit refers to a computational subgraph that is causally responsible for task-specific behaviors. Specifically, we extract the core circuits underlying item recommendation and analyze how individual modules within these circuits contribute to the forget and retain objectives. Based on this analysis, these modules are categorized into forget-specific, retain-specific, and task-shared groups, each subject to function-specific update rules to mitigate gradient conflicts during unlearning. Experiments on real-world datasets show that our approach achieves more effective unlearning than existing baselines.

---

## 论文详细总结（自动生成）

这是一份关于论文《CURE: Circuit-Aware Unlearning for LLM-based Recommendation》的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
随着隐私法规（如GDPR）的收紧，基于大语言模型的推荐系统（LLMRec）面临着必须移除特定用户数据（机器遗忘）的挑战。
*   **研究动机**：现有的LLMRec遗忘方法通常将“遗忘目标”和“保留目标”进行加权求和，并统一更新模型参数。这种做法存在两个痛点：
    1.  **梯度冲突**：优化遗忘目标的方向可能与保持模型效用的方向相反，导致优化不稳定，要么遗忘不彻底，要么推荐性能大幅下降。
    2.  **黑盒化**：缺乏对模型内部哪些模块（如注意力头、MLP）存储了敏感信息的理解，过程不透明。
*   **核心目标**：通过“电路感知”技术，识别并解耦模型中负责不同功能的计算子图，实现精准且透明的遗忘。

### 2. 方法论
论文提出了 **CURE** 框架，分为两个主要阶段：

*   **阶段一：定位 LLMRec 电路**
    *   **核心思想**：将模型视为计算有向无环图，识别对特定预测起关键作用的“电路”（子图）。
    *   **关键技术**：
        *   **激活干预（Activation Intervention）**：利用一阶泰勒展开高效估算各边对预测结果的贡献。
        *   **激活补丁（Activation Patching）**：通过对比原始输入和“损坏”输入的激活差异来精确定位。
        *   **图感知样本构建**：利用用户-物品图，通过个性化 PageRank (PPR) 算法识别与用户兴趣最不相关的物品，构建高质量的对比（损坏）样本。
*   **阶段二：选择性电路更新**
    *   **模块分类**：将神经元分为**遗忘特定**（仅对遗忘集贡献）、**保留特定**（仅对保留集贡献）和**功能共享**（两者共有）三类。
    *   **更新策略**：
        *   遗忘/保留特定神经元仅根据各自的损失函数更新。
        *   共享神经元采用**梯度投影策略**：当两个目标的梯度发生冲突（夹角大于90度）时，将一个梯度投影到另一个梯度的法平面上，消除破坏性分量。

### 3. 实验设计
*   **数据集**：MovieLens-1M (ML-1M) 和 GoodReads (GD)。
*   **模型骨干**：GPT-2 Large 和 Llama-2 (7B)。
*   **Benchmark 与对比方法**：
    *   **基准**：Retrain（重新训练，金标准）、SISA（分片重训）、RecEraser。
    *   **先进方法**：E2URec（教师-学生框架）、ROME（模型编辑）、WISE（知识内存编辑）、PCGrad（通用梯度手术）。
*   **评估指标**：推荐效用（AUC, ACC, LogLoss）、遗忘有效性（JSD 散度，衡量与重训模型输出的接近程度）、遗忘效率（耗时）。

### 4. 资源与算力
*   **硬件**：使用了 **NVIDIA A100 GPU**。
*   **技术细节**：针对 Llama-2 (7B) 使用了 **LoRA**（秩 $r=8$）以提高训练效率。
*   **时长**：论文提供了详细的时间对比表。例如，在 Llama-2 上，CURE 的遗忘过程大约需要 10,000-11,000 秒，比 E2URec 快约 3.5 倍，比完全重训（约 130,000 秒）快一个数量级。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了两种不同规模的模型（GPT-2 和 Llama-2）和两个不同领域的推荐数据集。
*   **消融实验**：对超参数 $\omega_r$（平衡因子）进行了敏感性分析。
*   **深度分析**：通过可视化梯度冲突分布（$\cos \psi$）和电路图，验证了方法在缓解冲突和提高透明度方面的有效性。
*   **评价**：实验设计较为全面，对比了从传统重训到最新模型编辑的各类方法，客观地展示了 CURE 在效用、有效性和效率之间的平衡。

### 6. 主要结论与发现
*   **性能优越**：CURE 在遗忘效率上提升了 18%，模型效用提升了 6%，且运行速度比主流基准快 3.5 倍。
*   **缓解冲突**：相比 E2URec，CURE 将严重冲突的梯度对比例降低了 55% 至 76%。
*   **透明度**：通过电路发现，能够清晰地看到模型在处理特定推荐任务时激活的内部路径，使遗忘过程更具可解释性。

### 7. 优点
*   **机制透明**：引入机械解释性（Mechanistic Interpretability），将黑盒遗忘转变为可解释的电路干预。
*   **优化稳定**：通过梯度投影和参数解耦，从根源上解决了多目标优化中的梯度冲突问题。
*   **模型无关**：该框架具有通用性，可以适配各种 LLM 骨干网络。

### 8. 不足与局限
*   **电路提取开销**：虽然使用了泰勒展开加速，但对于超大规模模型或极长序列，电路发现阶段仍存在一定的计算负担。
*   **图依赖性**：构建损坏样本依赖于用户-物品图的结构信息，对于缺乏图结构的纯文本推荐场景可能需要调整。
*   **应用限制**：主要针对二分类推荐任务（Yes/No），对于生成式长文本推荐的适用性尚待进一步验证。

（完）

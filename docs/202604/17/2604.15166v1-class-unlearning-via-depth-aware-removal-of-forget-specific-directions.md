---
title: Class Unlearning via Depth-Aware Removal of Forget-Specific Directions
title_zh: 通过深度感知的遗忘特定方向移除实现类别遗忘
authors: "Arman Hatami, Romina Aalishah, Ilya E. Monosov"
date: 2026-04-16
pdf: "https://arxiv.org/pdf/2604.15166v1"
tags: ["query:mu-ce"]
score: 10.0
evidence: 通过权重手术从表示中移除特定遗忘方向的类别遗忘方法
tldr: 针对类卸载中遗忘不彻底、内部表示残留信息的问题，本文提出了一种名为 DAMP 的单次闭式权重手术方法。该方法无需梯度优化，通过在各层投影去除特定于遗忘类的方向，并结合深度感知缩放策略，在保护保留类性能的同时，更有效地清除深层表示中的遗忘类结构，在多种架构和数据集上达到了接近重新训练的效果。
selection_source: fresh_fetch
motivation: 现有的类卸载方法往往只是抑制了分类头，而遗忘类的信息仍残留在模型的深层表示中，导致卸载不彻底且选择性差。
method: 提出 DAMP 方法，通过在各层计算类原型并利用投影矩阵移除遗忘类特有的残差方向，同时采用深度感知缩放规则来平衡知识擦除与性能保持。
result: 在 MNIST、CIFAR 和 Tiny ImageNet 等数据集上的实验表明，DAMP 在选择性遗忘和保留类准确率方面均优于现有方法，且更接近重新训练的基准。
conclusion: DAMP 提供了一种高效且无需训练的类卸载方案，证明了通过深度感知的权重投影可以实现更彻底的表示层级遗忘。
---

## 摘要
机器遗忘旨在从已训练模型中移除目标知识，而无需承担从头开始重新训练的成本。然而，在类别遗忘中，降低遗忘类别的准确率并不一定意味着真正的遗忘：遗忘的信息可能仍编码在内部表示中，而表象上的遗忘可能源于分类器头的抑制，而非表示层面的移除。我们展示了现有的类别遗忘方法通常表现出较弱或负向的选择性，在深层表示中保留了遗忘类别的结构，或者过度依赖最后一层的偏置偏移。随后，我们提出了 DAMP（通过投影进行的深度感知调制），这是一种单次、闭式解的权重手术方法，无需基于梯度的优化即可从预训练网络中移除遗忘特定方向。在每个阶段，DAMP 在下一个可学习算子的输入空间中计算类别原型，提取相对于保留类别原型的残差作为遗忘方向，并应用基于投影的更新以降低下游对这些方向的敏感性。为了保持效用，DAMP 使用了一种源自探针可分性的无参数深度感知缩放规则，在浅层应用较小的编辑，在深层应用较大的编辑。该方法通过低秩子空间移除，自然地扩展到多类别遗忘。在 MNIST、CIFAR-10、CIFAR-100 和 Tiny ImageNet 数据集以及卷积和 Transformer 架构上，DAMP 比以往的一些方法更接近重新训练这一金标准，在改善选择性遗忘的同时，更好地保持了保留类别的性能，并减少了深层中残留的遗忘类别结构。

## Abstract
Machine unlearning aims to remove targeted knowledge from a trained model without the cost of retraining from scratch. In class unlearning, however, reducing accuracy on forget classes does not necessarily imply true forgetting: forgotten information can remain encoded in internal representations, and apparent forgetting may arise from classifier-head suppression rather than representational removal. We show that existing class-unlearning methods often exhibit weak or negative selectivity, preserve forget-class structure in deep representations, or rely heavily on final-layer bias shifts. We then introduce DAMP (Depth-Aware Modulation by Projection), a one-shot, closed-form weight-surgery method that removes forget-specific directions from a pretrained network without gradient-based optimization. At each stage, DAMP computes class prototypes in the input space of the next learnable operator, extracts forget directions as residuals relative to retain-class prototypes, and applies a projection-based update to reduce downstream sensitivity to those directions. To preserve utility, DAMP uses a parameter-free depth-aware scaling rule derived from probe separability, applying smaller edits in early layers and larger edits in deeper layers. The method naturally extends to multi-class forgetting through low-rank subspace removal. Across MNIST, CIFAR-10, CIFAR-100, and Tiny ImageNet, and across convolutional and transformer architectures, DAMP more closely resembles the retraining gold standard than some of the prior methods, improving selective forgetting while better preserving retain-class performance and reducing residual forget-class structure in deep layers.

---

## 论文详细总结（自动生成）

以下是对论文《Class Unlearning via Depth-Aware Removal of Forget-Specific Directions》的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
论文关注的是**类别遗忘（Class Unlearning）**，即从已训练的模型中移除特定类别的知识，同时保留其他类别的性能。
*   **研究动机**：作者发现现有的遗忘方法存在“虚假遗忘”现象。许多方法仅仅是通过改变分类头（如调整偏置项）来抑制输出，而遗忘类别的特征信息仍然残留在模型的深层表示中。
*   **核心问题**：如何实现真正的“表示层级移除”，使得模型在内部特征空间中也无法识别遗忘类别，且无需高昂的重新训练成本。

### 2. 方法论：DAMP（深度感知投影调制）
DAMP 是一种**单次（One-shot）、闭式解（Closed-form）**的权重手术方法，其核心思想是直接从网络权重中投影掉“特定于遗忘类”的方向。
*   **核心思想**：在每一层，识别出仅属于遗忘类而不属于保留类的特征方向，然后修改权重矩阵，使其对这些方向“视而不见”。
*   **关键技术细节**：
    1.  **计算类原型**：在每个算子的输入空间计算保留类和遗忘类的均值向量（原型）。
    2.  **提取遗忘残差**：将遗忘类原型投影到保留类原型构成的子空间上，取其残差方向。这个残差代表了遗忘类特有的、无法被保留类解释的信息。
    3.  **权重投影更新**：使用公式 $W' = W(I - \alpha_\ell \tilde{Q}_\ell \tilde{Q}_\ell^\top)$ 更新权重。其中 $\tilde{Q}$ 是正交化的遗忘方向，$\alpha_\ell$ 是缩放系数。
    4.  **深度感知缩放（Depth-Aware Scaling）**：引入参数 $\alpha_\ell$，其值由两部分决定：一是基于线性探针（Probe）计算的层级可分性，二是随深度增加的确定性斜坡。这确保了浅层（共享特征多）编辑较轻，深层（类别特异性强）编辑较重。
*   **算法流程**：无需梯度优化，通过一次前向传递收集统计量，随后直接进行闭式权重修改。

### 3. 实验设计
*   **数据集**：MNIST、CIFAR-10、CIFAR-100、Tiny ImageNet。
*   **任务场景**：单类别遗忘、多类别遗忘（如随机选择 5% 的类别）、持续遗忘（Sequential Unlearning）以及语义分割任务。
*   **Benchmark（基准）**：以“从头开始重新训练（Retrain）”作为金标准。
*   **对比方法（9种）**：包括梯度上升（GAU）、知识蒸馏（KDU）、微调（DD-FT）、Logit掩码（LM）、随机重标记（RandRelabel）、选择性突触抑制（SSD）、显著性遗忘（SalUn）等。
*   **评估指标**：保留类准确率（Racc）、遗忘类准确率（Facc）、选择性得分（Selectivity）、表示相似度分析（RDM）、t-SNE 可视化、偏置偏移分析。

### 4. 资源与算力
*   **硬件**：实验在单台 **NVIDIA A100 GPU (80GB)** 上运行。
*   **算力消耗**：由于 DAMP 是单次闭式更新，其计算效率极高。相比于需要多次迭代微调的方法（如 GAU, SalUn），DAMP 的计算时间几乎可以忽略不计，且不需要反向传播（Backward Pass）。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了从简单（MNIST）到复杂（Tiny ImageNet）的多个数据集，并跨越了三种主流架构：**CNN-5、ResNet-18 和 ViT**。
*   **充分性**：作者不仅测试了分类准确率，还深入分析了内部表示空间（RDM 矩阵和 t-SNE），并进行了消融实验（验证深度感知系数 $\alpha$ 的有效性）和对抗攻击测试。
*   **客观性**：对比了几乎所有主流的后处理遗忘方法，并明确指出了现有方法在偏置项上的“作弊”行为，实验设计较为严谨、公平。

### 6. 主要结论与发现
*   **表示层移除**：DAMP 能有效移除深层表示中的遗忘类结构，而不仅仅是抑制输出。
*   **选择性高**：在降低遗忘类准确率的同时，对保留类的损伤极小，其“选择性”指标显著优于现有方法。
*   **无需偏置依赖**：DAMP 不需要像其他方法那样将遗忘类的偏置设为极大的负值来实现“伪遗忘”。
*   **架构通用性**：该方法在卷积网络和 Transformer 上均表现良好，证明了其作为“权重手术”的通用性。

### 7. 优点
*   **高效性**：单次计算，无需梯度下降，适合大规模模型或实时遗忘需求。
*   **深度感知设计**：科学地处理了神经网络中“浅层特征共享、深层特征特异”的特性，平衡了遗忘与性能保留。
*   **理论简洁**：基于线性代数投影，物理含义明确，易于实现。
*   **鲁棒性**：在对抗攻击下，DAMP 处理后的模型表现出更好的稳定性。

### 8. 不足与局限
*   **线性假设**：该方法假设类别表示在特征空间中是线性可分的，且可以用均值原型代表。对于高度多模态或各向异性的复杂分布，简单的均值残差可能不足以捕捉所有遗忘信息。
*   **超参数微调**：虽然号称“无参数”，但在某些复杂任务（如 ViT 在 Tiny ImageNet 上）中，仍需手动微调 $\alpha$ 的基数以达到最佳效果。
*   **应用限制**：目前主要验证了视觉分类和分割任务，在自然语言处理（NLP）等其他领域的适用性尚待验证。

（完）

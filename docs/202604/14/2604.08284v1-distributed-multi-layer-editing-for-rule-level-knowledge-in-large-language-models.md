---
title: Distributed Multi-Layer Editing for Rule-Level Knowledge in Large Language Models
title_zh: 大语言模型中规则级知识的分布式多层编辑
authors: "Yating Wang, Wenting Zhao, Yaqi Zhao, Yongshun Gong, Yilong Yin, Haoliang Sun"
date: 2026-04-09
pdf: "https://arxiv.org/pdf/2604.08284v1"
tags: ["query:mu-ce"]
score: 9.0
evidence: 大语言模型中规则级知识的模型编辑
tldr: 本研究针对大语言模型中规则级知识（如数理规则）的编辑难题，指出传统事实级编辑方法因假设知识局部化而无法处理多形式一致性。通过扩展RuleEdit基准并进行细粒度因果追踪，发现规则知识在Transformer层中呈分层分布：公式与描述位于前层，实例位于中层。据此提出分布式多层编辑（DMLE）方法，通过在不同层级同步更新不同形式的知识，显著提升了规则编辑的迁移性和理解力。
selection_source: fresh_fetch
motivation: 传统模型编辑方法假设知识是局部化的，无法有效处理需要在公式、描述和实例间保持一致性的分布式规则级知识。
method: 提出DMLE方法，根据因果追踪发现的分布规律，在前层应用公式和描述的共享更新，在层中应用实例的独立更新。
result: 在GPT-J、Qwen和LLaMA等模型上，DMLE在实例迁移性和规则理解方面分别比最强基准平均提升了13.91和50.19个百分点。
conclusion: 规则级知识在模型中具有非均匀的层级分布特征，采用针对性的分布式多层编辑策略是实现规则一致性修改的有效途径。
---

## 摘要
大语言模型不仅存储孤立的事实，还存储支持跨符号表达式、自然语言解释和具体实例进行推理的规则。然而，大多数模型编辑方法是针对事实级知识设计的，假设目标编辑可以通过局部干预来实现。这一假设对于规则级知识并不成立，因为单个规则必须在多个相互依赖的形式之间保持一致。我们通过对规则级知识编辑的机理性研究来探讨这一问题。为了支持这项研究，我们将 RuleEdit 基准测试从 80 条手动验证的规则扩展到 200 条，涵盖数学和物理领域。细粒度的因果追踪揭示了 Transformer 层中规则知识具有特定形式的组织结构：公式和描述集中在较早的层，而实例则更多地与中间层相关联。这些结果表明，规则知识并非均匀地局部化，因此无法通过单层或连续块干预进行可靠编辑。基于这一见解，我们提出了分布式多层编辑（DMLE），它对公式和描述应用共享的早期层更新，并对实例应用单独的中间层更新。在保持标准编辑指标竞争力的同时，DMLE 实现了显著更强的规则级编辑性能。在 GPT-J-6B、Qwen2.5-7B、Qwen2-7B 和 LLaMA-3-8B 上，与最强基线相比，它将实例迁移性和规则理解平均分别提高了 13.91 和 50.19 个百分点。代码可在 https://github.com/Pepper66/DMLE 获取。

## Abstract
Large language models store not only isolated facts but also rules that support reasoning across symbolic expressions, natural language explanations, and concrete instances. Yet most model editing methods are built for fact-level knowledge, assuming that a target edit can be achieved through a localized intervention. This assumption does not hold for rule-level knowledge, where a single rule must remain consistent across multiple interdependent forms. We investigate this problem through a mechanistic study of rule-level knowledge editing. To support this study, we extend the RuleEdit benchmark from 80 to 200 manually verified rules spanning mathematics and physics. Fine-grained causal tracing reveals a form-specific organization of rule knowledge in transformer layers: formulas and descriptions are concentrated in earlier layers, while instances are more associated with middle layers. These results suggest that rule knowledge is not uniformly localized, and therefore cannot be reliably edited by a single-layer or contiguous-block intervention. Based on this insight, we propose Distributed Multi-Layer Editing (DMLE), which applies a shared early-layer update to formulas and descriptions and a separate middle-layer update to instances. While remaining competitive on standard editing metrics, DMLE achieves substantially stronger rule-level editing performance. On average, it improves instance portability and rule understanding by 13.91 and 50.19 percentage points, respectively, over the strongest baseline across GPT-J-6B, Qwen2.5-7B, Qwen2-7B, and LLaMA-3-8B. The code is available at https://github.com/Pepper66/DMLE.

---

## 论文详细总结（自动生成）

以下是对论文《Distributed Multi-Layer Editing for Rule-Level Knowledge in Large Language Models》的结构化总结：

### 1. 核心问题与整体含义
*   **研究背景**：大语言模型（LLMs）不仅存储孤立的事实，还存储复杂的“规则级知识”（如数学公式、物理定律）。这些规则通常以多种形式存在：符号公式、自然语言描述和具体数值实例。
*   **核心问题**：现有的模型编辑方法（如 ROME, MEMIT）基于“局部化假设”，即认为知识存储在模型的特定单层或连续少数层中。然而，这种假设在处理规则级知识时失效，导致编辑后的模型在不同形式（如改了公式但实例计算仍按旧规则）之间缺乏一致性。
*   **研究动机**：探索规则级知识在模型内部的组织方式，并据此设计一种能保持跨形式一致性的编辑方法。

### 2. 方法论：分布式多层编辑 (DMLE)
*   **核心思想**：基于因果追踪（Causal Tracing）的发现，将编辑操作分布在模型不同的层级组中，而非集中于单一点。
*   **关键技术细节**：
    *   **因果追踪分析**：研究发现公式（Formula）和描述（Description）的因果贡献主要集中在模型的**早期层**（Early layers），而具体实例（Instance）的推理贡献则集中在**中间层**（Middle layers）。
    *   **分布式更新策略**：
        1.  **共享更新**：针对公式和描述，在早期层应用一个共享的权重更新（$\Delta W_{fd}$），通过联合最小二乘优化实现。
        2.  **独立更新**：针对数值实例，在中间层应用一个单独的权重更新（$\Delta W_i$）。
    *   **算法流程**：首先识别不同形式知识的因果峰值层，然后将规则分解为“公式-描述”组和“实例”组，分别在对应的层级范围内利用 MEMIT 的闭式解进行参数微调。

### 3. 实验设计
*   **数据集/Benchmark**：构建了 **RuleEdit-200**。该数据集将原有的 RuleEdit 从 80 条规则扩展到 200 条手动验证的数学和物理规则，每条规则包含对齐的公式、描述和实例，并配有反事实（Counterfactual）编辑目标。
*   **基准模型**：GPT-J-6B、Qwen2-7B、Qwen2.5-7B、LLaMA-3-8B。
*   **对比方法**：
    *   **参数更新类**：ROME、MEMIT（目前最强的局部编辑基准）。
    *   **非参数类**：GRACE（基于适配器）、PROMPT（基于上下文学习）。
*   **评估指标**：除了标准的可靠性（Rel.）、泛化性（Gen.）和局部性（Loc.），重点引入了**规则理解（RU）**和**实例迁移性（IP）**。

### 4. 资源与算力
*   **算力说明**：论文中**未明确指出**具体的 GPU 型号、数量或训练总时长。
*   **辅助工具**：提到使用 Gemini 生成反事实数据，并使用 DeepSeek-V3.2 作为自动评估器来判定生成结果的准确性。

### 5. 实验数量与充分性
*   **实验规模**：在 4 个不同架构的流行模型上进行了测试，涵盖了 200 条规则（共 600 个测试样本）。
*   **消融实验**：设计了三组消融实验（FLSU、FLJU、SFSU），分别验证了“固定层 vs 分布层”以及“独立更新 vs 联合更新”的影响。
*   **充分性评价**：实验设计较为充分，跨越了多个模型家族，且通过细粒度的因果追踪热图（Heatmaps）提供了坚实的机理性解释。对比实验涵盖了主流的参数化与非参数化编辑路径，结果具有客观性。

### 6. 主要结论与发现
*   **知识分布规律**：规则知识在 LLM 中并非局部化存储，而是呈分层组织：早期层处理抽象概念和符号，中间层处理具体的数值计算。
*   **性能提升**：DMLE 在规则级指标上表现卓越。相比最强基准，其实例迁移性（IP）平均提升 **13.91%**，规则理解（RU）平均提升 **50.19%**。
*   **一致性**：通过在不同层级同步更新，DMLE 成功解决了“公式改了但实例没改”的脱节问题。

### 7. 优点
*   **机理性强**：不同于盲目增加编辑层数，DMLE 的设计完全基于对模型内部机理（因果追踪）的观察。
*   **规则一致性**：首次系统性地解决了规则编辑中的跨形式对齐问题。
*   **数据集贡献**：扩展后的 RuleEdit-200 为该领域的后续研究提供了更高质量的基准。

### 8. 不足与局限
*   **模型规模局限**：实验主要在 6B 到 8B 参数的模型上完成，尚未在 70B 或更大规模的模型上验证该层级分布规律是否保持一致。
*   **领域覆盖**：目前仅限于数理规则，对于法律条文、逻辑谬误纠正等非数理类规则的适用性尚待探索。
*   **预分析开销**：DMLE 依赖于前置的因果追踪分析来确定层范围，这对于新模型来说增加了一定的计算负担。

（完）

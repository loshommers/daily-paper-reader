---
title: "HiEdit: Lifelong Model Editing with Hierarchical Reinforcement Learning"
title_zh: HiEdit：基于分层强化学习的终身模型编辑
authors: "Yangfan Wang, Tianyang Sun, Chen Tang, Jie Liu, Wei Cai, Jingchi Jiang"
date: 2026-04-13
pdf: "https://arxiv.org/pdf/2604.11214v1"
tags: ["query:mu-ce"]
score: 10.0
evidence: 纠正过时知识的终身模型编辑
tldr: 针对大语言模型在持续编辑过程中因静态层选择导致的知识遗忘和适应性差问题，本文提出HiEdit框架。该框架利用分层强化学习，为每个编辑实例动态选择最相关的模型层进行更新。通过引入稀疏性奖励，HiEdit实现了精确的局部参数微调，在减少一半更新层数的同时，显著提升了编辑性能并有效缓解了灾难性遗忘。
selection_source: fresh_fetch
motivation: 现有模型编辑方法对所有实例使用固定的静态层，忽略了知识存储的层级差异，导致编辑效率低且易引发知识遗忘。
method: 提出HiEdit框架，采用分层强化学习动态识别每个编辑实例最相关的层，并结合稀疏性奖励实现局部精准更新。
result: "在多种大模型上的实验表明，HiEdit在仅更新约一半层数的情况下，将基准方法RLEdit的性能平均提升了8.48%。"
conclusion: 实例感知的动态层选择是提升持续模型编辑效果的关键，HiEdit通过分层强化学习有效平衡了编辑精度与模型稳定性。
---

## 摘要
终身模型编辑 (LME) 旨在按顺序纠正已部署大语言模型 (LLMs) 中过时或不准确的知识，同时最大限度地减少对无关输入的副作用。然而，现有方法通常对所有编辑实例在 LLM 的一组静态且密集的层上应用参数扰动。这种做法是违背直觉的，因为我们假设不同的知识存储在模型的不同层中。忽视这种层级特异性会阻碍整合新知识的适应性，并导致通用知识和先前编辑过的知识出现灾难性遗忘。为了解决这一问题，我们提出了 HiEdit，这是一个分层强化学习框架，能够为每个编辑实例自适应地识别最相关的知识层。通过启用动态的、实例感知的层选择，并引入稀疏性的内在奖励，HiEdit 实现了精确的局部更新。在各种 LLM 上的实验表明，HiEdit 在每次编辑仅扰动一半层的情况下，将竞争性方法 RLEdit 的性能平均提升了 8.48%。我们的代码可在以下网址获取：https://github.com/yangfanww/hiedit。

## Abstract
Lifelong model editing (LME) aims to sequentially rectify outdated or inaccurate knowledge in deployed LLMs while minimizing side effects on unrelated inputs. However, existing approaches typically apply parameter perturbations to a static and dense set of LLM layers for all editing instances. This practice is counter-intuitive, as we hypothesize that different pieces of knowledge are stored in distinct layers of the model. Neglecting this layer-wise specificity can impede adaptability in integrating new knowledge and result in catastrophic forgetting for both general and previously edited knowledge. To address this, we propose HiEdit, a hierarchical reinforcement learning framework that adaptively identifies the most knowledge-relevant layers for each editing instance. By enabling dynamic, instance-aware layer selection and incorporating an intrinsic reward for sparsity, HiEdit achieves precise, localized updates. Experiments on various LLMs show that HiEdit boosts the performance of the competitive RLEdit by an average of 8.48% with perturbing only half of the layers per edit. Our code is available at: https://github.com/yangfanww/hiedit.

---

## 论文详细总结（自动生成）

以下是对论文《HiEdit: Lifelong Model Editing with Hierarchical Reinforcement Learning》的深度总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：终身模型编辑（LME）旨在连续纠正大语言模型（LLM）中的过时知识，同时不损害通用能力或先前编辑过的知识。
*   **研究动机**：现有方法（如 RLEdit, AlphaEdit）通常对所有编辑实例使用**静态且密集**的层（即每次编辑都修改固定的、较多数量的层）。作者认为这违背直觉，因为不同知识通常存储在 LLM 的不同层中。这种“一刀切”的修改方式会导致参数过度扰动，从而引发严重的灾难性遗忘（包括通用能力下降和旧编辑知识丢失）。

### 2. 方法论：核心思想与技术细节
HiEdit 引入了**分层强化学习（HRL）**框架，将编辑过程解耦为两个子任务：
*   **核心思想**：通过高层决策选择“在哪里编辑”（层选择），通过底层决策决定“如何编辑”（参数更新）。
*   **关键技术细节**：
    *   **高层超网络（Manager）**：接收当前知识实例的梯度信号，通过梯度编码器和层特异性模块，利用“重要性路由（Importance Router）”输出层重要性分布，并使用 **Top-K** 机制生成层选择掩码。
    *   **底层超网络（Worker）**：仅对高层选定的层进行参数更新。它基于低秩梯度分解（如 MEND 或 MALMEN 风格）生成扰动量。
    *   **内在奖励机制（Intrinsic Reward）**：为了鼓励稀疏且高效的更新，高层奖励被定义为“部分层更新”相对于“全层更新”的**相对优势**。公式表达为：$r_{high} = r_{low}(partial) - r_{low}(full)$。
    *   **梯度流优化**：使用直通估计器（Straight-Through Estimator, STE）解决离散路由选择导致的不可导问题，实现端到端训练。

### 3. 实验设计
*   **数据集**：使用了 **ZsRE**（零样本关系抽取）和 **CounterFact**（反事实推理）两个主流基准。
*   **实验场景**：设计了极具挑战性的“8000*1”设置（连续进行 8000 次单实例编辑），并进一步扩展到 20,000 次编辑以测试扩展性。
*   **对比方法**：涵盖了 10 种基准方法，包括传统微调（FT）、定位编辑（ROME, MEMIT）、正则化方法（PRUNE, RECT, AlphaEdit）以及超网络方法（MEND, MALMEN, DAFNet, RLEdit）。
*   **评估指标**：有效性（Efficacy）、泛化性（Generalization）、特异性（Specificity）以及新提出的**编辑保留率（Edited Retention）**。

### 4. 资源与算力
*   **硬件**：实验主要在单张 **NVIDIA A100-80GB GPU** 上完成。
*   **模型加载**：使用 HuggingFace Transformers 库，以半精度（Half-precision）模式加载 LLM。
*   **训练细节**：论文提到了超参数配置（如 $d_1=256$），但未详细说明具体的总训练时长。

### 5. 实验数量与充分性
*   **实验规模**：在 Llama-3-8B 和 Gemma-2-9B 两个主流模型上进行了验证，编辑序列长度从 2,000 延伸至 20,000。
*   **充分性**：
    *   **消融实验**：详细分析了内在奖励、分层设计、RL 训练策略、实现风格及编辑层数 $K$ 的影响。
    *   **通用能力测试**：在 GLUE 任务（SST, MMLU, MRPC 等）上评估了编辑对模型基础能力的影响。
    *   **可解释性分析**：可视化了不同领域知识（如音乐 vs. 疾病）在层选择上的差异。
*   **客观性**：实验设置了多种变体（HiEdit full/rand）并与最先进的 RLEdit 进行了直接对比，结果显示其在减少 50% 参数扰动的情况下仍能提升性能，具有较强的说服力。

### 6. 主要结论与发现
*   **性能提升**：HiEdit 在各项指标上均优于现有方法，相比 RLEdit 平均提升了 **8.48%**。
*   **稀疏性优势**：仅更新约一半的层即可达到甚至超过全层更新的效果，证明了动态层选择的必要性。
*   **扩展性**：在长达 20,000 次的连续编辑中，HiEdit 表现出极强的鲁棒性，通用能力保留率接近原始模型。
*   **知识定位**：实验证实了不同类别的知识确实倾向于激活不同的模型层。

### 7. 优点
*   **自适应性**：首次将 LME 建模为分层决策过程，实现了实例级别的动态层选择。
*   **高效性**：通过内在奖励引导稀疏更新，减少了对模型参数的过度干预。
*   **鲁棒性**：在极长序列的编辑任务中，有效缓解了其他方法常见的“模型崩溃”或“严重遗忘”问题。

### 8. 不足与局限
*   **超参数依赖**：Top-K 机制中的 $K$ 值仍需预设，尚未实现完全动态的层数选择（如基于阈值的 Top-p）。
*   **知识类型限制**：实验主要集中在结构化事实知识（ZsRE/CounterFact），尚未在非结构化文本或特定专业领域（如医疗、法律）的复杂推理任务中验证。
*   **算法复杂度**：虽然推理效率高，但分层强化学习的训练过程相比简单超网络可能更复杂。

（完）

---
title: "TRU: Targeted Reverse Update for Efficient Multimodal Recommendation Unlearning"
title_zh: TRU：面向高效多模态推荐遗忘的针对性反向更新
authors: "Zhanting Zhou, KaHou Tam, Ziqiang Zheng, Zeyu Ma, Zhanting Zhou"
date: 2026-04-02
pdf: "https://arxiv.org/pdf/2604.02183v1"
tags: ["query:mu-ce"]
score: 10.0
evidence: 高效的多模态推荐系统遗忘
tldr: 针对多模态推荐系统（MRS）中用户数据难以彻底删除的问题，本文提出了一种名为TRU（Targeted Reverse Update）的即插即用遗忘框架。研究发现，删除数据的影响在排序行为、模态分支和网络层级中分布不均，导致传统均匀反向更新效果不佳。TRU通过排序融合门、模态缩放和层隔离技术实现精准反向更新，在保持推荐性能的同时实现了更彻底的遗忘，且效率远高于重新训练。
selection_source: fresh_fetch
motivation: 现有的近似遗忘方法忽略了多模态推荐系统中删除数据影响的非均匀分布特性，导致遗忘不彻底或性能受损。
method: 提出TRU框架，通过排序融合门抑制目标项影响、模态分支缩放平衡特征表示、以及容量感知层隔离来定位敏感模块的更新。
result: 在多个数据集和模型上的实验表明，TRU在保留性能与遗忘效果的权衡上优于现有基准，且更接近完全重新训练的效果。
conclusion: TRU证明了针对性反向更新在处理复杂多模态模型遗忘问题中的有效性，为高效且安全的推荐系统数据删除提供了新方案。
---

## 摘要
多模态推荐系统 (MRS) 联合建模用户-物品交互图和丰富的物品内容，但这种紧密耦合使得用户数据一旦被学习就难以移除。近似机器遗忘 (Approximate machine unlearning) 为完全重新训练提供了一种高效的替代方案，但现有的 MRS 遗忘方法主要依赖于在整个模型中进行大致均匀的反向更新。我们表明，这一假设与现代 MRS 根本不匹配：被删除数据的影响并非均匀分布，而是不均匀地集中在排序行为 (ranking behavior)、模态分支 (modality branches) 和网络层 (network layers) 中。这种非均匀性导致了 MRS 遗忘中的三个瓶颈：协作图中目标物品的持久性、特征分支间的模态失衡，以及参数空间中的层级敏感性。为了解决这种不匹配，我们提出了针对性反向更新 (TRU)，这是一种用于 MRS 的即插即用遗忘框架。TRU 并非应用盲目的全局反转，而是在模型层级结构中执行三个协调的干预措施：一个用于抑制排序中残留目标物品影响的排序融合门 (ranking fusion gate)，用于保留剩余多模态表示的分支模态缩放 (branch-wise modality scaling)，以及将反向更新定位到删除敏感模块的容量感知层隔离 (capacity-aware layer isolation)。在两个代表性骨干网络、三个数据集和三种遗忘机制上的实验表明，TRU 始终比之前的近似基准方法实现更好的保留-遗忘权衡 (retain-forget trade-off)，同时安全审计进一步证实了更深层次的遗忘以及更接近于在保留数据上进行完全重新训练的行为。

## Abstract
Multimodal recommendation systems (MRS) jointly model user-item interaction graphs and rich item content, but this tight coupling makes user data difficult to remove once learned. Approximate machine unlearning offers an efficient alternative to full retraining, yet existing methods for MRS mainly rely on a largely uniform reverse update across the model. We show that this assumption is fundamentally mismatched to modern MRS: deleted-data influence is not uniformly distributed, but concentrated unevenly across \textit{ranking behavior}, \textit{modality branches}, and \textit{network layers}. This non-uniformity gives rise to three bottlenecks in MRS unlearning: target-item persistence in the collaborative graph, modality imbalance across feature branches, and layer-wise sensitivity in the parameter space. To address this mismatch, we propose \textbf{targeted reverse update} (TRU), a plug-and-play unlearning framework for MRS. Instead of applying a blind global reversal, TRU performs three coordinated interventions across the model hierarchy: a ranking fusion gate to suppress residual target-item influence in ranking, branch-wise modality scaling to preserve retained multimodal representations, and capacity-aware layer isolation to localize reverse updates to deletion-sensitive modules. Experiments across two representative backbones, three datasets, and three unlearning regimes show that TRU consistently achieves a better retain-forget trade-off than prior approximate baselines, while security audits further confirm deeper forgetting and behavior closer to a full retraining on the retained data.

---

## 论文详细总结（自动生成）

以下是对论文《TRU: Targeted Reverse Update for Efficient Multimodal Recommendation Unlearning》的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
论文关注的是**多模态推荐系统（MRS）中的机器遗忘（Machine Unlearning）**问题。
*   **研究背景**：随着隐私法规（如GDPR）的出台，用户有权要求删除其个人数据。MRS由于紧密耦合了用户-物品交互图和丰富的多模态内容（图像、文本），使得删除特定数据的影响变得极其困难。
*   **核心问题**：传统的“完全重新训练”成本过高，而现有的“近似遗忘”方法（如MMRecUn）错误地假设删除数据的影响在模型中是均匀分布的。
*   **核心发现**：作者通过实证分析证明，MRS中被删除数据的影响在**排序行为、模态分支和网络层级**上呈现显著的非均匀性，导致了目标物品持久性、模态失衡和层级敏感性三大瓶颈。

### 2. 方法论：TRU（针对性反向更新）
TRU 是一个即插即用的框架，核心思想是**放弃盲目的全局反向更新，转而采用多粒度的针对性干预**。其包含三个关键技术：
*   **排序融合门（Ranking Fusion Gate）**：针对协作图中的“流行度惯性”，在反向优化目标中加入针对融合参数的 $\ell_1$ 正则化惩罚，强制模型在输出端切断目标物品与保留拓扑的联系。
*   **分支模态缩放（Branch-wise Modality Scaling）**：针对不同模态（ID、图像、文本）对遗忘信号吸收不均的问题，根据保留集和遗忘集的梯度能量比，动态调整各模态分支的梯度步长，保护脆弱的保留特征。
*   **容量感知层隔离（Capacity-aware Layer Isolation）**：通过量化各模块的结构敏感性，构建自适应二进制掩码，将反向更新严格限制在对删除操作最敏感的层（如早期嵌入层），防止参数过度偏移。
*   **算法流程**：每个周期先估计分支缩放因子和模块敏感度，生成掩码，随后对遗忘集执行“门控+缩放+掩码”的反向更新，最后在保留集上进行修复更新。

### 3. 实验设计
*   **数据集**：使用了 Amazon 平台的三个子类目：**Baby、Sports 和 Clothing**。
*   **骨干网络（Backbones）**：选取了两个具有代表性的 MRS 模型：**MGCN**（多视图图卷积）和 **MIG-GT**（模态独立图网络+全局 Transformer）。
*   **遗忘机制**：涵盖了三种现实场景：**用户级（U-U）、物品级（I-I）和交互级（U-I）**。
*   **对比基准（Baselines）**：
    1.  **Original**（全量训练）
    2.  **Retrain**（完全重新训练，作为“金标准”）
    3.  **UltraRE**（基于分区的遗忘）
    4.  **MultiDelete**（多模态遗忘）
    5.  **ScaleGUN**（图遗忘）
    6.  **MMRecUn**（之前的 MRS 近似遗忘 SOTA）
*   **评估指标**：Recall@20、NDCG@20（衡量保留效用与遗忘程度）、成员推理攻击（MIA）准确率、后门攻击成功率（ASR）。

### 4. 资源与算力
*   **硬件设备**：实验在一台 **NVIDIA GeForce RTX 4080 GPU** 上完成。
*   **软件环境**：CUDA 12.1，Python 3.10。
*   **训练细节**：使用 Adam 优化器，学习率为 0.001，Batch Size 为 2048。
*   **效率提升**：论文提到 TRU 的收敛速度极快，比完全重新训练快 **50倍以上**。

### 5. 实验数量与充分性
*   **实验规模**：论文在 2 个模型、3 个数据集、3 种遗忘机制下进行了详尽对比，并提供了雷达图展示综合性能。
*   **消融实验**：针对 TRU 的三个核心组件（排序门、模态缩放、层隔离）分别做了消融分析，验证了各部分的必要性。
*   **安全性审计**：除了常规的推荐指标，还引入了 MIA 和后门攻击测试，从安全角度验证遗忘的彻底性。
*   **客观性**：实验设计考虑了“保留-遗忘”的权衡，避免了单一指标的偏差，对比方法涵盖了从图遗忘到多模态遗忘的多种前沿技术，实验较为充分且客观。

### 6. 主要结论与发现
*   **性能优越性**：TRU 在所有设置下均实现了最佳的“保留-遗忘”权衡，其表现最接近“完全重新训练”的效果。
*   **精准打击**：有效的 MRS 遗忘不取决于更强的全局反向更新，而取决于对排序输出、模态分支和敏感层的精准定位。
*   **效率与安全**：TRU 不仅在计算上高效，而且在抵御成员推理攻击和消除恶意后门方面显著优于现有的近似遗忘方法。

### 7. 优点
*   **即插即用**：无需修改原有的 MRS 骨干架构或推理过程，具有很强的通用性。
*   **深入的异构性分析**：首次揭示了 MRS 遗忘中数据影响的非均匀分布特性，为后续研究提供了理论依据。
*   **多维度评估**：结合了推荐效用、隐私安全和计算效率三个维度，评估体系非常完善。

### 8. 不足与局限
*   **隐私残留**：尽管 TRU 表现优异，但在某些极具挑战性的设置下（如 MGCN 在 Clothing 数据集上），其 MIA 指标距离理想的随机猜测水平（0.5）仍有一定差距，说明近似遗忘仍难以完全替代重新训练。
*   **数据集局限**：实验主要集中在 Amazon 电商数据集上，对于短视频、社交媒体等其他类型的多模态推荐场景（如包含音频、动态视频）的泛化能力有待进一步验证。
*   **超参数依赖**：虽然敏感度分析显示参数较稳定，但层隔离比例 $p$ 和模态缩放仍需根据具体模型进行微调。

（完）

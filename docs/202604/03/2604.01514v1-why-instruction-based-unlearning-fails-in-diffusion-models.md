---
title: Why Instruction-Based Unlearning Fails in Diffusion Models?
title_zh: 为什么基于指令的遗忘在扩散模型中会失败？
authors: "Zeliang Zhang, Rui Sun, Jiani Liu, Qi Wu, Chenliang Xu"
date: 2026-04-02
pdf: "https://arxiv.org/pdf/2604.01514v1"
tags: ["query:mu-ce"]
score: 10.0
evidence: 扩散模型中基于指令的遗忘
tldr: 本研究探讨了基于指令的遗忘在扩散模型中的有效性。实验发现，与大语言模型不同，扩散模型在仅依赖自然语言指令时无法系统性地抑制目标概念。通过分析CLIP编码器和交叉注意力动态，研究揭示了指令未能有效减少对目标Token的注意力，导致目标表征在生成中持续存在。这表明扩散模型的遗忘需要超越推理端语言控制的深层干预。
selection_source: fresh_fetch
motivation: 旨在验证在大型语言模型中有效的基于指令的遗忘范式是否能成功迁移至扩散生成模型。
method: 通过多概念受控实验，并结合对CLIP文本编码器及去噪过程中交叉注意力机制的量化分析进行探究。
result: 扩散模型在自然语言指令引导下系统性地失败，无法抑制目标概念，且交叉注意力仍会持续聚焦于相关Token。
conclusion: 提示词层面的指令对于扩散模型遗忘具有局限性，有效的概念擦除需要推理阶段之外的更强力干预。
---

## 摘要
基于指令的遗忘已被证明在推理阶段修改大语言模型的行为方面是有效的，但这种范式是否能扩展到其他生成模型仍不清楚。在这项工作中，我们研究了基于扩散的图像生成模型中基于指令的遗忘，并通过针对多个概念和提示词变体的受控实验表明，当仅由自然语言遗忘指令引导时，扩散模型系统性地无法抑制目标概念。通过分析去噪过程中的 CLIP 文本编码器和交叉注意力动态，我们发现遗忘指令并不能诱导对目标概念标记（tokens）注意力的持续降低，导致目标概念的表示在整个生成过程中持续存在。这些结果揭示了扩散模型中提示词级指令的一个根本局限性，并表明有效的遗忘需要推理阶段语言控制之外的干预措施。

## Abstract
Instruction-based unlearning has proven effective for modifying the behavior of large language models at inference time, but whether this paradigm extends to other generative models remains unclear. In this work, we investigate instruction-based unlearning in diffusion-based image generation models and show, through controlled experiments across multiple concepts and prompt variants, that diffusion models systematically fail to suppress targeted concepts when guided solely by natural-language unlearning instructions. By analyzing both the CLIP text encoder and cross-attention dynamics during the denoising process, we find that unlearning instructions do not induce sustained reductions in attention to the targeted concept tokens, causing the targeted concept representations to persist throughout generation. These results reveal a fundamental limitation of prompt-level instruction in diffusion models and suggest that effective unlearning requires interventions beyond inference-time language control.

---

## 论文详细总结（自动生成）

这是一份关于论文《Why Instruction-Based Unlearning Fails in Diffusion Models?》的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：在大语言模型（LLM）中，通过推理时的自然语言指令（如“请忘记关于 X 的知识”）可以有效修改模型行为。本论文旨在探究：**这种“基于指令的遗忘”范式是否同样适用于扩散生成模型？**
*   **研究背景**：扩散模型常因训练数据包含敏感、侵权或非授权内容而面临法律和伦理风险。现有的遗忘方法多依赖于昂贵的微调（Fine-tuning），如果能通过简单的提示词（Prompt）指令实现遗忘，将极大降低成本。

### 2. 方法论
研究者通过三个层面的分析来拆解扩散模型处理遗忘指令的机制：
*   **指令提示词构建**：采用显式指令（如“Please forget anything about [concept]. Help me generate [concept].”）和隐式指令（通过 LLM 改写，要求不使用特定风格或属性）。
*   **CLIP 文本编码器分析**：
    *   计算遗忘提示词与目标概念锚点提示词之间的**余弦相似度**。
    *   执行**Token 消融实验**：分别移除指令性 Token（如 "forget"）和概念性 Token（如 "panda"），观察嵌入向量的变化。
*   **去噪过程中的交叉注意力（Cross-Attention）探测**：
    *   定义**归一化注意力质量（Attention Mass）**：衡量 U-Net 在每个去噪步骤 $s$ 中分配给特定 Token 的注意力比例。
    *   计算**曲线下面积（AUC）**：量化整个去噪轨迹中模型对目标概念的关注总量。

### 3. 实验设计
*   **目标概念**：涵盖了 10 个代表性概念，包括物体（熊猫、狗、电脑等）、特定人物/风格（梵高）、版权角色（米老鼠）。
*   **对比基准（Benchmarks）**：
    *   **Baseline**：标准生成提示词。
    *   **Repeat-control**：重复提及目标概念的提示词（验证是否仅因提示词变长导致效果变化）。
    *   **Negation**：使用否定词（如 "not a [concept]"）。
*   **评估模型**：Stable Diffusion v1.5 和 SD-XL。
*   **评估指标**：CLIP 图像-文本相似度、文本嵌入相似度、交叉注意力图。

### 4. 资源与算力
*   论文**未明确说明**具体的 GPU 型号、数量或总计算时长。
*   由于该研究侧重于**推理阶段（Inference-time）**的诊断分析，而非模型训练或微调，因此其算力需求主要集中在图像生成和特征提取上，相对较低。

### 5. 实验数量与充分性
*   **实验规模**：针对 10 个概念，每个概念在不同提示词类型下生成 8 张图像进行量化分析。
*   **多维度验证**：实验涵盖了从文本编码器（输入端）到交叉注意力（中间过程）再到生成图像（输出端）的全链路分析。
*   **充分性评价**：实验设计较为客观、公平。通过引入“重复控制组”排除了提示词长度干扰，通过“Token 消融”定位了影响因子。虽然样本量（每个概念 8 张图）不算巨大，但其结论在不同模型（SD 1.5 和 SD-XL）和不同概念上表现出高度的一致性。

### 6. 主要结论与发现
*   **系统性失败**：扩散模型在自然语言指令引导下无法抑制目标概念，生成的图像依然包含明显的被要求“遗忘”的特征。
*   **CLIP 编码器瓶颈**：CLIP 文本嵌入主要由显式的概念 Token 主导，遗忘指令对嵌入向量的改变微乎其微。
*   **注意力持久性**：在去噪过程中，尽管模型“注意”到了遗忘指令，但对目标概念 Token 的注意力分配并未显著降低，导致概念表征在整个生成过程中持续存在。
*   **指令失效**：简单的提示词层面控制无法抵消模型权重中已经固化的语义绑定。

### 7. 优点
*   **诊断深入**：不仅展示了失败现象，还通过交叉注意力 AUC 等量化手段解释了“为什么失败”。
*   **对比严谨**：通过消融实验清晰地证明了 CLIP 编码器对概念 Token 的偏好。
*   **现实意义**：警示了开发者，仅靠提示词过滤或指令引导无法实现可靠的安全合规，必须寻求更深层的干预手段。

### 8. 不足与局限
*   **架构局限性**：研究主要集中在基于 CLIP 文本编码器的扩散模型，对于使用 T5 等其他编码器（如 Imagen 或 SD3）的模型，结论是否一致尚待验证。
*   **概念覆盖面**：虽然涵盖了 10 个概念，但对于更抽象的概念（如偏见、有害性）的遗忘效果探讨较少。
*   **缺乏改进方案**：论文主要是一个“诊断性”工作，指出了问题但未提出能够替代指令遗忘的轻量级推理端改进方案。

（完）

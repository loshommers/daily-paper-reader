---
title: "Designing to Forget: Deep Semi-parametric Models for Unlearning"
title_zh: 旨在遗忘的设计：用于机器遗忘的深度半参数模型
authors: "Amber Yijia Zheng, Yu-Shan Tai, Raymond A. Yeh"
date: 2026-03-24
pdf: "https://arxiv.org/pdf/2603.22870v1"
tags: ["query:mu-ce"]
score: 10.0
evidence: 用于高效机器遗忘的深度半参数模型
tldr: 本文针对机器卸载中模型更新成本高的问题，提出了一种深度半参数模型（SPM）。该模型通过融合模块聚合训练样本信息，使其在卸载时表现出非参数化特性，即在测试阶段直接删除特定样本而无需修改模型参数。实验证明，SPM在图像分类和生成任务中表现优异，卸载速度比现有方法快10倍以上，且能更有效地消除样本影响。
selection_source: fresh_fetch
motivation: 现有的机器卸载方法在参数化模型上效率较低且难以彻底清除信息，因此需要设计一种天生易于遗忘的模型架构。
method: 引入深度半参数模型（SPM），利用融合模块聚合单个训练样本的信息，从而实现在推理时通过直接排除样本来完成卸载。
result: 在ImageNet等任务上，SPM在保持性能竞争力的同时，将卸载速度提升了10倍以上，并显著缩小了与重新训练基准的预测差距。
conclusion: 通过将非参数化行为引入深度学习架构，SPM为实现高效且精确的机器卸载提供了一种全新的设计思路。
---

## 摘要
近期机器遗忘领域的进展主要集中在开发从已训练模型中移除特定训练样本的算法。与此相对，我们观察到并非所有模型都同样易于遗忘。因此，我们引入了一类深度半参数模型（SPMs），它们在遗忘过程中表现出非参数化行为。SPMs 使用一个融合模块来聚合每个训练样本的信息，从而能够在不改变模型参数的情况下，在测试阶段显式删除选定样本。实验表明，SPMs 在图像分类和生成任务中达到了与参数化模型相当的性能，同时在遗忘效率上显著更高。值得注意的是，在 ImageNet 分类任务上，SPMs 将相对于重新训练（真值）基准的预测差距缩小了 11%，且与现有的参数化模型遗忘方法相比，实现了超过 10 倍的遗忘速度提升。代码可在 https://github.com/amberyzheng/spm_unlearning 获取。

## Abstract
Recent advances in machine unlearning have focused on developing algorithms to remove specific training samples from a trained model. In contrast, we observe that not all models are equally easy to unlearn. Hence, we introduce a family of deep semi-parametric models (SPMs) that exhibit non-parametric behavior during unlearning. SPMs use a fusion module that aggregates information from each training sample, enabling explicit test-time deletion of selected samples without altering model parameters. Empirically, we demonstrate that SPMs achieve competitive task performance to parametric models in image classification and generation, while being significantly more efficient for unlearning. Notably, on ImageNet classification, SPMs reduce the prediction gap relative to a retrained (oracle) baseline by $11\%$ and achieve over $10\times$ faster unlearning compared to existing approaches on parametric models. The code is available at https://github.com/amberyzheng/spm_unlearning.

---

## 论文详细总结（自动生成）

这是一份关于论文《Designing to Forget: Deep Semi-parametric Models for Unlearning》的深度结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：传统的机器遗忘（Machine Unlearning, MU）主要关注如何开发算法来修改已训练好的“黑盒”参数化模型，以消除特定训练样本的影响。然而，这种事后补救的方法通常计算成本高、难以完全消除影响，且容易损害模型性能。
*   **研究动机**：作者提出“为遗忘而设计”（Designing to Forget）的思路。他们观察到非参数模型（如 KNN）通过直接删除数据即可实现完美遗忘，因此设想：能否设计一种既具备深度学习高性能、又具备非参数模型易遗忘特性的架构？
*   **整体含义**：本文引入了**深度半参数模型（Semi-parametric Models, SPMs）**，通过改变模型架构而非仅仅改进遗忘算法，实现了在推理阶段通过简单的“测试时删除”即可完成高效、精确的遗忘。

### 2. 论文提出的方法论
*   **核心思想**：将模型分为参数化分支和非参数化分支。模型在预测时会显式地参考一组输入训练样本（Inputted Set）。遗忘时，只需从该集合中剔除目标样本，无需更新模型参数。
*   **关键架构组件**：
    *   **参数化模块 $f$**：标准的深度神经网络层（如 ResNet 或 UNet 的编码器），提取输入特征。
    *   **非参数化模块 $h$**：处理训练集样本，将其转化为实例嵌入（Instance Embeddings）。该模块具有置换等变性（Permutation Equivariance）。
    *   **融合模块 $g$**：利用注意力机制（Attention）将输入特征与实例嵌入进行聚合。
*   **关键技术细节**：
    *   **测试时删除（Test-time Deletion）**：遗忘操作仅需在推理时的前向传播中排除特定样本。
    *   **标签置换增强（Label-permutation Augmentation）**：为了防止模型忽略非参数分支而退化为纯参数模型，训练时随机打乱标签索引，强制模型学习输入图像与实例之间的依赖关系。
    *   **效率优化**：引入基于检索（Retrieval）或聚类（Clustering）的方法，减少推理时需要处理的实例数量。

### 3. 实验设计
*   **任务场景**：图像分类（Image Classification）和类条件图像生成（Class-conditioned Image Generation）。
*   **数据集**：CIFAR-10 和大规模的 ImageNet-1K。
*   **Benchmark 与对比方法**：
    *   **分类任务**：对比了 ResNet18（参数化基准）、KNN（非参数化基准）以及 8 种主流遗忘算法（如 GA, FT, IU, SalUn, MUNBa 等）。
    *   **生成任务**：对比了 DDPM、GMM 以及 SA, SalUn, SFR-on 等生成模型遗忘方法。
    *   **评估指标**：除了准确率（UA/RA/TA）和 FID，还提出了更严格的**硬/软预测差距（Hard/Soft Prediction Gaps）**，用于衡量遗忘模型与“重新训练模型（Oracle）”之间的预测一致性。

### 4. 资源与算力
*   **硬件设备**：实验在 **Nvidia L40S GPU** 上运行（见附录 A）。
*   **训练时长**：
    *   分类：CIFAR-10 训练 100 epoch，ImageNet 微调 160 epoch。
    *   生成：DDPM 训练 2000 epoch。
*   **遗忘耗时**：SPM 的遗忘时间极短（< 1秒），因为其本质是索引操作而非参数更新。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了从小型数据集（CIFAR-10）到大规模工业级数据集（ImageNet-1K）的跨度。
*   **实验类型**：包括类遗忘（Class-wise unlearning）、随机子集遗忘（Random unlearning）、消融实验（验证标签置换的作用、不同融合模块的效果）。
*   **充分性评价**：实验设计非常充分且客观。作者不仅对比了最终性能，还通过可视化决策边界（图1）和数学证明（Claim 1）来支撑其方法的有效性。使用“重新训练（Retrain from scratch）”作为金标准（Oracle）进行对比，保证了评估的公平性。

### 6. 论文的主要结论与发现
*   **性能匹配**：SPM 在分类和生成任务上的表现与纯参数化模型相当，甚至在某些生成指标上优于 DDPM。
*   **遗忘效率极高**：在 ImageNet 上，SPM 的遗忘速度比现有参数化遗忘方法快 **10 倍以上**。
*   **遗忘质量极佳**：SPM 遗忘后的模型行为与从头重新训练的模型高度一致，显著缩小了预测差距（在 ImageNet 上缩小了 11%）。
*   **非参数化优势**：通过简单的测试时删除，SPM 能够实现“忠实”的遗忘，避免了参数微调带来的灾难性遗忘或残留信息风险。

### 7. 优点（亮点）
*   **范式创新**：从“如何遗忘（算法）”转向“如何设计易于遗忘的模型（架构）”，提供了一个全新的视角。
*   **即时遗忘**：无需任何梯度下降或权重修改即可完成遗忘，非常适合需要频繁处理数据删除请求的隐私合规场景（如 GDPR）。
*   **严格评估**：引入了预测差距（Prediction Gap）指标，比传统的准确率差距更能反映遗忘的彻底性。

### 8. 不足与局限
*   **推理开销**：由于是半参数模型，推理时需要访问部分训练数据或其嵌入，这增加了内存占用和计算延迟（尽管聚类和检索能缓解这一点）。
*   **存储需求**：需要存储训练集的实例嵌入，对于超大规模数据集，存储成本可能是一个挑战。
*   **应用限制**：目前主要在视觉任务（分类、生成）上验证，尚未在自然语言处理（NLP）等其他复杂序列任务中进行大规模测试。

（完）

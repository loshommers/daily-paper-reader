---
title: Machine Unlearning under Retain-Forget Entanglement
title_zh: 保留-遗忘纠缠下的机器遗忘
authors: "Jingpu Cheng, Ping Liu, Qianxiao Li, Chi Zhang"
date: 2026-03-27
pdf: "https://arxiv.org/pdf/2603.26569v1"
tags: ["query:mu-ce"]
score: 10.0
evidence: 处理保留-遗忘纠缠的机器遗忘框架
tldr: 针对机器卸载中遗忘集与保留集因语义相似或特征关联而产生的“纠缠”难题，本文提出了一种新型两阶段优化框架。该框架首先利用增广拉格朗日法在提升遗忘集损失的同时维持无关保留集的准确性，随后通过基于Wasserstein-2距离正则化的梯度投影步骤，保护受纠缠影响的相关保留集性能。实验证明，该方法在多种架构和任务中均能实现高效卸载，且在保留集精度和移除保真度上优于现有基准。
selection_source: fresh_fetch
motivation: 旨在解决机器卸载过程中，由于遗忘集与保留集之间存在语义或特征纠缠，导致保留集性能被无意损害的问题。
method: 提出一种结合增广拉格朗日法与Wasserstein-2正则化梯度投影的两阶段优化框架，以实现精准卸载并保护相关数据。
result: 实验结果显示，该方法在多个基准数据集和神经架构上均取得了优于现有卸载算法的准确率保持和遗忘效果。
conclusion: 该研究为处理复杂的保留-遗忘纠缠提供了有效的优化方案，显著提升了机器卸载的可靠性与实用性。
---

## 摘要
在机器遗忘中，遗忘一个子集很少是一个孤立的任务。通常，与遗忘集密切相关的保留样本可能会受到无意的影响，特别是当它们共享来自预训练的相关特征或表现出强烈的语义相似性时。为了应对这一挑战，我们提出了一种新颖的两阶段优化框架，专门用于处理此类保留-遗忘纠缠。在第一阶段，增广拉格朗日方法在增加遗忘集损失的同时，保持了相关性较低的保留样本的准确性。第二阶段应用了由 Wasserstein-2 距离正则化的梯度投影步骤，以在不损害遗忘目标的情况下，减轻语义相关保留样本的性能退化。我们通过在多个遗忘任务、标准基准数据集和多种神经架构上的全面实验验证了我们的方法，证明了它在实现有效且可靠的遗忘的同时，在准确性保持和移除保真度方面均优于现有基准。

## Abstract
Forgetting a subset in machine unlearning is rarely an isolated task. Often, retained samples that are closely related to the forget set can be unintentionally affected, particularly when they share correlated features from pretraining or exhibit strong semantic similarities. To address this challenge, we propose a novel two-phase optimization framework specifically designed to handle such retai-forget entanglements. In the first phase, an augmented Lagrangian method increases the loss on the forget set while preserving accuracy on less-related retained samples. The second phase applies a gradient projection step, regularized by the Wasserstein-2 distance, to mitigate performance degradation on semantically related retained samples without compromising the unlearning objective. We validate our approach through comprehensive experiments on multiple unlearning tasks, standard benchmark datasets, and diverse neural architectures, demonstrating that it achieves effective and reliable unlearning while outperforming existing baselines in both accuracy retention and removal fidelity.

---

## 论文详细总结（自动生成）

### 论文总结：保留-遗忘纠缠下的机器遗忘 (Machine Unlearning under Retain-Forget Entanglement)

#### 1. 核心问题与整体含义
机器遗忘（Machine Unlearning）旨在从已训练的模型中消除特定数据的影响。然而，论文指出遗忘并非孤立任务，存在**“保留-遗忘纠缠”（Retain-Forget Entanglement）**现象：遗忘集（Forget Set）与保留集（Retain Set）中的某些样本（称为“相邻保留集”）在语义或特征上高度相关。现有的遗忘方法往往只关注保留集的平均准确率，导致在遗忘特定数据时，无意中严重损害了与其相关的敏感保留样本的性能。本研究旨在解决这种纠缠问题，确保在彻底遗忘目标数据的同时，保护相关保留数据的可用性。

#### 2. 方法论
论文提出了一个**两阶段优化框架**：

*   **第一阶段：受控优化遗忘（Stage 1）**
    *   **核心思想**：在增加遗忘集损失的同时，确保与遗忘集关联较弱的“远程保留集”（Remote Retain Set）性能不受损。
    *   **技术细节**：采用**增广拉格朗日法（Augmented Lagrangian Method）**。将问题建模为约束优化：在保持远程保留集损失不变的约束下，最小化遗忘集的负损失。通过引入拉格朗日乘子和惩罚项，动态平衡遗忘目标与约束条件，避免了手动调节固定权重的繁琐。
*   **第二阶段：W2距离引导的梯度投影（Stage 2, W-PGD）**
    *   **核心思想**：恢复在第一阶段受损的“相邻保留集”性能，同时防止遗忘集准确率回升。
    *   **技术细节**：
        *   **发现问题**：传统的梯度投影（PGD）仅约束平均损失，会导致遗忘集损失分布极化（部分样本准确率回升）。
        *   **创新点**：引入 **Wasserstein-2 (W2) 距离正则化**。通过约束遗忘集损失的经验分布，确保其分布形状在优化相邻保留集时不发生剧烈偏移。
        *   **流程**：计算相邻保留集的梯度，并将其投影到由遗忘集损失（含W2正则）和远程保留集损失梯度构成的正交补空间中。

#### 3. 实验设计
*   **数据集与场景**：
    *   **CIFAR-100**：子类级遗忘（如遗忘“鱼”超类中的“水族馆鱼”）。
    *   **TinyImageNet**：超类级遗忘，使用 ViT 架构。
    *   **ToxiGen**：安全敏感语言任务，修正对特定群体（如LGBTQ）的偏见分类。
    *   **CelebA**：大规模人脸属性遗忘（基于性别、笑容等组合属性）。
*   **对比基准（Baselines）**：
    *   包括 Fine-Tune (FT)、Gradient Ascent (GA)、SCRUB、$\ell_1$-sparse、SSD、SalUn、DELETE、GDR、Munba 等 9 种主流及最新的机器遗忘算法。
*   **评估指标**：遗忘集、相邻保留集、远程保留集的训练/测试准确率，以及成员推理攻击（MIA）的防御效力。

#### 4. 资源与算力
*   **硬件**：使用 **NVIDIA RTX 3090 GPU**。
*   **耗时**：论文提供了详细的时间对比。例如在 CIFAR-100 上，该方法运行时间约为 **14.2 分钟**，虽然略长于简单的 GA（5.4 分钟），但远快于从头训练（Retrain，约 70.1 分钟）。
*   **显存**：在 Batch Size 为 128 时，显存占用约为 **3297 MB**，处于合理范围内。

#### 5. 实验数量与充分性
*   **实验规模**：涵盖了视觉（CNN, ViT）和自然语言处理（RoBERTa）两大领域，跨越了从小型到大规模的数据集。
*   **消融实验**：非常充分。作者验证了 W2 正则化的必要性、第一阶段约束相邻集的负面影响、超参数 $\alpha$ 和 $\mu$ 的敏感性、以及在相邻集划分不准确（含噪声）时的鲁棒性。
*   **客观性**：所有实验均采用 3 个随机种子取平均值（除确定性算法 SSD 外），并对比了最新的 2024/2025 年基准方法，实验设计公平且具有说服力。

#### 6. 主要结论与发现
*   **纠缠确实存在**：实验证实，强行遗忘目标子类会导致语义相近的子类准确率大幅下降。
*   **分布约束至关重要**：仅约束平均损失不足以保证遗忘效果，必须约束损失的分布（W2 距离）才能防止模型在遗忘集上“偷偷”恢复准确率。
*   **性能优越**：该方法在所有任务中均实现了极低的遗忘集准确率（接近 0%），同时在相邻保留集上的性能恢复显著优于其他方法。

#### 7. 优点
*   **问题定义精准**：首次系统性地关注并定义了“保留-遗忘纠缠”这一实际应用中的痛点。
*   **理论与直觉结合**：利用 W2 距离解决 PGD 导致的分布极化问题，既有数学依据（提供了准确率上界的理论证明），又符合直觉。
*   **自适应性强**：增广拉格朗日法减少了超参数调节的负担。

#### 8. 不足与局限
*   **先验依赖**：虽然论文展示了可以用 kNN 自动识别相邻集，但在复杂任务中，如何精准定义“相邻保留集”仍可能依赖领域知识或预训练特征的质量。
*   **计算开销**：相比于最简单的微调或梯度上升，两阶段优化及梯度投影计算相对复杂，增加了少量的计算开销。
*   **应用范围**：目前主要验证了分类任务，在生成式模型（如大语言模型生成内容）中的纠缠处理尚需进一步探索。

（完）

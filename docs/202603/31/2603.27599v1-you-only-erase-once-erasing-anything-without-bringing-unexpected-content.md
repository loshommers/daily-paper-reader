---
title: "You Only Erase Once: Erasing Anything without Bringing Unexpected Content"
title_zh: 你只需擦除一次：擦除任何内容且不引入非预期内容
authors: "Yixing Zhu, Qing Zhang, Wenju Xu, Wei-Shi Zheng"
date: 2026-03-29
pdf: "https://arxiv.org/pdf/2603.27599v1"
tags: ["query:mu-ce"]
score: 8.0
evidence: 扩散模型中的物体擦除
tldr: 针对扩散模型在物体擦除任务中常因缺乏成对训练数据而产生意外内容或伪影的问题，本文提出了YOEO。该方法利用大规模真实世界非成对数据，结合杂物检测器和基于实体分割的上下文一致性损失进行训练，确保擦除区域与背景自然融合且无冗余物体。此外，通过扩散蒸馏策略实现了高效的少步推理，在保证高质量擦除效果的同时显著提升了性能。
selection_source: fresh_fetch
motivation: 现有的基于扩散的擦除方法由于缺乏成对训练数据和显式生成约束，容易在遮罩区域生成不相关的物体或视觉伪影。
method: 采用非成对真实图像训练扩散模型，并引入杂物检测器、上下文一致性损失以及扩散蒸馏技术来优化生成质量与推理效率。
result: 大量实验证明，YOEO在消除意外内容和保持背景连贯性方面均优于现有的最先进物体擦除方法。
conclusion: YOEO通过创新的监督机制和蒸馏策略，为实现高质量、无伪影且高效的物体擦除提供了一种有效的解决方案。
---

## 摘要
我们提出了 YOEO，一种用于物体擦除的方法。与近期基于扩散的方法不同，由于缺乏充足的成对训练数据以及对内容生成的显式约束，这些方法难以在擦除目标物体的同时不在掩码区域内生成非预期内容；而我们的方法能够产生高质量的物体擦除结果，既没有多余的物体或伪影，同时能忠实地保持与周围内容的整体上下文连贯性。我们通过在仅包含大规模真实世界图像的非成对数据上训练物体擦除扩散模型来实现这一目标，并辅以基于实体分割模型构建的杂物检测器和上下文连贯性损失的监督。为了实现更高效的训练和推理，我们采用了一种扩散蒸馏策略来训练少步擦除扩散模型。大量实验表明，我们的方法优于现有的最先进物体擦除方法。代码将在 https://zyxunh.github.io/YOEO-ProjectPage/ 提供。

## Abstract
We present YOEO, an approach for object erasure. Unlike recent diffusion-based methods which struggle to erase target objects without generating unexpected content within the masked regions due to lack of sufficient paired training data and explicit constraint on content generation, our method allows to produce high-quality object erasure results free of unwanted objects or artifacts while faithfully preserving the overall context coherence to the surrounding content. We achieve this goal by training an object erasure diffusion model on unpaired data containing only large-scale real-world images, under the supervision of a sundries detector and a context coherence loss that are built upon an entity segmentation model. To enable more efficient training and inference, a diffusion distillation strategy is employed to train for a few-step erasure diffusion model. Extensive experiments show that our method outperforms the state-of-the-art object erasure methods. Code will be available at https://zyxunh.github.io/YOEO-ProjectPage/.

---

## 论文详细总结（自动生成）

以下是对论文《You Only Erase Once: Erasing Anything without Bringing Unexpected Content》（YOEO）的结构化深度总结：

### 1. 核心问题与研究动机
*   **核心问题**：现有的基于扩散模型的图像擦除方法在移除目标物体后，往往会在遮罩（Mask）区域内“幻觉”出新的、非预期的物体或伪影，导致背景不连贯。
*   **研究动机**：
    *   **训练数据缺陷**：现有模型多在合成的成对数据上训练（将物体贴回背景），模型学到的是“重建”而非真正的“擦除”。
    *   **缺乏显式约束**：标准监督微调（SFT）仅关注像素级重建，未教导模型如何抑制杂物生成。
    *   **部署成本高**：大型多模态模型（如 ChatGPT-5、Flux 架构）虽然效果好，但参数量巨大且推理延迟高，不适合边缘设备。

### 2. 方法论
YOEO 的核心思想是利用**非成对的真实世界数据**，通过**扩散蒸馏**和**实体感知监督**来实现高效、干净的物体擦除。

*   **核心架构**：基于 Stable Diffusion 1.5 Inpainting，通过扩散蒸馏（Flash Diffusion 策略）将多步教师模型蒸馏为仅需 2 步推理的少步学生模型。
*   **关键技术细节**：
    1.  **杂物抑制损失（Sundries Suppression, SS Loss）**：利用预训练的实体分割模型（Mask2Former）检测生成区域。若生成的实体与遮罩区域的重叠率（IoS）超过阈值，则判定为“杂物”，通过降低其分类概率并惩罚像素激活来抑制其生成。
    2.  **实体特征一致性损失（Entity Feature Coherence, EFC Loss）**：提取分割模型的深层特征，计算遮罩内生成区域与遮罩外背景区域的特征余弦相似度，强制生成内容在语义和结构上与周围环境对齐。
    3.  **训练流程**：分为三个阶段：教师模型微调 -> 学生模型初步蒸馏 -> 结合 SS 和 EFC 损失的联合训练（引入无标签真实图像）。

### 3. 实验设计
*   **数据集**：
    *   **训练**：Open Images（175万张图像），构建了成对数据集 $D_1$（随机遮挡背景）和非成对数据集 $D_2$（以真实物体作为遮罩）。
    *   **测试**：COCO val2017 和 Entity Segmentation 测试集。
*   **Benchmark 与对比方法**：
    *   **对比方法**：包括 GAN 类（MAT）、SD-1.5 类（SDInpaint, SmartEraser, EntityErasure 等）以及基于 12B 参数 Flux 的大型模型（ASUKA, GeoRemover, OmniPaint）。
    *   **评估指标**：MSN（平均杂物数量）、MARS（平均杂物面积比）、CFD（上下文特征距离，衡量一致性）和 FID（图像质量）。

### 4. 资源与算力
*   **硬件**：使用**单张 NVIDIA A100 GPU** 进行实验。
*   **模型规模**：基于 860M 参数的 SD-1.5。
*   **训练时长**：
    *   阶段 1：800 次迭代（Batch size 1000）。
    *   阶段 2：15,000 次迭代（Batch size 8）。
    *   阶段 3：3,600 次迭代（Batch size 7）。
*   **推理效率**：仅需 2 步推理，耗时约 0.21s，远快于 Flux 类模型（14s-68s）。

### 5. 实验数量与充分性
*   **实验规模**：在两个主流数据集上进行了数千个样本的定量评估。
*   **消融实验**：针对 EFC 损失、SS 损失以及非成对数据 $D_2$ 的贡献分别做了对比，验证了各组件的必要性。
*   **多样性测试**：涵盖了自然图像、动画、艺术肖像、素描和海报等多种领域。
*   **客观性**：对比了从轻量级到超大规模的多种 SOTA 方法，并引入了最新的多模态大模型（ChatGPT-5 等）作为参照，实验设计较为全面、客观。

### 6. 主要结论与发现
*   **擦除更干净**：YOEO 在 MSN 和 MARS 指标上显著优于所有对比方法，证明其能有效抑制非预期物体的生成。
*   **语义连贯性强**：通过 EFC 损失，生成区域能更好地融入背景，CFD 指标领先。
*   **小模型胜大模型**：尽管 YOEO 仅有 8.6 亿参数，但在擦除任务的专一性上超过了参数量大 10 倍以上的 Flux 类模型。
*   **泛化性广**：模型在非自然图像领域（如漫画、水彩画）也表现出良好的鲁棒性。

### 7. 优点
*   **高效性**：通过蒸馏实现 2 步推理，极大地提升了实用价值。
*   **创新监督机制**：巧妙地将预训练分割模型作为“判别器”，解决了擦除任务缺乏 Ground Truth 的难题。
*   **非成对数据利用**：成功利用大规模真实图像提升了模型在复杂场景下的表现。

### 8. 不足与局限
*   **基础模型限制**：受限于 SD-1.5 的固有能力，其指令遵循能力和精细纹理生成可能弱于最新的超大模型（如 Flux）。
*   **依赖分割模型**：SS 和 EFC 损失的效果高度依赖于预训练分割模型（Mask2Former）的准确性，若分割模型失效，指导信号将产生偏差。
*   **极端场景**：论文虽展示了成功案例，但在极度复杂的遮挡或光影交互场景下，可能仍存在细微的边界不自然问题。

（完）

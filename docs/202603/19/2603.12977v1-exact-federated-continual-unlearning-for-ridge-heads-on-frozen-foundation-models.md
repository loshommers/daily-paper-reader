---
title: Exact Federated Continual Unlearning for Ridge Heads on Frozen Foundation Models
title_zh: 冻结基础模型上岭回归头的精确联邦持续遗忘
authors: "Yijun Quan, Wentai Wu, Giovanni Montana"
date: 2026-03-13
pdf: "https://arxiv.org/pdf/2603.12977v1"
tags: ["query:mu-ce"]
score: 10.0
evidence: 针对被遗忘权的精确联邦遗忘技术
tldr: 本研究针对联邦学习中冻结基础模型加岭回归分类头的场景，提出了一种精确的持续遗忘学习方法。为满足“被遗忘权”需求，该方法利用两个加性充分统计量构建通信协议，支持任意的样本添加和删除请求。该方法在数学上与集中式重新训练完全等价，具有确定性的等效保证和零KL散度，且在保持高精度的同时显著提升了处理效率。
selection_source: fresh_fetch
motivation: 现有的联邦遗忘学习方法多为近似方案且计算成本高，难以在冻结基础模型架构下实现高效且精确的数据移除。
method: 利用岭回归的加性充分统计量设计通信协议，通过固定大小的消息处理动态增删请求，确保服务器模型与重新训练结果在数学上完全一致。
result: 在四个基准测试中，该方法与集中式重训的相对误差低于10^-9，并能以极快的速度完成每次遗忘请求。
conclusion: 该研究为联邦学习环境下的精确遗忘提供了一种具有确定性等效保证和高效通信能力的实用解决方案。
---

## 摘要
基础模型通常被部署为冻结的特征提取器，并配备一个小型可训练头，以在联邦设置中适应私有的用户生成数据。“被遗忘权”要求根据需求从已训练的模型中移除特定样本或用户的影响。现有的联邦遗忘方法主要针对通用的深度模型，并依赖于近似重构或选择性重训练，这使得精确性变得昂贵或难以实现。我们在一个具有实际意义但尚未得到充分探索的场景下研究该问题：带有岭回归头的冻结基础模型。其精确最优解仅通过两个加性充分统计量依赖于数据，我们将这些统计量转化为一种通信协议，通过固定大小的消息支持由“添加”和“删除”请求组成的任意流。服务器维护一个在精确算术下与每次请求后的集中式重训练逐点等效的头。我们提供了确定性的重训练等效性保证、顺序和分区不变性、两种服务器端变体，以及零 KL 散度的贝叶斯证明。在四个基准测试上的实验证实了这些保证：两种变体与集中式岭回归重训练的相对 Frobenius 误差均在 10^-9 以内，并且处理每个请求的速度提高了数个数量级。

## Abstract
Foundation models are commonly deployed as frozen feature extractors with a small trainable head to adapt to private, user-generated data in federated settings. The ``right to be forgotten'' requires removing the influence of specific samples or users from the trained model on demand. Existing federated unlearning methods target general deep models and rely on approximate reconstruction or selective retraining, making exactness costly or elusive. We study this problem in a practically relevant but under-explored regime: a frozen foundation model with a ridge-regression head. The exact optimum depends on the data only through two additive sufficient statistics, which we turn into a communication protocol supporting an arbitrary stream of \emph{add} and \emph{delete} requests via fixed-size messages. The server maintains a head that is, in exact arithmetic, \emph{pointwise identical} to centralized retraining after every request. We provide deterministic retrain-equivalence guarantees, order and partition invariance, two server-side variants, and a Bayesian certificate of zero KL divergence. Experiments on four benchmarks confirm the guarantees: both variants match centralized ridge retraining to within $10^{-9}$ relative Frobenius error and complete each request at orders-of-

---

## 论文详细总结（自动生成）

这是一份关于论文《Exact Federated Continual Unlearning for Ridge Heads on Frozen Foundation Models》的深度结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
随着基础模型（Foundation Models）的普及，一种常见的部署模式是：保持预训练模型（如 DINOv2, RoBERTa）的权重冻结作为特征提取器，仅在下游任务中训练一个轻量级的线性头（Head）。在联邦学习（FL）场景下，用户拥有“被遗忘权”（GDPR等法规要求），即需要从已训练模型中彻底移除特定样本或用户的影响。
*   **核心挑战**：现有的联邦遗忘方法大多针对通用深度模型，通常依赖近似计算或代价高昂的部分重训练，难以实现“精确遗忘”（即遗忘后的模型与从未见过该数据、重新训练的模型完全一致）。
*   **研究目标**：针对“冻结骨干+岭回归（Ridge）头”这一流行架构，利用岭回归的解析性质，实现一种通信高效、数学上完全精确的联邦持续遗忘协议。

### 2. 方法论：核心思想与关键技术
该论文提出了一种基于**充分统计量（Sufficient Statistics）**的联邦协议。
*   **核心思想**：岭回归的最优解 $W^* = (S + \gamma I)^{-1}G$ 仅取决于两个统计量：特征协方差矩阵 $S = F^\top F$ 和特征-标签乘积 $G = F^\top Y$。由于这两个统计量具有**加性（Additivity）**，增加或删除数据只需对 $S$ 和 $G$ 进行加减操作。
*   **关键技术细节**：
    *   **通信协议**：客户端不发送原始特征，仅发送本地增量/减量数据的统计量（$S^\pm, G^\pm$）。服务器维护一个全局统计量账本。
    *   **变体 A（精确重构）**：服务器直接更新全局 $S$ 和 $G$，通过 Cholesky 分解求解线性方程组。这种方法数值稳定性最高。
    *   **变体 B（增量更新）**：利用 Sherman-Morrison-Woodbury (SMW) 公式，在不重新求逆的情况下更新矩阵逆。通过客户端发送 QR 分解后的因子 $R$ 来降低通信开销（从 $O(d^2)$ 降至 $O(dr)$）。
    *   **理论保证**：证明了该方法在确定性上等价于集中式重训练，具有顺序无关性、分区无关性，并提供了一个贝叶斯证明，显示遗忘后的后验分布与重训练分布之间的 KL 散度为零。

### 3. 实验设计
*   **数据集与场景**：
    *   图像分类：CIFAR-10, CIFAR-100, FeMNIST（非独立同分布 Non-IID）。
    *   文本情感分析：Sentiment140。
*   **特征提取器（Frozen Backbones）**：DINOv2-ViT B/14 (图像) 和 RoBERTa (文本)，特征维度 $d=768$。
*   **对比方法（Benchmarks）**：
    *   **集中式基准**：Central Ridge Retrain（理想目标）。
    *   **联邦基准**：FedAvg Retrain（从头重训）。
    *   **先进遗忘算法**：FATS（TV稳定性方法）、Exact-Fun（基于量化的精确遗忘）。

### 4. 资源与算力
*   **硬件环境**：实验在一台配备 **4 张 NVIDIA RTX A6000 GPU** 的服务器上运行。
*   **软件框架**：使用 Flower 联邦学习框架进行模拟。
*   **训练细节**：由于骨干网络冻结，计算主要集中在特征提取（一次性缓存）和线性代数运算上。服务器端使用 **fp64（双精度）** 进行统计量聚合，以减少浮点截断误差。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 4 个主流基准数据集，设置了 10 到 100 个不等的客户端数量。
*   **实验类型**：
    *   **效用测试**：验证联邦模式下的准确率是否损失。
    *   **精确性验证**：测量联邦模型与集中式重训模型之间的相对 Frobenius 误差。
    *   **遗忘效率**：包括“大块数据删除”（每次删 20%）和“爆发式单点删除”（连续 200 次请求）的耗时对比。
    *   **持续学习测试**：验证“删除后再添加”是否能完美恢复模型。
*   **充分性评价**：实验设计非常全面，不仅验证了准确性，还深入探讨了数值稳定性（fp32 vs fp64）和通信效率，对比了当前最先进的联邦遗忘基线，具有很强的说服力。

### 6. 主要结论与发现
*   **极高的精确度**：在 fp64 精度下，该方法与集中式重训练的相对误差小于 **$10^{-9}$**，实现了真正意义上的数学等价。
*   **极高的效率**：在处理单点删除请求时，该方法比基于 FedAvg 的重训练快 **数个数量级**。
*   **通信优势**：变体 B 在小批量更新时显著降低了通信带宽需求。
*   **鲁棒性**：该方法对 Non-IID 数据分布和客户端分区方式完全不敏感。

### 7. 优点
*   **数学完备性**：利用岭回归的解析解特性，避开了深度学习中常见的近似梯度更新问题。
*   **单轮通信**：每次遗忘请求仅需一轮通信即可完成，无需多次迭代。
*   **持续性支持**：原生支持任意顺序的“添加”和“删除”流，非常适合动态生产环境。
*   **理论深度**：提供了贝叶斯视角的零 KL 散度证明，为合规性提供了坚实的理论支撑。

### 8. 不足与局限
*   **架构限制**：该方法**仅适用于冻结骨干+线性头**的架构。如果需要对预训练骨干网络本身进行遗忘（即骨干网络也参与了微调），该方法不适用。
*   **隐私风险**：虽然不传输原始数据，但传输的二阶统计量（Gram 矩阵）在理论上可能存在信息泄露风险，需结合安全聚合（Secure Aggregation）或差分隐私（DP）使用。
*   **维度瓶颈**：当特征维度 $d$ 极大（如数万维）时，变体 A 的 $O(d^2)$ 通信和 $O(d^3)$ 计算开销会变得显著，尽管文中提出了变体 B 和近似方案作为缓解。

（完）

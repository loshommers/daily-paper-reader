---
title: Unlearning-based sliding window for continual learning under concept drift
title_zh: 基于机器遗忘的滑动窗口，用于概念漂移下的持续学习
authors: "Michal Wozniak, Marek Klonowski, Maciej Maczynski, Bartosz Krawczyk"
date: 2026-03-15
pdf: "https://arxiv.org/pdf/2603.14484v1"
tags: ["query:mu-ce"]
score: 9.0
evidence: 基于机器遗忘的滑动窗口方法用于遗忘过时信息
tldr: 针对非平稳数据流中的概念漂移问题，本文提出了一种基于机器遗忘的滑动窗口持续学习方法。该方法不再从头训练模型，而是通过遗忘技术移除过期样本的影响并更新新数据，实现了高效的针对性遗忘与快速适应。这是首个将机器遗忘应用于缓解概念漂移的研究，在图像流分类任务中展现了极高的计算效率和竞争性能。
selection_source: fresh_fetch
motivation: 传统的滑动窗口方法在处理概念漂移时需要频繁从头重训模型，导致计算开销巨大且效率低下。
method: 利用机器遗忘技术精准移除滑动窗口中过期样本的影响，并结合新观测数据对模型进行增量更新。
result: 在多种概念漂移场景下的图像流分类实验证明，该方法在保持竞争力的同时显著降低了计算成本。
conclusion: 本研究首次将机器遗忘与概念漂移缓解相结合，为任务无关的持续学习提供了一种高效且具有竞争力的替代方案。
---

## 摘要
传统机器学习假设数据分布是平稳的，然而许多现实世界的应用运行在非平稳流上，其中的底层概念会随时间演变。这一问题也可以被视为概念漂移下的无任务持续学习（task-free continual learning），模型必须在没有明确任务标识或任务边界的情况下进行顺序适应。在这种设置下，有效的学习既需要对新数据进行快速适应，也需要遗忘过时的信息。一种常见的解决方案是基于滑动窗口，但这种方法通常计算量巨大，因为模型必须在最新的数据上反复从头开始重新训练。我们提出了一种基于机器遗忘（machine unlearning）的不同视角。我们不再是在每次活动窗口变化时重建模型，而是利用机器遗忘技术移除过时样本的影响，然后使用新观测到的数据更新模型。这实现了高效且有针对性的遗忘，同时保持了对演变分布的适应能力。据我们所知，这是首个将机器遗忘与概念漂移缓解联系起来，用于无任务持续学习的工作。在多种漂移场景下的图像流分类实验结果表明，所提方法为标准的滑动窗口重训练提供了一个具有竞争力且计算高效的替代方案。我们的实现代码可在 https://anonymous.4open.science/r/MUNDataStream-60F3 找到。

## Abstract
Traditional machine learning assumes a stationary data distribution, yet many real-world applications operate on nonstationary streams in which the underlying concept evolves over time. This problem can also be viewed as task-free continual learning under concept drift, where a model must adapt sequentially without explicit task identities or task boundaries. In such settings, effective learning requires both rapid adaptation to new data and forgetting of outdated information. A common solution is based on a sliding window, but this approach is often computationally demanding because the model must be repeatedly retrained from scratch on the most recent data. We propose a different perspective based on machine unlearning. Instead of rebuilding the model each time the active window changes, we remove the influence of outdated samples using unlearning and then update the model with newly observed data. This enables efficient, targeted forgetting while preserving adaptation to evolving distributions. To the best of our knowledge, this is the first work to connect machine unlearning with concept drift mitigation for task-free continual learning. Empirical results on image stream classification across multiple drift scenarios demonstrate that the proposed approach offers a competitive and computationally efficient alternative to standard sliding-window retraining. Our implementation can be found at \hrehttps://anonymous.4open.science/r/MUNDataStream-60F3}{https://anonymous.4open.science/r/MUNDataStream-60F3}.

---

## 论文详细总结（自动生成）

以下是对论文《Unlearning-based sliding window for continual learning under concept drift》的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
在现实世界的流数据应用中，数据分布往往随时间演变（即**概念漂移**）。传统的应对方法是使用**滑动窗口（Sliding Window, SW）**，即仅在最近的一段数据上训练模型。然而，SW 方法通常需要从头开始反复重新训练模型，计算开销巨大，难以扩展到大规模模型或长窗口。
本文的研究动机在于：**机器遗忘（Machine Unlearning）**技术通常用于隐私保护（删除特定数据的影响），但其本质是“精准移除旧数据贡献”。作者提出将机器遗忘引入持续学习，通过“遗忘”窗口中最旧的数据并“增量学习”最新数据，来替代昂贵的从头重训过程。这是首个将机器遗忘与缓解概念漂移联系起来的尝试。

### 2. 方法论：核心思想与技术细节
作者提出了 **UIL (Unlearned and Iteratively trained cLassifier)** 框架，其核心流程如下：
*   **核心思想**：当滑动窗口向前移动时，不再重置模型，而是执行两个步骤：
    1.  **遗忘步骤**：使用机器遗忘算法（如基于影响函数或近似牛顿步的方法）从当前模型中移除窗口中最旧数据块（Chunk）的影响。
    2.  **更新步骤**：使用随机梯度下降（SGD）等增量学习方法，在模型中加入最新到达的数据块。
*   **关键技术细节**：
    *   **算法流程**：在窗口未满时进行增量训练；窗口满后，每来一个新块 $D_k$，先 `Unlearn(ψ, D_{k-L})` 再 `Train(ψ, D_k)`。
    *   **理论分析**：论文证明了在损失函数满足强凸性和平滑性假设下，UIL 的预测行为可以近似从头重训的 SW。
    *   **误差界限**：分析指出 UIL 的累积误差与窗口长度 $L$ 的平方成正比，并给出了在平稳分布下模型保持稳定的条件。
    *   **计算复杂度**：UIL 的计算成本主要取决于单个数据块的大小，而与窗口长度 $L$ 无关，这使其在长窗口场景下比 SW 具有显著优势。

### 3. 实验设计
*   **数据集与场景**：
    *   **MNIST**：通过添加 50% 强度的高斯噪声模拟突发性漂移（无噪声 $\leftrightarrow$ 有噪声）。
    *   **Fashion-MNIST**：通过改变类别组合模拟“语义漂移”（例如：初始阶段识别某些类别，漂移后识别另一组类别）。
*   **Benchmark 与对比方法**：
    *   主要对比基准是标准的**滑动窗口（SW）**方法（即每次窗口滑动都从头重训）。
*   **评估指标**：准确率（Accuracy）、每块处理时间（Time per chunk）、内存占用、恢复时间（Recovery time，即漂移后性能恢复的速度）以及性能下降程度（Deterioration）。

### 4. 资源与算力
*   **模型架构**：所有实验均使用 **ResNet18** 深度残差网络。
*   **软件环境**：基于 Python，使用了 `scikit-learn` 和 `stream-learn` 库。
*   **算力说明**：论文**未明确列出具体的 GPU 型号或数量**。但提供了时间开销对比：SW 处理每个批次约需 6.27-6.67 秒，而 UIL 仅需 2.04-2.19 秒，计算效率提升了约 3 倍。

### 5. 实验数量与充分性
*   **实验规模**：作者针对两种数据集分别设计了双向的漂移场景（A $\to$ B 和 B $\to$ A），并进行了恢复分析和参数敏感性测试。
*   **充分性评价**：实验设计较为**直观且具有说明性**，涵盖了噪声干扰和语义变化两种典型的漂移类型。然而，实验主要集中在图像分类任务上，且数据集规模（MNIST/Fashion-MNIST）相对较小。对于更复杂、更大规模的数据集（如 ImageNet 级别的流数据）或回归任务，其实验充分性尚有提升空间。

### 6. 主要结论与发现
*   **效率显著提升**：UIL 在保持与 SW 相当的预测性能的同时，计算时间减少了约 60%-70%。
*   **适应性强**：在某些突发漂移场景下，UIL 的恢复速度甚至快于 SW，说明针对性遗忘能更有效地清除过时信息的干扰。
*   **稳定性**：理论和实验均证明，只要学习率设置合理，UIL 在平稳数据流中不会因为持续的遗忘/学习操作而崩溃。
*   **局限性发现**：语义漂移（Fashion-MNIST）比噪声漂移更难处理，UIL 在此类复杂结构变化下的恢复时间较长。

### 7. 优点
*   **视角新颖**：首次打破了机器遗忘仅用于隐私保护的固有印象，将其转化为持续学习的动力。
*   **计算独立性**：UIL 的更新成本不随窗口长度增加，解决了长窗口重训的痛点。
*   **理论支撑**：提供了严谨的收敛性和稳定性证明，而非纯经验性的观察。

### 8. 不足与局限
*   **误差累积风险**：由于使用的是近似遗忘而非精确遗忘，长期运行可能会产生累积误差，论文提到可能需要周期性的“锚点重训”来校准。
*   **语义漂移挑战**：在处理高层语义剧烈变化的漂移时，近似遗忘的效果不如简单的输入噪声漂移。
*   **超参数敏感性**：遗忘率和学习率的平衡对模型稳定性至关重要，若设置不当可能导致模型发散。
*   **实验覆盖面**：缺乏在自然语言处理（NLP）或大规模真实世界非平稳数据集上的验证。

（完）

---
title: "Stake the Points: Structure-Faithful Instance Unlearning"
title_zh: Stake the Points：结构保真的实例遗忘
authors: "Kiseong Hong, JungKyoo Shin, Eunwoo Kim"
date: 2026-03-13
pdf: "https://arxiv.org/pdf/2603.12915v1"
tags: ["query:mu-ce"]
score: 10.0
evidence: 结构忠实实例遗忘，用于移除指定数据的影响
tldr: 机器卸载（MU）旨在消除特定数据影响的同时保留模型效用。针对现有方法因忽视保留实例间的语义关系而导致模型结构坍塌的问题，本文提出了一种名为“Stake the Points”的结构忠实框架。该框架引入语义锚点（Stakes）作为参考点，利用CLIP等编码的语言属性描述来稳定知识结构，并通过结构感知对齐和参数正则化确保卸载过程中的知识组织不变。实验证明，该方法在多项视觉任务中显著提升了性能，优化了删除与保留的权衡。
selection_source: fresh_fetch
motivation: 现有机器卸载方法忽视了保留数据间的语义关系，导致模型结构坍塌并破坏了删除与保留之间的平衡。
method: 提出一种以语义锚点为核心的框架，利用语言驱动的属性描述稳定知识结构，并通过结构感知对齐和正则化保护关键参数。
result: "在图像分类、检索和人脸识别任务中，该方法分别实现了32.9%、22.5%和19.3%的性能提升，有效平衡了遗忘与保留。"
conclusion: 通过维护知识结构的完整性，该框架显著增强了机器卸载的有效性和模型的泛化能力。
---

## 摘要
机器遗忘（MU）旨在解决预训练模型中的隐私风险。MU 的主要目标是在保留剩余知识效用的同时，消除指定数据的影响。实现这一目标需要保留保留实例之间的语义关系，而现有研究往往忽略了这一点。我们观察到，如果没有这种保留，模型会遭受渐进式的结构坍塌，从而破坏删除与保留之间的平衡。在这项工作中，我们提出了一个新颖的结构保真框架，该框架引入了“桩”（stakes），即作为维护知识结构参考点的语义锚点。通过利用这些锚点，我们的框架捕获并稳定了知识的语义组织。具体而言，我们通过语义编码器（如 CLIP）编码的语言驱动属性描述来实例化这些锚点。我们通过结构感知对齐和正则化来强制保留知识结构：前者围绕锚点对齐遗忘前后保留知识的组织，而后者则调节对结构关键参数的更新。图像分类、检索和人脸识别的结果显示，性能平均提升了 32.9%、22.5% 和 19.3%，平衡了删除与保留之间的权衡，并增强了泛化能力。

## Abstract
Machine unlearning (MU) addresses privacy risks in pretrained models. The main goal of MU is to remove the influence of designated data while preserving the utility of retained knowledge. Achieving this goal requires preserving semantic relations among retained instances, which existing studies often overlook. We observe that without such preservation, models suffer from progressive structural collapse, undermining both the deletion-retention balance. In this work, we propose a novel structure-faithful framework that introduces stakes, i.e., semantic anchors that serve as reference points to maintain the knowledge structure. By leveraging these anchors, our framework captures and stabilizes the semantic organization of knowledge. Specifically, we instantiate the anchors from language-driven attribute descriptions encoded by a semantic encoder (e.g., CLIP). We enforce preservation of the knowledge structure via structure-aware alignment and regularization: the former aligns the organization of retained knowledge before and after unlearning around anchors, while the latter regulates updates to structure-critical parameters. Results from image classification, retrieval, and face recognition show average gains of 32.9%, 22.5%, and 19.3% in performance, balancing the deletion-retention trade-off and enhancing generalization.

---

## 论文详细总结（自动生成）

以下是对论文《Stake the Points: Structure-Faithful Instance Unlearning》的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：机器遗忘（Machine Unlearning, MU）旨在从预训练模型中移除特定数据的影响。现有的近似遗忘方法在删除实例时，往往会忽略保留数据之间的**语义关系结构**，导致“结构坍塌”（Structural Collapse）。
*   **研究动机**：
    *   **结构坍塌现象**：作者观察到，在遗忘过程中，模型表示空间的剧烈漂移会破坏保留实例间的相对位置（例如，猴子的嵌入在远离被删除的“苹果”时，可能会错误地靠近“葡萄”而远离“香蕉”）。
    *   **性能权衡**：实验表明，结构坍塌程度与“删除-保留”性能平衡呈负相关。保护知识的组织结构对于维持模型泛化能力至关重要。
    *   **现实需求**：现实中的删除请求通常针对特定个人（实例级），而非整个类别，且通常无法访问原始训练集（数据受限场景）。

### 2. 方法论
该论文提出了一个名为 **STRUCTGUARD** 的结构保真框架，核心思想是引入“语义锚点”（Stakes）来稳定知识结构。

*   **核心思想**：利用外部知识（LLM 生成的属性描述）创建稳定的语义参考点，通过约束实例与这些锚点之间的关系来防止结构坍塌。
*   **关键技术细节**：
    1.  **锚点生成（Anchor Generation）**：利用 GPT-4o 生成类别的视觉属性描述（如形状、纹理、上下文），通过预训练的 CLIP 文本编码器将其转化为固定的语义锚点向量 $A$。
    2.  **结构定义**：将“结构”定义为保留实例的嵌入向量 $V$ 与语义锚点 $A$ 之间的亲和力矩阵 $S = V \cdot A^\top$。
    3.  **结构感知对齐（Structure-aware Alignment, $L_{align}$）**：通过最大化遗忘前后亲和力矩阵的余弦相似度，确保实例相对于锚点的语义组织保持一致。
    4.  **结构感知正则化（Structure-aware Regularization, $L_{reg}$）**：计算参数对结构对齐的重要性，对关键参数的更新施加更大的惩罚，保护支撑语义一致性的模型权重。
    5.  **代理探测集**：在无法访问保留集的情况下，利用遗忘数据的对抗性变体作为保留数据的替代品进行训练。

### 3. 实验设计
*   **数据集与场景**：
    *   **图像分类**：CIFAR-10, CIFAR-100, ImageNet-1K。
    *   **人脸识别**：Lacuna-10（源自 VGG-Faces）。
    *   **图像检索**：CIFAR-10。
*   **遗忘粒度**：实例级遗忘，随机选择 $k$ 个样本（$k$ 取值从 16 到 256 不等）。
*   **对比方法（Benchmarks）**：
    *   **标准基准**：BEFORE（原始模型）、ORACLE（重新训练的模型）。
    *   **MU 算法**：Fisher、NegGrad、RAWP、Adv、L2UL（当时的最优方法）。
*   **评估指标**：测试集准确率 ($A_{test}$)、保留集准确率 ($A_r$)、遗忘集准确率 ($A_f$，目标是使其尽可能低，文中报告为 $100 - A_f$)。

### 4. 资源与算力
*   **模型架构**：ResNet-18（用于小数据集）和 ResNet-50（用于 ImageNet-1K）。
*   **外部模型**：GPT-4o（生成属性）、CLIP ViT-B/32（语义编码）。
*   **算力说明**：论文**未明确列出**具体的 GPU 型号、数量或总训练时长。但考虑到涉及 ImageNet-1K 的 ResNet-50 训练及对抗样本生成，预计需要工业级 GPU（如 A100 或 V100）支持。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了从小型（CIFAR）到大规模（ImageNet）的数据集，并跨越了分类、识别、检索三种任务，实验设计非常全面。
*   **消融实验**：详细分析了结构对齐（SA）、结构正则化（SR）和分类器正则化（CR）的贡献，验证了各组件的必要性。
*   **敏感性分析**：针对 LLM 提示词模板、LLM 模型种类、属性数量、锚点数量进行了鲁棒性测试。
*   **客观性**：通过可视化（Grad-CAM、混淆矩阵、表示一致性曲线）和定量指标相结合，多维度证明了方法的有效性，实验结果具有较强的说服力。

### 6. 主要结论与发现
*   **结构保真的重要性**：显式保护保留数据的语义结构能显著缓解遗忘过程中的性能衰减。
*   **性能提升**：在 ImageNet-1K 上，相比最强基准 L2UL，该方法在 $k=256$ 时将保留集准确率提升了约 10%，平均性能提升显著。
*   **泛化与隐私**：该方法不仅能有效删除信息，还能保持模型对未见数据的泛化能力，且不会产生“斯特赖桑德效应”（即遗忘样本不会表现出可预测的错误模式，从而保护隐私）。

### 7. 优点
*   **视角新颖**：首次将“结构坍塌”概念引入实例级遗忘领域，并提供了量化观察。
*   **利用多模态先验**：巧妙地利用 LLM 和 CLIP 的语义对齐能力作为遗忘过程中的“定海神针”，解决了无原始数据时的参考系缺失问题。
*   **任务通用性**：不仅限于分类，在检索和人脸识别任务中也表现出极强的鲁棒性。

### 8. 不足与局限
*   **外部依赖**：方法高度依赖于 LLM 生成属性的质量和 CLIP 编码器的语义空间。如果类别极其冷门或 LLM 产生幻觉，锚点的有效性可能受损。
*   **计算开销**：虽然比重新训练快，但生成对抗样本作为代理集以及计算参数重要性矩阵（Fisher 信息的变体）仍存在一定的计算负担。
*   **类别属性限制**：目前锚点是基于类别的，对于同类别内差异极大的实例级遗忘，可能需要更细粒度的锚点设计。

（完）

---
title: "Unlearning the Unpromptable: Prompt-free Instance Unlearning in Diffusion Models"
title_zh: 遗忘不可提示之物：扩散模型中无需提示的实例级遗忘
authors: "Kyungryeol Lee, Kyeonghyun Lee, Seongmin Hong, Byung Hyun Lee, Se Young Chun"
date: 2026-03-11
pdf: "https://arxiv.org/pdf/2603.10445v2"
tags: ["query:mu-ce"]
score: 10.0
evidence: 扩散模型中不可提示输出的实例遗忘
tldr: 针对扩散模型中无法通过文本提示词描述的特定图像遗忘难题，本文提出了一种无需提示词的实例遗忘学习方法。该方法通过图像编辑、时间步感知权重和梯度手术，引导模型在保留大部分生成能力的同时，精准移除特定的人脸或文化偏见内容。在Stable Diffusion 3等模型上的实验验证了其在隐私保护和伦理合规方面的有效性，为模型修复提供了实用方案。
selection_source: fresh_fetch
motivation: 现有的遗忘学习方法主要依赖文本提示词，无法处理难以用文字精确描述的特定图像实例（如特定人脸或文化误读）。
method: 提出一种基于代理的遗忘方法，利用图像编辑技术生成引导目标，并结合时间步感知权重和梯度手术优化模型参数。
result: 在条件与无条件扩散模型上的实验表明，该方法能有效移除特定实例并保持模型完整性，表现优于现有的提示词相关基准。
conclusion: 该研究为扩散模型提供了一种无需提示词的隐私与伦理修复手段，是确保模型合规性的重要技术补充。
---

## 摘要
机器遗忘旨在从已训练的模型中移除特定的输出，通常是在概念层面，例如遗忘特定名人的所有出现或通过文本提示过滤内容。然而，许多不希望出现的输出（如个人面部或在文化或事实上被误解的生成内容）往往无法通过文本提示来指定。我们针对这一尚未得到充分研究的场景，即针对不希望出现但不可提示的输出进行实例级遗忘，其目标是在保留其余内容的同时，选择性地遗忘目标输出。为此，我们引入了一种有效的基于代理的遗忘方法，该方法利用图像编辑、时间步感知加权和梯度手术来引导已训练的扩散模型遗忘特定输出。在条件扩散模型（Stable Diffusion 3）和无条件扩散模型（DDPM-CelebA）上的实验表明，与基于提示和无需提示的基准方法不同，我们的无需提示方法能够独特地遗忘不可提示的输出（如面部和文化上不准确的描述），并保持模型的完整性。我们提出的方法可以作为扩散模型提供商确保隐私保护和伦理合规的实用热修复方案。

## Abstract
Machine unlearning aims to remove specific outputs from trained models, often at the concept level, such as forgetting all occurrences of a particular celebrity or filtering content via text prompts. However, many undesired outputs, such as an individual's face or generations culturally or factually misinterpreted, cannot often be specified by text prompts. We address this underexplored setting of instance unlearning for outputs that are undesired but unpromptable, where the goal is to forget target outputs selectively while preserving the rest. To this end, we introduce an effective surrogate-based unlearning method that leverages image editing, timestep-aware weighting, and gradient surgery to guide trained diffusion models toward forgetting specific outputs. Experiments on conditional (Stable Diffusion 3) and unconditional (DDPM-CelebA) diffusion models demonstrate that our prompt-free method uniquely unlearns unpromptable outputs, such as faces and culturally inaccurate depictions, with preserved integrity, unlike prompt-based and prompt-free baselines. Our proposed method would serve as a practical hotfix for diffusion model providers to ensure privacy protection and ethical compliance.

---

## 论文详细总结（自动生成）

以下是对论文《Unlearning the Unpromptable: Prompt-free Instance Unlearning in Diffusion Models》的结构化总结：

### 1. 核心问题与整体含义
*   **研究动机**：现有的扩散模型（DM）遗忘学习方法大多基于“提示词（Prompt）”，即通过指定特定概念（如“裸体”或“特定艺术风格”）来移除内容。然而，现实中许多需要遗忘的内容是**不可提示的（Unpromptable）**，例如某个普通人的面孔，或者模型在生成特定历史人物（如薛西斯一世）或国旗时产生的细微文化/事实错误。这些内容无法通过简单的文本描述精准定位。
*   **核心目标**：在不依赖提示词的情况下，实现“实例级”遗忘，即精准移除特定的图像输出，同时最大限度地保留模型对其他内容的生成质量（模型完整性）。

### 2. 方法论
该论文提出了一种**无需提示的实例遗忘框架**，核心思想是将遗忘任务转化为一个“引导模型向代理目标演化”的过程：
*   **基于代理的遗忘目标（Surrogate-based Forgetting）**：不直接删除目标图像 $x_f$，而是利用图像编辑工具生成一个与原图结构相似但关键特征已改变的“代理图像” $x_s$。训练时，引导模型将原本生成 $x_f$ 的路径重定向为生成 $x_s$。
*   **时间步感知加权（Timestep-aware Weighting）**：根据扩散模型在不同时间步处理信息的特性（早期关注细节，后期关注全局结构），设计动态权重 $\lambda(t)$。在去噪早期阶段侧重于“记住”以保持细节质量，在后期阶段侧重于“遗忘”以改变整体身份。
*   **梯度手术（Gradient Surgery）**：为了解决“遗忘目标”与“保留其他内容”之间可能存在的梯度冲突，采用梯度投影技术。当两个目标的梯度方向相反时，将遗忘梯度投影到保留梯度的法平面上，避免破坏模型的原有能力。
*   **理论支撑**：论文通过岭回归（Ridge Regression）模型证明了使用“代理数据”比直接“精确删除”能更有效地减小参数偏移，从而更好地保持模型性能。

### 3. 实验设计
*   **数据集/场景**：
    *   **无条件生成**：使用 CelebA-HQ（人脸）和 FFHQ（用于测试域外泛化）。
    *   **条件生成**：使用 Stable Diffusion 3 (SD3)，针对文化误读（如错误的国旗、历史人物肤色错误）进行修复。
*   **基准方法（Benchmarks）**：
    *   **无需提示类**：NegGrad、EraseDiff、SISS。
    *   **基于提示类（作为对比）**：ESD、SPM、DUO。
*   **评估指标**：SSCD（相似度指标，用于验证是否成功遗忘）、LPIPS/SSIM（结构相似度）、FID（生成分布质量）。

### 4. 资源与算力
*   **硬件**：使用了单台 **NVIDIA A100 80GB GPU**。
*   **训练细节**：
    *   对于 DDPM 模型，训练 240 个步骤。
    *   对于 SD3 模型，使用 LoRA 插件进行微调，训练 100 个步骤。
    *   生成代理图像（如使用 TediGAN 或 SDEdit）平均每张图耗时约 5 秒。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了单实例遗忘、多实例顺序遗忘、域外（OOD）数据遗忘以及复杂条件模型修复。
*   **消融实验**：非常充分，分别验证了代理图像的编辑程度（CLIP 权重）、时间步调度策略、梯度投影方式以及不同代理构造方法（翻转、加噪 vs 编辑）的影响。
*   **客观性**：通过定量指标（FID, SSCD 等）和定性视觉对比相结合，且在不同架构（DDPM 和 Transformer-based SD3）上均验证了有效性，实验设计较为公平客观。

### 6. 主要结论与发现
*   **提示词并非万能**：在处理细粒度实例或无提示词模型时，传统方法失效，而本方法能精准移除目标。
*   **代理质量至关重要**：简单的加噪或翻转作为代理会导致模型质量下降，而通过高质量编辑生成的代理图像能实现最佳的“遗忘-保留”平衡。
*   **帕累托前沿**：该方法在保持模型完整性（FID/SSIM）和遗忘彻底性（SSCD）之间达到了比现有基准更好的平衡点。

### 7. 优点
*   **实用性强**：为模型提供商提供了一种“热修复”手段，无需重新训练即可解决隐私泄露或伦理偏见问题。
*   **无需提示词**：突破了现有方法对文本描述的依赖，扩展了机器遗忘的应用边界。
*   **理论与实践结合**：不仅有直观的实验结果，还提供了数学证明来解释为何代理方法优于直接删除。

### 8. 不足与局限
*   **依赖外部工具**：构造高质量代理图像需要依赖现有的图像编辑工具（如 TediGAN）或少量人工干预，这在处理超大规模遗忘请求时可能存在瓶颈。
*   **模型覆盖范围**：虽然测试了 SD3，但对于闭源模型（如 DALL-E 3 或 Midjourney）的适用性尚未验证（受限于模型访问权限）。
*   **潜在风险**：如果代理图像构造不当，仍可能在模型中引入新的偏差或伪影。

（完）

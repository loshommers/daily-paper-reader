---
title: Federated Learning and Unlearning for Recommendation with Personalized Data Sharing
title_zh: 具有个性化数据共享的推荐系统联邦学习与遗忘学习
authors: "Liang Qu, Jianxin Li, Wei Yuan, Shangfei Zheng, Lu Chen, Chengfei Liu, Hongzhi Yin"
date: 2026-03-12
pdf: "https://arxiv.org/pdf/2603.11610v1"
tags: ["query:mu-ce"]
score: 9.0
evidence: 联邦遗忘学习以消除先前共享数据的影响
tldr: 针对联邦推荐系统中用户隐私偏好差异及数据撤回需求，本文提出FedShare框架。该框架允许用户自主选择共享数据比例以提升推荐质量，并引入对比学习对齐本地与全局表示。针对数据撤回，设计了基于历史嵌入快照的对比遗忘学习机制，在保证推荐性能的同时显著降低了存储开销，实现了高效的联邦学习与遗忘。
selection_source: fresh_fetch
motivation: 现有联邦推荐系统忽视了用户个性化的数据共享意愿，且缺乏有效机制处理用户撤回已共享数据及其对模型影响的需求。
method: 提出FedShare框架，利用共享数据构建服务端高阶图并结合对比学习对齐表示，同时通过历史嵌入快照实现轻量级的对比遗忘学习。
result: 在三个公开数据集上的实验表明，FedShare在学习和遗忘阶段均表现优异，且相比现有方法大幅减少了存储开销。
conclusion: FedShare成功平衡了用户个性化隐私需求与推荐性能，为联邦推荐系统中的数据共享与动态遗忘提供了高效的解决方案。
---

## 摘要
联邦推荐系统（FedRS）已成为保护用户隐私的一种范式，它通过将交互数据保留在本地设备上，同时通过中央服务器协调模型训练。然而，大多数现有的联邦推荐系统对用户隐私采用“一刀切”的假设，即要求所有用户严格将数据保留在本地。这种设置忽略了那些愿意与服务器共享数据以换取更好推荐性能的用户。尽管最近的一些研究探索了 FedRS 中的个性化用户数据共享，但它们假设用户隐私偏好是静态的，且无法处理用户要求删除先前共享的数据及其对已训练模型相应影响的请求。为了解决这一局限性，我们提出了 FedShare，这是一个具有个性化用户数据共享的推荐系统联邦学习-遗忘学习框架。FedShare 不仅允许用户控制与服务器共享多少交互数据，还支持通过从已训练模型中消除取消共享数据的影响来响应数据取消共享请求。具体而言，FedShare 利用共享数据构建服务器端的高阶用户-物品图，并使用对比学习来联合对齐本地和全局表示。在遗忘学习阶段，我们设计了一种对比遗忘学习机制，利用少量的历史嵌入快照选择性地移除由取消共享数据诱导的表示，从而避免了现有联邦推荐遗忘学习方法所需的存储大量历史梯度信息的需求。在三个公开数据集上的广泛实验表明，FedShare 在学习和遗忘学习阶段均实现了强大的推荐性能，同时与最先进的基准方法相比，显著降低了遗忘学习阶段的存储开销。

## Abstract
Federated recommender systems (FedRS) have emerged as a paradigm for protecting user privacy by keeping interaction data on local devices while coordinating model training through a central server. However, most existing federated recommender systems adopt a one-size-fits-all assumption on user privacy, where all users are required to keep their data strictly local. This setting overlooks users who are willing to share their data with the server in exchange for better recommendation performance. Although several recent studies have explored personalized user data sharing in FedRS, they assume static user privacy preferences and cannot handle user requests to remove previously shared data and its corresponding influence on the trained model. To address this limitation, we propose FedShare, a federated learn-unlearn framework for recommender systems with personalized user data sharing. FedShare not only allows users to control how much interaction data is shared with the server, but also supports data unsharing requests by removing the influence of the unshared data from the trained model. Specifically, FedShare leverages shared data to construct a server-side high-order user-item graph and uses contrastive learning to jointly align local and global representations. In the unlearning phase, we design a contrastive unlearning mechanism that selectively removes representations induced by the unshared data using a small number of historical embedding snapshots, avoiding the need to store large amounts of historical gradient information as required by existing federated recommendation unlearning methods. Extensive experiments on three public datasets demonstrate that FedShare achieves strong recommendation performance in both the learning and unlearning phases, while significantly reducing storage overhead in the unlearning phase compared with state-of-the-art baselines.

---

## 论文详细总结（自动生成）

以下是对论文《Federated Learning and Unlearning for Recommendation with Personalized Data Sharing》的结构化深入总结：

### 1. 核心问题与研究动机
*   **核心问题**：如何在联邦推荐系统（FedRS）中平衡**个性化隐私偏好**与**推荐性能**，并支持高效的**数据遗忘（Unlearning）**。
*   **研究动机**：
    *   **隐私偏好差异化**：传统联邦推荐假设所有用户都拒绝共享数据，忽视了部分用户愿意共享部分数据以换取更好推荐服务的需求。
    *   **动态隐私需求**：用户可能在共享后后悔或误操作，需要行使“被遗忘权”（如 GDPR 要求），从已训练模型中消除特定数据的影响。
    *   **存储瓶颈**：现有的联邦遗忘学习方法（如 FRU、CUFRU）通常需要客户端或服务器存储大量的历史梯度信息，这对资源受限的设备极不友好。

### 2. 方法论：FedShare 框架
FedShare 包含两个主要阶段：**学习阶段**和**遗忘学习阶段**。

*   **核心思想**：利用用户共享的数据在服务端构建高阶图，通过**对比学习**对齐本地与全局表示；在遗忘阶段，利用**历史嵌入快照**而非梯度信息进行对比遗忘。
*   **关键技术细节**：
    *   **学习阶段（Learning Phase）**：
        *   **服务端图构建**：根据用户自愿共享的交互数据构建高阶用户-物品二部图。
        *   **双视图对齐**：通过联邦聚合（FedAvg）获得“本地视图”，通过服务端高阶图传播获得“全局视图”。
        *   **对比学习（CL）**：使用 InfoNCE 损失函数对齐两个视图，使模型能同时捕获本地的一阶信号和全局的高阶协同信号。
    *   **遗忘阶段（Unlearning Phase）**：
        *   **图更新**：从服务端图中移除用户请求遗忘的交互边。
        *   **对比遗忘机制（CU）**：
            1.  **新本地视图**：剩余用户通过常规联邦训练更新嵌入。
            2.  **遗忘视图**：利用存储的少量历史全局嵌入快照，在“遗忘图”上推断出代表被删除数据特征的表示。
            3.  **三元对齐/分离**：将新本地视图与新全局视图对齐（保持性能），同时将其与遗忘视图推开（消除影响）。
*   **算法流程**：通过存储 $M$ 个历史嵌入表（Snapshot）代替海量梯度，极大地降低了存储开销。

### 3. 实验设计
*   **数据集**：使用了三个公开的推荐系统基准数据集：
    *   **MovieLens-1M**：电影评分数据。
    *   **Fashion**：亚马逊服装类评论数据。
    *   **Video Games**：亚马逊视频游戏评论数据。
*   **场景设置**：将用户分为全共享、部分共享（30%比例）和不共享三类，比例为 1:2:7。遗忘阶段设定 30% 的共享用户发起撤回请求。
*   **对比方法（Benchmark）**：
    *   **学习阶段基准**：FedShare-FedAvg（朴素聚合）、CDCGNNFed（图补全对比学习）、PDC-FRS（差分隐私共享）。
    *   **遗忘阶段基准**：Retrain（从头重练，作为理想上限）、FRU（基于梯度的遗忘）、CUFRU（基于梯度传输站的遗忘）。

### 4. 资源与算力
*   **算力说明**：论文中**未明确提及**具体的 GPU 型号、数量及总训练时长。
*   **资源消耗提及**：论文重点强调了**存储开销（Storage Overhead）**的对比，实验结果显示 FedShare 的存储需求远低于 FRU 和 CUFRU，因为它只存储少量的嵌入快照（MB 级别），而非梯度序列。

### 5. 实验数量与充分性
*   **实验规模**：
    *   在 3 个数据集上进行了完整的性能对比（HR@20, NDCG@20）。
    *   **消融实验**：分别验证了对比学习（CL）、BPR 损失、遗忘图、剩余数据联邦训练等组件的有效性。
    *   **超参数敏感性分析**：探讨了用户共享比例、对比损失权重、历史快照数量 $M$、遗忘比例对结果的影响。
*   **充分性评价**：实验设计较为全面，涵盖了从推荐精度到遗忘效率、再到存储成本的多个维度。对比了从头训练（Retrain）这一“金标准”，保证了实验的客观性与公平性。

### 6. 主要结论与发现
*   **性能优越性**：在学习阶段，FedShare 通过捕获高阶协同信号，推荐精度优于现有的个性化共享方法。
*   **遗忘有效性**：FedShare 的遗忘效果接近从头重练（Retrain），且在推荐精度上优于基于梯度的遗忘方法（FRU/CUFRU）。
*   **高效率低开销**：FedShare 在遗忘阶段的耗时远低于 Retrain，且存储开销比现有遗忘算法降低了数倍甚至数十倍。
*   **鲁棒性**：随着共享数据比例增加，性能稳步提升；在不同遗忘比例下均能保持稳定。

### 7. 优点与亮点
*   **首创性**：首次在联邦推荐中统一考虑了个性化数据共享与动态遗忘学习。
*   **存储友好**：巧妙地用“嵌入快照”代替“历史梯度”，解决了联邦遗忘学习在移动端设备上的存储瓶颈。
*   **双向优化**：对比学习的设计不仅提升了正常训练的精度，也为遗忘阶段提供了精准的“负样本”参考。

### 8. 不足与局限
*   **客户端依赖**：遗忘过程仍需要剩余用户参与联邦训练来辅助更新，这会增加剩余用户的通信和计算负担。
*   **覆盖范围有限**：目前的遗忘机制主要针对“已共享到服务端的数据”，对于从未共享数据但参与过模型聚合的用户，其遗忘机制尚待扩展。
*   **算力细节缺失**：缺乏具体的硬件环境和训练时间成本的量化数据，不便于其他研究者评估其实际部署的计算开销。

（完）

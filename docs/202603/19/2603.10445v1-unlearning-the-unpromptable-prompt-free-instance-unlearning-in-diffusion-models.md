---
title: "Unlearning the Unpromptable: Prompt-free Instance Unlearning in Diffusion Models"
title_zh: 遗忘不可提示之物：扩散模型中的无提示实例遗忘
authors: "Kyungryeol Lee, Kyeonghyun Lee, Seongmin Hong, Byung Hyun Lee, Se Young Chun"
date: 2026-03-11
pdf: "https://arxiv.org/pdf/2603.10445v1"
tags: ["query:mu-ce"]
score: 10.0
evidence: 扩散模型中不可提示输出的实例遗忘
tldr: 本研究针对扩散模型中难以通过文本提示词描述的“不可提示”输出（如特定人脸或文化误读），提出了一种无需提示词的实例卸载方法。该方法利用图像编辑生成代理目标，并结合时间步感知权重与梯度手术技术，引导模型选择性地遗忘特定实例，同时保留其他生成能力。实验证明，该方法在条件和无条件扩散模型上均优于现有基准，为模型隐私保护和伦理合规提供了有效的技术手段。
selection_source: fresh_fetch
motivation: 现有的机器卸载方法大多依赖文本提示词，无法有效处理难以用文字精确描述的特定图像实例或文化偏见问题。
method: 提出一种基于代理的无提示词卸载框架，通过图像编辑、时间步感知权重和梯度手术引导模型遗忘特定输出。
result: 在Stable Diffusion 3和DDPM等模型上的实验表明，该方法能精准卸载不可提示的实例，且对模型原有性能影响极小。
conclusion: 该研究为扩散模型提供了一种实用的隐私与伦理修复方案，填补了实例级不可提示内容卸载的技术空白。
---

## 摘要
机器遗忘旨在从已训练的模型中移除特定的输出，通常是在概念层面，例如遗忘特定名人的所有出现或通过文本提示过滤内容。然而，许多不希望出现的输出（例如个人面部或在文化或事实上被误解的生成内容）通常无法通过文本提示来指定。我们研究了这种尚未得到充分探索的实例遗忘场景，即针对不希望出现但不可提示的输出，其目标是在保留其余内容的同时有选择地遗忘目标输出。为此，我们引入了一种有效的基于代理的遗忘方法，该方法利用图像编辑、时间步感知加权和梯度手术来引导已训练的扩散模型遗忘特定输出。在条件扩散模型（Stable Diffusion 3）和无条件扩散模型（DDPM-CelebA）上的实验表明，与基于提示和无提示的基准方法不同，我们的无提示方法能够独特地遗忘不可提示的输出（如面部和文化上不准确的描述），并保持模型的完整性。我们提出的方法可以作为扩散模型提供商确保隐私保护和伦理合规的实用热修复方案。

## Abstract
Machine unlearning aims to remove specific outputs from trained models, often at the concept level, such as forgetting all occurrences of a particular celebrity or filtering content via text prompts. However, many undesired outputs, such as an individual's face or generations culturally or factually misinterpreted, cannot often be specified by text prompts. We address this underexplored setting of instance unlearning for outputs that are undesired but unpromptable, where the goal is to forget target outputs selectively while preserving the rest. To this end, we introduce an effective surrogate-based unlearning method that leverages image editing, timestep-aware weighting, and gradient surgery to guide trained diffusion models toward forgetting specific outputs. Experiments on conditional (Stable Diffusion 3) and unconditional (DDPM-CelebA) diffusion models demonstrate that our prompt-free method uniquely unlearns unpromptable outputs, such as faces and culturally inaccurate depictions, with preserved integrity, unlike prompt-based and prompt-free baselines. Our proposed method would serve as a practical hotfix for diffusion model providers to ensure privacy protection and ethical compliance.

---

## 论文详细总结（自动生成）

### 论文总结：Unlearning the Unpromptable: Prompt-free Instance Unlearning in Diffusion Models

#### 1. 论文的核心问题与整体含义
*   **研究动机**：扩散模型（DM）在生成高质量图像方面取得了巨大成功，但也带来了隐私泄露（如生成特定人脸）和伦理风险（如文化误读、事实错误）。
*   **核心挑战**：现有的机器遗忘（Machine Unlearning）方法大多依赖**文本提示词（Prompt-based）**。然而，许多不希望出现的输出是“不可提示”的（Unpromptable），即无法通过简单的文字描述来精确锁定特定实例。此外，在无条件扩散模型中，根本不存在提示词。
*   **研究目标**：提出一种**无需提示词的实例级遗忘方法**，旨在精准移除特定的、难以描述的图像实例，同时最大限度地保留模型的整体生成能力和图像质量。

#### 2. 论文提出的方法论
该论文提出了一种基于代理（Surrogate-based）的无提示遗忘框架，核心思想是将目标遗忘实例映射到一个“代理图像”上，而非简单地破坏其分布。
*   **核心技术细节**：
    1.  **代理图像构建（Surrogate Construction）**：利用现成的图像编辑工具（如 TediGAN、SDEdit 或手动涂鸦）修改目标图像的特定属性（如身份、颜色），生成一个结构相似但语义不同的代理图像 $x_s$。
    2.  **遗忘损失函数**：通过修改扩散模型的去噪目标，引导模型在输入目标图像的加噪版本 $x_f^t$ 时，预测出的噪声能将其还原为代理图像 $x_s$，从而实现对原始身份的“覆盖”。
    3.  **时间步感知权重（Timestep-aware Weighting）**：引入动态权重 $\lambda(t) = 1 - \beta t$。在较小的时间步（关注细节）强调记忆损失 $L_r$，在较大的时间步（关注全局结构）强调遗忘损失 $L_f$。
    4.  **梯度手术（Gradient Surgery）**：为了解决遗忘目标与保留目标之间的梯度冲突，采用投影方法（类似 PCGrad），将遗忘梯度投影到与记忆梯度不冲突的方向上，防止模型崩溃。
*   **理论支持**：论文通过岭回归（Ridge Regression）模型在理论上证明了，相比于直接删除数据的“精确遗忘”，使用相似的代理图像进行遗忘能更有效地保留原始模型的参数结构。

#### 3. 实验设计
*   **数据集与场景**：
    *   **无条件模型**：使用 DDPM 在 CelebA-HQ（人脸）数据集上进行单实例和多实例顺序遗忘实验。
    *   **条件模型**：使用 Stable Diffusion 3 (SD3) 修复文化误读（如错误的国旗、历史人物误读）。
    *   **泛化性测试**：在 FFHQ 数据集上进行域外（OOD）实例遗忘测试。
*   **Benchmark 与对比方法**：
    *   **无提示基准**：NegGrad、EraseDiff、SISS。
    *   **有提示基准（用于 SD3 场景对比）**：ESD、SPM、DUO。
*   **评估指标**：SSCD（自监督副本检测，用于验证遗忘程度）、LPIPS/SSIM（结构相似度）、FID（生成分布质量）。

#### 4. 资源与算力
*   **硬件环境**：实验在一台 **NVIDIA A100 80GB GPU** 上完成。
*   **训练参数**：
    *   **DDPM**：学习率 $5 \times 10^{-6}$，训练 240 步，batch size 为 8。
    *   **SD3**：使用 LoRA 插件，学习率 $1 \times 10^{-5}$，训练 100 步。
*   **耗时**：代理图像的构建（如使用 SDEdit）在 5 秒内即可完成，整体遗忘过程属于轻量级微调。

#### 5. 实验数量与充分性
*   **实验组数**：涵盖了单实例遗忘、四个名人的顺序遗忘、三种不同类型的文化/语义误读修复，以及 OOD 泛化实验。
*   **消融实验**：非常充分。作者针对代理图像的编辑强度（CLIP 权重）、时间步调度策略、梯度投影方法、以及不同的代理构建方式（翻转、加噪 vs. 编辑）均做了详细对比。
*   **客观性**：通过定量指标（FID, SSCD）与定性视觉对比相结合，并使用了 10,000 张生成图像进行统计评估，实验设计较为严谨、客观。

#### 6. 论文的主要结论与发现
*   **精准遗忘**：该方法能成功让模型不再生成特定的“不可提示”实例（SSCD < 0.4），且不会像基准方法那样产生严重的伪影或模式崩溃。
*   **模型完整性**：在遗忘特定目标后，模型生成其他无关内容的能力几乎不受影响，FID 和 SSIM 指标显著优于 NegGrad 和 EraseDiff。
*   **代理法优势**：实验证实，使用“编辑后的图像”作为代理比简单的“加噪”或“翻转”更能平衡遗忘效果与模型稳定性。
*   **实用性**：该方法可作为模型服务商的“热修复”手段，快速处理隐私投诉或伦理偏见。

#### 7. 优点
*   **切入点新颖**：首次系统性地探讨了“不可提示”实例的遗忘问题，具有很强的现实应用价值。
*   **理论与实践结合**：不仅有直观的算法设计，还提供了数学证明来解释为何代理图像法优于直接剔除法。
*   **兼容性强**：同时适用于无条件和条件扩散模型，且支持 LoRA 等高效微调技术。

#### 8. 不足与局限
*   **代理构建依赖**：虽然作者声称代理构建可自动化，但对于复杂的语义错误，可能仍需要人工干预或高质量的编辑模型来生成合适的代理图像。
*   **模型覆盖范围**：主要在 DDPM 和 SD3 上验证，对于最新的视频生成模型或其他架构（如基于 Transformer 的 DiT）的适用性有待进一步验证。
*   **闭源模型限制**：由于算力和 API 限制，无法在闭源模型（如 DALL-E 3）上直接测试，只能通过模拟实验进行推导。

（完）

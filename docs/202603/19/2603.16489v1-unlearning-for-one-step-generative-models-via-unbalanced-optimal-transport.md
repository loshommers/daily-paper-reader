---
title: Unlearning for One-Step Generative Models via Unbalanced Optimal Transport
title_zh: 基于非平衡最优传输的一步生成模型机器遗忘
authors: "Hyundo Choi, Junhyeong An, Jinseong Park, Jaewoong Choi"
date: 2026-03-17
pdf: "https://arxiv.org/pdf/2603.16489v1"
tags: ["query:mu-ce"]
score: 10.0
evidence: 针对单步生成模型的类别遗忘框架
tldr: 本研究针对单步生成模型（如流映射模型）的机器遗忘问题，提出了名为 UOT-Unlearn 的即插即用框架。由于现有扩散模型遗忘方法依赖多步迭代，不适用于单步模型，作者利用非平衡最优传输（UOT）理论，将遗忘过程建模为遗忘成本与保真度惩罚之间的权衡。该方法能将遗忘类别的概率质量平滑重新分配到保留类别中，有效避免了生成质量坍缩，在 CIFAR-10 和 ImageNet-256 实验中展现了卓越的遗忘效果和图像保真度。
selection_source: fresh_fetch
motivation: 现有的扩散模型遗忘方法依赖多步迭代过程，无法直接应用于高效的单步生成模型，且单步模型的安全遗忘研究尚属空白。
method: 提出基于非平衡最优传输（UOT）的框架，通过在遗忘成本与 f-散度惩罚间取得平衡，实现遗忘类概率质量向保留类的平滑重分布。
result: 实验证明该方法在 CIFAR-10 和 ImageNet-256 上取得了领先的遗忘成功率和保留质量，显著优于现有基准。
conclusion: UOT-Unlearn 为单步生成模型提供了一种原则性的遗忘方案，在确保特定类别被有效移除的同时，较好地维持了模型的整体生成性能。
---

## 摘要
近期，一步生成框架（如流映射模型）的进展通过在单次前向传递中学习直接的噪声到数据映射，显著提高了图像生成的效率。然而，旨在确保这些强大生成器安全性的机器遗忘（machine unlearning）仍处于完全未探索的状态。现有的扩散遗忘方法由于依赖多步迭代去噪过程，在本质上与这些一步模型不兼容。在这项工作中，我们提出了 UOT-Unlearn，这是一种基于非平衡最优传输（UOT）的、适用于一步生成模型的新型即插即用类别遗忘框架。我们的方法将遗忘过程表述为遗忘代价（抑制目标类别）与 $f$-散度惩罚（通过松弛边缘约束保持整体生成保真度）之间的原则性权衡。通过利用 UOT，我们的方法能够将遗忘类别的概率质量平滑地重新分配到剩余类别，而不是坍缩为低质量或类噪声样本。在 CIFAR-10 和 ImageNet-256 上的实验结果表明，我们的框架实现了卓越的遗忘成功率（PUL）和保留质量（u-FID），显著优于基准方法。

## Abstract
Recent advances in one-step generative frameworks, such as flow map models, have significantly improved the efficiency of image generation by learning direct noise-to-data mappings in a single forward pass. However, machine unlearning for ensuring the safety of these powerful generators remains entirely unexplored. Existing diffusion unlearning methods are inherently incompatible with these one-step models, as they rely on a multi-step iterative denoising process. In this work, we propose UOT-Unlearn, a novel plug-and-play class unlearning framework for one-step generative models based on the Unbalanced Optimal Transport (UOT). Our method formulates unlearning as a principled trade-off between a forget cost, which suppresses the target class, and an $f$-divergence penalty, which preserves overall generation fidelity via relaxed marginal constraints. By leveraging UOT, our method enables the probability mass of the forgotten class to be smoothly redistributed to the remaining classes, rather than collapsing into low-quality or noise-like samples. Experimental results on CIFAR-10 and ImageNet-256 demonstrate that our framework achieves superior unlearning success (PUL) and retention quality (u-FID), significantly outperforming baselines.

---

## 论文详细总结（自动生成）

以下是对论文《Unlearning for One-Step Generative Models via Unbalanced Optimal Transport》的结构化深度总结：

### 1. 核心问题与研究动机
*   **核心问题**：如何针对**一步（One-Step）生成模型**（如一致性轨迹模型 CTM、流映射模型 MeanFlow）实现有效的机器遗忘（Machine Unlearning）。
*   **研究动机**：
    *   **生成效率与风险并存**：一步生成模型通过单次前向传递即可生成高质量图像，极大地提高了效率，但也加速了有害内容（如 NSFW、版权内容）的传播风险。
    *   **现有方法不兼容**：现有的扩散模型遗忘方法（如针对 Stable Diffusion 的方法）高度依赖多步迭代去噪过程中的噪声预测或梯度调整，无法直接应用于没有中间步骤的一步生成架构。
    *   **遗忘质量挑战**：简单的遗忘手段（如梯度上升）容易导致模型生成质量坍缩或产生噪声，而非平滑地移除特定概念。

### 2. 方法论：UOT-Unlearn 框架
*   **核心思想**：将遗忘问题建模为**非平衡最优传输（UOT）**问题。利用 UOT 允许边缘分布不完全匹配的特性，在“移除遗忘类”和“保持整体分布保真度”之间取得原则性的权衡。
*   **关键技术细节**：
    *   **UOT 公式化**：通过最小化传输成本（Transport Cost）和 $f$-散度惩罚项（$f$-divergence penalty），将预训练模型的分布平滑地转换为遗忘后的分布。
    *   **半对偶表述（Semi-dual Formulation）**：利用神经网络参数化传输映射 $T_\theta$ 和对偶势能 $v_\phi$，将复杂的 UOT 问题转化为可优化的目标函数。
    *   **遗忘成本设计 ($c_{ul}$)**：
        *   **主动驱逐（Active Expulsion）**：对于落在遗忘区域（由特征空间中的“遗忘锚点” $\mu_f$ 定义）的样本，施加铰链损失（Hinge Loss），将其推离目标概念。
        *   **保真度维持（Fidelity Retention）**：对于非遗忘区域的样本，使用 $L_2$ 损失约束，确保其尽可能接近原始生成效果。
    *   **数据解耦**：该方法在优化阶段**不需要真实的保留集数据**，仅需少量遗忘类样本预计算特征锚点，实现了高效的“数据无关”优化。

### 3. 实验设计
*   **数据集与场景**：
    *   **2D 合成数据**：验证概率质量是否能从遗忘模态平滑迁移到保留模态。
    *   **CIFAR-10**：针对 CTM 和 MeanFlow 模型，执行单类别遗忘（如汽车、青蛙、船）。
    *   **ImageNet-256**：在条件生成模型上验证大规模、高分辨率场景下的语义偏移效果（如遗忘“金鱼”类）。
*   **Benchmark 与对比方法**：
    *   **对比方法**：梯度上升（GA）、选择性失忆（SA）、显著性遗忘（SalUn）、变分扩散遗忘（VDU）。
    *   **适配性调整**：由于基准方法多为扩散模型设计，作者将其目标函数适配到了单步生成框架中。
*   **评估指标**：
    *   **PUL (Percentage of Unlearning)**：遗忘成功率，衡量目标类别生成频率的下降程度。
    *   **u-FID (Unlearned FID)**：衡量保留类别的生成质量。

### 4. 资源与算力
*   **算力说明**：论文**未明确说明**具体的 GPU 型号、数量或总训练时长。
*   **实现细节**：提到了 Batch Size（CIFAR-10 为 128，ImageNet-256 为 8），并使用了预训练的 ResNet-50/56 作为特征提取器，DenseNet-121/ViT-L 作为评估分类器。

### 5. 实验数量与充分性
*   **实验规模**：
    *   涵盖了从 2D 玩具实验到 256x256 高分辨率图像的跨度。
    *   针对两种主流的一步生成架构（CTM, MeanFlow）进行了验证。
    *   包含针对核心超参数（遗忘权重 $\lambda$、语义边界距离 $m$）的消融实验。
*   **充分性评价**：实验设计较为充分且客观。通过固定随机种子对比生成结果，直观展示了语义的平滑迁移。使用了独立于训练过程的第三方分类器评估 PUL，保证了评估的公正性。

### 6. 主要结论与发现
*   **平滑重分布**：UOT-Unlearn 能够将遗忘类的概率质量重新分配到保留类，而不是让模型产生低质量样本。
*   **性能领先**：在所有测试场景中，UOT-Unlearn 在保持极高遗忘率（PUL）的同时，其 u-FID 显著低于其他基准方法，证明了其在维持模型保真度方面的卓越能力。
*   **无需保留数据**：证明了仅利用合成数据和特征锚点即可实现高质量遗忘，降低了对原始数据的依赖。

### 7. 优点
*   **理论严谨**：基于最优传输理论，为一步生成模型的遗忘提供了坚实的数学基础。
*   **即插即用**：不改变原始模型架构，适用于任何基于流映射的一步生成器。
*   **质量平衡**：有效解决了传统遗忘方法中常见的“过度遗忘”导致整体质量下降的问题。

### 8. 不足与局限
*   **特征提取器依赖**：遗忘效果高度依赖于预训练特征提取器（如 ResNet）的质量，如果特征空间无法很好地分离特定概念，遗忘效果可能受限。
*   **语义偏移风险**：虽然概率质量被重新分配，但在极端情况下，可能会导致某些保留类的生成分布发生非预期的微小偏移。
*   **算力细节缺失**：缺乏具体的训练开销分析，难以评估其在超大规模模型上的微调成本。

（完）

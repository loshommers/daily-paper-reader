---
title: "SCAN: Sparse Circuit Anchor Interpretable Neuron for Lifelong Knowledge Editing"
title_zh: SCAN：用于终身知识编辑的稀疏电路锚定可解释神经元
authors: "Yuhuan Liu, Haitian Zhong, Xinyuan Xia, Qiang Liu, Shu Wu, Liang Wang"
date: 2026-03-16
pdf: "https://arxiv.org/pdf/2603.15226v1"
tags: ["query:mu-ce"]
score: 9.0
evidence: 终身知识编辑与防止灾难性遗忘
tldr: "针对大语言模型在连续知识编辑中容易出现的灾难性遗忘和模型崩溃问题，本文提出了 SCAN 框架。该框架通过稀疏转码器构建知识电路，将编辑过程转化为机制感知的稀疏操作，从而实现精准干预。实验证明，SCAN 在进行 3,000 次连续编辑后仍能保持模型在 MMLU 和 GSM8K 等基准上的性能，显著优于传统密集编辑方法，有效解决了终身学习中的稳定性挑战。"
selection_source: fresh_fetch
motivation: 传统的密集编辑范式因其黑盒性质和粗粒度的参数干预，容易在连续编辑过程中破坏模型原有知识并导致模型崩溃。
method: 提出 SCAN 框架，利用稀疏转码器构建知识电路，通过锚定特定神经元实现机制感知的稀疏知识编辑。
result: "在多个主流模型和数据集上的实验表明，SCAN 在完成 3,000 次连续编辑后仍能保持模型完整性，而其他方法则会出现性能退化或崩溃。"
conclusion: SCAN 通过引入稀疏电路和可解释性机制，为实现稳定且高效的终身知识编辑提供了一种全新的有效方案。
---

## 摘要
大语言模型（LLMs）在连续知识编辑过程中经常遭受灾难性遗忘和崩溃。这种脆弱性源于目前盛行的稠密编辑范式，该范式将模型视为黑盒，并依赖于粗粒度的参数干预，这不可避免地会破坏已保留的知识。为了解决这一问题，我们提出了 SCAN（一种基于稀疏电路锚定神经元的稀疏编辑框架），它通过稀疏转码器（Sparse Transcoders）构建知识电路，将编辑转化为一种机制感知的操作。在 Gemma2、Qwen3 和 Llama3.1 上针对 CounterFact、ZsRE 和 WikiFactDiff 进行的实验表明，SCAN 取得了卓越的性能。即使在进行 3,000 次连续编辑后，SCAN 仍能在 MMLU 和 GSM8K 等基准测试上保持模型完整性，而现有的其他方法随着编辑次数的累积性能逐渐恶化，最终导致模型崩溃。

## Abstract
Large Language Models (LLMs) often suffer from catastrophic forgetting and collapse during sequential knowledge editing. This vulnerability stems from the prevailing dense editing paradigm, which treats models as black boxes and relies on coarse-grained parameter interventions that inevitably disrupt preserved knowledge. To address this, we propose SCAN (a sparse editing framework based on Sparse Circuit Anchored Neuron) which transforms editing into a mechanism-aware manipulation by constructing a knowledge circuit via Sparse Transcoders. Experiments on Gemma2, Qwen3, and Llama3.1 across CounterFact, ZsRE and WikiFactDiff demonstrate that SCAN achieves a superior performance, maintaining model integrity on benchmarks like MMLU and GSM8K even after 3,000 sequential edits, whereas other existing methods deteriorate progressively as editing accumulates, eventually resulting in model collapse.

---

## 论文详细总结（自动生成）

### 论文总结：SCAN —— 用于终身知识编辑的稀疏电路锚定可解释神经元

#### 1. 论文的核心问题与整体含义（研究动机和背景）
大语言模型（LLMs）在预训练后需要通过“知识编辑”来更新过时信息。然而，现有的编辑方法在**终身编辑（Lifelong Editing）**场景下表现不佳，即在连续进行成百上千次编辑后，模型容易出现**灾难性遗忘**（丢失旧知识）或**模型崩溃**（通用能力丧失）。
*   **核心痛点**：现有的“稠密编辑”范式将模型视为黑盒，干预的参数粒度过粗（如修改整个 MLP 层），导致无关知识被破坏，且神经元的“多语义性”使得编辑一个概念会干扰多个无关概念。
*   **研究目标**：引入机械可解释性（Mechanistic Interpretability）中的**稀疏性**原则，实现更精准、透明且干扰极小的知识更新。

#### 2. 论文提出的方法论：SCAN 框架
SCAN（Sparse Circuit Anchor Interpretable Neuron）的核心思想是将知识编辑转化为对“知识电路”中特定稀疏特征的定向操纵。
*   **核心组件**：
    1.  **稀疏转码器（Sparse Transcoder）**：将 MLP 的隐藏状态投影到高维稀疏特征空间，使每个特征具有单语义性（Monosemanticity）。
    2.  **归因图构建（Attribution Graph）**：通过计算特征节点间的因果影响分数，构建一个包含嵌入、特征、误差和逻辑值的加权全图。
    3.  **梯度归因与剪枝**：利用泰勒展开的一阶项近似计算节点间的影响力，并考虑多步路径（Total Attribution）。通过累积阈值剪枝，识别出负责特定事实的最小化“知识电路”。
    4.  **稀疏编辑与注入**：仅针对电路中与主体（Subject）锚定的稀疏特征修改其解码器权重。在推理时，通过计算特征激活的 Jaccard 相似度来触发编辑后的“引导向量（Steering Vector）”。
*   **关键逻辑**：识别“主体到答案”的流水线——浅层编码主体身份，中深层结合关系提取答案。

#### 3. 实验设计
*   **数据集**：
    *   **编辑性能**：CounterFact（反事实挑战）、ZsRE（问答对）、WikiFactDiff（真实事实更新）。
    *   **通用能力**：MMLU、GSM8K、ARC、CommonSenseQA、OpenBookQA、SciQ。
*   **对比方法**：
    *   参数修改类：Fine-Tuning (FT)、MEMIT、RECT、AlphaEdit。
    *   参数保留类：GRACE、MELO。
*   **评估指标**：可靠性（Rel）、泛化性（Gen）、局部性（Loc，即不影响无关知识）以及通用能力准确率。

#### 4. 资源与算力
*   **算力说明**：论文**未明确列出**具体的 GPU 型号、数量或总训练时长。
*   **实现细节**：使用了公开的预训练稀疏转码器权重（如 Gemma2-2B, Qwen3-8B, Llama3.1-8B 的转码器），无需从头训练转码器，这降低了实际应用的算力门槛。

#### 5. 实验数量与充分性
*   **实验规模**：
    *   进行了高达 **3,000 次**的连续顺序编辑实验，远超常规论文的 1,000 次。
    *   覆盖了 1B、2B、8B 不同规模的多个模型家族（Gemma2, Qwen3, Llama3.1/3.2）。
    *   针对归因图的节点分布、层级功能、特征语义进行了深入的消融分析和案例研究。
*   **评价**：实验设计非常充分且严谨。通过长程编辑压力测试，客观地展示了 SCAN 在防止模型崩溃方面的显著优势，对比基准涵盖了当前最先进的（SOTA）编辑技术。

#### 6. 论文的主要结论与发现
*   **稳定性**：SCAN 在 3,000 次编辑后仍能保持 MMLU 等基准测试的性能，而 MEMIT 和 RECT 等方法在 2,000 次编辑左右会导致模型能力彻底崩溃（准确率趋近于 0）。
*   **精准定位**：发现事实知识高度集中在“主体末尾标记”和“句子末尾标记”对应的神经元上。
*   **功能分区**：模型存在明显的“主体-答案”处理链条，浅层负责稳定主体身份，深层负责检索事实。
*   **单语义优势**：通过稀疏特征编辑，可以有效区分“语言概念”等通用特征与“特定事实”特征，从而减少副作用。

#### 7. 优点
*   **高可解释性**：将黑盒编辑转变为白盒电路分析，提供了知识存储位置的直观证据。
*   **极强的鲁棒性**：在终身学习场景下表现极佳，解决了长期困扰编辑领域的模型崩溃问题。
*   **干预粒度细**：相比修改整个权重矩阵，SCAN 只干预极少数关键特征，对模型原有分布破坏最小。

#### 8. 不足与局限
*   **依赖预训练转码器**：SCAN 的效果高度依赖于 Sparse Transcoder 的质量。如果目标模型没有现成的转码器，则需要巨大的算力先训练一个。
*   **计算开销**：虽然使用了一阶泰勒展开简化，但构建归因图和进行多步路径计算在实时编辑场景下仍可能存在一定的延迟。
*   **特征重叠风险**：尽管追求单语义性，但在极大规模编辑下，是否会出现特征耗尽或冲突仍需进一步探讨。

（完）

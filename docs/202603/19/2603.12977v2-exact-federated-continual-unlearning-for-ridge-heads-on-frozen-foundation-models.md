---
title: Exact Federated Continual Unlearning for Ridge Heads on Frozen Foundation Models
title_zh: 针对冻结基础模型上岭回归头的精确联邦持续遗忘
authors: "Yijun Quan, Wentai Wu, Giovanni Montana"
date: 2026-03-13
pdf: "https://arxiv.org/pdf/2603.12977v2"
tags: ["query:mu-ce"]
score: 10.0
evidence: 针对被遗忘权的精确联邦遗忘
tldr: 本研究针对联邦学习中冻结基础模型加岭回归头的场景，提出了一种精确的持续遗忘学习方法。针对“被遗忘权”需求，该方法利用岭回归的加性充分统计量，通过固定大小的消息传递实现对样本或用户的动态增删。该方案在数学上与集中式重新训练完全等价，具有确定性的等效保证和极高的计算效率，有效解决了现有联邦遗忘学习方法在精确性和成本之间的权衡问题。
selection_source: fresh_fetch
motivation: 现有的联邦遗忘学习方法在处理冻结基础模型时，难以在保证遗忘精确性的同时降低重新训练的计算成本。
method: 利用岭回归的加性充分统计量构建通信协议，通过维护全局统计量实现与集中式重新训练完全一致的模型更新。
result: 实验证明该方法在多个基准测试中达到了与重新训练几乎一致的精度（误差小于10^-9），且处理速度比传统联邦重训快几个数量级。
conclusion: 该研究为联邦环境下的精确遗忘学习提供了一种高效且数学完备的解决方案，特别适用于基于基础模型的特征提取场景。
---

## 摘要
基础模型通常被部署为冻结的特征提取器，并配备一个小型可训练头，以在联邦设置下适应私有的用户生成数据。“被遗忘权”要求根据需求从已训练模型中移除特定样本或用户的影响。现有的联邦遗忘方法主要针对通用的深度模型，并依赖于近似重构或选择性重训练，这使得精确性要么代价高昂，要么难以实现。我们研究了一个具有实际应用价值但尚未被充分探索的场景：带有岭回归头的冻结基础模型。其精确最优解仅通过两个加性充分统计量与数据相关，我们将此转化为一种通信协议，通过固定大小的消息支持任意的添加和删除请求流。服务器维护的头在精确算术下与每次请求后的集中式重训练结果完全一致。我们提供了确定性的重训练等效性保证、顺序和分区不变性、两种服务器端变体，以及零 KL 散度的贝叶斯证明。在四个基准测试上的实验证实了上述保证：两种变体与集中式岭回归重训练的相对 Frobenius 误差均在 $10^{-9}$ 以内，且处理每个请求的成本比联邦重训练基准低几个数量级。

## Abstract
Foundation models are commonly deployed as frozen feature extractors with a small trainable head to adapt to private, user-generated data in federated settings. The ``right to be forgotten'' requires removing the influence of specific samples or users from the trained model on demand. Existing federated unlearning methods target general deep models and rely on approximate reconstruction or selective retraining, making exactness costly or elusive. We study this problem in a practically relevant but under-explored regime: a frozen foundation model with a ridge-regression head. The exact optimum depends on the data only through two additive sufficient statistics, which we turn into a communication protocol supporting an arbitrary stream of add and delete requests via fixed-size messages. The server maintains a head that is, in exact arithmetic, pointwise identical to centralized retraining after every request. We provide deterministic retrain-equivalence guarantees, order and partition invariance, two server-side variants, and a Bayesian certificate of zero KL divergence. Experiments on four benchmarks confirm the guarantees: both variants match centralized ridge retraining to within $10^{-9}$ relative Frobenius error and complete each request at orders-of-magnitude lower cost than federated retraining baselines.

---

## 论文详细总结（自动生成）

这篇论文针对联邦学习（FL）中日益流行的“冻结基础模型+轻量级预测头”架构，提出了一种实现**精确且持续（Continual）**数据遗忘的高效方案。以下是对该论文的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：在联邦学习环境下，如何满足“被遗忘权”（如 GDPR），即在不进行高昂的全局重新训练的前提下，从已训练模型中彻底、精确地移除特定样本或用户的影响？
*   **研究背景**：
    *   现有联邦遗忘方法（如 FATS, Exact-Fun）多针对通用深度模型，依赖近似计算或选择性重训，难以兼顾“精确性”与“低成本”。
    *   当前工业界普遍采用冻结基础模型（Foundation Models）作为特征提取器，仅训练末端线性头（如岭回归头）。
*   **整体含义**：本文利用岭回归（Ridge Regression）的解析解特性，证明了在基础模型冻结的情况下，联邦遗忘可以转化为简单的统计量加减运算，从而实现与“从头重训”在数学上完全等价的遗忘效果。

### 2. 方法论：核心思想与技术细节
*   **核心思想**：岭回归的最优解仅依赖于两个**充分统计量**：特征格拉姆矩阵（Gram matrix）$S = F^\top F$ 和特征-标签矩 $G = F^\top Y$。由于这两个统计量具有**加性（Additivity）**，增加或删除数据只需对全局统计量进行加减。
*   **关键技术流程**：
    1.  **客户端**：计算新增或删除样本的特征向量，生成局部统计量更新（$S_{\pm}, G_{\pm}$）。
    2.  **服务器**：维护一个全局统计量账本，根据客户端发来的增删请求更新 $S$ 和 $G$。
    3.  **模型恢复**：服务器通过更新后的统计量直接求解线性方程组 $(S + \gamma I)W = G$ 得到新权重。
*   **两种变体**：
    *   **Variant A（精确重构）**：直接对更新后的全局矩阵进行 Cholesky 分解求逆，数值稳定性高。
    *   **Variant B（增量更新）**：利用 Sherman-Morrison-Woodbury (SMW) 公式进行秩更新，适合小批量增删，计算复杂度更低。
*   **理论保证**：论文证明了该协议具有**确定性重训等效性**、**顺序无关性**、**分区无关性**，并从贝叶斯角度证明了遗忘后的模型与重训模型之间的 KL 散度为零。

### 3. 实验设计
*   **数据集与场景**：
    *   **图像分类**：CIFAR-10, CIFAR-100, FeMNIST（非独立同分布 Non-IID 设置）。
    *   **情感分析**：Sentiment140（NLP 任务）。
*   **基础模型（Backbones）**：DINOv2-ViT（视觉）、RoBERTa（文本）。
*   **对比基准（Benchmarks）**：
    *   **Centralized Ridge Retrain**：理想的黄金标准（集中式重训）。
    *   **FedAvg Retrain**：标准的联邦学习重训。
    *   **FATS / Exact-Fun**：现有的最先进（SOTA）联邦遗忘算法。
*   **评估指标**：测试准确率、相对 Frobenius 误差（衡量参数精确度）、处理时间（耗时）。

### 4. 资源与算力
*   **硬件环境**：实验在一台配备 **4 张 NVIDIA RTX A6000 GPU** 的服务器上运行。
*   **软件框架**：使用 Flower 联邦学习框架进行模拟。
*   **训练细节**：由于是解析解，不需要传统意义上的迭代训练时长，主要耗时在于特征提取和矩阵运算。论文强调其处理 200 次连续删除请求的时间比基准方法快几个数量级。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 CV 和 NLP 两个领域，设置了 10 到 100 个不等的客户端数量，并模拟了 Non-IID 数据分布。
*   **实验类型**：
    *   **效用测试**：验证模型准确率是否受损。
    *   **精确度测试**：通过 fp64 精度验证参数误差（达到 $10^{-9}$ 级别）。
    *   **压力测试**：进行了 200 步连续的单点删除和“删除后重新添加”实验。
*   **充分性评价**：实验设计非常充分，不仅验证了准确性，还深入探讨了数值稳定性（fp32 vs fp64）和持续增删下的表现，对比实验覆盖了当前主流的联邦遗忘框架，结果具有说服力。

### 6. 主要结论与发现
*   **精确等效**：该方法在数值上几乎完美匹配集中式重训结果，相对误差极小。
*   **极高效率**：相比于需要多轮通信的 FedAvg 重训，该方法仅需单轮通信即可完成遗忘，且服务器端计算开销极低。
*   **持续性支持**：能够完美处理任意交错的添加和删除请求流，不会产生误差累积（尤其是 Variant A）。
*   **数值精度重要性**：在服务器端使用 fp64 精度处理第二级统计量对于维持 $10^{-9}$ 级别的精确度至关重要。

### 7. 优点
*   **数学完备性**：提供了严谨的确定性保证，而非概率性或近似性的遗忘。
*   **通信高效**：消息大小固定（与特征维度 $d$ 相关，与样本量 $n$ 无关），且仅需一轮交互。
*   **通用性**：适用于任何冻结特征提取器+线性头的场景，不限模态（视觉、文本均可）。
*   **零 KL 散度**：从信息论角度证明了遗忘的彻底性。

### 8. 不足与局限
*   **架构限制**：仅适用于**冻结**骨干网络的情况。如果基础模型本身也需要微调或针对敏感数据训练，该方法无法移除骨干网络中隐藏的信息。
*   **维度瓶颈**：通信成本随特征维度 $d$ 的平方增长（$O(d^2)$）。虽然论文讨论了低秩近似，但在超高维特征下，原始统计量的传输可能存在压力。
*   **隐私风险**：虽然不传输原始数据，但二阶统计量（Gram 矩阵）在某些极端情况下可能存在泄露成员身份或特征分布的风险，需结合安全聚合或差分隐私使用。

（完）

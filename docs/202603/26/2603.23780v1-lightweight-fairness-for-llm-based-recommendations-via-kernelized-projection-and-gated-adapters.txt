Title: Lightweight Fairness for LLM-Based Recommendations via Kernelized Projection and Gated Adapters

URL Source: https://arxiv.org/pdf/2603.23780v1

Published Time: Thu, 26 Mar 2026 00:20:21 GMT

Number of Pages: 18

Markdown Content:
# Lightweight Fairness for LLM-Based Recommendations via Kernelized Projection and Gated Adapters 

Nan Cui, Wendy Hui Wang, and Yue Ning 

Department of Computer Science, Stevens Institute of Technology, Hoboken NJ, USA 

ncui@stevens.edu, hwang4@stevens.edu, yue.ning@stevens.edu 

Abstract. Large Language Models (LLMs) have introduced new capa-bilities to recommender systems, enabling dynamic, context-aware, and conversational recommendations. However, LLM-based recommender sys-tems inherit and may amplify social biases embedded in their pre-training data, especially when demographic cues are present. Existing fairness solutions either require extra parameters fine-tuning, or suffer from opti-mization instability. We propose a lightweight and scalable bias mitiga-tion method that combines a kernelized Iterative Null-space Projection (INLP) with a gated Mixture-of-Experts (MoE) adapter. Our approach estimates a closed-form projection that removes single or multiple sensi-tive attributes from LLM representations with no additional trainable pa-rameters. To preserve task utility, we introduce a two-level MoE adapter that selectively restores useful signals without reintroducing bias. Exper-iments on two public datasets show that our method reduces attribute leakage across multiple protected variables while maintaining competi-tive recommendation accuracy. 

Keywords: Large language models (LLM) · LLM-based Recommenda-tion systems · Fairness in recommendations. 

# 1 Introduction 

The advent of Large Language Models (LLMs) is reshaping the design and capabilities of recommender systems. By leveraging the rich semantic under-standing and generative abilities of LLMs, new systems can move beyond static suggestions to support dynamic, context-aware, and interactive recommenda-tions. LLM-based recommender systems (RecLLMs) [32] enable users to engage in natural conversations while receiving personalized and situationally relevant suggestions. This shift opens up new possibilities for personalization, including handling complex user intents, understanding implicit preferences, and adapting to evolving user contexts in real time. While prompt-only methods using general-purpose LLMs may be sufficient for small-scale or low-stakes applications, they often fall short in accuracy, particularly in tasks like named product generation. These limitations point to the need for tighter integration between LLMs and collaborative filtering, user profiles, and retrieval components to build scalable 1

> arXiv:2603.23780v1 [cs.LG] 24 Mar 2026

and reliable recommendation systems. Prior work such as Liao et al. [24] jointly aligns embeddings from traditional recommender systems [26,35] with LLM em-beddings and produce more accurate recommendations. Despite the recent success of RecLLMs, they inevitably inherit - and some-times amplify - social biases present in their pre-training corpora [9]. When sensitive information such as gender , age , or occupation appear in a prompt, the generated recommendation list can privilege or penalize specific demographic groups. Existing bias mitigation techniques employ two main strategies. The first calibrates item exposure [22], which balances item popularity but ignores user-side disparities. The second protects the user-side fairess with gradient-reversal discriminators (notably UP5 [21]). While effective, these adversarial modules introduce large scale of trainable parameters and remain brittle to op-timization hyper-parameters. For example, UP5 [21] mitigates the per-attribute retraining by adding an attention controller, yet the adversarial coupling still destabilizes optimization. There are also bias diagnostic tools that offer purely diagnostic probes, such as CFaiRLLM [5] construct a neutral prompt and an otherwise identical prompt that names one (or an intersection of) sensitive at-tributes (e.g., gender, age, etc.), then compares the two top-k lists with their specialized metrics; it flags discrepancies as consumer-side unfairness but inten-tionally proposes no debiasing remedy. In practice, an LLM primarily serves its utility task, and any debiasing step that requires additional training cycles or add-on networks will lead to increased computational costs. At present, no ex-isting method focuses on optimization-effective debiasing for RecLLMs from an user-side view. Thus, we need a “ lightweight ” alternative that removes sensitive information without adding optimization headaches. Iterative Null-space Projection (INLP) [30] answers this call because it de-biases by closed-form linear algebra rather than by re-optimizing the whole net-work. Given a well-trained RecLLM, the recipe is simple: (i) freeze the language model, (ii) extract the sequence-level representation (i.e., embedding vector for an input prompt) from the model, train a probe (i.e., a linear classifier) on this vector to predict a sensitive label (say, gender), and (iii) project the sequence-leval representation onto the null subspace , the set of directions orthogonal to the probe’s weight vector. In plain language, the probe is a measure for bias, and the null subspace is the part of representation space it cannot see. We get the projection matrix from a least-square solver, so no gradient ever touches the backbone. After applying the projection matrix to each embedding vector, this projected representation replaces the original vector in the language model before its output layer. Because of this algebraic nature, INLP has become a popular off-the-shelf debiasing tool for word embeddings [15] and BERT encoders [12]. However, two obstacles emerge in practice. First, a single linear probe cannot capture the non-linear signals an LLM exploits, leaving left-over bias. Second, mitigating the bias of multiple sensitive attributes would require a separate probe - and a separate projection - for each attribute. Stacking those projections can erase task-useful variance and will rapidly reduce the performance of the model. 2To tackle the first obstacle (i.e., non-linear leakage ), we kernelize the probe with Random Fourier Features (RFF) [28], thereby upgrading linear INLP to a universal approximation while still yielding a closed-form projector. We fur-ther inject isotropic Gaussian perturbation into each hidden state during probe fitting. This operation widens the decision margin and makes the null space ro-bust to the representation shift introduced by fine-tuning. The resulting matrix is stored as a non-trainable buffer with no gradients, no optimizer state, and negligible overhead, which we named “lightweight”. To address the second obstacle (i.e., handling multiple sensitive attributes ), we attach P with a two-level gated Mixture of Experts (MoE) adapter. MoE adapter consists of two gates. The first gate reads the current representation and outputs a probability vector that softly blends a set of attribute-specific projectors. This allows the model to control the intensity of removing each at-tribute bias. The partly purified embedding then enters several low-rank LoRA experts; inside every expert a second residual gate scales its own update before all updates are summed into a single task vector, which is then lightly fine-tuned after debiasing to improve main-task accuracy. This “erase-then-repair” pipeline lets RecLLMs provide fair and accurate recommendations. Our work has two main contributions: (1) we propose a kernelized INLP projector that removes the information of the sensitive attributes in one closed-form step, incurring zero optimization cost; (2) we devise a two-level gated MoE adapter that recovers task utility in the multi-attribute setting while introducing only Opkq additional parameters for k sensitive attributes. We validate the ap-proach on two public LLM-based recommendation benchmarks, demonstrating consistent fairness improvements without sacrificing recommendation quality. 

# 2 Related Work 

LLM-Based Recommendation Systems Essentially, the research on Re-cLLMs can be categorized into two types: (i) Prompt-only pipelines , which trans-late recommendation queries as free-text questions that are answerable by general-purpose LLMs. Gao et al. [11] retrieve review snippets to ground ChatGPT re-sponses. Geng et al. [13] formulate the recommendation tasks in a unified text-to-text fashion. (ii) Parameter-efficient fine-tuning based methods which equip the instruction-tuned backbones with lightweight adapters. Ngo et al. [26] design the recommender systems based on instruction-tuned LLMs, and demonstrates that these systems outperform the conventional collaborative-filtering based recom-mender systems [36]. Some recent works utilize a hybrid architecture that injects entity embeddings into instruction-tuned backbones [24]. Unlike prompt-only systems, our work fine-tunes an open Llama model [6] due to its full exploit of identifier-level interaction signals preservation of the conversational flexibility. 

Fairness in RecLLMs Most of the research of fairness in RecLLMs focus on mitigating the bias at either the user side or the item side. Early work re-ranks or re-weights predicted preference scores to curb popularity bias and protect under-served items [2], whereas Ekstrand et al. [8] regularize latent factors and 3Wu et al. [33] employ gradient-reversal debiasing to improve user equity; surveys by Caton et al. [3] confirm that both strategies leave measurable residual bias. With the rise of large language models, Hua et al. [21] attach a counterfactual discriminator to a pre-trained encoder, Deldjoo et al. [5] propose benchmark protocols for consumer fairness, and Zhang et al. [34] diagnose systematic bias in zero-shot ChatGPT recommendations; complementary surveys by Gallegos 

et al. [10] trace broader LLM biases, while Rarrick et al. [29] release challenge sets that surface gender disparity in translation. Our study extends this line by introducing a kernel-lifted projection and gated adapter that eliminate multi-attribute leakage in fine-tuned LLM recommenders without additional trainable parameters, thereby complementing earlier re-ranking and adversarial methods. 

Sub-space Projection for Debiasing Projection methods first locate the directions that encode a protected attribute and then drop those directions by orthogonal projection. The idea began with static word embeddings: Boluk-basi et al. [1] showed that subtracting a single “gender direction”—estimated from pairs such as he–she —reduces analogy bias in word2vec vectors [25]. Iter-ative Null-space Projection (INLP) extends this trick to contextual encoders by repeatedly (i) training a linear classifer and (ii) projecting representations onto its null-space until the attribute is no longer linearly predictable [30]. Because linear classifer can miss non-linear leakage, later work performs the projection in a reproducing-kernel Hilbert space and approximates it with random Fourier features (RFF), so that kernel-linear signals are removed as well [28,31]. Most re-cently, Cho et al. connect this “project-then-repair” view to information-theoretic sufficiency, showing that a representation can stay predictive for the downstream label yet be conditionally independent of the sensitive attribute [4]. 

# 3 Preliminaries 

3.1 LLM-based Recommender Systems 

Let U be the set of users, I the set of items, and C the set of contextual information, such as user interaction history, item metadata, or natural lan-guage queries. Given a large language model M parameterized by θ, the objec-tive of an LLM-based recommender system (RecLLM) is to learn a mapping: 

fθ : pU, I, Cq Ñ R that assigns a relevance score or generates a ranked list of items ti1, i 2, . . . , i ku Ď I for a user u P U, conditioned on context c P C. The function fθ operates via prompt generation. The objective is to optimize fθ such that recommendations align with user intent and context while leveraging the generalization capabilities of LLMs. A typical RecLLM accepts a prompt as an input, and replies with one item identifier. In this paper, we study two different types of prompts for Re-cLLMs [13,21]. Figure 1 illustrates an example of both types of recommendations. 

Direct recommendation. The prompt names the user and lists a set of can-didate items, without showing any user-item historical interactions. The LLM in-spects the prompt and outputs a single item for recommendation. 4Direct Recommendation                              

> Input: Which movie user_{user_ID} would like to watch among the following candidates? {List of 100 IDs}. Output: {movie_ID}
> Sequential Recommendation
> Input: User_{user_ID} has already watched the following movies {sequence of movie IDs}. Which movie user_{user_ID} would like to watch next? Output: {movie_ID}

Fig. 1: Prompt templates for LLM-based recommendations. 

Sequential recommendation. The prompt includes a sequence of user’s re-cent interacted items associated with specific users. The LLM outputs the recommended item based on the histor-ical interactions. In RecLLMs, the goal is to convert structured recommendation data (e.g., user-item interactions, ratings, times-tamps) into text sequences that the model can interpret and reason over. Given its complexity, it is difficult to train a RecLLM from scratch. There-fore, in this paper, we only consider fine-tuning of RecLLMs with Low-Rank Adaptation (LoRA) [20], which intro-duces a low-rank decomposition to represent the parameter updates by fine-tuning. We call each such update a “LoRA patch”. Throughout the paper, we only consider training of LoRA patches while the backbone of the RecLLM model stays frozen. 

3.2 Counterfactual Fairness 

In this paper, we follow the definition of counterfactual fairness [23]: A recom-mender system is fair for an individual if its predicted recommendations would remain unchanged were the person assigned any other demographic label. Un-like group-level fairness metrics such as demographic parity [27], which can yield unstable estimates in the presence of severe class imbalance [18], the counterfac-tual fairness is defined at the individual level and thus is robust to the skewed distribution. As a result, it has been increasingly adopted to the LLM setting as a standard for fine-grained fairness assessment. 

Definition 1 (Counterfactually Fair Recommendations [23]). 

Let A P t a1, . . . , a mu be a sensitive attribute and let hu denote the part of the user representation that is causally independent of A. Write Lphu, A q for the (possibly stochastic) top-K list returned by the recommender. The recommender is counterfactually fair iff 

P`Lphu, A q “ ℓ | A “ ai

˘ “ P`Lphu, A q “ ℓ | A “ aj

˘, @ℓ, @ai, a j P t a1, . . . , a mu.

In other words, an individual would receive exactly the same distribution of recommendations had their demographic label been any other feasible value. 

In practice, enumerating all counterfactual prompts is infeasible. Inspired by Hua et al. [21], we adapt the protocol such that: we freeze the LLM’s sequence-level representation h P Rd and train a two-layer MLP gθ : Rd Ñ Rm to predict a sensitive attribute A P t a1, . . . , a mu. For each one-vs-rest setting, we compute the 5MLP’s AUC, measured as the probability that its score for a randomly chosen user in class ai exceeds the score for an independently drawn user outside the class: 

AUC i “ AUC `gθ phq, A “ ai vs. A ‰ ai

˘, i “ 1, . . . , m. (1) The bias of the sensitive attribute A can be measured by the Counterfactual Leakage Gap (CLG) on Ai, defined as the average deviation of the AUC scores across all classes of A from the random-guessing baseline (i.e., AUC = 0.5). 

∆CL “ 1

m

> m

ÿ

> i“1

| AUC i ´0.5| (2) Intuitively, the perfect counterfactual fairness on the sensitive attribute A is reflected by ∆CL “ 0. A high ∆CL value reflects greater bias in the recommen-dations concerning the attribute A.If the model representations are independent from the sensitive information, then the recommendations should be bias free (i.e., ∆CL “ 0.5). Therefore, the main objective of this paper is to learn fair data representations that are i) informative enough for the downstream task, and ii) independent from the sensitive attributes. Formally, let h be the sequence-level representation learned by the given RecLLM model. Our goal is to convert h to h‹ such that h‹ is independent of A.

# 4 Method 

Given the sequence-level representation h P Rd, a d-dimensional vector that sum-marizes each prompt, we perform the following two steps to remove the bias encoded in h. By Step 1, we erase the information of a single sensitive attribute encoded in h. Step 2 extends Step 1 to removing bias from multiple sensitive attributes while minimizing the accuracy loss of the recommendations. 

Step 1: Debiasing on a single sensitive attribute. This step takes the sequence-level representation h of the RecLLM model and a specific sensitive attribute A as the input, and transforms h to h‹ which is independent from 

A. To capture the non-linear signal in the representations, h is first lifted to a higher dimension. Then the lifted h is projected to h‹ by INLP. 

Step 2: Dealing with multiple sensitive attributes. When multiple sen-sitive attributes are present, independently generating a fair INLP representation for each attribute using Step 1 can lead to significant degradation in recommen-dation accuracy. To address this challenge, we propose a two-level gated LoRA adapter that introduces two gating mechanisms. The first gate produces a soft weighting vector that adaptively scales the INLP projector associated with each sensitive attribute, enabling more nuanced debiasing. The second gate modu-lates a LoRA-based patch applied to the debiased representation, allowing for lightweight fine-tuning that compensates for the accuracy loss introduced by the projection step. 64.1 Debiasing on a Single Sensitive Attribute 

Intuitively, applying INLP directly on the sequence-level representation can re-move the bias of a given sensitive attribute. However, such projection is only able to produce linear representations and may lose the non-linear signals exist in LLM models. To address this issue, we conduct the following steps: (1) Ran-dom Fourier Lift : The representation vector is lifted into a random-Fourier feature space [28] whose inner product unbiasedly approximates the Gaussian kernel to make the bias become more distinct for removal; (2) INLP projection :The representation in the lifted space is debiased by INLP; (3) Refinement of projection : the projection is further refined, aiming to recover the non-linear signals for LLMs to utilize. Fig. 2 illustrates the flow of the three steps. Next, we explain the details of each step. Sequence-level representations 

> Add Gaussian Noise
> Random Fourier Lift
> INLP in the Lifted Space
> No.
> Projected
> Representations
> RFF-INLP Projector
> Task Head / Loss
> Yes.

Fig. 2: Debiasing on a sensitive at-tribute using RFF-INLP. 

Step 1: Random Fourier Lift. The goal of this step is to re-express represen-tation in a way that makes any subtle bias to be more distinct for an easier removal later. Given the sequence-level representa-tion h P Rd, we first add an isotropic per-turbation ϵ to h, aiming to prevent INLP from ignoring low-variance directions: 

ϵ „ N p0, η 2Idq, ˜h “ h ` ϵ, 

Here, ϵ is a d-dimensional Gaussian noise vector with mean 0 and covariance η2I,where η ą 0 controls the noise magnitude and I is the d ˆ d identity matrix. The isotropic perturbation adds the same tiny variance η2 to every coordinate, raising low-variance dimensions to the same scale as the dominant ones. Once this variance is equalized, any remaining attribute sig-nal can no longer hide in small-amplitude directions [7]. Next, we draw 

Ω „ N p0, σ ´2Idq, b „ Unif r0, 2πsD ,

where Ω P RDˆd stores D “frequency” vectors drawn from the Gaussian law; b P

RD supplies independent phase shifts sampled uniformly from r0, 2πs. We then map the balanced representation ˜h into an D-dimensional feature vector using the function φσ :

φσ p˜hq “ 

b 2 

> D

cos pΩ˜h ` bq P RD

7whose inner product unbiasedly approximates the Gaussian kernel kσ . Finally, the random-Fourier feature is generated as follows [28] 

ˆh “ r ˜h; φσ p˜hq s P Rd`D ,

where r¨ ; ¨s is a concatenation operation. We call ˆh the RFF-augmented features. A nice property of RFF-augmented features is that, taking the dot product of two feature vectors (e.g., ˜h, ˜h1) already gives (in expectation) the same value as their Gaussian-kernel similarity [16, 28], which means 

EpΩ,bq

“xφσ p˜hq, φ σ p˜h1qys “ kσ p˜h, ˜h1q, @ ˜h, ˜h1 P Rd. (3) The INLP probes trained on these coordinates therefore perceive - and subse-quently remove - every kernel-linear leakage direction that was invisible in the original feature space. 

Step 2: INLP Projection. Let tˆhpnquNn“1 P Rd`D be the RFF representa-tions output by Step 1, where N is the size of the samples. INLP iteratively trains a sequence of linear probes ˆw1, . . . , ˆwT P Rd`D by T iterations that each maximizes the conditional log-likelihood of the sensitive attribute on the current sub-space, then removes that direction. Stacking row-wise the probe weights gives the matrix 

ˆW “

»–

ˆwJ

> 1

...

ˆwJ

> T

fi fl P RT ˆp d`Dq,

where T is the number of iterations for computing the linear probes and we assume that T ď p d ` Dq. Each probe will first compute the projector P , then we test the projected representation whether it passes the counterfactual fairness threshold. If not, we continue to compute another linear probe. After T times we can obtain a representation satisfying the fairness threshold. The final projector is obtained by orthogonally removing the row space of ˆW. For a full-row-rank matrix this projector has the classical closed form [14] 

ˆP “ Id`D ´ ˆWJp ˆW ˆWJq´1 ˆW, ˆP2 “ ˆPJ “ ˆP,

where ˆP is the orthogonal projection onto the null-space of ˆW [30]. Down-stream layers still expect a d-dimensional vector, so we must extract the part of the full projector ˆP that acts on those d coordinates. We therefore write ˆP in block form 

ˆP “

« ˆP11 ˆP12 

ˆP21 ˆP22 

ff 

, ˆP11 P Rdˆd,

We only keep the upper-left block ˆP11 as the projection result, following the well-known Schur-complement identity [14] for orthogonal projectors: it tells us that the action of ˆP on the first d coordinates is captured entirely by ˆP11 . The 8move is purely for dimension matching and does not itself influence bias removal; it simply lets the debiased vector flow into the original network without changing its size. Following that, we have 

h‹ “ P h “ ˆP11 h, P “ ˆP11 P Rdˆd.

Here, h‹ is the debiased representation. Notably, as the matrix P inherits symmetry and idempotence from ˆP, all directions spanned by the original probes 

t ˆwtuTt“1 intersecting the h-subspace have been removed. 

Step 3: Refinement of Projection. At the end of every training epoch of the LLM model, we compute the Counterfactual Leakage Gap ∆CL (Eqn. (2)). We use a parameter, τ P p 0, 1q, to control the degree of fairness. A smaller τ

value requires stricter fairness. Then, we repeat Steps 1 & 2 to update P and h

if ∆CL ą τ , and retain the current P for the next training epoch otherwise. Our empirical evaluations shows that no more than three updates needed to achieve a good performance, indicating the efficiency of our approach. 

4.2 Handling Multiple Sensitive Attributes 

Debiasing multiple sensitive attributes with the INLP projectors raises two prob-lems. First, each attribute shows up in the representation with a different in-tensity. For example, the gender attribute is usually predicted from only a few components of the representations, whereas the age and occupation attributes spread across many more components. In this case, using one fixed projection for all of them risks removing too much useful information for gender or, con-versely, removing too little bias for age and occupation. Second, these attributes are often correlated in the embedding: the same components that help the model guess a user’s age can also capture general preferences shared across all users. If we zero-out those correlated dimensions to reduce age bias, this will incur high loss of the recommendation accuracy. We address both issues by designing a lightweight two-level gated MoE adapter , which is inserted after every self-attention block of the backbone of LLMs. The adapter comprises two gates: (1) A context-dependent outer gate softly sets, for each attribute, how much of its projector to mix in, sliding from zero to full strength; and (2) An inner gate 

adds a low-rank LoRA patch, based on the already-debiased vector, to bring back only the features that improve accuracy without re-introducing sensitive information. Together the gates form an “erase-then-repair” cycle that handles all attributes at once without duplicating the model or adding extra optimizers. For layer ℓ and token position t, let hℓ,t P Rd be the token-level hidden state immediately before the layer’s pre-trained output projection Oℓ P Rdˆd. A pooled user-context vector cu “ Pool pt hℓ,t utq P Rd is the input for the gating functions described below; intuitively, cu is a single d dimensional summary of the entire prompt, obtained by averaging (or attention-weighting) all token hidden states at layer ℓ. It captures the overall user context in one compact vector. Each adapter instantiation maintains K attribute-specific experts, implemented as rank-r LoRA updates. 9Level-1 soft projection (attribute weighting) Given the pooled user context cu P

Rd we first compute a contextual attention vector 

α “ softmax `G1, cu

˘ P ∆K , G1 P RKˆd.

Here ∆K “ t p P RK | pk ě 0, řKk“1 pk “ 1u designates the K-dimensional proba-bility simplex ; its dummy variable p is replaced in practice by α “ p α1, . . . , α K q.The matrix G1 is the only trainable component at this level; its k-th row acts as a key that measures how salient the k-th sensitive attribute is in the current user trace. Each attribute owns a hard projector Ppkq P Rdˆd, computed once in Subsection 4.1, that is orthogonal and idempotent ( Ppkq“PpkqPpkq) and removes the linear signal of attribute Ak. Because α lies on the simplex, it serves two roles: 1. Adaptive attribute weighting. A larger coefficient αk indicates stronger residual leakage for attribute Ak; hence a larger correction is applied, whereas already “clean” attributes receive near-zero weights and incur negligible util-ity loss. 2. Controlled projection. The softly projected representation h‹ “ `Id ´

ř 

> k

αkpId ´Ppkqq˘h depends on a convex combination, whose Jacobian Bh‹ 

> Bh

“

Id ´ř 

> k

αkpId ´Ppkqq remains full rank whenever at least one αk ă 1, thereby avoiding catastrophic loss of expressivity. The softly projected representation fed to the second gate is a convex com-bination of the original representations h and its attribute-wise hard-projected versions Ppkqh:

h‹ “ h `

> K

ÿ

> k“1

αk

`Ppkqh ´ h˘ “

´

Id ´

> K

ÿ

> k“1

αkpId ´ Ppkqq

¯

h.

This construction exposes the backbone to a continuum that spans the untouched feature ( αk “ 0) and the fully projected one ( αk “ 1), allowing the network to find an information–fairness trade-off in between. The update remains residual :because Erαks “ 1 

> K

under a uniform prior, we have Erh‹ | αs “ h, so gradients flow through the adapter as stably as through a standard skip-connection—an empirical necessity when stacking many transformer layers. 

Level-2 attribute-conditioned expert fusion Soft projection suppresses most sen-sitive cues yet inevitably removes some task-useful variation. To restore that ca-pacity we attach, to each sensitive attribute Akpk P t 1, . . . , K uq , a rank-r LoRA expert that perturbs the pre-trained output projection Oℓ of the current layer in the RecLLM: 

∆Ok “ Uk Vk, Vk P Rrˆd

looooomooooon  

> rank-rdown-projection

Uk P Rdˆr

looooomooooon 

> up-projection to hidden size

so that Oℓ ` ∆Ok spans the subspace most relevant to attribute Ak. The contribution of this expert is gated by a sigmoid weight 

βk “ σ`gJ 

> k

˜cu

˘, gk P Rd,

10 where ˜cu is a LayerNorm-stabilised summary of the soft-purified sequence. Be-cause βk P p 0, 1q, the gate acts as an analogue switch: it closes when leakage for attribute Ak has been eliminated and opens proportionally when a modest utility-bearing signal remains. The token representation finally forwarded to the next transformer block is 

y “ Oℓh‹ `

> K

ÿ

> k“1

βk ∆Ok h‹,

where the first term preserves debiased backbone semantics and the second term selectively re-injects attribute-specific nuance. Each ∆Ok being rank r limits the additional footprint to OpKdr q parameters and flops; sharing Uk across layers further reduces memory. To ensure the gates make clear yet well-balanced choices, we add two regular-izers: (i) an entropy penalty on α to prevent the level-1 gate from collapsing to a premature one-hot vector, and (ii) an ℓ1 penalty on the vector β “ p β1, . . . , β K q

to promote sparse expert activation. Together, the two gating layers realise an attribute-aware decouple–repair mechanism: the first gate attenuates unfair di-rections while preserving rank, and the second gate injects low-rank corrections only when they do not re-introduce sensitive-attribute information. 

4.3 Training and Inference 

We train two types of LoRA adapters simultaneously: (1) Task-LoRA adapters 

are inserted into the linear layers that project queries, keys, values, and outputs inside each self-attention block of the LLM’s backbone, and (2) Attribute-LoRA experts reside inside the level-2 gate and are scaled by βk “ σpgJ 

> k

˜cuq.These experts are applied only to the output-projection layer. Both types of adapters receive gradients solely from the token-level negative log-likelihood 

Ltask “ ´ log Pθ

`i‹ | xu

˘,

where Pθ is the backbone’s predictive distribution, xu is the full text prompt that encodes the target user’s historical interactions, and i‹ is the ground-truth item. No additional entropy or sparsity terms is needed. Gradient updates only happen in the adapter parameters tG1, gk, Uk, VkuKk“1

and the task-LoRA weights. The orthogonal projectors Ppkq and their composite 

P‹ remain frozen, and are updated only when the leakage probe detects ∆CL ą τ

(Section 4.1). 

# 5 Experimental Evaluation 

We present empirical results on two real-world benchmarks encompassing both direct and sequential recommendation. Our INLP Projection + Two-Level MoE Adapter, built on an Instruct Llama-3 backbone enhanced with LLaRA-style [24] low-rank adapters, is evaluated against the fairness-oriented baseline UP5 [21]. We report utility (Hit@1,3,10) alongside fairness, quantified by the Counterfac-tual Leakage Gap ∆CL .11 5.1 Experimental Setup Table 1: Performance of the back-bone approach on MovieLens and 

Insurance datasets. Utility: Hit rate (% Ò); fairness: counterfactual–leakage gap ∆CL (% Ó).                                          

> MovieLens Insurance
> LLaRA Sequential Direct Direct Hit@1 Ò58 .03 45 .20 57 .66
> Hit@3 Ò76 .58 60 .20 91 .24
> Hit@10 Ò82 .66 71 .20 99 .10
> ∆CL Gender Ó21 .90 29 .80 0.90
> ∆CL Marital Ó––11 .20
> ∆CL Age Ó19 .10 18 .40 1.90
> ∆CL Occup. Ó12 .40 11 .00 3.40
> ∆CL Avg. Ó17 .80 16 .40 4.35

Datasets. We use two datasets in our experiments: (1) MovieLens-1M data [17], which is a timestamped movie-rating data collected by the GroupLens research group; (2) Insur-ance dataset 1 drawn from an African insurer’s customer portfolios. We con-sider the following sensitive attributes for the MovieLens dataset: Gender (2 classes), Age (7 classes), and Occupa-tion (21 classes). After sorting each user’s ratings chronologically, we train on all but the last two interactions, validate on the penultimate one, and test on the final one. We consider the following sensitive attributes for the Insurance dataset: Marital status (8 classes), Age (5 classes), and Occupa-tion (6 classes). To train the LLM, we sort the interactions in the Insurance dataset by date, using the prefix up to the third-last item interaction for training, the second-last item for validation, and the last one for testing. 

Evaluation metrics. Utility is assessed with Hit@ t1, 3, 10 u, computed on the held-out target item given a candidate set of one positive and 99 sampled negatives; Hit@ k records whether the true item appears in the top-k ranks. Fairness is measured by the Counterfactual Leakage Gap (CLG) ∆CL in Eq. 2: after fine-tuning we freeze the sequence-level representation h, train a two-layer MLP probe for each sensitive attribute, compute one-vs-rest AUCs, subtract the random-guess baseline 0.5 and average the deviations over all classes; the closer this average is to zero, the less linearly recoverable sensitive information remains. We compute the average performance when multiple sensitive attributes are considered. 

LLM backbone. All of our experiments are conducted within the RecLLM framework based on LLaRA [24], using an Instruct Llama-3.2 2 1B model as the frozen base encoder–decoder. On this backbone we add two debiasing com-ponents only: (i) a fixed RFF–INLP projector with D “ 4096 random Fourier features and a noise-refine scale η “ 0.05 ; and (ii) a two-level gated MoE adapter whose task-side LoRA updates have rank 32 and whose attribute-specific experts have rank 8. These hyper-parameters follow the public LLaRA configuration [24] and are kept identical for every dataset and baseline. 

Baselines. For comparison we include (i) the standard LLaRA [24] model without any debiasing modules, (ii) P5 [13], which adapts the backbone through   

> 1https://www.kaggle.com/datasets/mrmorj/insurance-recommendation
> 2https://huggingface.co/datasets/meta-llama/Llama-3.2-1B-Instruct-evals

12 Table 2: Sequential recommendation results on MovieLens dataset. Utility metric: Hitting rate (% Ò). Fairness metric: counterfactual-leakage gap ∆CL (% Ó). Bold = best. 

Sensitive Hit@1 Ò Hit@3 Ò Hit@10 Ò ∆CL Ó

attribute UP5 Ours UP5 Ours UP5 Ours UP5 Ours Gender (G) 26.82 39.69 45.18 54.45 64.38 64.93 4.19 0.90 

Age (A) 31.23 38.16 51.18 53.61 67.70 65.13 2.91 0.60 

Occupation (O) 31.66 38.58 50.73 54.02 67.45 65.22 0.00 1.70 G + A + O – 56.08 – 72.28 – 81.67 – 0.17 

Table 3: Direct recommendation results on MovieLens and Insurance 

datasets. Utility metric: Hit rate (% Ò). Fairness metric: counterfactual-leakage gap ∆CL (% Ó). Bold = best.                                                                                 

> Dataset Attribute Hit@1 ÒHit@3 ÒHit@10 Ò∆CL Ó
> UP5 Ours UP5 Ours UP5 Ours UP5 Ours MovieLens Gender (G) 16.38 36.92 35.04 48.28 65.82 73.01 4.19 0.60
> Age (A) 21.22 37.14 39.22 49.35 67.30 69.72 2.91 0.00
> Occupation (O) 21.00 34.60 38.50 46.57 69.00 64.34 0.00 0.60 G + A + O 20.18 43.20 38.79 49.42 66.78 66.06 3.21 0.78
> Insurance Marital (M) 81.03 54.23 90.58 93.39 97.66 99.16 2.19 1.30
> Age (A) 82.53 55.59 92.68 93.45 98.89 99.04 0.00 0.50 Occupation (O) 82.53 56.08 82.68 94.04 98.89 99.26 3.28 1.90
> M + A + O 81.63 57.37 91.52 91.47 97.37 99.35 0.74 0.13

prompt engineering, and (iii) UP5 [21], a fairness-oriented extension of P5. Re-ported scores for P5 and UP5 follow the configurations in their original study, ensuring that all methods are evaluated under the same candidate-sampling pro-tocol and metric definitions. 

5.2 Performance Evaluation 

Table 1 shows the backbone results for LLaRA [24]. On MovieLens-1M [17], the model attains strong hit rates—especially in the sequential setting—yet still leaves noticeable counterfactual leakage, with gender and age gaps around 20 %. On the much smaller Insurance dataset, the same backbone achieves near-perfect hit rates while reducing every leakage metric to single-digit levels. These mixed outcomes set a clear target for our debiasing method: keep the utility values while pushing every ∆CL closer to zero. Table 2 shows that our projector–adapter stack meets that target in the 

sequential recommendation task on MovieLens. Debiasing a single attribute al-ready lifts Hit@ 1, 3, 10 by 8–13 absolute points over the strongest baseline while 13 forcing every single-attribute leakage gap below 2 % . When all three attributes are debiased jointly, utility rises still further—Hit@1 “ 56 .1 % —and the aver-age gap collapses to a mere 0.17 % . These figures confirm that (i) the kernelised RFF–INLP projector suppresses almost all linearly recoverable bias and that (ii) the two-level gated adapter restores task-useful variation without re-opening protected channels. The results of direct recommendation task in Table 3 exhibits similar find-ings. Specifically, on the MovieLens dataset, our model shows more than doubles Hit@1 for the gender attribute ( 16 .4% Ñ 36 .9% ) and yields double-digit gains for 

age and occupation attributes, while driving the mean leakage gap from 3.21 % 

to 0.78 % . The residual UP5 advantage on Hit@10–occupation is under two points and coincides with four-times larger leakage, indicating an unfavorable utility–fairness trade-off for the baseline. On the other hand, as the Insurance dataset lacks reliable timestamps, user interactions cannot form a chronological sequence. Transformers such as SASRec, therefore, degenerate to bag-of-items encoders, and any “sequential” score must be synthesized by randomly shuffling each basket. Because such pseudo-histories encode no behavioral signal, we restrict evaluation to the direct recommendation, as reflected in Table 3. Under this realistic setting, our method halves the average leakage gap (from 0.74 % to 0.13 % ) and wins or ties eight out of twelve utility cells; the sole UP5 lead—Hit@1 on marital status—coincides with more than twice the leakage, again favoring our accuracy–fairness exchange. Taken together, the results demonstrate that a single closed-form projector plus a light two-level gated adapter can achieve near-ideal counterfactual fairness (∆CL « 0) while matching or surpassing state-of-the-art utility, and it does so without a single step of full-model fine-tuning. 

# 6 Conclusion 

We present a fairness module for large language model-based recommender sys-tems that operates in two stages. The first stage removes the signal of a selected sensitive attribute using an orthogonal projection matrix derived in closed form. The second stage extends this to multiple attributes by introducing a two-gated adapter, which allows for controlled suppression of each attribute while retaining information relevant for accurate recommendations. The module is lightweight, adding only a small number of parameters without introducing new loss func-tions or causing noticeable inference delays. This makes it a practical approach for improving fairness in LLM-based recommendation systems while maintain-ing efficiency and accuracy. Empirical evaluations on two real-world datasets demonstrate that the proposed method performs competitively in both recom-mendation quality and fairness, effectively reducing the ability to infer users’ sensitive attributes. One limitation of our method is its strong dependence on users’ interaction histories. In future work, we plan to explore novel debiasing approaches that do not rely on user history. 14 Acknowledgments 

This work was supported in part by the U.S. National Science Foundation (NSF) under grants IIS-2047843, IIS-2437621, CNS-2029038, and CNS-2135988. Any opinions, findings, conclusions, or recommendations expressed in this material are those of the authors and do not necessarily reflect the views of NSF. 

# References 

1. T. Bolukbasi, K. Chang, J. Y. Zou, V. Saligrama, and A. T. Kalai. Man is to computer programmer as woman is to homemaker? debiasing word embeddings. In Proc. NeurIPS ’16 , pages 4349–4357, 2016. 2. R. Burke, N. Mattei, V. Grozin, A. Voida, and N. Sonboli. Multi-agent social choice for dynamic fairness-aware recommendation. In Proc. UMAP ’22 , pages 234–244, 2022. 3. S. Caton and C. Haas. Fairness in machine learning: A survey. ACM Comput. Surv. , 56(7):166:1–166:38, 2024. 4. B. Cho, Y. Mukhin, K. Gan, and I. Malenica. Kernel debiased plug-in estimation: Simultaneous, automated debiasing without influence functions for many target parameters. In Proc. ICML ’24 , 2024. 5. Y. Deldjoo and T. Di Noia. Cfairllm: Consumer fairness evaluation in large-language model recommender system. In Proc. ACM Trans. Intell. Syst. Technol. (ACM TIST) ’25 , 2025. 6. A. Dubey, A. Jauhri, A. Pandey, , and et al. The llama 3 herd of models. CoRR ,abs/2407.21783, 2024. 7. D. Eftekhari and V. Papyan. On the importance of gaussianizing representations. 

arXiv preprint arXiv:2505.00685 , 2025. 8. M. D. Ekstrand, R. Burke, and F. Diaz. Fairness and discrimination in recommen-dation and retrieval. In Proc. ACM RecSys ’19 , pages 576–577, 2019. 9. I. O. Gallegos, R. A. Rossi, J. Barrow, M. M. Tanjim, S. Kim, F. Dernoncourt, T. Yu, R. Zhang, and N. K. Ahmed. Bias and fairness in large language models: A survey. Comput. Linguistics , 50(3):1097–1179, 2024. 10. I. O. Gallegos, R. A. Rossi, J. Barrow, M. M. Tanjim, S. Kim, F. Dernoncourt, T. Yu, R. Zhang, and N. K. Ahmed. Bias and fairness in large language models: A survey. Comput. Linguistics , 50(3):1097–1179, 2024. 11. Y. Gao, T. Sheng, Y. Xiang, Y. Xiong, H. Wang, and J. Zhang. Chat-rec: To-wards interactive and explainable llms-augmented recommender system. CoRR ,abs/2303.14524, 2023. 12. A. Garimella, A. Amarnath, K. Kumar, A. P. Yalla, A. Natarajan, N. Chhaya, and B. V. Srinivasan. He is very intelligent, she is very beautiful? on mitigating social biases in language modelling and generation. In Proc. Findings of ACL ’21 , pages 4534–4545, 2021. 13. S. Geng, S. Liu, Z. Fu, Y. Ge, and Y. Zhang. Recommendation as language pro-cessing (RLP): A unified pretrain, personalized prompt & predict paradigm (P5). In Proc. RecSys ’22 , pages 299–315, 2022. 14. G. H. Golub and C. F. Van Loan. Matrix computations . JHU press, 2013. 15. H. Gonen and Y. Goldberg. Lipstick on a pig: Debiasing methods cover up sys-tematic gender biases in word embeddings but do not remove them. In Proc. Wi ACL@NLP Workshop ’19 , pages 60–63, 2019. 

15 16. S. Grünewälder and A. Khaleghi. Oblivious data for fairness with kernels. J. Mach. Learn. Res. , 22:208:1–208:36, 2021. 17. F. M. Harper and J. A. Konstan. The movielens datasets: History and context. 

ACM Trans. Interact. Intell. Syst. , 5(4):19:1–19:19, 2016. 18. C. Hong, M. Liu, D. M. Wojdyla, J. Hickey, M. Pencina, and R. Henao. Trans-balance: Reducing demographic disparity for prediction models in the presence of class imbalance. J. Biomed. Inform. , 149, 2024. 19. R. A. Horn and C. R. Johnson. Matrix analysis . Cambridge university press, 2012. 20. E. J. Hu, Y. Shen, P. Wallis, Z. Allen-Zhu, Y. Li, S. Wang, L. Wang, and W. Chen. Lora: Low-rank adaptation of large language models. In Proc. ICLR ’22 , 2022. 21. W. Hua, Y. Ge, S. Xu, J. Ji, Z. Li, and Y. Zhang. UP5: unbiased foundation model for fairness-aware recommendation. In Proc. EACL ’24 , pages 1899–1912, 2024. 22. M. Jiang, K. Bao, J. Zhang, W. Wang, Z. Yang, F. Feng, and X. He. Item-side fairness of large language model-based recommendation system. In Proc. The Web Conf. ’24 (WWW ’24) , pages 4717–4726, 2024. 23. M. J. Kusner, J. R. Loftus, C. Russell, and R. Silva. Counterfactual fairness. In 

Proc. NeurIPS ’17 , pages 4066–4076, 2017. 24. J. Liao, S. Li, Z. Yang, J. Wu, Y. Yuan, X. Wang, and X. He. Llara: Large language-recommendation assistant. In Proc. SIGIR ’24 , pages 1785–1795, 2024. 25. T. Mikolov, K. Chen, G. Corrado, and J. Dean. Efficient estimation of word representations in vector space. In Proc. ICLR Workshop ’13 , 2013. 26. H. Ngo and D. Q. Nguyen. Recgpt: Generative pre-training for text-based recom-mendation. In Proc. ACL ’24 , pages 302–313, 2024. 27. D. Pedreshi, S. Ruggieri, and F. Turini. Discrimination-aware data mining. In 

Proc. KDD ’08 , page 560–568, 2008. 28. A. Rahimi and B. Recht. Random features for large-scale kernel machines. In Proc. NeurIPS ’07 , pages 1177–1184, 2007. 29. S. Rarrick, R. Naik, S. Poudel, and V. Chowdhary. GATE X-E : A challenge set for gender-fair translations from weakly-gendered languages. In Proc. Findings of ACL ’24 , pages 8526–8546, 2024. 30. S. Ravfogel, Y. Elazar, H. Gonen, M. Twiton, and Y. Goldberg. Null it out: Guarding protected attributes by iterative nullspace projection. In Proc. ACL ’20 ,pages 7237–7256, 2020. 31. S. Ravfogel, M. Twiton, Y. Goldberg, and R. Cotterell. Linear adversarial concept erasure. In Proc. ICML ’22 , volume 162, pages 18400–18421, 2022. 32. L. Wu, Z. Zheng, Z. Qiu, H. Wang, H. Gu, T. Shen, C. Qin, C. Zhu, H. Zhu, Q. Liu, H. Xiong, and E. Chen. A survey on large language models for recommendation, 2024. 33. Y. Wu, R. Xie, Y. Zhu, F. Zhuang, X. Ao, X. Zhang, L. Lin, and Q. He. Selective fairness in recommendation via prompts. In Proc. SIGIR ’22 , pages 2657–2662, 2022. 34. J. Zhang, K. Bao, Y. Zhang, W. Wang, F. Feng, and X. He. Is chatgpt fair for recommendation? evaluating fairness in large language model recommendation. In 

Proc. ACM RecSys ’23 , pages 993–999, 2023. 35. Y. Zhang, F. Feng, J. Zhang, K. Bao, Q. Wang, and X. He. Collm: Integrating collaborative embeddings into large language models for recommendation. IEEE Trans. Knowl. Data Eng. , 37(5):2329–2340, 2025. 36. Z. Zhao, W. Fan, J. Li, Y. Liu, X. Mei, Y. Wang, Z. Wen, F. Wang, X. Zhao, J. Tang, and Q. Li. Recommender systems in the era of large language models (llms). IEEE Trans. Knowl. Data Eng. , 36(11):6889–6907, 2024. 

16 A Derivation of the INLP Projection Matrix 

Let ˆW P RT ˆp d`Dq have full row rank ( T ď d`D and rank ˆW “ T ). We seek the orthogonal projector ˆP onto the null-space N “ t z P Rd`D : ˆWz “ 0u.For any x P Rd`D this projector satisfies ˆPx “ argmin yPN }x ´ y}22. Introducing Lagrange multipliers λ P RT gives the unconstrained problem 

min 

> y,λ

12 }x ´ y}22 ` λJ ˆWy .

Stationarity with respect to y yields y ´ x ` ˆWJλ “ 0, so the optimum must satisfy y “ x ´ ˆWJλ. Imposing the constraint ˆWy “ 0 then gives the normal equation ˆW ˆWJλ “ ˆWx .

Because ˆW ˆWJ ą 0, it is invertible [19], and λ “ p ˆW ˆWJq´1 ˆWx . Substituting back into the expression for y we obtain 

ˆPx “ x ´ ˆWJp ˆW ˆWJq´1 ˆWx , @ x P Rd`D ,

hence the matrix form 

ˆP “ Id`D ´ ˆWJp ˆW ˆWJq´1 ˆW . (A.1) Since p ˆW ˆWJq´1 is symmetric, so is the product ˆWJp ˆW ˆWJq´1 ˆW, whence 

ˆPJ “ ˆP. A symmetric idempotent matrix is an orthogonal projector onto its range [19]. Write M “ p ˆW ˆWJq´1. Then 

ˆP2 “ `I ´ ˆWJM ˆW˘2 “ I ´ 2 ˆWJM ˆW ` ˆWJM ˆW ˆWJM ˆW.

Because M ˆW ˆWJ “ IT , the second and third terms cancel, leaving ˆP2 “ I ´

ˆWJM ˆW “ ˆP.Multiplying (A.1) on the left by ˆW gives ˆW ˆP “ ˆW ´ ˆW ˆWJp ˆW ˆWJq´1 ˆW “

0, so range p ˆPq Ď N . Conversely, for any z P N , ˆPz “ z, hence N Ď range p ˆPq.Therefore ˆP is the orthogonal projector onto N , and 

ˆP2 “ ˆPJ “ ˆP with range p ˆPq “ N , null p ˆPq “ row p ˆWq,

completing the derivation [14, 30]. 

# B Block-Decomposition Proof of the Backbone-Space Projector 

Recall the lifted orthogonal projector ˆP P Rpd`Dqˆp d`Dq obtained in Appendix A. Partition it as 

ˆP “

„ ˆP11 ˆP12 

ˆP21 ˆP22 

ȷ

, ˆP11 P Rdˆd,

where the first block of d coordinates is the backbone space and the remaining 

D coordinates correspond to the random Fourier features (RFFs). 17 Symmetry and idempotence of the upper-left block. Because ˆP is an orthogonal 

projector, ˆPJ “ ˆP and ˆP2 “ ˆP. Write ˆP “

„A B C D 

ȷ

for brevity ( A “ ˆP11 , etc.). Then 

ˆPJ “

„AJ CJ

BJ DJ

ȷ

“ ˆP ùñ AJ “ A, C “ BJ. (B.1) Now compute the p1, 1q block of ˆP2:

p ˆP2q11 “ AA ` BC “ AA ` BB J.

Idempotence requires p ˆP2q11 “ A, hence 

AA ` BB J “ A ùñ A2 “ A ´ BB J. (B.2) But from (B.1) we know A is symmetric; substituting BB J from (B.2) into p ˆP2q11 

yields A2 “ A, so A is both symmetric and idempotent. Therefore A “ ˆP11 is itself an orthogonal projector onto some d-dimensional subspace [14]. 

Action on backbone vectors. Take any backbone vector h P Rd and lift it to 

˜h “

„h0

ȷ

P Rd`D . A single matrix multiplication shows 

ˆP˜h “

„Ah

Ch

ȷ

“

„ ˆP11 h

ˆP21 h

ȷ

.

Projecting back to the original coordinates with the selector S “ r Id 0dˆD s

gives S ˆP˜h “ ˆP11 h. Thus within the backbone space the lifted projector acts exactly as the d ˆ d matrix 

P :“ ˆP11 .

Because P inherits symmetry and idempotence from ˆP, it is the orthogonal projector onto null-space N X Rd, the intersection of the INLP null-space with the backbone coordinate subspace. All probe directions t ˆwtuTt“1 that intersect 

Rd have therefore been removed, while the RFF coordinates (needed only for computing ˆP) play no role at inference time. 18
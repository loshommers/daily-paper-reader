---
title: Lightweight Fairness for LLM-Based Recommendations via Kernelized Projection and Gated Adapters
title_zh: 基于核化投影与门控适配器的轻量级大语言模型推荐公平性方法
authors: "Nan Cui, Wendy Hui Wang, Yue Ning"
date: 2026-03-24
pdf: "https://arxiv.org/pdf/2603.23780v1"
tags: ["query:mu-ce"]
score: 9.0
evidence: 从 LLM 表示中移除敏感属性
tldr: 针对大语言模型推荐系统中的社会偏见问题，本文提出一种轻量级且可扩展的公平性缓解方法。该方法结合了核化迭代零空间投影（INLP）与门控专家混合（MoE）适配器，在无需额外训练参数的情况下，通过闭式投影消除表示中的敏感属性，并利用MoE适配器在保持推荐准确性的同时防止偏见重新引入，实现了公平性与效用的平衡。
selection_source: fresh_fetch
motivation: 现有的LLM推荐系统公平性方案通常面临微调参数过多或优化不稳定的挑战，难以高效处理多种敏感属性。
method: 结合核化迭代零空间投影（INLP）消除敏感属性，并引入两层门控MoE适配器在不引入偏见的前提下恢复任务效用。
result: 在两个公开数据集上的实验证明，该方法在显著降低多个受保护变量泄露的同时，保持了竞争性的推荐准确度。
conclusion: 本研究提供了一种高效、轻量且可扩展的方案，为构建公平且高性能的大语言模型推荐系统提供了新思路。
---

## 摘要
大语言模型（LLM）为推荐系统引入了新的能力，实现了动态、上下文感知和对话式推荐。然而，基于 LLM 的推荐系统继承并可能放大预训练数据中嵌入的社会偏见，尤其是在存在人口统计学线索时。现有的公平性解决方案要么需要额外的参数微调，要么面临优化不稳定的问题。我们提出了一种轻量级且可扩展的偏见缓解方法，该方法结合了核化迭代零空间投影（INLP）与门控混合专家（MoE）适配器。我们的方法估计了一个闭式投影，在不增加额外可训练参数的情况下，从 LLM 表示中移除单个或多个敏感属性。为了保持任务效用，我们引入了一个两级 MoE 适配器，在不重新引入偏见的情况下选择性地恢复有用信号。在两个公开数据集上的实验表明，我们的方法在保持具有竞争力的推荐准确性的同时，减少了多个受保护变量的属性泄露。

## Abstract
Large Language Models (LLMs) have introduced new capabilities to recommender systems, enabling dynamic, context-aware, and conversational recommendations. However, LLM-based recommender systems inherit and may amplify social biases embedded in their pre-training data, especially when demographic cues are present. Existing fairness solutions either require extra parameters fine-tuning, or suffer from optimization instability. We propose a lightweight and scalable bias mitigation method that combines a kernelized Iterative Null-space Projection (INLP) with a gated Mixture-of-Experts (MoE) adapter. Our approach estimates a closed-form projection that removes single or multiple sensitive attributes from LLM representations with no additional trainable parameters. To preserve task utility, we introduce a two-level MoE adapter that selectively restores useful signals without reintroducing bias. Experiments on two public datasets show that our method reduces attribute leakage across multiple protected variables while maintaining competitive recommendation accuracy.

---

## 论文详细总结（自动生成）

### 论文总结：基于核化投影与门控适配器的轻量级大语言模型推荐公平性方法

#### 1. 核心问题与整体含义（研究动机和背景）
随着大语言模型（LLM）在推荐系统（RecLLMs）中的广泛应用，其强大的语义理解能力提升了推荐的个性化程度。然而，LLM 往往会继承并放大预训练数据中的社会偏见（如性别、年龄、职业等）。现有的公平性缓解方案通常存在两个主要痛点：
*   **优化成本高且不稳定**：如对抗性训练（Adversarial Training）需要引入大量额外参数，且对超参数极其敏感，容易导致训练崩溃。
*   **多属性处理困难**：简单的投影方法在处理多个敏感属性时，容易过度消除有用的任务信号，导致推荐准确率大幅下降。
本文旨在提出一种**轻量级、无需大规模重训练**的方案，在消除多维敏感属性信息的同时，保持推荐系统的效用。

#### 2. 论文提出的方法论
该方法由两个核心阶段组成，被称为“擦除-修复”流水线：

*   **阶段一：单属性核化去偏（Kernelized INLP）**
    *   **核心思想**：利用迭代零空间投影（INLP）的闭式解特性，将表示向量投影到与敏感属性正交的子空间中。
    *   **关键技术**：
        1.  **各向同性高斯扰动**：在表示中加入微小噪声，防止偏见信号隐藏在低方差维度中。
        2.  **随机傅里叶特征（RFF）提升**：通过 RFF 将原始表示映射到高维核空间，使 INLP 能够捕捉并消除非线性偏见信号，而不仅仅是线性相关性。
        3.  **闭式投影**：通过最小二乘法直接计算投影矩阵 $P$，无需梯度下降优化。

*   **阶段二：多属性门控 MoE 适配器（Two-level Gated MoE）**
    *   **核心思想**：针对多属性场景，引入混合专家（MoE）结构动态平衡公平性与准确性。
    *   **关键技术**：
        1.  **一级门控（属性权重）**：根据当前用户上下文，动态计算不同属性投影矩阵的软权重，实现自适应去偏。
        2.  **二级门控（效用修复）**：在去偏后的表示上附加低秩 LoRA 专家，通过门控机制选择性地恢复对推荐任务有用的信号，同时通过正则化确保不重新引入偏见。

#### 3. 实验设计
*   **数据集**：
    *   **MovieLens-1M**：包含性别、年龄、职业三个敏感属性。
    *   **Insurance（保险数据集）**：包含婚姻状况、年龄、职业三个敏感属性。
*   **实验场景**：涵盖了**直接推荐**（根据用户 ID 推荐）和**序列推荐**（根据历史行为推荐）两种主流 LLM 推荐范式。
*   **Benchmark 与对比方法**：
    *   **基础模型**：LLaRA（基于 Llama-3 的先进推荐模型）。
    *   **对比基准**：P5（统一推荐框架）、UP5（目前主流的基于对抗训练的公平性模型）。
*   **评价指标**：
    *   **效用指标**：Hit@1, Hit@3, Hit@10。
    *   **公平性指标**：反事实泄露间隙（$\Delta_{CL}$），通过训练额外的探测器（Probe）来衡量表示中残留的敏感属性信息量。

#### 4. 资源与算力
*   **模型规模**：实验基于 **Llama-3.2-1B-Instruct** 模型。
*   **算力说明**：论文**未明确说明**具体的 GPU 型号、数量及总训练时长。但作者强调了该方法的“轻量级”特性，即投影矩阵是闭式解生成的，且 MoE 适配器仅引入了极少量的可训练参数（$O(Kdr)$ 级别，$K$ 为属性数，$r$ 为 LoRA 秩）。

#### 5. 实验数量与充分性
*   **实验规模**：作者在两个不同领域的数据集上进行了多组实验，涵盖了单属性去偏和多属性联合去偏的对比。
*   **充分性**：实验对比了 Hit Rate 和 $\Delta_{CL}$ 两个维度，证明了方法在“效用-公平性”权衡上的优越性。
*   **客观性**：通过与强基准 UP5 的直接对比，展示了在相同候选集采样协议下的性能提升。实验设计遵循了反事实公平性的标准协议，具有较高的客观性。

#### 6. 论文的主要结论与发现
*   **高效去偏**：该方法能将敏感属性的泄露量（$\Delta_{CL}$）降至接近 0（例如在 MovieLens 上从 21.9% 降至 0.17%）。
*   **效用保持甚至提升**：与 UP5 相比，该方法在保持公平性的同时，Hit@1 等准确率指标显著优于基准，证明了 MoE 适配器修复信号的有效性。
*   **轻量化优势**：无需复杂的对抗优化，通过代数投影即可实现快速部署。

#### 7. 优点
*   **数学严谨性**：结合了核方法与线性代数投影，解决了传统 INLP 无法处理非线性偏见的问题。
*   **架构创新**：两级门控机制巧妙地解决了多属性去偏时的“过度抹除”问题，实现了精细化的信号恢复。
*   **实用性强**：作为插件式模块，可以轻松集成到现有的基于 LoRA 的 LLM 推荐系统中，且不增加推理延迟。

#### 8. 不足与局限
*   **历史依赖性**：该方法高度依赖用户的交互历史，对于冷启动用户或缺乏历史数据的场景，去偏效果可能受限。
*   **模型规模限制**：实验主要在 1B 规模的模型上进行，尚未在大规模模型（如 70B+）上验证其扩展性。
*   **算力细节缺失**：未提供具体的训练/推理耗时数据，难以量化其在实际生产环境中的资源节省程度。
*   **属性预定义**：需要预先知道敏感属性的标签才能构建投影矩阵，无法处理未知的潜在偏见。

（完）

---
title: "SEM: Sparse Embedding Modulation for Post-Hoc Debiasing of Vision-Language Models"
title_zh: SEM：用于视觉-语言模型事后去偏的稀疏嵌入调制
authors: "Quentin Guimard, Federico Bartsch, Simone Caldarella, Rahaf Aljundi, Elisa Ricci, Massimiliano Mancini"
date: 2026-03-19
pdf: "https://arxiv.org/pdf/2603.19028v1"
tags: ["query:mu-ce"]
score: 8.0
evidence: 潜空间中的事后去偏和概念移除
tldr: 针对视觉语言模型（如CLIP）中存在的社会和虚假偏见，本文提出了一种名为SEM的后置零样本去偏框架。该方法利用稀疏自编码器（SAE）将密集的CLIP嵌入分解为解耦的稀疏特征，从而在稀疏潜空间中精准识别并调节偏见相关的神经元，同时保留任务相关的语义信息。实验证明，SEM在多个基准测试中显著提升了检索和分类的公平性，为多模态模型的公平性研究提供了新视角。
selection_source: fresh_fetch
motivation: 现有的后置去偏方法在密集的嵌入空间中操作，导致偏见与任务信息高度纠缠，难以在不损害语义的情况下有效去偏。
method: 提出稀疏嵌入调制（SEM），通过稀疏自编码器将CLIP文本嵌入分解为解耦特征，并对偏见相关的神经元进行非线性干预。
result: 在四个基准数据集和两种CLIP骨干网络上，SEM在检索和零样本分类任务中均取得了显著的公平性提升。
conclusion: 研究证明稀疏潜表征能有效解耦偏见与语义，为视觉语言模型的后置去偏提供了一个比密集空间更精准的基础。
---

## 摘要
连接视觉和语言的模型（如 CLIP）是多模态人工智能的关键组件，但其大规模、未经处理的训练数据引入了严重的社会偏见和伪偏差。现有的事后去偏方法通常直接在稠密的 CLIP 嵌入空间中操作，其中偏差和任务相关信息高度纠缠。这种纠缠限制了它们在不降低语义保真度的情况下消除偏差的能力。在这项工作中，我们提出了稀疏嵌入调制（SEM），这是一个在稀疏自编码器（SAE）潜空间中运行的事后、零样本去偏框架。通过将 CLIP 文本嵌入分解为解耦特征，SEM 能够识别并调制与偏差相关的神经元，同时保留与查询相关的神经元。这实现了更精确的非线性干预。在四个基准数据集和两个 CLIP 骨干网络上，SEM 在检索和零样本分类中实现了显著的公平性提升。我们的结果表明，稀疏潜表示为视觉-语言模型的事后去偏提供了有效的基础。

## Abstract
Models that bridge vision and language, such as CLIP, are key components of multimodal AI, yet their large-scale, uncurated training data introduce severe social and spurious biases. Existing post-hoc debiasing methods often operate directly in the dense CLIP embedding space, where bias and task-relevant information are highly entangled. This entanglement limits their ability to remove bias without degrading semantic fidelity. In this work, we propose Sparse Embedding Modulation (SEM), a post-hoc, zero-shot debiasing framework that operates in a Sparse Autoencoder (SAE) latent space. By decomposing CLIP text embeddings into disentangled features, SEM identifies and modulates bias-relevant neurons while preserving query-relevant ones. This enables more precise, non-linear interventions. Across four benchmark datasets and two CLIP backbones, SEM achieves substantial fairness gains in retrieval and zero-shot classification. Our results demonstrate that sparse latent representations provide an effective foundation for post-hoc debiasing of vision-language models.

---

## 论文详细总结（自动生成）

以下是对论文《SEM: Sparse Embedding Modulation for Post-Hoc Debiasing of Vision-Language Models》的结构化深度总结：

### 1. 核心问题与整体含义（研究动机和背景）
论文关注的是视觉-语言模型（如 CLIP）中普遍存在的**社会偏见**（如性别、种族歧视）和**虚假相关性**（如背景偏差）。
*   **核心矛盾**：现有的后置（Post-hoc）去偏方法大多直接在 CLIP 的**稠密嵌入空间**（Dense Embedding Space）中操作。由于该空间中语义信息与偏差信息高度“纠缠”，简单的线性投影或过滤往往会在去除偏差的同时损害模型的语义表达能力。
*   **研究目标**：提出一种能够精准分离偏见与语义、在不损害任务性能的前提下实现零样本（Zero-shot）去偏的新框架。

### 2. 方法论：核心思想与关键技术
论文提出了 **SEM（Sparse Embedding Modulation，稀疏嵌入调制）**，其核心思想是利用**稀疏自编码器（SAE）**将稠密嵌入分解到高维稀疏空间中，在该空间内偏见和语义特征更易解耦。

*   **核心流程**：
    1.  **空间转换**：使用预训练的 SAE 将 CLIP 文本嵌入 $z$ 映射到稀疏潜空间 $h$。
    2.  **神经元评分（Scoring）**：
        *   **内容相关性评分 ($S_{concept}$)**：通过对比查询词与一组多样化中性提示词（Diverse Prompts）的激活程度，识别哪些神经元代表核心语义。
        *   **偏见敏感性评分 ($S_{bias}$)**：利用特定的偏见提示词（如“一个男人的照片”），识别对特定偏见属性敏感的神经元。
    3.  **激活调制（Steering）**：计算调制系数 $M$，放大内容神经元，衰减偏见神经元。
    4.  **重构**：将调制后的稀疏向量 $h_{debias}$ 通过 SAE 解码器还原为去偏后的稠密嵌入 $z_{debias}$。
*   **三种变体**：
    *   **SEM-i (偏见不可知)**：仅利用 LLM 生成的改写词来增强内容神经元的识别，衰减其他可能包含噪声或偏见的特征。
    *   **SEM-b (偏见已知)**：利用偏见提示词列表进行结构化的偏见神经元识别。
    *   **SEM-bi (全功能)**：结合上述两者，实现最精准的干预。

### 3. 实验设计
*   **数据集与场景**：
    *   **跨模态检索**：FairFace、UTKFace（种族/性别偏见）、CelebA（性别/发色偏见）。
    *   **零样本分类**：CelebA（性别偏见）、Waterbirds（背景虚假相关性）。
*   **骨干网络**：ViT-B/16、ViT-L/14@336px，以及补充材料中的 ResNet-50/101。
*   **对比方法（Baselines）**：包括 RoboShot（偏见不可知）、Orth-Proj（正交投影）、PRISM、BendVLM（需要参考图像的后置方法）等。
*   **评估指标**：KL 散度（公平性）、MaxSkew（最大偏斜）、最差组准确率（Worst-Group Accuracy, WG）、Gap（准确率差距）。

### 4. 资源与算力
*   **算力设备**：使用了单张 **NVIDIA A100 GPU (64GB)**。
*   **训练时长**：
    *   SAE 训练：在 CC12M 数据集上训练一个 SAE 大约需要 **1.5 小时**。
    *   推理阶段：由于是后置方法且涉及稀疏运算，推理过程非常高效。
*   **数据规模**：SAE 在 850 万条清洗后的 CC12M 标题上进行训练。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 4 种主流骨干网络、4 个核心基准数据集、2 种主要下游任务。
*   **充分性**：
    *   采用了 **10 折交叉验证**，确保了结果的统计显著性。
    *   进行了详尽的**消融实验**，验证了内容增强项、偏见评分项以及 SAE 空间必要性的作用。
    *   提供了 **PCA 可视化分析**，直观展示了 SEM 在解耦纠缠概念上的优越性。
*   **公平性**：对比实验在相同的提示词信息和测试环境下进行，分类清晰（偏见已知 vs. 偏见不可知），对比客观。

### 6. 主要结论与发现
*   **稀疏空间更易去偏**：实验量化证明，SAE 潜空间的特征解耦度比原始 CLIP 空间高出数倍（性别提升 2 倍以上，种族提升 5 倍以上）。
*   **显著提升最差组性能**：在 Waterbirds 这种极具挑战性的虚假相关性任务中，SEM 将最差组准确率提升了约 28 个百分点，远超之前的零样本方法。
*   **模块化与互补性**：SEM 可以作为其他去偏方法（如 BendVLM）的前置步骤，进一步提升其公平性表现，证明了其作为通用工具的潜力。

### 7. 优点（亮点）
*   **非线性干预**：突破了传统方法只能进行线性投影的限制，通过神经元级别的调制实现更精细的控制。
*   **零样本且高效**：不需要针对特定任务微调 CLIP 模型，仅需一次性预训练的 SAE 即可应用于多种下游任务。
*   **解耦能力强**：利用 SAE 成功解决了稠密向量中语义与偏见“剪不断理还乱”的痛点。

### 8. 不足与局限
*   **对 SAE 的依赖**：去偏效果高度依赖于 SAE 的重构质量和稀疏度，如果 SAE 训练不佳，可能会引入重构误差。
*   **提示词敏感性**：虽然引入了 LLM 增强，但方法仍依赖于偏见提示词的定义，对于未定义的隐性偏见可能覆盖不足。
*   **计算开销**：虽然推理快，但相比直接投影，增加了一层 SAE 的编码和解码计算。

（完）

---
title: "Attack by Unlearning: Unlearning-Induced Adversarial Attacks on Graph Neural Networks"
title_zh: 以遗忘进行攻击：图神经网络中由遗忘诱导的对抗性攻击
authors: "Jiahao Zhang, Yilong Wang, Suhang Wang"
date: 2026-03-19
pdf: "https://arxiv.org/pdf/2603.18570v1"
tags: ["query:mu-ce"]
score: 9.0
evidence: 近似图遗忘及其对抗性副作用
tldr: 本研究探讨了图神经网络（GNN）中近似遗忘算法带来的安全风险。为了遵守隐私法规，模型需移除特定数据的影响，但这一过程可能导致性能下降。作者提出了“遗忘腐蚀攻击”，攻击者在训练阶段注入特定节点并在之后请求删除，使模型在遗忘操作后准确率大幅崩溃。该攻击利用双层优化和代理模型实现，具有极高的隐蔽性，为受监管环境下的图学习安全提出了严峻挑战。
selection_source: fresh_fetch
motivation: 旨在揭示近似图遗忘过程中细微的性能退化如何被放大为一种隐蔽且难以避免的对抗攻击。
method: 将攻击建模为双层优化问题，通过梯度更新模拟遗忘过程，并利用代理模型生成伪标签以解决黑盒遗忘和标签稀缺问题。
result: 在多个基准数据集和遗忘算法上的实验表明，极小规模的遗忘请求就能诱发显著的模型准确率下降。
conclusion: 研究证明了图遗忘机制可能成为新的攻击面，提醒开发者在满足隐私合规要求的同时必须关注模型的鲁棒性。
---

## 摘要
图神经网络 (GNNs) 广泛应用于社交网络、推荐系统和金融平台等领域的图结构数据学习。为了遵守 GDPR、CCPA 和 PIPEDA 等隐私法规，近似图遗忘（approximate graph unlearning）旨在不进行完全重新训练的情况下，从已训练模型中移除特定数据点的影响，已成为可信图学习中日益重要的组成部分。然而，近似遗忘通常会导致细微的性能下降，这可能会产生负面且非预期的副作用。在这项工作中，我们展示了这种性能下降可以被放大为对抗性攻击。我们引入了“遗忘破坏攻击”（unlearning corruption attacks）的概念，即攻击者向训练图中注入精心选择的节点，随后请求删除这些节点。由于删除请求是法律强制要求的且无法被拒绝，这种攻击面既不可避免又具有隐蔽性：模型在训练期间表现正常，但仅在应用遗忘操作后准确率才会崩溃。在技术上，我们将此攻击建模为一个双层优化问题：为了克服黑盒遗忘和标签稀缺的挑战，我们通过基于梯度的更新来近似遗忘过程，并利用代理模型生成伪标签进行优化。在多个基准测试和遗忘算法上的广泛实验表明，少量且精心设计的遗忘请求即可诱导显著的准确率下降，这引发了对现实监管需求下 GNN 遗忘鲁棒性的紧迫关注。源码将在论文被接收后发布。

## Abstract
Graph neural networks (GNNs) are widely used for learning from graph-structured data in domains such as social networks, recommender systems, and financial platforms. To comply with privacy regulations like the GDPR, CCPA, and PIPEDA, approximate graph unlearning, which aims to remove the influence of specific data points from trained models without full retraining, has become an increasingly important component of trustworthy graph learning. However, approximate unlearning often incurs subtle performance degradation, which may incur negative and unintended side effects. In this work, we show that such degradations can be amplified into adversarial attacks. We introduce the notion of \textbf{unlearning corruption attacks}, where an adversary injects carefully chosen nodes into the training graph and later requests their deletion. Because deletion requests are legally mandated and cannot be denied, this attack surface is both unavoidable and stealthy: the model performs normally during training, but accuracy collapses only after unlearning is applied. Technically, we formulate this attack as a bi-level optimization problem: to overcome the challenges of black-box unlearning and label scarcity, we approximate the unlearning process via gradient-based updates and employ a surrogate model to generate pseudo-labels for the optimization. Extensive experiments across benchmarks and unlearning algorithms demonstrate that small, carefully designed unlearning requests can induce significant accuracy degradation, raising urgent concerns about the robustness of GNN unlearning under real-world regulatory demands. The source code will be released upon paper acceptance.

---

## 论文详细总结（自动生成）

### 论文总结：以遗忘进行攻击：图神经网络中由遗忘诱导的对抗性攻击

#### 1. 核心问题与研究动机
*   **研究背景**：随着 GDPR、CCPA 等隐私法规的实施，机器学习模型必须支持“被遗忘权”。**图遗忘（Graph Unlearning）**技术应运而生，旨在不重新训练整个模型的情况下，高效移除特定节点或边的影响。
*   **核心问题**：现有的近似遗忘算法（如基于影响函数或梯度上升的方法）往往会带来细微且不可避免的性能退化。
*   **研究动机**：本文探讨这种性能退化是否能被恶意放大并武器化。作者提出了一种全新的**遗忘腐蚀攻击（Unlearning Corruption Attack）**：攻击者先向图中注入精心设计的“毒节点”，在模型训练完成后，利用法律权利强制要求模型所有者删除这些节点。这种攻击在训练阶段表现正常（隐蔽性高），但在执行遗忘操作后会导致模型准确率大幅崩溃。

#### 2. 方法论
*   **核心思想**：将攻击建模为一个**双层优化问题**，旨在寻找最优的注入节点特征和连接结构，使得遗忘这些节点后的模型损失最大化。
*   **关键技术细节**：
    *   **三项目标优化**：(G1) 最大化遗忘后的破坏力；(G2) 保持遗忘前的模型效用（确保隐蔽）；(G3) 确保遗忘其他正常节点时不会触发性能崩溃。
    *   **近似黑盒遗忘**：由于遗忘算法通常是黑盒或复杂的，作者使用**单步梯度更新**来近似模拟遗忘过程，使其在数学上可微，从而允许通过反向传播优化注入内容。
    *   **伪标签生成**：针对攻击者缺乏未标记节点真实标签的问题，训练一个**代理模型（Surrogate Model）**来生成伪标签，以此作为优化目标。
    *   **离散结构优化**：使用 Sigmoid 参数化方法处理边的离散性，并在优化最后阶段通过欧几里得投影满足边预算约束。
    *   **可扩展性优化**：针对大规模图，采用**邻域限制策略**，仅优化目标受害者节点 $k$ 步邻域内的连接。

#### 3. 实验设计
*   **数据集**：使用了四个标准基准数据集：Cora、Citeseer、Pubmed（引文网络）和 Flickr（社交/图像网络）。
*   **受害者模型与遗忘算法**：
    *   模型：2层 GCN（同时也测试了 SGC 和 GAT 的迁移性）。
    *   遗忘算法：GIF（基于影响函数）、CEU（认证遗忘）、GA（梯度上升）。
*   **对比方法（Baselines）**：
    *   **启发式基准**：Random（随机注入）、Copy（复制训练节点）、TestCopy（复制测试节点）、TestLink（连接测试节点）等。
    *   **传统 GNN 攻击**：SBA、UGBA（后门攻击）、TDGIA（注入攻击）。
*   **评估指标**：原始准确率（Original Acc）、遗忘后准确率（Unlearned Acc）、准确率下降值（$\Delta$Acc）。

#### 4. 资源与算力
*   论文中**未明确说明**具体的 GPU 型号、数量或总训练时长。但从实验设置看，涉及了 200 步的梯度优化（Flickr 为 10 步），且使用了 PyTorch 框架进行自动微分计算。

#### 5. 实验数量与充分性
*   **实验规模**：涵盖了 4 个数据集、3 种主流遗忘算法、10 余种对比方法。
*   **充分性分析**：
    *   **消融实验**：验证了隐蔽性正则化项的必要性。
    *   **预算分析**：测试了不同节点注入比例（1%-9%）和边预算（1-20）下的攻击效果。
    *   **迁移性测试**：验证了在 GCN 上生成的攻击样本对 GAT 和 SGC 同样有效。
    *   **客观性**：实验设置遵循了图遗忘领域的标准协议（如 90/10 训练测试拆分），对比方法包含了最新的后门和注入攻击，实验设计较为公平、客观。

#### 6. 主要结论与发现
*   **攻击极具威胁**：在 Pubmed 数据集上，精心设计的遗忘请求可导致模型准确率下降约 **59%**，远超随机注入或简单复制策略。
*   **高隐蔽性**：在触发遗忘之前，受毒化模型的准确率与干净模型几乎一致，管理员难以察觉。
*   **强迁移性**：即使攻击者不知道受害者具体使用的遗忘算法或模型架构，基于梯度近似的攻击依然具有很强的通用性。
*   **合规性悖论**：法律强制的“被遗忘权”反而成为了攻击者的合法武器，模型所有者无法拒绝此类删除请求。

#### 7. 优点
*   **视角独特**：首次揭示了图遗忘机制本身可以作为对抗攻击的触发器，具有前瞻性。
*   **威胁模型现实**：利用法律合规性作为攻击入口，比传统的推理时间攻击更难防御。
*   **技术完备**：通过双层优化、梯度近似和邻域限制，解决了黑盒、离散和大规模计算的难题。

#### 8. 不足与局限
*   **算力开销**：虽然提出了邻域限制，但双层优化（尤其是涉及二阶导数或复杂梯度链时）在超大规模图上的计算成本依然可能较高。
*   **防御手段缺失**：论文主要侧重于揭示漏洞，虽然讨论了潜在防御，但未提出一种能完美平衡“合规性”与“鲁棒性”的成熟防御方案。
*   **白盒假设**：虽然声称是黑盒攻击，但仍假设攻击者能获取原始图结构（尽管作者解释可以通过 API 爬取，但在某些私有金融网络中可能较难实现）。

（完）

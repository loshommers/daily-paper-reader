---
title: "A Concept is More Than a Word: Diversified Unlearning in Text-to-Image Diffusion Models"
title_zh: 概念不仅仅是一个词：文本到图像扩散模型中的多样化遗忘
authors: "Duc Hao Pham, Van Duy Truong, Duy Khanh Dinh, Tien Cuong Nguyen, Dien Hy Ngo, Tuan Anh Bui"
date: 2026-03-19
pdf: "https://arxiv.org/pdf/2603.18767v1"
tags: ["query:mu-ce"]
score: 10.0
evidence: 用于选择性概念擦除的多样化遗忘技术
tldr: 针对文本生成图像模型中概念擦除依赖单一关键词导致的不稳定和过度遗忘问题，本文提出“多样化擦除”框架。该方法通过一组上下文多样的提示词而非单一词汇来表征概念的语义分布，从而实现更精确、鲁棒的擦除。实验证明，该方法作为现有流程的增强组件，能有效提升擦除效果，并增强对对抗性攻击的防御能力。
selection_source: fresh_fetch
motivation: 现有的基于关键词的擦除方法因无法覆盖概念的完整语义分布，导致擦除效果脆弱且容易产生过度遗忘。
method: 提出一种分布式的“多样化擦除”框架，利用一组具有上下文多样性的提示词来更全面、精准地表征目标概念。
result: 实验表明，该方法在增强擦除强度的同时，能更好地保留无关概念，并显著提升了对抗性恢复攻击的鲁棒性。
conclusion: 研究证明将概念视为多维分布而非单一词汇，是实现扩散模型中精确且稳健概念擦除的关键路径。
---

## 摘要
概念遗忘（Concept unlearning）已成为降低文本到图像扩散模型中生成有害内容风险的一个极具前景的方向，它通过从模型参数中选择性地擦除不理想的概念来实现。现有方法通常依赖关键词来识别需要遗忘的目标概念。然而，我们证明了这种基于关键词的表述具有内在局限性：视觉概念是多维的，可以用多种文本形式表达，并且在潜空间中经常与相关概念重叠，这使得仅依赖关键词的遗忘（由于对目标概念的指示不精确）变得脆弱且容易导致过度遗忘。这是因为单个关键词仅代表了概念的一个狭窄点估计，未能覆盖其完整的语义分布以及在潜空间中纠缠的变体。为了解决这一局限性，我们提出了多样化遗忘（Diversified Unlearning），这是一个分布框架，通过一组上下文多样化的提示词（prompts）而非单个关键词来表示概念。这种更丰富的表示方式实现了更精确、更鲁棒的遗忘。通过在多个基准测试和最先进基准模型上的广泛实验，我们证明了将多样化遗忘作为附加组件集成到现有的遗忘流水线中，能够一致地实现更强的擦除效果、更好的无关概念保留，以及更强的抗对抗恢复攻击的鲁棒性。

## Abstract
Concept unlearning has emerged as a promising direction for reducing the risks of harmful content generation in text-to-image diffusion models by selectively erasing undesirable concepts from a model's parameters. Existing approaches typically rely on keywords to identify the target concept to be unlearned. However, we show that this keyword-based formulation is inherently limited: a visual concept is multi-dimensional, can be expressed in diverse textual forms, and often overlap with related concepts in the latent space, making keyword-only unlearning, which imprecisely indicate the target concept is brittle and prone to over-forgetting. This occurs because a single keyword represents only a narrow point estimate of the concept, failing to cover its full semantic distribution and entangled variations in the latent space. To address this limitation, we propose Diversified Unlearning, a distributional framework that represents a concept through a set of contextually diverse prompts rather than a single keyword. This richer representation enables more precise and robust unlearning. Through extensive experiments across multiple benchmarks and state-of-the-art baselines, we demonstrate that integrating Diversified Unlearning as an add-on component into existing unlearning pipelines consistently achieves stronger erasure, better retention of unrelated concepts, and improved robustness against adversarial recovery attacks.

---

## 论文详细总结（自动生成）

### 论文总结：文本到图像扩散模型中的多样化遗忘（Diversified Unlearning）

#### 1. 核心问题与研究动机
*   **核心问题**：现有的扩散模型概念擦除（Concept Unlearning）方法大多基于“关键词指示器”（如仅使用“Barack Obama”这个词）。
*   **研究动机**：
    *   **脆弱性**：视觉概念是多维且分布式的，单一关键词无法覆盖其完整的语义空间，导致用户可以通过改写提示词（Prompt Rephrasing）轻易绕过防御。
    *   **过度遗忘（Over-forgetting）**：关键词在潜空间中往往与相关概念纠缠（如擦除“男人”会误伤“女人”），导致模型生成无关内容的能力受损。
    *   **语义粒度缺失**：单一词汇只是概念的一个点估计，缺乏上下文多样性。

#### 2. 方法论
论文提出了 **Diversified Unlearning** 框架，将概念遗忘从“点估计”扩展到“分布估计”。
*   **核心思想**：通过一组上下文多样的提示词集合来表征一个概念，而非依赖单一单词。
*   **关键技术细节**：
    *   **多样化提示（Diversified Prompting）**：针对**基于输出**的方法（如 ESD, AP），将目标概念 $c_{target}$ 嵌入到不同的自然上下文中（如“正在挥手的 $c_{target}$”），并将其映射到带上下文的锚点概念（如“正在挥手的男人”）。这扩大了擦除的覆盖面。
    *   **多样化嵌入混淆（Diversified Embedding Mixup）**：针对**基于注意力**的方法（如 UCE），直接在嵌入空间进行操作。为了防止模型崩溃，将目标概念的 Token 嵌入与上下文嵌入进行 Token 级别的插值（Mixup），在保留目标身份的同时引入多样性正则化。
    *   **上下文集合构建**：利用大语言模型（如 ChatGPT）生成符合逻辑的上下文（如动作、场景、光照等），并通过验证流水线确保这些提示词确实能生成目标概念。

#### 3. 实验设计
*   **实验场景**：涵盖了五类典型任务：
    1.  **名人擦除**：同时擦除 10 位名人身份。
    2.  **物体擦除**：擦除特定物体（如垃圾车、降落伞）。
    3.  **版权角色**：擦除“马里奥（Mario）”等受版权保护的角色。
    4.  **裸露内容（NSFW）**：擦除色情/裸露概念。
    5.  **艺术风格**：擦除特定艺术家的绘画风格。
*   **基准模型（Baselines）**：对比了 ESD, UCE, AP, AGE, ACE, AdvUnlearn, Receler, RECE 等 10 种主流遗忘方法。
*   **评估指标**：CLIP-i/t（视觉/文本一致性）、LPIPS（感知相似度）、**GPT-Score**（利用 Qwen2.5-VL-72B 进行身份识别评分）、NudeNet（裸露检测）、FID（图像质量）。

#### 4. 资源与算力
*   **算力说明**：论文**未明确列出**具体的 GPU 型号、数量或总训练时长。
*   **实现细节**：提到输出型方法通常需要数千次微调迭代，而注意力型方法（如 UCE 变体）由于是闭式解或轻量级微调，效率较高。

#### 5. 实验数量与充分性
*   **实验规模**：论文在 5 个不同领域进行了广泛测试，总计评估了 10 种方法及其多样化增强版本。
*   **消融实验**：针对上下文复杂度、Mixup 系数 $\lambda$、提示词数量等关键超参数做了详细的消融研究。
*   **充分性评价**：实验设计非常全面，不仅关注擦除成功率，还重点评估了**无关概念的保留能力**以及**抗对抗攻击的鲁棒性**（使用了 Ring-A-Bell 等三种攻击手段），实验结果具有较强的说服力和客观性。

#### 6. 主要结论与发现
*   **性能提升**：将“多样化遗忘”作为插件集成到现有方法中，能显著提升擦除效果（如名人擦除的 GPT-Score 提升约 50%）。
*   **保留效果好**：通过上下文引导，减少了对语义邻近概念的误伤，更好地保留了模型的通用生成能力。
*   **鲁棒性强**：相比基准方法，该方法能更有效地抵御提示词改写和噪声注入等对抗性恢复攻击，证明其实现了更深层次的概念移除。

#### 7. 优点与亮点
*   **即插即用**：作为一种通用的分布式框架，可以轻松集成到现有的各种遗忘算法中。
*   **视角新颖**：首次从语义分布的角度重新定义了扩散模型中的概念遗忘问题。
*   **评估严谨**：引入了先进的多模态大模型（VLM）作为评估器，比传统的分类器或 CLIP 分数更接近人类视觉感知。

#### 8. 不足与局限
*   **LLM 依赖**：上下文的生成高度依赖于 LLM 的质量，可能引入 LLM 本身的偏见或无法覆盖极端罕见的上下文。
*   **计算开销**：虽然是轻量级插件，但引入多个提示词集合会略微增加微调时的计算负担（尤其是输出型方法）。
*   **抽象概念挑战**：在艺术风格等抽象概念上，构建精准且多样化的上下文提示词仍具有挑战性，提升幅度相对具体实体（如名人）较小。

（完）

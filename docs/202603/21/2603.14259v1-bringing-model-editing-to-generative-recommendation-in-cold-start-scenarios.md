---
title: Bringing Model Editing to Generative Recommendation in Cold-Start Scenarios
title_zh: 将模型编辑引入冷启动场景下的生成式推荐
authors: "Chenglei Shen, Teng Shi, Weijie Yu, Xiao Zhang, Jun Xu"
date: 2026-03-15
pdf: "https://arxiv.org/pdf/2603.14259v1"
tags: ["query:mu-ce"]
score: 9.0
evidence: 用于知识注入的模型编辑
tldr: "针对生成式推荐系统在冷启动场景下性能骤降的问题，本文提出 GenRecEdit 框架，首次将模型编辑技术引入推荐领域。该方法通过显式建模序列上下文与生成标记的关系，并采用迭代式标记级编辑和一对一触发机制，实现了在不重新训练的情况下高效注入新项目知识。实验证明，该方法在显著提升冷启动推荐准确率的同时，大幅降低了更新成本，仅需重训练时间的 9.5% 即可完成高效更新。"
selection_source: fresh_fetch
motivation: 生成式推荐模型在冷启动场景下存在严重的性能坍塌，且传统的重训练方法面临数据稀疏、计算成本高和更新延迟等挑战。
method: 提出 GenRecEdit 框架，通过迭代式标记级编辑注入多标记项目表示，并引入一对一触发机制以减少推理过程中多个编辑间的干扰。
result: 实验表明 GenRecEdit 在显著提升冷启动项目推荐准确率的同时，保持了原始推荐质量，且更新效率比重训练提高了约十倍。
conclusion: 将模型编辑引入生成式推荐是解决冷启动问题的有效途径，能够以极低的计算开销实现推荐系统知识的快速注入与更新。
---

## 摘要
生成式推荐（GR）在端到端生成范式的序列推荐中展现出强大的潜力。然而，现有的 GR 模型面临严重的冷启动崩溃问题：它们在冷启动物品上的推荐准确率可能降至接近零。目前的解决方案通常依赖于利用冷启动交互进行重训练，但这受到反馈稀疏、计算成本高和更新延迟的阻碍，限制了其在快速演变的推荐目录中的实际效用。受自然语言处理（NLP）中模型编辑的启发（该技术能够实现向大语言模型中进行免训练的知识注入），我们探索了如何将这一范式引入生成式推荐。然而，这面临两个关键挑战：首先，GR 缺乏自然语言中常见的显式主客体绑定，使得针对性编辑变得困难；其次，GR 不具备稳定的 token 共现模式，导致注入多 token 物品表示变得不可靠。为了应对这些挑战，我们提出了 GenRecEdit，一个专为生成式推荐定制的模型编辑框架。GenRecEdit 显式地建模了完整序列上下文与下一 token 生成之间的关系，采用迭代式 token 级编辑来注入多 token 物品表示，并引入了一对一触发机制以减少推理过程中多个编辑之间的干扰。在多个数据集上的广泛实验表明，GenRecEdit 在保持模型原始推荐质量的同时，显著提升了冷启动物品的推荐性能。此外，它仅需重训练所需训练时间的约 9.5% 即可实现这些增益，从而实现了更高效、更频繁的模型更新。

## Abstract
Generative recommendation (GR) has shown strong potential for sequential recommendation in an end-to-end generation paradigm. However, existing GR models suffer from severe cold-start collapse: their recommendation accuracy on cold-start items can drop to near zero. Current solutions typically rely on retraining with cold-start interactions, which is hindered by sparse feedback, high computational cost, and delayed updates, limiting practical utility in rapidly evolving recommendation catalogs. Inspired by model editing in NLP, which enables training-free knowledge injection into large language models, we explore how to bring this paradigm to generative recommendation. This, however, faces two key challenges: GR lacks the explicit subject-object binding common in natural language, making targeted edits difficult; and GR does not exhibit stable token co-occurrence patterns, making the injection of multi-token item representations unreliable. To address these challenges, we propose GenRecEdit, a model editing framework tailored for generative recommendation. GenRecEdit explicitly models the relationship between the full sequence context and next-token generation, adopts iterative token-level editing to inject multi-token item representations, and introduces a one-to-one trigger mechanism to reduce interference among multiple edits during inference. Extensive experiments on multiple datasets show that GenRecEdit substantially improves recommendation performance on cold-start items while preserving the model's original recommendation quality. Moreover, it achieves these gains using only about 9.5% of the training time required for retraining, enabling more efficient and frequent model updates.

---

## 论文详细总结（自动生成）

这是一份关于论文《Bringing Model Editing to Generative Recommendation in Cold-Start Scenarios》的深度结构化总结：

### 1. 核心问题与整体含义（研究动机与背景）
*   **核心问题**：生成式推荐（Generative Recommendation, GR）虽然在序列推荐中表现优异，但在面对**冷启动物品**（训练集中未出现的物品）时会出现“冷启动崩溃”（Cold-start collapse），即推荐准确率几乎降至零。
*   **研究动机**：
    *   **现有方案缺陷**：传统的重训练或增量微调方法在处理冷启动时面临反馈稀疏、计算成本高昂以及模型更新延迟等问题，无法满足快速迭代的业务需求。
    *   **跨领域借鉴**：NLP 领域的“模型编辑”技术可以在不重新训练的情况下向大模型注入新知识。本文旨在探索如何将这一范式引入 GR 领域，以实现冷启动物品知识的即时注入。
    *   **技术挑战**：GR 数据缺乏 NLP 中的显式“主-谓-宾”结构，且物品的语义 ID（Semantic IDs）序列缺乏稳定的 Token 共现模式，导致直接套用 NLP 编辑方法效果不佳。

### 2. 方法论：GenRecEdit 框架
GenRecEdit 是首个专为生成式推荐定制的模型编辑框架，其核心思想是将冷启动物品的语义 ID 模式视为可编辑的知识。
*   **位置感知知识准备（Position-Wise Knowledge Preparation）**：
    *   针对冷启动物品，利用其元数据（如标题）通过 Sentence-T5 编码，检索相似的热启动物品。
    *   将热启动物品的真实交互历史作为冷启动物品的“伪历史”，构建 `(上下文, 目标 Token)` 的编辑请求对。
*   **定位-编辑框架（Locate-Then-Edit Framework）**：
    *   **层定位**：训练一个线性探测分类器（Linear Probing Classifier），识别哪些层最能区分新旧知识，从而选定特定的 FFN 层进行编辑。
    *   **参数更新**：通过最小化干预损失函数，计算 FFN 输出的偏移量 $\delta$，并利用闭式解（Closed-form update）更新权重矩阵 $W_{out}$，在注入新知识的同时，通过协方差矩阵约束来保留原始推荐能力。
*   **一对一触发策略（One-One Triggering Policy）**：
    *   在推理生成阶段，根据当前解码的 Token 位置，仅激活对应位置的编辑层。这种“分而治之”的策略避免了多个位置编辑之间的相互干扰，确保了多 Token 序列生成的稳定性。

### 3. 实验设计
*   **数据集**：使用了 Amazon 2023 Review 数据集中的三个类别：**Video Games**、**Software**、**Cell Phones and Accessories**。
*   **评估场景**：分为“整体测试集”（Overall）和“冷启动子集”（Cold Subset）。
*   **Benchmark 与对比方法**：
    *   **传统 ID 类**：SASRec, BERT4Rec。
    *   **生成式语义 ID 类**：VQ-Rec, TIGER, LC-Rec。
    *   **冷启动专项类**：Retraining（全量重训）、Finetuning（增量微调）、SpecGR（基于检索的推测生成）。
*   **评估指标**：NDCG@K, Recall@K, 以及衡量模型生成分布的 IID Ratio。

### 4. 资源与算力
*   **算力配置**：论文中**未明确说明**具体的 GPU 型号和数量。
*   **训练/更新时长**：论文强调了效率优势，指出 GenRecEdit 的模型更新时间仅为**重训练（Retraining）时间的 9.5%**。
*   **模型架构**：基于 T5 架构，解码器层数为 6 层，使用了 gated-silu 激活函数。

### 5. 实验数量与充分性
*   **实验规模**：在 3 个不同规模和特性的数据集上进行了测试。
*   **实验类型**：
    *   主实验：对比了 8 种基准方法。
    *   消融实验：验证了位置感知准备、分类器定位、一对一触发机制的必要性。
    *   敏感性分析：探讨了伪数据质量、注入知识数量、超参数 $\lambda$（平衡新旧知识）的影响。
*   **充分性评价**：实验设计较为全面，不仅关注准确率，还深入分析了“冷启动崩溃”的内在原因（通过 IID Ratio 指标），并对比了更新效率，实验结果具有较强的说服力和客观性。

### 6. 主要结论与发现
*   **冷启动潜力**：GR 模型其实具备识别冷启动物品粗略类别的潜力（首个 Token 生成较准），但后续 Token 会受限于已见模式。
*   **性能提升**：GenRecEdit 在冷启动子集上的表现显著优于所有非专项方法，且在整体性能上保持领先。
*   **知识保留**：相比于容易产生灾难性遗忘的 Finetuning，GenRecEdit 能在注入新物品知识的同时，将热启动物品的性能损失控制在极低水平（如 NDCG 仅下降 6.5%）。
*   **极高效率**：模型编辑范式在处理频繁更新的推荐场景时，比重训练具有压倒性的时间成本优势。

### 7. 优点（亮点）
*   **范式创新**：首次将模型编辑引入生成式推荐，为解决冷启动问题提供了全新的非训练视角。
*   **针对性设计**：通过“位置感知”和“一对一触发”解决了 GR 数据非结构化、Token 关联弱的痛点。
*   **实用性强**：9.5% 的更新时间成本使其非常适合新闻、短视频等物品库快速更迭的实际业务场景。

### 8. 不足与局限
*   **伪数据依赖**：实验显示 GenRecEdit 与“理想上限”（使用真实历史进行编辑）之间仍存在性能差距（Quality Gap），说明其效果高度依赖于相似性检索构建的伪交互质量。
*   **Tokenizer 限制**：该方法依赖于预先定义的语义 ID 体系，如果 Tokenizer 本身无法有效表征新物品，编辑的效果可能会受限。
*   **长序列挑战**：实验主要针对 4 位长度的语义 ID，对于更长、更复杂的物品表示，位置级编辑的复杂度和干扰可能会增加。

（完）

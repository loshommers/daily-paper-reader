---
title: Which Concepts to Forget and How to Refuse? Decomposing Concepts for Continual Unlearning in Large Vision-Language Models
title_zh: 哪些概念需要遗忘以及如何拒绝？大型视觉语言模型中持续遗忘的概念分解
authors: "Hyundong Jin, Dongyoon Han, Eunwoo Kim"
date: 2026-03-23
pdf: "https://arxiv.org/pdf/2603.21484v1"
tags: ["query:mu-ce"]
score: 10.0
evidence: 针对大型视觉语言模型的持续遗忘框架
tldr: 本研究针对大型视觉语言模型（LVLM）中的持续遗忘问题，提出了一种基于概念分解的创新框架。针对序列化遗忘请求导致的表示扭曲和错误拒绝问题，该框架通过概念调节器识别待遗忘类别的细粒度视觉-语言组合，并利用拒绝专家混合机制（MoE）生成针对性的拒绝响应。通过多模态概念驱动路由，实现了对相似任务专家的复用和对新任务的适配，在保持模型通用能力的同时，显著提升了遗忘的精准度。
selection_source: fresh_fetch
motivation: 序列化遗忘更新会扭曲模型的共享表示，导致视觉-语言对与拒绝行为之间产生错误关联，从而引发不当的拒绝响应。
method: 提出一种将拒绝行为建立在细粒度概念分解基础上的框架，利用概念调节器识别特征并结合拒绝专家混合机制生成特定响应。
result: 在多个视觉语言基准测试上的实验表明，该框架在生成基于概念的拒绝响应和保持模型通用效用方面均优于现有方法。
conclusion: 通过分解视觉-语言概念并采用动态路由的拒绝专家机制，可以有效解决LVLM在持续遗忘过程中的表示偏移和误拒问题。
---

## 摘要
持续遗忘（Continual unlearning）面临的挑战是，如何使大型视觉语言模型能够根据序列化的删除请求，选择性地拒绝特定的图像-指令对，同时保持通用效用。然而，序列化的遗忘更新会扭曲共享表示，在视觉语言对与拒绝行为之间产生伪关联，从而阻碍对遗忘目标的精确识别，导致不恰当的拒绝。为了应对这一挑战，我们提出了一种新型的持续遗忘框架，该框架将拒绝行为建立在从删除目标中分解出的视觉和文本概念的细粒度描述之上。我们首先通过概念调节器（concept modulator）识别哪些视觉-语言概念组合表征了每个遗忘类别，然后通过拒绝专家混合（mixture of refusal experts，称为 refusers）确定如何生成适当的拒绝响应，每个专家都专门用于生成与概念对齐的拒绝。为了在序列任务中生成特定于概念的拒绝响应，我们引入了一种多模态、概念驱动的路由方案，该方案对共享相似概念的任务重用 refusers，并使未充分利用的 refusers 适应新概念。在视觉语言基准测试上的广泛实验表明，所提出的框架通过生成基于概念的拒绝响应并在遗忘序列中保持通用效用，优于现有方法。

## Abstract
Continual unlearning poses the challenge of enabling large vision-language models to selectively refuse specific image-instruction pairs in response to sequential deletion requests, while preserving general utility. However, sequential unlearning updates distort shared representations, creating spurious associations between vision-language pairs and refusal behaviors that hinder precise identification of refusal targets, resulting in inappropriate refusals. To address this challenge, we propose a novel continual unlearning framework that grounds refusal behavior in fine-grained descriptions of visual and textual concepts decomposed from deletion targets. We first identify which visual-linguistic concept combinations characterize each forget category through a concept modulator, then determine how to generate appropriate refusal responses via a mixture of refusal experts, termed refusers, each specialized for concept-aligned refusal generation. To generate concept-specific refusal responses across sequential tasks, we introduce a multimodal, concept-driven routing scheme that reuses refusers for tasks sharing similar concepts and adapts underutilized ones for novel concepts. Extensive experiments on vision-language benchmarks demonstrate that the proposed framework outperforms existing methods by generating concept-grounded refusal responses and preserving the general utility across unlearning sequences.

---

## 论文详细总结（自动生成）

### 论文总结：哪些概念需要遗忘以及如何拒绝？大型视觉语言模型中的持续遗忘概念分解

#### 1. 核心问题与整体含义（研究动机和背景）
随着 AI 监管和用户隐私需求的增加，大型视觉语言模型（LVLM）需要具备“持续遗忘”（Continual Unlearning）的能力，即按顺序响应多个删除特定知识的请求。
*   **研究动机**：现有的遗忘方法在处理序列化请求时，容易导致模型内部的视觉-语言表示发生扭曲。这会引发两个主要问题：
    1.  **无关拒绝**：学习新遗忘任务时覆盖了旧的拒绝模式，导致拒绝响应与上下文不符。
    2.  **过度拒绝**：模型将正常的查询误认为是受限请求，从而拒绝回答本应保留的知识。
*   **核心目标**：在不损害模型通用能力的前提下，实现精准、可解释且持续的特定知识擦除。

#### 2. 方法论：CORE (COncept-aware REfuser)
论文提出了一种基于概念分解的框架，将“遗忘什么”和“如何拒绝”解耦。
*   **核心思想**：将视觉-指令对分解为细粒度的视觉属性和文本意图概念，通过识别这些概念组合来触发精准的拒绝行为。
*   **关键技术细节**：
    1.  **概念识别与调节**：利用 LLM 生成描述遗忘类别的视觉/文本概念集。通过**概念模块（Concept Modules）**计算输入与概念的匹配度，并使用**概念调节器（Concept Modulator）**通过学习重权（Reweighting）来强化相关概念、抑制噪声。
    2.  **拒绝专家混合（MoE Refusers）**：引入一组专门的连接模块（Refusers）。**路由方案（Routing Scheme）**根据当前输入与历史任务的概念相关性，决定是复用旧的拒绝专家还是训练新的专家。
    3.  **推理校准（Inference-Time Calibration）**：在推理阶段，根据输入查询与已遗忘任务的最高相关性得分 $\beta$，动态调整拒绝模块的贡献权重，以防止对保留数据的误伤。
*   **损失函数**：结合了概念对齐损失 ($L_{con}$)、分类损失 ($L_{mod}$)、拒绝响应交叉熵损失 ($L_{ce}$) 和路由对比损失 ($L_{ref}$)。

#### 3. 实验设计
*   **数据集与场景**：
    *   **问答任务**：使用安全基准数据集（包含武器、暴力等 6 类安全风险），分为 12 个序列遗忘任务。
    *   **分类任务**：使用 ImageNet-R，选取 80 个类别分为 4 个序列遗忘任务。
    *   **通用能力评估**：使用 MMBench、SEEDBench 和 ScienceQA 评估模型在非遗忘任务上的表现。
*   **对比方法**：
    *   持续学习方法：EWC, LwF, GMM, EProj, MoEAdapter。
    *   机器遗忘方法：SCRUB, $O^3$。
*   **Benchmark 指标**：上下文感知拒绝率（CRR）、拒绝间隙（$\Delta RR$）、保留数据回答率（AR）以及通用能力得分（S）。

#### 4. 资源与算力
*   **硬件**：实验在 **NVIDIA RTX 3090 GPU** 上进行。
*   **算力细节**：
    *   训练时长：每个任务的第一阶段（概念模块）约 5.26 分钟，第二阶段（Refusers）约 15.32 分钟。
    *   显存占用：峰值显存约为 **37 GB**。
    *   模型基础：采用了 Vicuna-7B 和 Llama-2-7B 作为语言模型，配合 ViT-g/14 视觉编码器。

#### 5. 实验数量与充分性
*   **实验规模**：总共进行了 16 个序列遗忘任务的测试，涵盖了生成式问答和判别式分类。
*   **充分性**：
    *   **多模型验证**：在 Vicuna 和 Llama-2 两种主流架构上均验证了有效性。
    *   **消融实验**：详细分析了概念调节器（MOD）、路由激活（ACT）和推理校准（CAL）的作用。
    *   **鲁棒性测试**：测试了不同任务顺序、不同数量的概念描述（5 到 20 个）以及不同 LLM 生成的概念描述风格对结果的影响。
*   **客观性**：实验对比了多种最先进的持续学习和遗忘算法，并使用了自动评估与语义相似度指标，设计较为客观公平。

#### 6. 主要结论与发现
*   **性能领先**：CORE 在保持高拒绝率（CRR > 90%）的同时，对保留数据的回答率（AR）显著高于对比方法（高出约 6%-30%）。
*   **缓解灾难性遗忘**：通过概念路由机制，模型能够有效积累拒绝知识，避免了后续任务对前期遗忘逻辑的破坏。
*   **精准拒绝**：可视化结果显示，CORE 能够准确识别触发拒绝的细粒度视觉特征（如“抗议者手中的标语”），而非盲目拒绝所有包含类似背景的图像。

#### 7. 优点
*   **细粒度可解释性**：通过概念分解，使得遗忘过程不再是黑盒，用户可以清晰看到是哪些属性触发了拒绝。
*   **高效的参数利用**：采用 MoE 架构，仅更新少量的连接模块参数，保护了预训练模型的核心权重。
*   **动态适配**：推理时的校准机制有效解决了持续遗忘中常见的“过度泛化”问题。

#### 8. 不足与局限
*   **概念生成依赖**：框架高度依赖外部 LLM（如 ChatGPT）来生成初始概念描述，若 LLM 存在偏见或描述不准，可能影响遗忘效果。
*   **计算开销**：虽然比全量微调快，但相比简单的梯度上升方法，增加概念模块和多阶段训练会带来额外的计算负担。
*   **应用限制**：目前主要针对明确的类别遗忘，对于更抽象的风格遗忘或复杂的逻辑遗忘（如特定人物的关联关系）尚未深入探讨。

（完）
